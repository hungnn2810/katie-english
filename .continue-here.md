---
paused: 2026-06-23T04:11:15Z
milestone: v1.0
status: audit_complete_gaps_remain
next_command: fix TUITION-05 and TUITION-06, then /gsd-audit-milestone, then /gsd-complete-milestone v1.0
---

# Handoff — v1.0 Milestone Final Fixes

## Where We Are

All 17 phases complete. Ran `/gsd-audit-milestone`. Audit status: `gaps_found`.

Two real blockers remain before milestone can close:

## Blocker 1 — TUITION-05: PaymentRecordDialog orphaned

**What:** `frontend/app/admin/tuition/_components/PaymentRecordDialog.tsx` is fully implemented but never imported or mounted anywhere. There is no UI path for recording a payment.

**Backend:** `PATCH /admin/tuition/records/:id` exists and works.

**Fix steps:**
1. Read `frontend/app/admin/tuition/page.tsx`
2. Read `frontend/app/admin/tuition/_components/PaymentRecordDialog.tsx` (understand its props)
3. Read `frontend/app/admin/tuition/_components/TuitionReportTable.tsx` (understand row structure)
4. Add import of `PaymentRecordDialog` to `page.tsx`
5. Add state: `const [paymentDialog, setPaymentDialog] = useState<{recordId: number; studentName: string; totalAmount: number} | null>(null)`
6. Add "Ghi nhận thanh toán" / "Mark Paid" action button per row in `TuitionReportTable` — pass `onMarkPaid` callback prop
7. Wire dialog open on button click, close on dialog cancel/confirm
8. Verify: button appears per row, dialog opens, PATCH request fires on confirm

## Blocker 2 — TUITION-06: ZaloSendModal permanently disabled

**What:** `frontend/app/admin/tuition/page.tsx` passes `recordIds={[]}` to `ZaloSendModal`. The modal has `disabled={loading || recordIds.length === 0}` — Send button is always disabled.

**Fix steps:**
1. Read `frontend/app/admin/tuition/page.tsx` (find ZaloSendModal usage)
2. Read `frontend/app/admin/tuition/_components/ZaloSendModal.tsx` (understand recordIds prop)
3. Read `frontend/app/admin/tuition/_components/TuitionReportTable.tsx` (understand row data)
4. Add checkbox column to `TuitionReportTable` rows; add `selectedRecordIds: number[]` + `onSelectionChange` props
5. Add state in `page.tsx`: `const [selectedRecordIds, setSelectedRecordIds] = useState<number[]>([])`
6. Wire `TuitionReportTable` selection → `setSelectedRecordIds`
7. Replace `recordIds={[]}` with `recordIds={selectedRecordIds}`
8. Verify: check some rows → Send button enables → ZNS API call fires

## After Fixing Both

1. Run relevant tests:
   ```
   cd frontend && npx tsc --noEmit
   cd backend && npx jest --no-coverage
   ```
2. Run `/gsd-audit-milestone` — should get `status: passed`
3. Run `/gsd-complete-milestone v1.0`

## False Positives (Already Verified — Do Not Re-investigate)

- **BLOCKER-A** (student auth cookie): `frontend/app/api/auth/student-login/route.ts:21` sets `student-token` HttpOnly cookie. WORKING.
- **BLOCKER-B** (speaking-result endpoint): `backend/src/game/game.controller.ts:70` has `@Post('session/:id/speaking-result')`. EXISTS.

## Optional Tech Debt (Non-Blocking for Milestone)

| Item | File | Fix |
|------|------|-----|
| CR-02 Docker health | `bfa-service/Dockerfile` | Add `curl` to apt-get install |
| CR-01 /align crash | `bfa-service/main.py` | Add try/except on error paths |
| BFA_TRANSCRIPTION_MATCH_THRESHOLD | `bfa-service/main.py` | Wire env var to `_transcription_matches_word()` |
| Duplicate VocabItem interfaces | `frontend/lib/admin-api.ts` | Remove duplicate declarations |

## Key Files for TUITION-05/06

```
frontend/app/admin/tuition/
├── page.tsx                          ← ZaloSendModal + TuitionReportTable mounted here
└── _components/
    ├── PaymentRecordDialog.tsx       ← Implemented, never imported (TUITION-05)
    ├── TuitionReportTable.tsx        ← Rows need Mark Paid button (TUITION-05) + checkboxes (TUITION-06)
    └── ZaloSendModal.tsx             ← recordIds prop drives Send button (TUITION-06)
```

## Modified Files (Uncommitted)

- `.planning/STATE.md` — minor updates
- `.planning/v1.0-MILESTONE-AUDIT.md` — audit report + false positive corrections

---
_Paused: 2026-06-23 by gsd-pause-work_
