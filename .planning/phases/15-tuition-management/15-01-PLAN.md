---
phase: "15"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/prisma/schema.prisma
  - docs/db/tuition.md
  - docs/db/README.md
autonomous: true
requirements:
  - TUITION-01
  - TUITION-02
  - TUITION-03
  - TUITION-04
  - TUITION-05

must_haves:
  truths:
    - "schema.prisma contains enum TuitionStatus with PENDING, PAID, OVERDUE values"
    - "schema.prisma contains model TuitionConfig with classId, pricePerSession, bookFee, dueDayOfMonth"
    - "schema.prisma contains model TuitionRecord with studentId, classId, month, year, tuitionAmount, bookFee, totalAmount, dueDate, status, paidAt, paidBy"
    - "schema.prisma contains model TuitionNotificationLog with tuitionRecordId, sentAt, zaloResponse, success"
    - "Class model has tuitionConfig and tuitionRecords relation fields"
    - "Student model has tuitionRecords relation field"
    - "npx prisma db push exits 0 with tables tuition_configs, tuition_records, tuition_notification_logs in database"
    - "docs/db/tuition.md exists and documents all three models and TuitionStatus enum"
    - "docs/db/README.md Enums table includes TuitionStatus row"
    - "docs/db/README.md Tables section includes tuition_configs, tuition_records, tuition_notification_logs rows"
  artifacts:
    - path: "backend/prisma/schema.prisma"
      provides: "TuitionStatus enum + TuitionConfig + TuitionRecord + TuitionNotificationLog models"
      contains: "enum TuitionStatus"
    - path: "docs/db/tuition.md"
      provides: "DB documentation for all tuition models"
    - path: "docs/db/README.md"
      provides: "Updated overview with TuitionStatus enum and tuition tables"
  key_links:
    - from: "TuitionRecord"
      to: "Student"
      via: "studentId FK"
      pattern: "studentId.*FK.*students"
    - from: "TuitionRecord"
      to: "Class"
      via: "classId FK"
      pattern: "classId.*FK.*classes"
    - from: "TuitionNotificationLog"
      to: "TuitionRecord"
      via: "tuitionRecordId FK"
      pattern: "tuitionRecordId.*FK.*tuition_records"
---

<objective>
Add TuitionStatus enum and three new Prisma models (TuitionConfig, TuitionRecord, TuitionNotificationLog) to the schema. Extend Class and Student with relation back-references. Push schema to database. Create docs/db/tuition.md and update docs/db/README.md to satisfy CLAUDE.md doc rule.

Purpose: Establishes the database foundation for all tuition management features (TUITION-01 through TUITION-05). Without the schema, the backend module (Plan 02) and frontend (Plans 03/04) cannot be built.

Output: Updated schema.prisma, tables in DB, new docs/db/tuition.md, updated docs/db/README.md.
</objective>

<execution_context>
@J:/sources/katie-english/.claude/get-shit-done/workflows/execute-plan.md
@J:/sources/katie-english/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@J:/sources/katie-english/.planning/ROADMAP.md
@J:/sources/katie-english/.planning/phases/15-tuition-management/15-CONTEXT.md
@J:/sources/katie-english/.planning/phases/15-tuition-management/15-RESEARCH.md
@J:/sources/katie-english/.planning/phases/15-tuition-management/15-PATTERNS.md
@J:/sources/katie-english/backend/prisma/schema.prisma
@J:/sources/katie-english/docs/db/README.md
@J:/sources/katie-english/docs/db/classes.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend schema.prisma with TuitionStatus enum and three tuition models</name>
  <files>backend/prisma/schema.prisma</files>
  <read_first>
    - backend/prisma/schema.prisma — full file; understand existing enum placement, model conventions, relation patterns between Class/Student/ParentInfo
    - .planning/phases/15-tuition-management/15-PATTERNS.md — schema block section for exact field names, types, constraints, and @@map values
    - .planning/phases/15-tuition-management/15-CONTEXT.md — D-05 (LOCKED): exact three models and enum required
  </read_first>
  <action>
    Append to backend/prisma/schema.prisma after the existing UserRole enum and User model (end of file). Do not modify any existing model or enum. Make only additive changes.

    1. Add enum TuitionStatus with three values: PENDING, PAID, OVERDUE. Map it after the UserRole enum block.

    2. Add model TuitionConfig: fields id (Int @id @default(autoincrement())), classId (Int @unique), class relation (Class @relation fields [classId] references [id] onDelete: Cascade), pricePerSession (Int — VNĐ per session), bookFee (Int? — nullable), dueDayOfMonth (Int — range 1-31), createdAt (DateTime @default(now())), updatedAt (DateTime @updatedAt). Use @@map("tuition_configs").

    3. Add model TuitionRecord: fields id (Int @id @default(autoincrement())), studentId (Int), student (Student relation), classId (Int), class (Class relation), month (Int — 1-12), year (Int), tuitionAmount (Int), bookFee (Int — 0 if not charged), totalAmount (Int), dueDate (DateTime), status (TuitionStatus @default(PENDING)), paidAt (DateTime?), paidBy (String?), notificationLogs (TuitionNotificationLog[]), createdAt (DateTime @default(now())), updatedAt (DateTime @updatedAt). Add @@unique([studentId, classId, month, year]) to prevent duplicate records per student/month. Use @@map("tuition_records").

    4. Add model TuitionNotificationLog: fields id (Int @id @default(autoincrement())), tuitionRecordId (Int), tuitionRecord (TuitionRecord relation fields [tuitionRecordId] references [id] onDelete: Cascade), sentAt (DateTime), zaloResponse (String — JSON stringified Zalo API response), success (Boolean), createdAt (DateTime @default(now())). Use @@map("tuition_notification_logs").

    5. Add back-reference fields to existing models WITHOUT modifying any other field:
       - In model Class (after the existing `assignments` field): add `tuitionConfig TuitionConfig?` and `tuitionRecords TuitionRecord[]`
       - In model Student (after the existing `sessions` field): add `tuitionRecords TuitionRecord[]`

    Per D-05 (LOCKED): exact three models and enum required. Per D-03 (LOCKED): tuitionAmount = sessions × pricePerSession, this is reflected in the field definition (Int type for VNĐ amounts — no decimals needed for Vietnamese dong amounts).
  </action>
  <verify>
    <automated>cd J:/sources/katie-english/backend && npx prisma validate</automated>
  </verify>
  <acceptance_criteria>
    - schema.prisma contains `enum TuitionStatus { PENDING PAID OVERDUE }`
    - schema.prisma contains `model TuitionConfig` with `classId Int @unique` and `dueDayOfMonth Int`
    - schema.prisma contains `model TuitionRecord` with `@@unique([studentId, classId, month, year])`
    - schema.prisma contains `model TuitionNotificationLog` with `zaloResponse String`
    - model Class contains `tuitionConfig TuitionConfig?`
    - model Student contains `tuitionRecords TuitionRecord[]`
    - npx prisma validate exits 0 with no errors
  </acceptance_criteria>
  <done>schema.prisma validates cleanly with all three models, one enum, and back-references on Class and Student.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: [BLOCKING] Push schema to database</name>
  <files></files>
  <read_first>
    - backend/prisma/schema.prisma — verify Task 1 changes present before pushing
  </read_first>
  <action>
    Run the Prisma schema push command from the backend directory. This is a BLOCKING step — the backend module (Plan 02) cannot be built until tables exist.

    Command: cd backend && npx prisma db push

    If the command prompts about data loss on existing tables (should not happen as these are new tables), use: npx prisma db push --accept-data-loss

    After push succeeds, verify the three new tables exist by running: npx prisma studio --port 5556 is NOT appropriate here — instead confirm via: npx prisma db execute --stdin <<< "SELECT table_name FROM information_schema.tables WHERE table_schema='public' AND table_name IN ('tuition_configs','tuition_records','tuition_notification_logs');" or simply trust the prisma db push exit code 0.
  </action>
  <verify>
    <automated>cd J:/sources/katie-english/backend && npx prisma db push 2>&1; echo "Exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - npx prisma db push exits 0
    - Output includes "Your database is now in sync with your Prisma schema" or similar success message
    - No error messages about missing tables or constraint violations
  </acceptance_criteria>
  <done>Database has tuition_configs, tuition_records, tuition_notification_logs tables with correct columns and constraints.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create docs/db/tuition.md and update docs/db/README.md (CLAUDE.md rule)</name>
  <files>docs/db/tuition.md, docs/db/README.md</files>
  <read_first>
    - docs/db/README.md — current Enums table and Tables table to understand where to add rows
    - docs/db/classes.md — doc format convention (column table with Type/Constraints/Description columns)
    - .planning/phases/15-tuition-management/15-PATTERNS.md — tuition.md block with exact documentation content
    - CLAUDE.md — rule: whenever schema.prisma is modified, docs/db/ must be updated in the same commit
  </read_first>
  <action>
    1. CREATE docs/db/tuition.md. Include documentation for all three models (tuition_configs, tuition_records, tuition_notification_logs) and TuitionStatus enum. Follow the exact same column table format as docs/db/classes.md: columns Type, Constraints, Mô tả. Include:
       - TuitionConfig table with all 7 fields
       - TuitionRecord table with all 14 fields; note the @@unique([studentId, classId, month, year]) constraint and OVERDUE computation rule
       - TuitionNotificationLog table with all 6 fields
       - TuitionStatus enum values section
       - Computation rules section (per D-03 LOCKED): tuitionAmount = countSessionsInMonth(scheduleSlots) × pricePerSession; totalAmount = tuitionAmount + (bookFee || 0); dueDate = new Date(year, month-1, dueDayOfMonth)
       - OVERDUE status rule: status = paidAt ? PAID : dueDate < now ? OVERDUE : PENDING (computed at query time, not persisted)
       - Relationships diagram

    2. UPDATE docs/db/README.md:
       - In the Enums table: add row `| \`TuitionStatus\` | \`PENDING\`, \`PAID\`, \`OVERDUE\` |`
       - In the Tables section: add three rows for tuition_configs (Tuition domain), tuition_records (Tuition domain), tuition_notification_logs (Tuition domain)
       - In the Domain Files section: add `- [tuition.md](tuition.md) — \`tuition_configs\`, \`tuition_records\`, \`tuition_notification_logs\``
       - In the Key Relationships diagram: add lines `Class ──(1:1)──► TuitionConfig` and `Student ──(1:N)──► TuitionRecord ──(1:N)──► TuitionNotificationLog`
  </action>
  <verify>
    <automated>test -f J:/sources/katie-english/docs/db/tuition.md && echo "tuition.md exists" && grep -c "TuitionConfig\|TuitionRecord\|TuitionNotificationLog" J:/sources/katie-english/docs/db/tuition.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/db/tuition.md exists and contains sections for tuition_configs, tuition_records, tuition_notification_logs
    - docs/db/tuition.md documents TuitionStatus enum with PENDING/PAID/OVERDUE
    - docs/db/tuition.md documents OVERDUE computation rule (query-time, not persisted)
    - docs/db/README.md Enums table contains TuitionStatus row
    - docs/db/README.md Tables section contains tuition_configs, tuition_records, tuition_notification_logs rows
    - docs/db/README.md Domain Files section contains tuition.md link
  </acceptance_criteria>
  <done>docs/db/tuition.md created with all model documentation; docs/db/README.md updated with enum, table, and domain-file entries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Schema → Database | Prisma schema push modifies production-adjacent DB; additive-only changes prevent data loss |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering | schema.prisma | mitigate | Only additive changes; no modification to existing models; npx prisma validate gates correctness before push |
| T-15-02 | Tampering | TuitionRecord @@unique | mitigate | Unique constraint on (studentId, classId, month, year) prevents duplicate record generation at DB level |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in Plan 01 — Prisma already in project |
</threat_model>

<verification>
After all three tasks:
- npx prisma validate exits 0
- npx prisma db push exits 0
- docs/db/tuition.md exists with content for all 3 models
- docs/db/README.md contains TuitionStatus in Enums table
- git diff --stat shows: schema.prisma, docs/db/tuition.md (new), docs/db/README.md
</verification>

<success_criteria>
1. schema.prisma contains TuitionStatus enum + TuitionConfig + TuitionRecord + TuitionNotificationLog models with correct fields and relations
2. Database tables tuition_configs, tuition_records, tuition_notification_logs created (npx prisma db push exits 0)
3. docs/db/tuition.md created with complete model documentation
4. docs/db/README.md updated with new enum and table rows
</success_criteria>

<output>
Create .planning/phases/15-tuition-management/15-01-SUMMARY.md when done.

## Artifacts this phase produces

### New Schema Additions
- `enum TuitionStatus` — PENDING | PAID | OVERDUE (in backend/prisma/schema.prisma)
- `model TuitionConfig` — maps to `tuition_configs` table (classId, pricePerSession, bookFee, dueDayOfMonth)
- `model TuitionRecord` — maps to `tuition_records` table (studentId, classId, month, year, tuitionAmount, bookFee, totalAmount, dueDate, status, paidAt, paidBy)
- `model TuitionNotificationLog` — maps to `tuition_notification_logs` table (tuitionRecordId, sentAt, zaloResponse, success)

### Schema Relation Extensions
- `Class.tuitionConfig` — back-reference to TuitionConfig
- `Class.tuitionRecords` — back-reference to TuitionRecord[]
- `Student.tuitionRecords` — back-reference to TuitionRecord[]

### New Files
- `docs/db/tuition.md` — documentation for all three tuition models + TuitionStatus enum + computation rules

### Updated Files
- `backend/prisma/schema.prisma` — 4 new declarations added (1 enum + 3 models + 2 back-reference fields)
- `docs/db/README.md` — TuitionStatus in Enums; 3 rows in Tables; tuition.md in Domain Files; 2 lines in Key Relationships
</output>
