---
phase: "15"
plan: "05"
type: execute
wave: 4
depends_on:
  - "15-04"
files_modified:
  - backend/src/auth/auth.guard.ts
  - backend/src/tuition/tuition.controller.ts
  - frontend/lib/teacher-tuition-api.ts
  - frontend/app/teacher/tuition/page.tsx
autonomous: true
requirements:
  - TUITION-01
  - TUITION-03
  - TUITION-04
  - TUITION-05
  - TUITION-06
gap_closure: true

must_haves:
  truths:
    - "TEACHER role receives HTTP 200 (not 403) on all /admin/tuition/* endpoints when authenticated with a valid TEACHER JWT"
    - "Teacher tuition page uses teacher JWT (localStorage 'token' via authHeaders()) for all tuition API calls — never adminAuthHeaders()"
    - "TuitionGuard accepts payload.role === 'ADMIN' OR payload.role === 'TEACHER'; rejects all other roles with 403"
  artifacts:
    - path: "backend/src/auth/auth.guard.ts"
      provides: "New TuitionGuard class accepting ADMIN and TEACHER roles"
      contains: "TuitionGuard"
    - path: "backend/src/tuition/tuition.controller.ts"
      provides: "Controller uses TuitionGuard instead of AdminGuard"
      contains: "TuitionGuard"
    - path: "frontend/lib/teacher-tuition-api.ts"
      provides: "Tuition API functions using authHeaders() (teacher JWT) instead of adminAuthHeaders()"
      contains: "teacherAuthHeaders"
    - path: "frontend/app/teacher/tuition/page.tsx"
      provides: "Imports tuition functions from teacher-tuition-api.ts — no adminAuthHeaders dependency"
      contains: "teacher-tuition-api"
  key_links:
    - from: "tuition.controller.ts"
      to: "TuitionGuard"
      via: "@UseGuards(TuitionGuard)"
      pattern: "TuitionGuard"
    - from: "teacher/tuition/page.tsx"
      to: "teacher-tuition-api.ts"
      via: "import"
      pattern: "teacher-tuition-api"
---

<objective>
Close Gap 1 (D-06 LOCKED: TEACHER role blocked). Create TuitionGuard that accepts both ADMIN and TEACHER roles, swap it into tuition.controller.ts, and create a teacher-tuition-api.ts that calls /admin/tuition/* endpoints using the teacher JWT (authHeaders) so teacher page API calls are authenticated correctly.

Purpose: D-06 LOCKED specifies both ADMIN and TEACHER must access this module. Currently AdminGuard blocks TEACHER with HTTP 403, and the teacher page uses adminAuthHeaders() which reads admin_token (unavailable to teachers).

Output: 1 new guard class, 1 updated controller, 1 new API helper file, 1 updated teacher page.
</objective>

<execution_context>
@J:/sources/katie-english/.claude/get-shit-done/workflows/execute-plan.md
@J:/sources/katie-english/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@J:/sources/katie-english/.planning/ROADMAP.md
@J:/sources/katie-english/.planning/phases/15-tuition-management/15-CONTEXT.md
@J:/sources/katie-english/.planning/phases/15-tuition-management/15-VERIFICATION.md
@J:/sources/katie-english/backend/src/auth/auth.guard.ts
@J:/sources/katie-english/backend/src/tuition/tuition.controller.ts
@J:/sources/katie-english/backend/src/tuition/tuition.module.ts
@J:/sources/katie-english/frontend/lib/auth.ts
@J:/sources/katie-english/frontend/lib/admin-portal-api.ts
@J:/sources/katie-english/frontend/app/teacher/tuition/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add TuitionGuard to auth.guard.ts and wire into tuition.controller.ts</name>
  <files>
    backend/src/auth/auth.guard.ts,
    backend/src/tuition/tuition.controller.ts
  </files>
  <read_first>
    - backend/src/auth/auth.guard.ts — read the full file; understand the existing AdminGuard structure at line 56 (constructor, prisma lookup, role check at line 68). TuitionGuard must mirror AdminGuard's structure exactly but change the role check condition.
    - backend/src/auth/auth.module.ts — confirm exports array; TuitionGuard must be added to both providers and exports so TuitionModule can inject it.
    - backend/src/tuition/tuition.controller.ts — read the full file; note the @UseGuards(AdminGuard) decorator at line 23 and the AdminGuard import from '../auth/auth.guard'.
    - backend/src/tuition/tuition.module.ts — confirm AuthModule is already imported (it is); no module change needed.
  </read_first>
  <action>
    In backend/src/auth/auth.guard.ts:

    Append a new TuitionGuard class at the end of the file (after the existing AdminGuard class). The class must be decorated with @Injectable() and implement CanActivate. Copy the exact constructor signature from AdminGuard (takes TokenService and PrismaService). In canActivate, replicate the same steps as AdminGuard but replace the role check at line 68 with: if (payload.role !== 'ADMIN' && payload.role !== 'TEACHER') throw new ForbiddenException('Admins and teachers only'). Set (req as any).user = payload and return true.

    Add TuitionGuard to the @Module exports in auth.module.ts: add TuitionGuard to both the providers array and the exports array alongside the existing AdminGuard.

    In backend/src/tuition/tuition.controller.ts:

    Change the import line from `import { AdminGuard } from '../auth/auth.guard'` to `import { TuitionGuard } from '../auth/auth.guard'`. Change the class-level decorator from `@UseGuards(AdminGuard)` to `@UseGuards(TuitionGuard)`. No other changes to the controller.
  </action>
  <verify>
    <automated>cd J:/sources/katie-english/backend && npx tsc --noEmit 2>&1 | grep -v "node_modules" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - backend/src/auth/auth.guard.ts contains the string "TuitionGuard" as a class declaration
    - TuitionGuard condition is `payload.role !== 'ADMIN' && payload.role !== 'TEACHER'` (both checks combined with &&)
    - backend/src/auth/auth.module.ts providers and exports arrays both contain TuitionGuard
    - backend/src/tuition/tuition.controller.ts imports TuitionGuard (not AdminGuard)
    - backend/src/tuition/tuition.controller.ts has @UseGuards(TuitionGuard) at class level
    - npx tsc --noEmit exits 0 with no errors in tuition or auth files
  </acceptance_criteria>
  <done>TuitionGuard accepts ADMIN and TEACHER; tuition controller uses TuitionGuard; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Create teacher-tuition-api.ts and update teacher tuition page</name>
  <files>
    frontend/lib/teacher-tuition-api.ts,
    frontend/app/teacher/tuition/page.tsx
  </files>
  <read_first>
    - frontend/lib/auth.ts — authHeaders() function: reads localStorage 'token' (teacher JWT set by login flow); returns { Authorization: 'Bearer {token}', 'Content-Type': 'application/json' }. This is the correct function for teacher API calls.
    - frontend/lib/admin-portal-api.ts — read lines 150-253 (the Tuition section): copy the TuitionConfig, TuitionRecord, TuitionReportItem, CreateTuitionConfigInput, GenerateRecordsInput, RecordPaymentInput, SendNotificationsInput, SendNotificationsResult interfaces and the 6 tuition API functions exactly — only the req() helper changes (authHeaders instead of adminAuthHeaders).
    - frontend/app/teacher/tuition/page.tsx — read full file; identify every import from '../../admin/tuition/_components/*' (TuitionConfigForm, GenerateRecordsModal, TuitionReportTable) and every import from '@/lib/admin-api' (getClasses, ClassItem). The tuition API calls currently come from admin-portal-api.ts via the shared req() that uses adminAuthHeaders — these must be replaced with teacher-tuition-api.ts equivalents.
  </read_first>
  <action>
    Create frontend/lib/teacher-tuition-api.ts as a new file:

    Import authHeaders from './auth'. Define API_URL as `const API_URL = process.env.NEXT_PUBLIC_API_URL ?? 'http://localhost:3000'`.

    Define a local parseApiError function (identical to admin-portal-api.ts implementation).

    Define a local req helper that uses authHeaders() instead of adminAuthHeaders(). The function signature: `async function req<T>(path: string, options?: RequestInit): Promise<T>`.

    Re-export all tuition-related interfaces from admin-portal-api.ts using re-export syntax: `export type { TuitionConfig, TuitionRecord, TuitionReportItem, CreateTuitionConfigInput, GenerateRecordsInput, RecordPaymentInput, SendNotificationsInput, SendNotificationsResult } from './admin-portal-api'`.

    Then re-implement the 6 tuition API functions with the local req() (which uses authHeaders): getTuitionConfig, updateTuitionConfig, createTuitionRecords, recordTuitionPayment, sendTuitionNotifications, getTuitionReport. The function bodies are identical to admin-portal-api.ts — only the req() call uses the local teacher-authed version.

    Update frontend/app/teacher/tuition/page.tsx:

    The page currently imports getClasses and ClassItem from '@/lib/admin-api' (correct — admin-api uses authHeaders). Do NOT change those imports.

    The tuition components (TuitionConfigForm, GenerateRecordsModal, TuitionReportTable) are imported from '../../admin/tuition/_components/*' — these are shared UI components with no auth logic; keep those imports unchanged.

    Identify if the page directly calls any tuition API functions (getTuitionConfig, updateTuitionConfig, createTuitionRecords, etc.) directly from admin-portal-api. If any such direct calls exist in teacher/tuition/page.tsx, update those import lines to reference '@/lib/teacher-tuition-api' instead. The child components (TuitionConfigForm, GenerateRecordsModal) internally import from admin-portal-api — those files will be updated in plan 15-06 via onSaved callbacks that don't expose auth concerns; accept this as-is for the teacher page itself.

    Add a comment at the top of teacher/tuition/page.tsx (after 'use client'): `// Teacher tuition: uses authHeaders() (teacher JWT) via teacher-tuition-api for all direct API calls`.
  </action>
  <verify>
    <automated>cd J:/sources/katie-english/frontend && npx tsc --noEmit 2>&1 | grep -v "node_modules" | grep -i "teacher-tuition\|teacher/tuition" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - frontend/lib/teacher-tuition-api.ts exists
    - teacher-tuition-api.ts imports authHeaders from './auth' (not adminAuthHeaders from './admin-auth')
    - teacher-tuition-api.ts defines getTuitionConfig, updateTuitionConfig, createTuitionRecords, recordTuitionPayment, sendTuitionNotifications, getTuitionReport functions
    - teacher-tuition-api.ts local req() uses authHeaders() not adminAuthHeaders()
    - frontend/app/teacher/tuition/page.tsx does not import adminAuthHeaders (grep for 'adminAuthHeaders' in this file returns 0 matches)
    - frontend/app/teacher/tuition/page.tsx does not import from 'admin-portal-api' directly (grep for 'admin-portal-api' in teacher/tuition/page.tsx returns 0 matches)
    - npx tsc --noEmit exits 0 for frontend
  </acceptance_criteria>
  <done>teacher-tuition-api.ts uses teacher JWT; teacher/tuition/page.tsx free of adminAuthHeaders dependency; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Teacher → /admin/tuition/* | Teacher JWT crosses into admin-namespaced endpoints; TuitionGuard validates role is ADMIN or TEACHER |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-14 | Elevation of Privilege | TuitionGuard | mitigate | Explicitly check role is ADMIN or TEACHER; any other role (STUDENT, unknown) gets ForbiddenException('Admins and teachers only') |
| T-15-15 | Spoofing | teacher-tuition-api.ts | accept | Uses same JWT verification as AuthGuard/TeacherGuard; token validated in backend by TokenService |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | No new packages — guard is pure NestJS code; teacher-tuition-api.ts uses existing fetch and auth.ts |
</threat_model>

<verification>
After all tasks:
- grep -c "TuitionGuard" backend/src/auth/auth.guard.ts returns >= 1
- grep "TuitionGuard" backend/src/tuition/tuition.controller.ts returns @UseGuards(TuitionGuard)
- grep -c "adminAuthHeaders" frontend/app/teacher/tuition/page.tsx returns 0
- frontend/lib/teacher-tuition-api.ts exists and imports authHeaders from './auth'
- cd backend && npx tsc --noEmit exits 0
- cd frontend && npx tsc --noEmit exits 0
</verification>

<success_criteria>
1. TuitionGuard class exists in auth.guard.ts with condition: payload.role !== 'ADMIN' && payload.role !== 'TEACHER'
2. tuition.controller.ts uses @UseGuards(TuitionGuard) at class level
3. teacher-tuition-api.ts exists with all 6 tuition API functions using teacher JWT
4. teacher/tuition/page.tsx has zero adminAuthHeaders references
5. Both backend and frontend TypeScript compile clean
</success_criteria>

<output>
Create .planning/phases/15-tuition-management/15-05-SUMMARY.md when done.

## Artifacts this phase produces

### New Files
- `backend/src/auth/auth.guard.ts` — TuitionGuard class appended (accepts ADMIN | TEACHER; rejects others with 403)
- `frontend/lib/teacher-tuition-api.ts` — Mirror of admin tuition API functions using authHeaders() (teacher JWT)

### Updated Files
- `backend/src/auth/auth.module.ts` — TuitionGuard added to providers and exports arrays
- `backend/src/tuition/tuition.controller.ts` — @UseGuards switched from AdminGuard to TuitionGuard
- `frontend/app/teacher/tuition/page.tsx` — Removed adminAuthHeaders dependency; comment added
</output>
