---
phase: "15"
plan: "06"
type: execute
wave: 4
depends_on:
  - "15-04"
files_modified:
  - frontend/app/admin/tuition/_components/TuitionReportTable.tsx
  - frontend/app/admin/tuition/page.tsx
autonomous: true
requirements:
  - TUITION-05
  - TUITION-06
gap_closure: true

must_haves:
  truths:
    - "Admin can open PaymentRecordDialog from a row in the tuition report table and mark a record as PAID"
    - "Admin can select one or more records in the tuition report table and send ZNS notifications to those records via ZaloSendModal"
    - "ZaloSendModal Send button is enabled when at least one record is selected"
    - "TuitionReportTable emits onPaymentRecord(recordId, studentName, totalAmount) and onSelectionChange(selectedIds) callbacks"
  artifacts:
    - path: "frontend/app/admin/tuition/_components/TuitionReportTable.tsx"
      provides: "Row-level checkboxes for ZNS selection + Mark Paid button per row; emits onSelectionChange and onPaymentRecord callbacks"
      contains: "onSelectionChange"
    - path: "frontend/app/admin/tuition/page.tsx"
      provides: "PaymentRecordDialog mounted and wired to selectedRecord state; ZaloSendModal receives real selectedIds from table"
      contains: "PaymentRecordDialog"
  key_links:
    - from: "admin/tuition/page.tsx"
      to: "PaymentRecordDialog"
      via: "import + open state"
      pattern: "PaymentRecordDialog"
    - from: "admin/tuition/page.tsx"
      to: "TuitionReportTable"
      via: "onPaymentRecord + onSelectionChange props"
      pattern: "onSelectionChange"
    - from: "ZaloSendModal in tab 2"
      to: "selectedRecordIds state"
      via: "recordIds={selectedRecordIds}"
      pattern: "selectedRecordIds"
---

<objective>
Close Gap 2 (ZNS permanently disabled) and Gap 3 (PaymentRecordDialog orphaned). Extend TuitionReportTable with row checkboxes and a "Mark Paid" button per row. Wire the callbacks into admin/tuition/page.tsx to mount PaymentRecordDialog and pass selected record IDs to ZaloSendModal.

Purpose: TUITION-05 (payment recording) and TUITION-06 (ZNS send) are fully implemented in backend and components but unreachable from the UI. This plan connects the existing pieces.

Output: 2 files modified — TuitionReportTable gains selection + action columns; admin page gains PaymentRecordDialog mount + real recordIds.
</objective>

<execution_context>
@J:/sources/katie-english/.claude/get-shit-done/workflows/execute-plan.md
@J:/sources/katie-english/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@J:/sources/katie-english/.planning/ROADMAP.md
@J:/sources/katie-english/.planning/phases/15-tuition-management/15-CONTEXT.md
@J:/sources/katie-english/.planning/phases/15-tuition-management/15-VERIFICATION.md
@J:/sources/katie-english/frontend/app/admin/tuition/_components/TuitionReportTable.tsx
@J:/sources/katie-english/frontend/app/admin/tuition/_components/PaymentRecordDialog.tsx
@J:/sources/katie-english/frontend/app/admin/tuition/_components/ZaloSendModal.tsx
@J:/sources/katie-english/frontend/app/admin/tuition/page.tsx
@J:/sources/katie-english/frontend/lib/admin-portal-api.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add row selection checkboxes and Mark Paid button to TuitionReportTable</name>
  <files>frontend/app/admin/tuition/_components/TuitionReportTable.tsx</files>
  <read_first>
    - frontend/app/admin/tuition/_components/TuitionReportTable.tsx — read full file; understand the COLUMNS array (7 columns), the TableShell/TableRow pattern, the rows state (TuitionReportItem[]), and the existing props signature (classId, month, year). The component must gain two new optional callback props and two new columns.
    - frontend/app/admin/tuition/_components/PaymentRecordDialog.tsx — read the props interface: open (boolean), recordId (number), studentName (string), totalAmount (number), onClose (() => void), onSaved (() => void). These are the values the onPaymentRecord callback must supply.
    - frontend/lib/admin-portal-api.ts — TuitionReportItem interface at lines 178-190: fields are id (number), studentId, studentName, tuitionAmount, bookFee, totalAmount, dueDate, paidAt, status. The row.id is what goes into selectedIds.
  </read_first>
  <action>
    Extend the TuitionReportTable component props to add two optional callbacks:
    - onSelectionChange?: (selectedIds: number[]) => void
    - onPaymentRecord?: (recordId: number, studentName: string, totalAmount: number) => void

    Add state: selectedIds (number[], default []).

    When rows change (inside fetchReport after setRows), reset selectedIds to []. This prevents stale selections after a filter change.

    Add a toggleSelect(id: number) function: if id is in selectedIds, remove it; otherwise add it. After update, call onSelectionChange?.(newIds).

    Add a selectAll/deselectAll checkbox in the table header: checked when selectedIds.length === rows.length && rows.length > 0; indeterminate when selectedIds.length > 0 && selectedIds.length < rows.length. Toggling it selects all row IDs or clears all.

    Update the COLUMNS array to add two columns at the start and end:
    - First column (before Học sinh): a checkbox column with empty label, width '0.4fr'
    - Last column (after Ngày đóng): an action column with empty label, width '0.8fr'

    In each TableRow, add:
    - First cell: MUI Checkbox from '@mui/material/Checkbox'; checked={selectedIds.includes(row.id)}; onChange={() => toggleSelect(row.id)}; size="small". Only render this checkbox if row.status !== 'PAID' (already paid records cannot be re-paid or re-notified). For PAID rows, render an empty cell.
    - Last cell: if row.status !== 'PAID', render a MUI Button with variant="outlined" size="small" label "Ghi nhận đóng" that calls onPaymentRecord?.(row.id, row.studentName, row.totalAmount). For PAID rows, render null or an empty span.

    Import Checkbox from '@mui/material/Checkbox'. No other new packages.

    The StatusBadge component and all existing columns remain unchanged.
  </action>
  <verify>
    <automated>cd J:/sources/katie-english/frontend && npx tsc --noEmit 2>&1 | grep -v "node_modules" | grep "TuitionReportTable\|tuition" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - TuitionReportTable.tsx exports onSelectionChange and onPaymentRecord as optional props in the function signature
    - TuitionReportTable.tsx has state: selectedIds (useState<number[]>([]))
    - COLUMNS array has 9 entries (was 7; added checkbox column + action column)
    - Each non-PAID TableRow first cell renders MUI Checkbox with checked={selectedIds.includes(row.id)}
    - Each non-PAID TableRow last cell renders a Button that calls onPaymentRecord?.(row.id, row.studentName, row.totalAmount)
    - Checkbox import is from '@mui/material/Checkbox'
    - npx tsc --noEmit exits 0 for TuitionReportTable.tsx
  </acceptance_criteria>
  <done>TuitionReportTable rows have checkboxes (non-PAID) and Mark Paid buttons (non-PAID); onSelectionChange and onPaymentRecord callbacks wired; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Mount PaymentRecordDialog and wire ZaloSendModal with real record IDs in admin page</name>
  <files>frontend/app/admin/tuition/page.tsx</files>
  <read_first>
    - frontend/app/admin/tuition/page.tsx — read full file; understand existing state (generateOpen, zaloOpen, selectedClassId, reportMonth, reportYear) and the tab 2 (ZNS) and tab 3 (Báo cáo) content. Line 152: ZaloSendModal currently receives recordIds={[]}. Tab 3 renders TuitionReportTable without onSelectionChange or onPaymentRecord props.
    - frontend/app/admin/tuition/_components/PaymentRecordDialog.tsx — props: open, recordId, studentName, totalAmount, onClose, onSaved. All required.
    - frontend/app/admin/tuition/_components/TuitionReportTable.tsx — new props after Task 1: onSelectionChange?: (ids: number[]) => void; onPaymentRecord?: (recordId: number, studentName: string, totalAmount: number) => void.
    - frontend/app/admin/tuition/_components/ZaloSendModal.tsx — line 62: disabled={loading || recordIds.length === 0}. Receives recordIds: number[].
  </read_first>
  <action>
    In frontend/app/admin/tuition/page.tsx:

    Add import: `import PaymentRecordDialog from './_components/PaymentRecordDialog'`

    Add three new state variables (after the existing zaloOpen state):
    - selectedRecordIds: number[], initialized to []  (state: useState<number[]>([]))
    - paymentDialogOpen: boolean, initialized to false  (state: useState(false))
    - selectedRecord: { id: number; studentName: string; totalAmount: number } | null, initialized to null  (state: useState<...>(null))

    Add a handler: function handlePaymentRecord(recordId: number, studentName: string, totalAmount: number) { setSelectedRecord({ id: recordId, studentName, totalAmount }); setPaymentDialogOpen(true); }

    In tab 2 (the "Thông báo ZNS" tab content, activeTab === 2):
    - Change ZaloSendModal recordIds prop from `recordIds={[]}` to `recordIds={selectedRecordIds}`
    - Add a Typography note below the existing description text (if selectedRecordIds.length === 0): `Chọn phiếu thu từ tab Báo cáo để gửi thông báo ZNS.` shown as variant="body2" color="text.secondary". If selectedRecordIds.length > 0: show `Đã chọn {selectedRecordIds.length} phiếu thu.` in the same style.

    In tab 3 (Báo cáo, activeTab === 3):
    - Pass two new props to TuitionReportTable: onSelectionChange={(ids) => setSelectedRecordIds(ids)} and onPaymentRecord={handlePaymentRecord}
    - Keep all existing props (classId, month, year) unchanged

    After the ZaloSendModal JSX (anywhere inside the return, sibling to other Dialog components), mount PaymentRecordDialog:
    - open={paymentDialogOpen}
    - recordId={selectedRecord?.id ?? 0}
    - studentName={selectedRecord?.studentName ?? ''}
    - totalAmount={selectedRecord?.totalAmount ?? 0}
    - onClose={() => setPaymentDialogOpen(false)}
    - onSaved={() => { setPaymentDialogOpen(false); setSelectedRecordIds([]); }}

    The onSaved callback resets selectedRecordIds to [] because the paid record is no longer selectable and the table will refetch on next filter interaction.
  </action>
  <verify>
    <automated>cd J:/sources/katie-english/frontend && npm run build 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - admin/tuition/page.tsx imports PaymentRecordDialog from './_components/PaymentRecordDialog'
    - admin/tuition/page.tsx has state: selectedRecordIds (useState<number[]>([]))
    - admin/tuition/page.tsx has state: paymentDialogOpen (useState(false))
    - admin/tuition/page.tsx has state: selectedRecord (useState<{id:number;studentName:string;totalAmount:number}|null>(null))
    - ZaloSendModal in tab 2 receives recordIds={selectedRecordIds} (grep for 'recordIds={selectedRecordIds}' returns 1 match)
    - TuitionReportTable in tab 3 has onSelectionChange and onPaymentRecord props
    - PaymentRecordDialog is mounted in the JSX return with open={paymentDialogOpen}
    - npm run build exits 0
  </acceptance_criteria>
  <done>PaymentRecordDialog mounted and wired; ZaloSendModal receives real selectedRecordIds; npm run build passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Gap closure for Phase 15:
    - Gap 1 (teacher role): TuitionGuard accepts ADMIN + TEACHER; teacher-tuition-api.ts uses teacher JWT
    - Gap 2 (ZNS): TuitionReportTable rows have checkboxes; selecting rows populates selectedRecordIds; ZaloSendModal Send button becomes enabled
    - Gap 3 (PaymentRecordDialog): "Ghi nhận đóng" button per non-PAID row opens PaymentRecordDialog with correct student data
  </what-built>
  <how-to-verify>
    1. Start backend: cd backend && npm run start:dev
    2. Start frontend: cd frontend && npm run dev
    3. LOGIN AS TEACHER at http://localhost:3001/login (teacher credentials)
    4. Navigate to /teacher/tuition — verify page loads and shows teacher's classes (no 403 error)
    5. Teacher test — Tab Cấu hình: verify tuition config form loads for a class (HTTP 200, not 403)
    6. Teacher test — Tab Báo cáo: verify report table loads data
    7. LOGIN AS ADMIN at http://localhost:3001/admin/login
    8. Navigate to /admin/tuition — select a class — Tab Báo cáo
    9. Verify report rows have checkboxes on left and "Ghi nhận đóng" buttons on right (for non-PAID rows)
    10. Check at least one checkbox — navigate to Tab Thông báo ZNS — verify "Gửi thông báo" button is now ENABLED (was disabled before) and shows "Đã chọn N phiếu thu."
    11. Click "Ghi nhận đóng" on a PENDING row — verify PaymentRecordDialog opens with correct student name and total amount shown in dialog title
    12. Fill in paidAt date and paidBy name — click "Xác nhận đã đóng" — verify success toast appears
    13. Navigate back to Tab Báo cáo — verify the record now shows PAID status (green badge) and "Ghi nhận đóng" button is gone for that row
  </how-to-verify>
  <resume-signal>Type "approved" if all 13 steps pass. Describe any failures with the step number for targeted fixes.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin → PATCH /admin/tuition/records/:id | Admin/Teacher submits payment record; TuitionGuard validates JWT role |
| Admin → POST /admin/tuition/notify | Admin submits selected record IDs; backend validates record ownership (classId) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-16 | Tampering | selectedRecordIds in admin page | accept | IDs come from the report table which is populated by authenticated API response; backend re-validates record IDs belong to the user's accessible class |
| T-15-17 | Tampering | PaymentRecordDialog paidAt/paidBy inputs | accept | Free-text fields; no injection risk (JSON-serialized); backend stores as string/datetime, no eval |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | No new packages — Checkbox is from existing @mui/material already in project |
</threat_model>

<verification>
After all tasks:
- grep -c "PaymentRecordDialog" frontend/app/admin/tuition/page.tsx returns >= 2 (import + JSX usage)
- grep -c "selectedRecordIds" frontend/app/admin/tuition/page.tsx returns >= 3 (state init + ZaloSendModal prop + TuitionReportTable prop)
- grep -c "onSelectionChange" frontend/app/admin/tuition/_components/TuitionReportTable.tsx returns >= 2 (prop def + call)
- grep -c "onPaymentRecord" frontend/app/admin/tuition/_components/TuitionReportTable.tsx returns >= 2 (prop def + call)
- npm run build exits 0 in frontend
- Human verify checkpoint passed
</verification>

<success_criteria>
1. TuitionReportTable rows have Checkbox (non-PAID) and "Ghi nhận đóng" Button (non-PAID)
2. onSelectionChange and onPaymentRecord are optional props on TuitionReportTable
3. admin/tuition/page.tsx selectedRecordIds state wired to both TuitionReportTable and ZaloSendModal
4. PaymentRecordDialog mounted in admin page with all required props
5. ZaloSendModal Send button is enabled when selectedRecordIds.length > 0
6. npm run build exits 0
7. Human verification: teacher accesses /teacher/tuition (no 403), admin can mark paid + send ZNS
</success_criteria>

<output>
Create .planning/phases/15-tuition-management/15-06-SUMMARY.md when done.

## Artifacts this phase produces

### Updated Files
- `frontend/app/admin/tuition/_components/TuitionReportTable.tsx` — Added onSelectionChange/onPaymentRecord callbacks, row checkboxes (non-PAID), "Ghi nhận đóng" button per non-PAID row, header select-all checkbox
- `frontend/app/admin/tuition/page.tsx` — PaymentRecordDialog imported and mounted; selectedRecordIds state; ZaloSendModal wired with real IDs; TuitionReportTable receives onSelectionChange + onPaymentRecord
</output>
