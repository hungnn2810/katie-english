---
phase: "15"
plan: "02"
type: execute
wave: 2
depends_on:
  - "15-01"
files_modified:
  - backend/src/tuition/tuition.module.ts
  - backend/src/tuition/tuition.controller.ts
  - backend/src/tuition/tuition.service.ts
  - backend/src/tuition/tuition.repository.ts
  - backend/src/tuition/tuition.dto.ts
  - backend/src/tuition/zalo-zns.service.ts
  - backend/src/tuition/session-counter.util.ts
  - backend/src/tuition/phone-formatter.util.ts
  - backend/src/tuition/tuition.service.spec.ts
  - backend/src/tuition/session-counter.util.spec.ts
  - backend/src/tuition/phone-formatter.util.spec.ts
  - backend/src/app.module.ts
  - backend/.env.example
autonomous: true
requirements:
  - TUITION-01
  - TUITION-02
  - TUITION-03
  - TUITION-04
  - TUITION-05
  - TUITION-06
  - TUITION-07

must_haves:
  truths:
    - "PUT /admin/tuition/config/:classId upserts TuitionConfig and returns 200"
    - "POST /admin/tuition/records/generate creates TuitionRecord per student in class using session-count formula"
    - "PATCH /admin/tuition/records/:id marks record PAID with paidAt and paidBy"
    - "POST /admin/tuition/notify sends Zalo ZNS to parent phone numbers and logs to TuitionNotificationLog"
    - "GET /admin/tuition/report returns TuitionRecord[] with computed OVERDUE status (dueDate < now when status != PAID)"
    - "GET /admin/tuition/config/:classId returns existing TuitionConfig or 404"
    - "countSessionsInMonth correctly counts dayOfWeek occurrences in a month (per D-03 LOCKED)"
    - "formatPhoneForZalo transforms 0912345678 to 84912345678 (idempotent)"
    - "All endpoints protected by AdminGuard (role ADMIN or TEACHER per D-06 LOCKED)"
    - "npm run test -- --testPathPattern=tuition passes"
  artifacts:
    - path: "backend/src/tuition/tuition.module.ts"
      provides: "TuitionModule with all providers registered"
      contains: "TuitionModule"
    - path: "backend/src/tuition/tuition.controller.ts"
      provides: "HTTP endpoints for all tuition operations"
      contains: "TuitionController"
    - path: "backend/src/tuition/tuition.service.ts"
      provides: "Business logic for config CRUD, record generation, payment recording, report, ZNS send"
      contains: "TuitionService"
    - path: "backend/src/tuition/tuition.repository.ts"
      provides: "Prisma query layer for all tuition models"
      contains: "TuitionRepository"
    - path: "backend/src/tuition/zalo-zns.service.ts"
      provides: "HTTP client to Zalo ZNS API via axios (per D-01 LOCKED)"
      contains: "ZaloZnsService"
    - path: "backend/src/tuition/session-counter.util.ts"
      provides: "countSessionsInMonth function (per D-03 LOCKED)"
      contains: "countSessionsInMonth"
    - path: "backend/src/tuition/phone-formatter.util.ts"
      provides: "formatPhoneForZalo function for Vietnamese phone transformation (per D-01 LOCKED)"
      contains: "formatPhoneForZalo"
  key_links:
    - from: "tuition.controller.ts"
      to: "tuition.service.ts"
      via: "constructor injection"
      pattern: "constructor.*TuitionService"
    - from: "tuition.service.ts"
      to: "tuition.repository.ts"
      via: "constructor injection"
      pattern: "constructor.*TuitionRepository"
    - from: "tuition.service.ts"
      to: "zalo-zns.service.ts"
      via: "constructor injection"
      pattern: "constructor.*ZaloZnsService"
    - from: "tuition.service.ts"
      to: "session-counter.util.ts"
      via: "import + call"
      pattern: "countSessionsInMonth"
    - from: "app.module.ts"
      to: "tuition.module.ts"
      via: "imports array"
      pattern: "TuitionModule"
---

<objective>
Build the complete NestJS TuitionModule with service, repository, controller, DTOs, ZaloZnsService, and two utility functions. Register the module in app.module.ts. Write unit tests for session-counter, phone-formatter, and service logic. Add Zalo env vars to .env.example.

Purpose: Delivers all seven TUITION-XX backend capabilities in one wave. Frontend plans 03 and 04 depend on these endpoints being available.

Output: 8 source files + 3 spec files + app.module.ts update + .env.example update.
</objective>

<execution_context>
@J:/sources/katie-english/.claude/get-shit-done/workflows/execute-plan.md
@J:/sources/katie-english/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@J:/sources/katie-english/.planning/ROADMAP.md
@J:/sources/katie-english/.planning/phases/15-tuition-management/15-CONTEXT.md
@J:/sources/katie-english/.planning/phases/15-tuition-management/15-RESEARCH.md
@J:/sources/katie-english/.planning/phases/15-tuition-management/15-PATTERNS.md
@J:/sources/katie-english/.planning/phases/15-tuition-management/15-01-SUMMARY.md
@J:/sources/katie-english/backend/src/admin/admin-classes.controller.ts
@J:/sources/katie-english/backend/src/homework/homework.module.ts
@J:/sources/katie-english/backend/src/admin/admin.module.ts
@J:/sources/katie-english/backend/src/app.module.ts
@J:/sources/katie-english/backend/.env.example
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Utility functions — session-counter.util.ts + phone-formatter.util.ts + specs</name>
  <files>
    backend/src/tuition/session-counter.util.ts,
    backend/src/tuition/phone-formatter.util.ts,
    backend/src/tuition/session-counter.util.spec.ts,
    backend/src/tuition/phone-formatter.util.spec.ts
  </files>
  <read_first>
    - .planning/phases/15-tuition-management/15-RESEARCH.md — Pattern 2 (session counting logic) and Pattern 3 (phone formatter logic)
    - .planning/phases/15-tuition-management/15-PATTERNS.md — session-counter.util.ts and phone-formatter.util.ts blocks with exact implementation
    - .planning/phases/15-tuition-management/15-CONTEXT.md — D-03 LOCKED (formula), D-01 LOCKED (phone format: 0xxx → 84xxx)
    - backend/prisma/schema.prisma — confirm Class.scheduleSlots is Json type to understand the ScheduleSlot interface
  </read_first>
  <behavior>
    - countSessionsInMonth([{dayOfWeek:1},{dayOfWeek:3}], 6, 2026) returns 8 (4 Mon + 4 Wed in June 2026)
    - countSessionsInMonth([], 6, 2026) returns 0
    - countSessionsInMonth([{dayOfWeek:0}], 2, 2026) returns 4 (4 Sundays in Feb 2026: 1,8,15,22)
    - countSessionsInMonth([{dayOfWeek:1},{dayOfWeek:1}], 6, 2026) returns 4 (deduplication via Set — same day twice counts once)
    - formatPhoneForZalo('0912345678') returns '84912345678'
    - formatPhoneForZalo('84912345678') returns '84912345678' (idempotent)
    - formatPhoneForZalo('0 912 345 678') returns '84912345678' (strips spaces)
    - formatPhoneForZalo('912345678') returns '84912345678' (no leading 0, no 84 prefix)
  </behavior>
  <action>
    Write tests first in spec files, then implement.

    session-counter.util.ts: Export function countSessionsInMonth(scheduleSlots: ScheduleSlot[], month: number, year: number): number. ScheduleSlot interface has dayOfWeek (0-6), startTime (string), endTime (string). Use Set(scheduleSlots.map(s => s.dayOfWeek)) to deduplicate. Iterate from first to last day of month using new Date(year, month-1, 1) and new Date(year, month, 0). For each day, check d.getDay() against uniqueDays Set. Returns 0 for empty/null scheduleSlots.

    phone-formatter.util.ts: Export function formatPhoneForZalo(phoneNumber: string): string. Strip spaces, hyphens, parentheses with replace(/[\s\-()]/g, ''). If starts with '0', replace with '84'. If does not start with '84', prepend '84'. Return result. This function must be idempotent (calling twice on '84xxx' returns same '84xxx').

    spec files: Use Jest describe/it blocks. No external dependencies. Test all behavior cases listed above. Run with: cd backend && npm run test -- session-counter.util.spec.ts phone-formatter.util.spec.ts
  </action>
  <verify>
    <automated>cd J:/sources/katie-english/backend && npm run test -- --testPathPattern="session-counter|phone-formatter" --passWithNoTests 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - session-counter.util.spec.ts passes: countSessionsInMonth([{dayOfWeek:1},{dayOfWeek:3}], 6, 2026) === 8
    - session-counter.util.spec.ts passes: countSessionsInMonth([], 6, 2026) === 0
    - session-counter.util.spec.ts passes: deduplication test (same dayOfWeek twice counts once)
    - phone-formatter.util.spec.ts passes: formatPhoneForZalo('0912345678') === '84912345678'
    - phone-formatter.util.spec.ts passes: formatPhoneForZalo('84912345678') === '84912345678' (idempotent)
    - Both spec files show PASS in npm test output
  </acceptance_criteria>
  <done>Both utilities implemented and tested. All spec cases green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: TuitionRepository, DTOs, ZaloZnsService, TuitionService, TuitionController, TuitionModule, app.module.ts + env update</name>
  <files>
    backend/src/tuition/tuition.repository.ts,
    backend/src/tuition/tuition.dto.ts,
    backend/src/tuition/zalo-zns.service.ts,
    backend/src/tuition/tuition.service.ts,
    backend/src/tuition/tuition.controller.ts,
    backend/src/tuition/tuition.module.ts,
    backend/src/app.module.ts,
    backend/.env.example
  </files>
  <read_first>
    - backend/src/admin/admin-classes.controller.ts — controller structure: imports, @UseGuards(AdminGuard), @Controller('admin/...'), ParseIntPipe pattern
    - backend/src/admin/admin.module.ts — how module registers providers and controllers
    - backend/src/homework/homework.module.ts — minimal module pattern (imports PrismaModule, AuthModule)
    - backend/src/app.module.ts — how to import new module (look for existing module imports array)
    - .planning/phases/15-tuition-management/15-RESEARCH.md — full code examples for all service methods, controller endpoints, ZaloZnsService
    - .planning/phases/15-tuition-management/15-PATTERNS.md — all pattern assignments with exact import paths and code structure
    - backend/.env.example — current env var list to know where to append Zalo vars
    - backend/prisma/schema.prisma — verify TuitionConfig, TuitionRecord, TuitionNotificationLog models exist (Task 1 of Plan 01)
  </read_first>
  <action>
    Create backend/src/tuition/ directory (mkdir if needed). Create all files in this order to respect import dependencies.

    1. tuition.dto.ts: Export four DTO classes:
       - CreateTuitionConfigDto: pricePerSession (number), bookFee (number | null | undefined), dueDayOfMonth (number)
       - GenerateRecordsDto: classId (number), month (number 1-12), year (number)
       - RecordPaymentDto: paidAt (string — ISO date string from frontend), paidBy (string)
       - SendNotificationsDto: recordIds (number[])
       No class-validator decorators needed for MVP (AdminGuard handles auth; input validated in service).

    2. tuition.repository.ts: Injectable class. Constructor injects PrismaService. Methods:
       - findConfig(classId: number): prisma.tuitionConfig.findUnique({ where: { classId } })
       - upsertConfig(classId, dto): prisma.tuitionConfig.upsert({ where: { classId }, update: dto fields, create: { classId, ...dto fields } })
       - findStudentsByClass(classId): prisma.student.findMany({ where: { classId } })
       - countRecords(classId, month, year): prisma.tuitionRecord.count({ where: { classId, month, year } })
       - createRecord(data): prisma.tuitionRecord.create({ data })
       - findRecordsByReport(classId, month, year): prisma.tuitionRecord.findMany({ where: { classId, month, year }, include: { student: true, class: true }, orderBy: { createdAt: 'asc' } })
       - findRecordsByIds(ids): prisma.tuitionRecord.findMany({ where: { id: { in: ids } }, include: { student: { include: { parents: true } }, class: true } })
       - updateRecord(id, data): prisma.tuitionRecord.update({ where: { id }, data })
       - logNotification(data): prisma.tuitionNotificationLog.create({ data })

    3. zalo-zns.service.ts: Injectable. Uses axios (import axios from 'axios'). Private fields: logger = new Logger('ZaloZnsService'), reads process.env.ZALO_OA_ACCESS_TOKEN and process.env.ZALO_ZNS_TEMPLATE_ID. Method sendTemplate(payload: { phone: string, template_id: string, template_data: Record<string, any> }): Promise with axios.post to 'https://business.openapi.zalo.me/message/template', Authorization: Bearer ${this.accessToken} header, timeout: 10_000. Log success: logger.log with phone + status. Log error: logger.error with phone + message. Return response.data. Throw on axios error (re-throw for service to catch). Never log the access token value itself — log only "[REDACTED]" if needed.

    4. tuition.service.ts: Injectable. Constructor injects TuitionRepository and ZaloZnsService. Methods:
       - getConfig(classId): repo.findConfig(classId) — returns null if not found (controller handles 404)
       - createOrUpdateConfig(classId, dto): validate classId > 0 and pricePerSession > 0 (throw BadRequestException otherwise); repo.upsertConfig(classId, dto)
       - generateMonthlyRecords(classId, month, year): validate month 1-12, year > 2020 (throw BadRequestException); get config (throw NotFoundException if missing); get students (throw BadRequestException if empty); check countRecords > 0 (throw BadRequestException 'Records already exist'); call countSessionsInMonth(JSON.parse(config relation scheduleSlots), month, year) — NOTE: scheduleSlots on Class is Json type so must be JSON.parsed or cast; compute tuitionAmount = sessionCount * config.pricePerSession; bookFee = config.bookFee ?? 0; totalAmount = tuitionAmount + bookFee; dueDate = new Date(year, month - 1, config.dueDayOfMonth); batch create with Promise.all; return records
       - recordPayment(recordId, dto): repo.updateRecord(recordId, { status: 'PAID', paidAt: new Date(dto.paidAt), paidBy: dto.paidBy })
       - sendNotifications(recordIds): fetch records with findRecordsByIds; for each record iterate parents; call formatPhoneForZalo on parent.phoneNumber; call zaloZns.sendTemplate with template_data { student_name, amount (toLocaleString 'vi-VN'), due_date (toLocaleDateString 'vi-VN'), class_name, parent_name }; catch errors per parent and continue (partial send); repo.logNotification for each attempt; return { totalRecords, successCount, results }
       - getReport(classId, month, year, statuses?): fetch records via findRecordsByReport; compute effective status at query time: if record.status === 'PAID' → 'PAID', else if record.dueDate < new Date() → 'OVERDUE', else → 'PENDING'; filter by statuses if provided; return mapped array with studentName, amounts, dueDate, paidAt, status, daysOverdue

    5. tuition.controller.ts: @UseGuards(AdminGuard), @Controller('admin/tuition'). Endpoints:
       - GET config/:classId → getConfig → if null throw NotFoundException
       - PUT config/:classId → createOrUpdateConfig (per D-01 upsert pattern; returns 200)
       - POST records/generate → generateMonthlyRecords
       - PATCH records/:id → recordPayment (ParseIntPipe on :id)
       - POST notify → sendNotifications
       - GET report → getReport (Query params: classId ParseIntPipe, month ParseIntPipe, year ParseIntPipe, status? as comma-separated string parsed to array)

    6. tuition.module.ts: @Module({ imports: [PrismaModule, AuthModule], providers: [TuitionRepository, TuitionService, ZaloZnsService], controllers: [TuitionController] }) export class TuitionModule.

    7. app.module.ts: Read the file first. Add TuitionModule to the imports array. Import from './tuition/tuition.module'.

    8. backend/.env.example: Append two lines at the bottom of the file under a new comment section:
       ```
       # Zalo ZNS (Zalo Notification Service for tuition notifications)
       ZALO_OA_ACCESS_TOKEN=
       ZALO_ZNS_TEMPLATE_ID=
       ```

    Per D-06 (LOCKED): AdminGuard must accept both ADMIN and TEACHER roles — confirm AdminGuard already handles this by reading backend/src/auth/auth.guard.ts if needed.
    Per D-01 (LOCKED): Zalo ZNS via REST API to https://business.openapi.zalo.me/message/template, phone format 0xxx → 84xxx.
    Per D-03 (LOCKED): tuitionAmount = countSessionsInMonth(scheduleSlots) × pricePerSession.
  </action>
  <verify>
    <automated>cd J:/sources/katie-english/backend && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - npm run build exits 0 (no TypeScript errors)
    - backend/src/tuition/ directory exists with all 8 source files
    - tuition.controller.ts contains @UseGuards(AdminGuard) decorator
    - tuition.service.ts contains sendNotifications method with ZaloZnsService call
    - tuition.service.ts contains generateMonthlyRecords method calling countSessionsInMonth
    - app.module.ts imports array contains TuitionModule
    - backend/.env.example contains ZALO_OA_ACCESS_TOKEN= and ZALO_ZNS_TEMPLATE_ID=
    - GET /admin/tuition/config/:classId endpoint registered (visible in route list)
    - PUT /admin/tuition/config/:classId endpoint registered
    - POST /admin/tuition/records/generate endpoint registered
    - PATCH /admin/tuition/records/:id endpoint registered
    - POST /admin/tuition/notify endpoint registered
    - GET /admin/tuition/report endpoint registered
  </acceptance_criteria>
  <done>TuitionModule fully registered, backend builds clean, all 6 endpoints present, Zalo env vars documented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Service unit tests (tuition.service.spec.ts)</name>
  <files>backend/src/tuition/tuition.service.spec.ts</files>
  <read_first>
    - backend/src/tuition/tuition.service.ts — implementations to test (just written above)
    - backend/src/admin/admin-classes.service.spec.ts — Jest mock pattern for NestJS services
    - .planning/phases/15-tuition-management/15-RESEARCH.md — Validation Architecture section (test map for TUITION-01 through TUITION-07)
  </read_first>
  <behavior>
    - createOrUpdateConfig with pricePerSession=0 throws BadRequestException
    - createOrUpdateConfig with valid dto calls repo.upsertConfig
    - generateMonthlyRecords with month=13 throws BadRequestException
    - generateMonthlyRecords when no config found throws NotFoundException
    - generateMonthlyRecords when records already exist throws BadRequestException 'already exist'
    - generateMonthlyRecords creates records with totalAmount = tuitionAmount + bookFee
    - recordPayment calls repo.updateRecord with status PAID and paidAt as Date
    - getReport returns OVERDUE for record with dueDate in the past and status PENDING
    - getReport returns PAID for record with status PAID even if dueDate in the past
    - sendNotifications logs success/failure for each parent
  </behavior>
  <action>
    Create tuition.service.spec.ts. Use Jest and mock TuitionRepository and ZaloZnsService with jest.fn() stubs.

    Test createOrUpdateConfig: mock repo.upsertConfig, verify BadRequestException on pricePerSession=0.

    Test generateMonthlyRecords: mock repo.findConfig to return null → NotFoundException. Mock to return valid config but repo.countRecords returns 1 → BadRequestException. Mock complete success path with 2 students → verify Promise.all creates 2 records with correct amounts.

    Test recordPayment: mock repo.updateRecord, verify called with status 'PAID'.

    Test getReport: create two records, one with status PENDING and dueDate yesterday (expect OVERDUE), one with status PAID and dueDate yesterday (expect PAID).

    Test sendNotifications: mock repo.findRecordsByIds returning a record with one parent; mock zaloZns.sendTemplate returning { status: 0 }; verify repo.logNotification called with success: true.

    Run: cd backend && npm run test -- tuition.service.spec.ts
  </action>
  <verify>
    <automated>cd J:/sources/katie-english/backend && npm run test -- --testPathPattern="tuition.service.spec" --passWithNoTests 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - tuition.service.spec.ts contains at least 8 test cases (it/test blocks)
    - All test cases pass
    - Test covers OVERDUE status computation (dueDate < now when status != PAID)
    - Test covers BadRequestException on invalid input
    - npm test output shows PASS for tuition.service.spec.ts
  </acceptance_criteria>
  <done>Service spec covers all critical logic paths — all tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin/Teacher → API | All endpoints require valid JWT with ADMIN or TEACHER role (AdminGuard) |
| API → Zalo ZNS | Bearer token sent over HTTPS; token value never logged |
| Parent phone → Zalo | Phone number transformed from DB format to E.164-style before calling Zalo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-03 | Elevation of Privilege | TuitionController endpoints | mitigate | @UseGuards(AdminGuard) on controller class applies to all endpoints; STUDENT role rejected per D-06 |
| T-15-04 | Tampering | generateMonthlyRecords | mitigate | @@unique([studentId, classId, month, year]) at DB level + countRecords check in service prevents duplicate generation |
| T-15-05 | Information Disclosure | ZaloZnsService | mitigate | ZALO_OA_ACCESS_TOKEN read from process.env, never logged or returned to client; error messages sanitized before throwing |
| T-15-06 | Tampering | Phone number transformation | mitigate | formatPhoneForZalo idempotent + unit tested; invalid phone lengths cause Zalo API error logged to TuitionNotificationLog |
| T-15-07 | Tampering | SQL injection via query params | mitigate | All query params use ParseIntPipe (classId, month, year) — non-integers rejected with 400 before service call |
| T-15-08 | Tampering | OVERDUE status bypass | accept | paidBy field records who set status PAID; TuitionNotificationLog provides audit trail; no business critical enforcement needed for MVP |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | No new packages — axios and @nestjs/common already in project |
</threat_model>

<verification>
After all three tasks:
- npm run build exits 0 (TypeScript clean)
- npm run test -- --testPathPattern="tuition|session-counter|phone-formatter" all pass
- backend/src/tuition/ contains 8 source files + 3 spec files
- app.module.ts imports TuitionModule
- backend/.env.example contains ZALO_OA_ACCESS_TOKEN= and ZALO_ZNS_TEMPLATE_ID=
</verification>

<success_criteria>
1. All 6 tuition endpoints available (PUT config, GET config, POST generate, PATCH record, POST notify, GET report)
2. countSessionsInMonth correctly computes sessions from scheduleSlots JSON (tested)
3. formatPhoneForZalo transforms Vietnamese phone numbers correctly (tested)
4. OVERDUE status computed at query time (dueDate < now when status != PAID)
5. ZaloZnsService sends to Zalo ZNS API with Bearer token auth and logs all attempts
6. npm run build exits 0, all unit tests pass
</success_criteria>

<output>
Create .planning/phases/15-tuition-management/15-02-SUMMARY.md when done.

## Artifacts this phase produces

### New Files (backend/src/tuition/)
- `tuition.module.ts` — TuitionModule class (NestJS module)
- `tuition.controller.ts` — TuitionController class with 6 endpoints under /admin/tuition
- `tuition.service.ts` — TuitionService class (createOrUpdateConfig, generateMonthlyRecords, recordPayment, sendNotifications, getReport, getConfig)
- `tuition.repository.ts` — TuitionRepository class (Prisma query layer, 9 methods)
- `tuition.dto.ts` — CreateTuitionConfigDto, GenerateRecordsDto, RecordPaymentDto, SendNotificationsDto
- `zalo-zns.service.ts` — ZaloZnsService class (sendTemplate method, axios POST to Zalo ZNS API)
- `session-counter.util.ts` — countSessionsInMonth(scheduleSlots, month, year): number
- `phone-formatter.util.ts` — formatPhoneForZalo(phoneNumber: string): string
- `tuition.service.spec.ts` — Jest unit tests for TuitionService (10+ test cases)
- `session-counter.util.spec.ts` — Jest unit tests for countSessionsInMonth
- `phone-formatter.util.spec.ts` — Jest unit tests for formatPhoneForZalo

### Updated Files
- `backend/src/app.module.ts` — TuitionModule added to imports array
- `backend/.env.example` — ZALO_OA_ACCESS_TOKEN and ZALO_ZNS_TEMPLATE_ID added

### API Endpoints
- GET /admin/tuition/config/:classId — fetch TuitionConfig for a class
- PUT /admin/tuition/config/:classId — upsert TuitionConfig
- POST /admin/tuition/records/generate — generate monthly TuitionRecords for a class
- PATCH /admin/tuition/records/:id — mark TuitionRecord as PAID
- POST /admin/tuition/notify — send Zalo ZNS to parents for selected records
- GET /admin/tuition/report — report filtered by classId + month + year (+ optional status filter)
</output>
