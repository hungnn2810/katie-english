---
phase: "15"
plan: "03"
type: execute
wave: 3
depends_on:
  - "15-02"
files_modified:
  - frontend/app/admin/tuition/page.tsx
  - frontend/app/admin/tuition/_components/TuitionConfigForm.tsx
  - frontend/app/admin/tuition/_components/GenerateRecordsModal.tsx
  - frontend/app/admin/tuition/_components/PaymentRecordDialog.tsx
  - frontend/app/admin/tuition/_components/ZaloSendModal.tsx
  - frontend/app/teacher/tuition/page.tsx
  - frontend/lib/admin-portal-api.ts
autonomous: true
requirements:
  - TUITION-01
  - TUITION-03
  - TUITION-04
  - TUITION-05
  - TUITION-06

must_haves:
  truths:
    - "Admin can navigate to /admin/tuition and see a tuition management page"
    - "Tuition config form shows pricePerSession, bookFee (optional), and dueDayOfMonth fields for a selected class"
    - "Saving tuition config calls PUT /admin/tuition/config/:classId and shows success toast"
    - "Generate Records modal shows class + month + year fields; submitting calls POST /admin/tuition/records/generate"
    - "PaymentRecordDialog shows paidAt date and paidBy fields; submitting calls PATCH /admin/tuition/records/:id"
    - "ZaloSendModal confirms before sending; calls POST /admin/tuition/notify for selected records"
    - "Teacher portal /teacher/tuition page shows teacher's own classes in a class selector"
    - "API functions getTuitionConfig, createTuitionRecords, recordTuitionPayment, sendTuitionNotifications exist in admin-portal-api.ts"
  artifacts:
    - path: "frontend/app/admin/tuition/page.tsx"
      provides: "Admin tuition management page with tabs: Configuration, Generate Records, Record Payment, Send Notifications"
      contains: "TuitionConfigForm"
    - path: "frontend/app/admin/tuition/_components/TuitionConfigForm.tsx"
      provides: "Form to configure pricePerSession + bookFee + dueDayOfMonth per class (per TUITION-01, TUITION-03)"
      contains: "pricePerSession"
    - path: "frontend/app/admin/tuition/_components/GenerateRecordsModal.tsx"
      provides: "Modal to generate monthly tuition records (per TUITION-04)"
      contains: "month"
    - path: "frontend/app/admin/tuition/_components/PaymentRecordDialog.tsx"
      provides: "Dialog to mark tuition record as PAID (per TUITION-05)"
      contains: "paidAt"
    - path: "frontend/app/admin/tuition/_components/ZaloSendModal.tsx"
      provides: "Confirmation dialog to send Zalo ZNS notifications (per TUITION-06)"
      contains: "sendTuitionNotifications"
    - path: "frontend/app/teacher/tuition/page.tsx"
      provides: "Teacher-scoped tuition page (class selector + tabs for config/generate/report)"
      contains: "TeacherTuitionPage"
    - path: "frontend/lib/admin-portal-api.ts"
      provides: "API function wrappers for all tuition backend endpoints"
      contains: "getTuitionConfig"
  key_links:
    - from: "frontend/app/admin/tuition/page.tsx"
      to: "frontend/lib/admin-portal-api.ts"
      via: "import of getTuitionConfig, createTuitionRecords, etc."
      pattern: "admin-portal-api"
    - from: "frontend/app/admin/tuition/_components/TuitionConfigForm.tsx"
      to: "PUT /admin/tuition/config/:classId"
      via: "updateTuitionConfig API call"
      pattern: "PUT.*admin/tuition/config"
    - from: "frontend/app/teacher/tuition/page.tsx"
      to: "frontend/app/admin/tuition/_components"
      via: "shared component imports (reuse admin components)"
      pattern: "admin/tuition/_components"
---

<objective>
Build the admin-portal and teacher-portal tuition management UI. Admin sees a tabbed page with TuitionConfigForm, GenerateRecordsModal, PaymentRecordDialog, and ZaloSendModal. Teacher sees the same components scoped to their own classes. Add API wrapper functions to admin-portal-api.ts.

Purpose: Delivers TUITION-01, TUITION-03, TUITION-04, TUITION-05, TUITION-06 as interactive UI backed by the endpoints from Plan 02. Plan 04 (reporting) runs in parallel in Wave 3 and does not overlap in files.

Output: 5 components + 2 page files + admin-portal-api.ts additions.
</objective>

<execution_context>
@J:/sources/katie-english/.claude/get-shit-done/workflows/execute-plan.md
@J:/sources/katie-english/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@J:/sources/katie-english/.planning/ROADMAP.md
@J:/sources/katie-english/.planning/phases/15-tuition-management/15-CONTEXT.md
@J:/sources/katie-english/.planning/phases/15-tuition-management/15-RESEARCH.md
@J:/sources/katie-english/.planning/phases/15-tuition-management/15-PATTERNS.md
@J:/sources/katie-english/.planning/phases/15-tuition-management/15-02-SUMMARY.md
@J:/sources/katie-english/frontend/lib/admin-portal-api.ts
@J:/sources/katie-english/frontend/app/admin/classes/page.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add tuition API functions to admin-portal-api.ts</name>
  <files>frontend/lib/admin-portal-api.ts</files>
  <read_first>
    - frontend/lib/admin-portal-api.ts — read the full file to understand the req() helper pattern, existing type exports, and where to append without breaking existing code
    - .planning/phases/15-tuition-management/15-02-SUMMARY.md — confirm exact endpoint paths from Plan 02
    - .planning/phases/15-tuition-management/15-RESEARCH.md — TuitionReportItem interface (needed by Plan 04 as well)
  </read_first>
  <action>
    Append to frontend/lib/admin-portal-api.ts (do NOT modify existing functions). Add the following exports at the bottom of the file:

    Type exports:
    - TuitionConfig interface: { id: number, classId: number, pricePerSession: number, bookFee: number | null, dueDayOfMonth: number, createdAt: string, updatedAt: string }
    - TuitionRecord interface: { id: number, studentId: number, classId: number, month: number, year: number, tuitionAmount: number, bookFee: number, totalAmount: number, dueDate: string, status: 'PENDING' | 'PAID' | 'OVERDUE', paidAt: string | null, paidBy: string | null, createdAt: string }
    - TuitionReportItem interface: { id: number, studentId: number, studentName: string, tuitionAmount: number, bookFee: number, totalAmount: number, dueDate: string, paidAt: string | null, status: 'PENDING' | 'PAID' | 'OVERDUE', daysOverdue: number }
    - CreateTuitionConfigInput: { pricePerSession: number, bookFee?: number | null, dueDayOfMonth: number }
    - GenerateRecordsInput: { classId: number, month: number, year: number }
    - RecordPaymentInput: { paidAt: string, paidBy: string }
    - SendNotificationsInput: { recordIds: number[] }
    - SendNotificationsResult: { totalRecords: number, successCount: number, results: Array<{ recordId: number, success: boolean, error?: string }> }

    Function exports (all use the existing req() helper):
    - getTuitionConfig(classId: number): req<TuitionConfig>(`/admin/tuition/config/${classId}`)
    - updateTuitionConfig(classId: number, data: CreateTuitionConfigInput): req<TuitionConfig>(`/admin/tuition/config/${classId}`, { method: 'PUT', headers: {'Content-Type':'application/json'}, body: JSON.stringify(data) })
    - createTuitionRecords(data: GenerateRecordsInput): req<TuitionRecord[]>('/admin/tuition/records/generate', { method: 'POST', headers: {'Content-Type':'application/json'}, body: JSON.stringify(data) })
    - recordTuitionPayment(recordId: number, data: RecordPaymentInput): req<TuitionRecord>(`/admin/tuition/records/${recordId}`, { method: 'PATCH', headers: {'Content-Type':'application/json'}, body: JSON.stringify(data) })
    - sendTuitionNotifications(data: SendNotificationsInput): req<SendNotificationsResult>('/admin/tuition/notify', { method: 'POST', headers: {'Content-Type':'application/json'}, body: JSON.stringify(data) })
    - getTuitionReport(params: { classId: number, month: number, year: number, statuses?: string[] }): req<TuitionReportItem[]>(`/admin/tuition/report?classId=${params.classId}&month=${params.month}&year=${params.year}${params.statuses?.length ? '&status=' + params.statuses.join(',') : ''}`)
  </action>
  <verify>
    <automated>cd J:/sources/katie-english/frontend && npx tsc --noEmit 2>&1 | grep -v "node_modules" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - admin-portal-api.ts exports getTuitionConfig, updateTuitionConfig, createTuitionRecords, recordTuitionPayment, sendTuitionNotifications, getTuitionReport
    - TuitionReportItem interface exported (shared with Plan 04)
    - npx tsc --noEmit shows no TypeScript errors for admin-portal-api.ts
    - Existing exports in the file are unchanged
  </acceptance_criteria>
  <done>All 6 tuition API functions and 8 types exported from admin-portal-api.ts; no TypeScript errors.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Build tuition UI components and admin/teacher pages</name>
  <files>
    frontend/app/admin/tuition/page.tsx,
    frontend/app/admin/tuition/_components/TuitionConfigForm.tsx,
    frontend/app/admin/tuition/_components/GenerateRecordsModal.tsx,
    frontend/app/admin/tuition/_components/PaymentRecordDialog.tsx,
    frontend/app/admin/tuition/_components/ZaloSendModal.tsx,
    frontend/app/teacher/tuition/page.tsx
  </files>
  <read_first>
    - frontend/app/admin/classes/page.tsx — full file; understand the Dialog, TableShell, useToast, adminAuthHeaders, Select/MenuItem, FormLabel, TextField, Button, Card, Typography, CircularProgress import patterns and state management style
    - frontend/lib/admin-portal-api.ts — confirm all tuition functions just added (Task 1 above)
    - .planning/phases/15-tuition-management/15-PATTERNS.md — all component code patterns with exact MUI imports and state patterns
    - frontend/app/admin/layout.tsx — AdminShell wrapping pattern (page.tsx should be placed inside the layout, no extra shell needed)
    - frontend/app/teacher/layout.tsx — TeacherShell wrapping pattern
  </read_first>
  <action>
    Create the directory structure: frontend/app/admin/tuition/ and frontend/app/admin/tuition/_components/.

    1. TuitionConfigForm.tsx: Client component. Props: classId (number), onClose (() => void), onSaved (() => void). State: form with pricePerSession (number, default 0), bookFee (number | null, default null), dueDayOfMonth (number, default 5). On mount, call getTuitionConfig(classId) and populate form if config exists (catch 404 → keep defaults). Submit calls updateTuitionConfig(classId, form), showToast success, onSaved(), onClose(). Render as MUI Dialog with DialogTitle 'Cấu hình học phí', three TextField fields (pricePerSession required, bookFee optional, dueDayOfMonth required 1-31), Cancel + Save buttons with loading state.

    2. GenerateRecordsModal.tsx: Client component. Props: open (boolean), classId (number), onClose, onSaved. State: month (current month 1-12), year (current year). Submit calls createTuitionRecords({ classId, month, year }), showToast, onSaved, onClose. Render as MUI Dialog with two TextField fields (month 1-12, year). Display warning text: 'Nếu phiếu thu đã tồn tại cho tháng này, hệ thống sẽ báo lỗi.' Cancel + Generate buttons.

    3. PaymentRecordDialog.tsx: Client component. Props: open (boolean), recordId (number), studentName (string), totalAmount (number), onClose, onSaved. State: paidAt (today ISO date string), paidBy (empty string). Submit calls recordTuitionPayment(recordId, { paidAt, paidBy }), showToast. Render as MUI Dialog with DialogTitle showing 'Ghi nhận đóng học phí — {studentName} ({totalAmount.toLocaleString('vi-VN')} VNĐ)', paidAt date TextField, paidBy TextField. Cancel + Confirm buttons.

    4. ZaloSendModal.tsx: Client component. Props: open (boolean), recordIds (number[]), onClose, onSent. Loading state. On confirm, calls sendTuitionNotifications({ recordIds }), showToast with `Đã gửi ${result.successCount}/${result.totalRecords} thông báo`. Render as MUI Dialog with Typography explaining count of records to notify. Cancel + Send buttons.

    5. frontend/app/admin/tuition/page.tsx: 'use client'. Imports: getClasses (from admin-portal-api.ts — check existing getClasses function), getTuitionConfig, useToast, MUI Box/Card/CardContent/Tabs/Tab/Typography/Select/MenuItem/FormLabel/Button. State: classes (ClassItem[]), selectedClassId, activeTab (0-3), generateOpen, paymentRecordId + paymentOpen, zaloRecordIds + zaloOpen. On mount, fetch classes with getClasses(). Render:
       - Typography h5 'Quản lý học phí'
       - FormLabel + Select to choose class (classId from classes list)
       - Tabs: 'Cấu hình' | 'Tạo phiếu thu' | 'Thông báo ZNS' | 'Xem báo cáo (→ tab)'
       - Tab 0: TuitionConfigForm (inline, no dialog; classId={selectedClassId} onClose={() => {}} onSaved={() => {}})
       - Tab 1: Button 'Tạo phiếu thu tháng này' opens GenerateRecordsModal
       - Tab 2: Button 'Gửi thông báo ZNS' — for now, opens ZaloSendModal with empty recordIds (user picks in report tab); add note 'Chọn học sinh từ tab Báo cáo để gửi ZNS'
       - Tab 3: Link/note 'Xem báo cáo tại tab Báo cáo (Plan 04)' — placeholder for Plan 04 TuitionReportTable component
       Note: Report tab content is provided by Plan 04 (TuitionReportTable). Leave tab 3 content as a placeholder Typography: 'Báo cáo học phí — xem tại tab Báo cáo'. Plan 04 will add the TuitionReportTable import here.

    6. frontend/app/teacher/tuition/page.tsx: 'use client'. Similar to admin page but fetches teacher's own classes. Look for getTeacherClasses or getClasses function in admin-portal-api.ts (or use existing getClasses — the backend already filters by teacher JWT). Render a class selector, then show TuitionConfigForm (tab 0) and GenerateRecordsModal (tab 1). Teacher view only shows tabs: 'Cấu hình', 'Tạo phiếu thu' (no ZNS or full report — those are in admin portal). For the report, reuse TuitionReportTable from Plan 04 (add as placeholder for now). Import components from '../admin/tuition/_components/...'.

    All components: use 'use client' directive. Use useToast() from '@/lib/toast-context'. Wrap async calls in try/catch with showToast on error. Use loading boolean state on submit buttons. Follow MUI Dialog pattern from PATTERNS.md (DialogTitle + DialogContent + DialogActions).
  </action>
  <verify>
    <automated>cd J:/sources/katie-english/frontend && npx tsc --noEmit 2>&1 | grep -v "node_modules" | head -30</automated>
  </verify>
  <acceptance_criteria>
    - frontend/app/admin/tuition/page.tsx exists with 'use client' and Tabs component
    - frontend/app/admin/tuition/_components/TuitionConfigForm.tsx has pricePerSession TextField
    - frontend/app/admin/tuition/_components/GenerateRecordsModal.tsx has month and year TextFields
    - frontend/app/admin/tuition/_components/PaymentRecordDialog.tsx has paidAt and paidBy TextFields
    - frontend/app/admin/tuition/_components/ZaloSendModal.tsx calls sendTuitionNotifications
    - frontend/app/teacher/tuition/page.tsx imports components from '../admin/tuition/_components/'
    - npx tsc --noEmit shows no TypeScript errors for tuition files
    - npm run build exits 0
  </acceptance_criteria>
  <done>All tuition UI components built. Admin and teacher pages render. TypeScript clean. npm build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → API | All API calls use adminAuthHeaders() — JWT token required |
| Admin form → API | Input values submitted to backend; backend validates (not frontend-only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-09 | Tampering | TuitionConfigForm input | accept | Frontend inputs are informational; backend validates pricePerSession > 0 and month 1-12 with BadRequestException |
| T-15-10 | Information Disclosure | ZaloSendModal | mitigate | Modal shows record count, not phone numbers; actual phone data never rendered in frontend |
| T-15-11 | Elevation of Privilege | Teacher page component reuse | mitigate | Teacher page reuses admin components but calls backend endpoints protected by AdminGuard; backend enforces TEACHER role access to own classes only |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | No new packages — MUI and React already in project |
</threat_model>

<verification>
After both tasks:
- cd frontend && npm run build exits 0
- frontend/app/admin/tuition/ directory has page.tsx + _components/ with 4 component files
- frontend/app/teacher/tuition/page.tsx exists
- admin-portal-api.ts exports getTuitionConfig, updateTuitionConfig, createTuitionRecords, recordTuitionPayment, sendTuitionNotifications, getTuitionReport
- TypeScript reports no errors for tuition files
</verification>

<success_criteria>
1. /admin/tuition page renders with class selector and tabs (Config, Generate, ZNS, Report placeholder)
2. TuitionConfigForm submits to PUT /admin/tuition/config/:classId with pricePerSession, bookFee, dueDayOfMonth
3. GenerateRecordsModal submits to POST /admin/tuition/records/generate with classId, month, year
4. PaymentRecordDialog submits to PATCH /admin/tuition/records/:id with paidAt, paidBy
5. ZaloSendModal submits to POST /admin/tuition/notify with selected recordIds
6. /teacher/tuition page exists scoped to teacher's own classes
7. npm run build exits 0 after all changes
</success_criteria>

<output>
Create .planning/phases/15-tuition-management/15-03-SUMMARY.md when done.

## Artifacts this phase produces

### New Files
- `frontend/app/admin/tuition/page.tsx` — AdminTuitionPage (tabbed UI: Config, Generate, ZNS, Report placeholder)
- `frontend/app/admin/tuition/_components/TuitionConfigForm.tsx` — TuitionConfigForm component (pricePerSession, bookFee, dueDayOfMonth fields)
- `frontend/app/admin/tuition/_components/GenerateRecordsModal.tsx` — GenerateRecordsModal component (month + year picker)
- `frontend/app/admin/tuition/_components/PaymentRecordDialog.tsx` — PaymentRecordDialog component (paidAt + paidBy fields)
- `frontend/app/admin/tuition/_components/ZaloSendModal.tsx` — ZaloSendModal component (confirmation dialog with record count)
- `frontend/app/teacher/tuition/page.tsx` — TeacherTuitionPage (class-scoped view with tabs for Config, Generate)

### Updated Files
- `frontend/lib/admin-portal-api.ts` — Added 6 tuition API functions + 8 TypeScript interfaces/types
</output>
