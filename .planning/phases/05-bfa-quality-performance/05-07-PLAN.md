---
phase: 05-bfa-quality-performance
plan: 07
type: execute
wave: 7
depends_on:
  - 05-06
files_modified:
  - bfa-service/main.py
  - bfa-service/Dockerfile
  - bfa-service/requirements.txt
  - backend/src/bfa/bfa.service.ts
  - backend/src/bfa/bfa.service.spec.ts
  - backend/package.json
  - backend/Dockerfile
  - docker-compose.yml
files_deleted:
  - backend/src/bfa/azure-audio.util.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "bfa-service/ directory exists with main.py, Dockerfile, requirements.txt"
    - "POST /analyze returns BfaAnalyzeResult shape with phonemes[], feedback[], score, transcription.text"
    - "POST /analyze-speaking returns BfaSpeakingResult shape"
    - "POST /transcribe returns WhisperXResult shape"
    - "GET /health returns {status: ok}"
    - "All ASR calls go to Groq API (GROQ_API_KEY env var) — no local WhisperX model loaded"
    - "Phoneme scoring uses text-based G2P comparison (phonemizer + espeak) — no GPU required"
    - "bfa.service.ts uses axios HTTP calls to BFA_URL — no Azure SDK"
    - "azure-audio.util.ts deleted"
    - "microsoft-cognitiveservices-speech-sdk removed from backend/package.json"
    - "docker-compose.yml has bfa: service block with GROQ_API_KEY env var"
    - "backend depends_on bfa: condition service_healthy"
    - "npm run test in backend passes"
---

# Plan 07: Rebuild Self-hosted BFA Service (Groq ASR + local phoneme scoring)

**Goal:** Replace Azure PA SDK with self-hosted Python FastAPI service that uses Groq Whisper API
for ASR and local phonemizer-based scoring for phoneme feedback. Zero GPU required.

**Architecture:**
```
NestJS BfaService → HTTP/axios → [Docker: bfa-service Python FastAPI]
                                    Groq Whisper API (ASR, cloud)
                                    phonemizer + espeak (G2P, local)
                                    string alignment scoring (local)
```

**Why this works without GPU:**
- Groq Whisper Large-v3-turbo: 228x realtime, <200ms for 3s audio
- phonemizer: pure Python G2P, <10ms
- String alignment (Levenshtein): pure Python, <1ms
- Total latency: ~300-500ms vs Azure ~500-800ms

**Phoneme scoring approach (no forced acoustic alignment):**
1. Groq ASR → transcribed text (e.g. "cat")
2. phonemizer on expected word → expected phoneme sequence (e.g. [k, æ, t])
3. phonemizer on ASR output → actual phoneme sequence (e.g. [k, ɛ, t])
4. Sequence alignment (Levenshtein ops) → map expected↔actual phonemes
5. SIMILAR_PAIRS lookup → correct / similar / substituted / missing / extra
6. Estimate timestamps from word duration / phoneme count

---

## Task 1 — Create bfa-service/requirements.txt

**Create:** `bfa-service/requirements.txt`

```
fastapi==0.115.0
uvicorn[standard]==0.30.0
python-multipart==0.0.12
requests==2.32.3
phonemizer==3.3.0
espeak-ng==1.51.1
```

> Note: `espeak-ng` system package also needed in Dockerfile. `phonemizer` uses espeak as backend.

---

## Task 2 — Create bfa-service/Dockerfile

**Create:** `bfa-service/Dockerfile`

```dockerfile
FROM python:3.11-slim

RUN apt-get update && apt-get install -y --no-install-recommends \
    ffmpeg \
    espeak-ng \
    libespeak-ng-dev \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY main.py .

EXPOSE 8000
CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000", "--workers", "1"]
```

---

## Task 3 — Create bfa-service/main.py

**Create:** `bfa-service/main.py`

```python
import difflib
import json
import logging
import os
import re
import subprocess
import tempfile
import uuid
from pathlib import Path
from typing import List, Optional

import requests
from fastapi import FastAPI, File, Form, HTTPException, UploadFile
from fastapi.responses import JSONResponse

logger = logging.getLogger("bfa_service")
logging.basicConfig(format="%(message)s", level=logging.INFO)

app = FastAPI()

GROQ_API_KEY = os.getenv("GROQ_API_KEY", "")
GROQ_MODEL = os.getenv("GROQ_MODEL", "whisper-large-v3-turbo")
GROQ_URL = "https://api.groq.com/openai/v1/audio/transcriptions"

MAX_UPLOAD_BYTES = int(os.getenv("BFA_MAX_UPLOAD_BYTES", str(20 * 1024 * 1024)))
MIN_WORD_SCORE = int(os.getenv("BFA_MIN_WORD_SCORE", "70"))

# Phonetically similar pairs (ARPAbet-ish symbols)
_SIMILAR_PAIRS = {
    frozenset(["r", "l"]),
    frozenset(["b", "p"]),
    frozenset(["d", "t"]),
    frozenset(["g", "k"]),
    frozenset(["v", "f"]),
    frozenset(["z", "s"]),
    frozenset(["dʒ", "tʃ"]),
    frozenset(["ð", "θ"]),
    frozenset(["æ", "ɛ"]),
    frozenset(["ɪ", "iː"]),
    frozenset(["ʊ", "uː"]),
    frozenset(["ɒ", "ɔː"]),
    frozenset(["m", "n"]),
    frozenset(["n", "ŋ"]),
}


def _safe_suffix(filename: Optional[str]) -> str:
    raw = Path(filename or "audio.webm").suffix or ".webm"
    if re.fullmatch(r'\.[a-zA-Z0-9]{1,10}', raw):
        return raw
    return ".webm"


def _to_wav(input_path: Path, output_path: Path) -> None:
    result = subprocess.run(
        ["ffmpeg", "-y", "-i", str(input_path),
         "-ar", "16000", "-ac", "1", "-c:a", "pcm_s16le",
         "-f", "wav", str(output_path)],
        capture_output=True, timeout=30,
    )
    if result.returncode != 0:
        raise RuntimeError(f"ffmpeg failed: {result.stderr.decode()}")


def _groq_transcribe(wav_path: Path, prompt: Optional[str] = None) -> dict:
    """Call Groq Whisper API. Returns {text, words[{word, start, end}]}."""
    if not GROQ_API_KEY:
        raise RuntimeError("GROQ_API_KEY not set")
    with open(wav_path, "rb") as f:
        data = {
            "model": GROQ_MODEL,
            "language": "en",
            "response_format": "verbose_json",
            "timestamp_granularities[]": "word",
        }
        if prompt:
            data["prompt"] = prompt
        resp = requests.post(
            GROQ_URL,
            headers={"Authorization": f"Bearer {GROQ_API_KEY}"},
            files={"file": ("audio.wav", f, "audio/wav")},
            data=data,
            timeout=30,
        )
    if not resp.ok:
        raise RuntimeError(f"Groq API error {resp.status_code}: {resp.text}")
    return resp.json()


def _g2p(text: str) -> List[str]:
    """Convert text to phoneme sequence using espeak via phonemizer."""
    try:
        from phonemizer import phonemize
        from phonemizer.backend import EspeakBackend
        phonemes_str = phonemize(
            text.lower().strip(),
            backend="espeak",
            language="en-us",
            with_stress=False,
            preserve_punctuation=False,
            njobs=1,
        )
        # Split on spaces and filter empty
        return [p for p in re.split(r'\s+', phonemes_str.strip()) if p]
    except Exception as e:
        logger.warning(f"G2P failed for '{text}': {e}")
        return list(text.lower().replace(" ", ""))


def _is_similar(a: str, b: str) -> bool:
    return frozenset([a, b]) in _SIMILAR_PAIRS


def _score_phonemes(expected: List[str], actual: List[str]) -> List[dict]:
    """
    Align expected vs actual phoneme sequences using SequenceMatcher.
    Returns list of ops: {status, expected, aligned}
    """
    ops = []
    matcher = difflib.SequenceMatcher(None, expected, actual, autojunk=False)
    for tag, i1, i2, j1, j2 in matcher.get_opcodes():
        if tag == "equal":
            for k in range(i2 - i1):
                ops.append({"status": "correct", "expected": expected[i1 + k], "aligned": actual[j1 + k]})
        elif tag == "replace":
            exp_chunk = expected[i1:i2]
            act_chunk = actual[j1:j2]
            max_len = max(len(exp_chunk), len(act_chunk))
            for k in range(max_len):
                e = exp_chunk[k] if k < len(exp_chunk) else None
                a = act_chunk[k] if k < len(act_chunk) else None
                if e and a:
                    status = "similar" if _is_similar(e, a) else "substituted"
                    ops.append({"status": status, "expected": e, "aligned": a})
                elif e:
                    ops.append({"status": "missing", "expected": e, "aligned": None})
                else:
                    ops.append({"status": "extra", "expected": None, "aligned": a})
        elif tag == "delete":
            for e in expected[i1:i2]:
                ops.append({"status": "missing", "expected": e, "aligned": None})
        elif tag == "insert":
            for a in actual[j1:j2]:
                ops.append({"status": "extra", "expected": None, "aligned": a})
    return ops


def _distribute_timestamps(word_start: float, word_end: float, phoneme_ops: List[dict]) -> List[dict]:
    """Estimate per-phoneme timestamps by dividing word duration evenly."""
    present = [op for op in phoneme_ops if op["status"] != "missing"]
    if not present:
        return phoneme_ops
    dur = (word_end - word_start) / len(present)
    result = []
    idx = 0
    for op in phoneme_ops:
        if op["status"] == "missing":
            result.append({**op, "start": None, "end": None, "duration": None})
        else:
            start = word_start + idx * dur
            result.append({**op, "start": round(start, 4), "end": round(start + dur, 4), "duration": round(dur, 4)})
            idx += 1
    return result


def _calc_score(ops: List[dict]) -> int:
    if not ops:
        return 0
    scored = [op for op in ops if op["status"] != "extra"]
    if not scored:
        return 0
    correct = sum(1 for op in scored if op["status"] in ("correct", "similar"))
    return round(correct / len(scored) * 100)


@app.get("/health")
def health():
    return {"status": "ok", "groq_key_set": bool(GROQ_API_KEY)}


@app.post("/analyze")
async def analyze(
    audio: UploadFile = File(...),
    word: str = Form(...),
    expected_phonemes: str = Form("[]"),
):
    """Phonics single-word assessment."""
    raw = await audio.read(MAX_UPLOAD_BYTES + 1)
    if len(raw) > MAX_UPLOAD_BYTES:
        raise HTTPException(400, "audio too large")

    suffix = _safe_suffix(audio.filename)
    uid = uuid.uuid4().hex
    with tempfile.TemporaryDirectory() as tmp:
        in_path = Path(tmp) / f"{uid}{suffix}"
        wav_path = Path(tmp) / f"{uid}.wav"
        in_path.write_bytes(raw)
        _to_wav(in_path, wav_path)

        # ASR via Groq with word as prompt (constrained decoding hint)
        try:
            groq_result = _groq_transcribe(wav_path, prompt=word)
        except Exception as e:
            logger.warning(f"Groq ASR failed: {e}")
            groq_result = {"text": "", "words": []}

        transcript = groq_result.get("text", "").strip()
        groq_words = groq_result.get("words", [])
        word_start = groq_words[0].get("start", 0.0) if groq_words else 0.0
        word_end = groq_words[-1].get("end", 1.0) if groq_words else 1.0

        # G2P: expected vs actual
        expected_ph = _g2p(word)
        actual_ph = _g2p(transcript) if transcript else []

        # Score phonemes
        raw_ops = _score_phonemes(expected_ph, actual_ph)
        ops = _distribute_timestamps(word_start, word_end, raw_ops)
        score = _calc_score(ops)

        # Build phonemes array (non-missing only)
        phonemes = [
            {
                "symbol": op["expected"] or op["aligned"],
                "ipa": op["expected"] or op["aligned"],
                "start": op.get("start") or 0.0,
                "end": op.get("end") or 0.0,
                "duration": op.get("duration") or 0.0,
            }
            for op in ops if op["status"] != "missing"
        ]

        return {
            "success": True,
            "transcription": {"text": transcript},
            "phonemes": phonemes,
            "score": score,
            "feedback": ops,
            "word": word,
            "espeak_fallback": False,
        }


@app.post("/analyze-speaking")
async def analyze_speaking(
    audio: UploadFile = File(...),
    target_text: str = Form(...),
    mode: str = Form("SCRIPT_MATCH"),
):
    """Multi-word speaking assessment."""
    raw = await audio.read(MAX_UPLOAD_BYTES + 1)
    if len(raw) > MAX_UPLOAD_BYTES:
        raise HTTPException(400, "audio too large")

    target_text = target_text.strip()
    suffix = _safe_suffix(audio.filename)
    uid = uuid.uuid4().hex
    with tempfile.TemporaryDirectory() as tmp:
        in_path = Path(tmp) / f"{uid}{suffix}"
        wav_path = Path(tmp) / f"{uid}.wav"
        in_path.write_bytes(raw)
        _to_wav(in_path, wav_path)

        prompt = target_text if mode == "SCRIPT_MATCH" else None
        try:
            groq_result = _groq_transcribe(wav_path, prompt=prompt)
        except Exception as e:
            logger.warning(f"Groq ASR failed: {e}")
            groq_result = {"text": "", "words": []}

        transcript = groq_result.get("text", "").strip()
        groq_words = groq_result.get("words", [])

        # Build word-level results
        target_words = target_text.split()
        word_results = []
        for i, tw in enumerate(target_words):
            gw = groq_words[i] if i < len(groq_words) else None
            w_start = gw.get("start", i * 0.5) if gw else i * 0.5
            w_end = gw.get("end", w_start + 0.4) if gw else w_start + 0.4
            actual_word = gw.get("word", "").strip(".,!?").lower() if gw else ""

            exp_ph = _g2p(tw)
            act_ph = _g2p(actual_word) if actual_word else []
            raw_ops = _score_phonemes(exp_ph, act_ph)
            ops = _distribute_timestamps(w_start, w_end, raw_ops)
            word_score = _calc_score(ops)
            phonemes = [
                {"symbol": op["expected"] or op["aligned"], "ipa": op["expected"] or op["aligned"],
                 "start": op.get("start") or 0.0, "end": op.get("end") or 0.0, "duration": op.get("duration") or 0.0}
                for op in ops if op["status"] != "missing"
            ]
            word_results.append({"word": tw, "phonemes": phonemes, "score": word_score, "feedback": ops})

        matched = sum(1 for w in word_results if w["score"] >= MIN_WORD_SCORE)
        overall = _calc_score([op for wr in word_results for op in wr["feedback"]])

        return {
            "success": True,
            "transcription": {"text": transcript},
            "words": word_results,
            "overall_score": overall,
            "matched_words": matched,
            "total_words": len(target_words),
        }


@app.post("/transcribe")
async def transcribe(audio: UploadFile = File(...)):
    """STT only — no scoring."""
    raw = await audio.read(MAX_UPLOAD_BYTES + 1)
    if len(raw) > MAX_UPLOAD_BYTES:
        raise HTTPException(400, "audio too large")

    suffix = _safe_suffix(audio.filename)
    uid = uuid.uuid4().hex
    with tempfile.TemporaryDirectory() as tmp:
        in_path = Path(tmp) / f"{uid}{suffix}"
        wav_path = Path(tmp) / f"{uid}.wav"
        in_path.write_bytes(raw)
        _to_wav(in_path, wav_path)

        try:
            groq_result = _groq_transcribe(wav_path)
        except Exception as e:
            logger.warning(f"Groq transcribe failed: {e}")
            return {"text": "", "words": []}

        words = [
            {"word": w.get("word", ""), "start": w.get("start", 0.0),
             "end": w.get("end", 0.0), "score": 1.0}
            for w in groq_result.get("words", [])
        ]
        return {"text": groq_result.get("text", "").strip(), "words": words}


@app.post("/align")
async def align(
    audio: UploadFile = File(...),
    word: str = Form(...),
    expected_phonemes: str = Form("[]"),
):
    """Legacy endpoint — delegates to /analyze."""
    result = await analyze(audio=audio, word=word, expected_phonemes=expected_phonemes)
    result_dict = result if isinstance(result, dict) else result.body
    result_dict.pop("transcription", None)
    return result_dict
```

**Acceptance criteria:**
- `bfa-service/main.py` exists with all 5 endpoints
- `bfa-service/requirements.txt` exists
- `bfa-service/Dockerfile` exists

---

## Task 4 — Revert backend/src/bfa/bfa.service.ts to HTTP/axios

**Read first:** `backend/src/bfa/bfa.service.ts` (current Azure SDK version)

Replace entire file with axios HTTP version:

```typescript
import { Injectable, Logger } from '@nestjs/common';
import axios from 'axios';
import FormData = require('form-data');
import { BfaAlignResult, BfaAnalyzeResult, BfaSpeakingResult, WhisperXResult } from './bfa.dto';

function mimeToExt(mimeType: string): string {
  if (mimeType.includes('webm')) return 'webm';
  if (mimeType.includes('m4a')) return 'm4a';
  if (mimeType.includes('mp4')) return 'mp4';
  if (mimeType.includes('quicktime')) return 'mov';
  if (mimeType.includes('ogg')) return 'ogg';
  if (mimeType.includes('aac')) return 'aac';
  return 'wav';
}

@Injectable()
export class BfaService {
  private readonly logger = new Logger(BfaService.name);
  private readonly baseUrl = process.env.BFA_URL ?? 'http://localhost:3002';

  async align(audioBuffer: Buffer, mimeType: string, word: string, expectedPhonemes: string[]): Promise<BfaAlignResult> {
    const form = new FormData();
    const ext = mimeToExt(mimeType);
    form.append('audio', audioBuffer, { filename: `audio.${ext}`, contentType: mimeType });
    form.append('word', word);
    form.append('expected_phonemes', JSON.stringify(expectedPhonemes));
    const response = await axios.post<BfaAlignResult>(`${this.baseUrl}/align`, form, { headers: form.getHeaders(), timeout: 60_000 });
    return response.data;
  }

  async transcribe(audioBuffer: Buffer, mimeType: string): Promise<WhisperXResult> {
    const form = new FormData();
    const ext = mimeToExt(mimeType);
    form.append('audio', audioBuffer, { filename: `audio.${ext}`, contentType: mimeType });
    const response = await axios.post<WhisperXResult>(`${this.baseUrl}/transcribe`, form, { headers: form.getHeaders(), timeout: 120_000 });
    return response.data;
  }

  async analyze(audioBuffer: Buffer, mimeType: string, word: string, expectedPhonemes: string[]): Promise<BfaAnalyzeResult> {
    const form = new FormData();
    const ext = mimeToExt(mimeType);
    form.append('audio', audioBuffer, { filename: `audio.${ext}`, contentType: mimeType });
    form.append('word', word);
    form.append('expected_phonemes', JSON.stringify(expectedPhonemes));
    const response = await axios.post<BfaAnalyzeResult>(`${this.baseUrl}/analyze`, form, { headers: form.getHeaders(), timeout: 120_000 });
    return response.data;
  }

  async analyzeSpeaking(audioBuffer: Buffer, mimeType: string, targetText: string, mode: 'SCRIPT_MATCH' | 'FREE_SPEAK' = 'SCRIPT_MATCH'): Promise<BfaSpeakingResult> {
    const form = new FormData();
    const ext = mimeToExt(mimeType);
    form.append('audio', audioBuffer, { filename: `audio.${ext}`, contentType: mimeType });
    form.append('target_text', targetText);
    form.append('mode', mode);
    const response = await axios.post<BfaSpeakingResult>(`${this.baseUrl}/analyze-speaking`, form, { headers: form.getHeaders(), timeout: 180_000 });
    return response.data;
  }
}
```

**Acceptance criteria:**
- No `microsoft-cognitiveservices-speech-sdk` import
- No `azure-audio.util` import
- `this.baseUrl` uses `BFA_URL` env var
- `npx tsc --noEmit` passes

---

## Task 5 — Remove Azure SDK + azure-audio.util.ts

```bash
cd backend
npm uninstall microsoft-cognitiveservices-speech-sdk
```

Delete file: `backend/src/bfa/azure-audio.util.ts`

Also remove `ffmpeg` from backend Dockerfile runtime stage (no longer needed in NestJS — ffmpeg moves to bfa-service):

In `backend/Dockerfile`, remove line: `RUN apk add --no-cache ffmpeg`

**Acceptance criteria:**
- `microsoft-cognitiveservices-speech-sdk` not in `backend/package.json`
- `backend/src/bfa/azure-audio.util.ts` does not exist
- `backend/Dockerfile` has no ffmpeg install line

---

## Task 6 — Revert bfa.service.spec.ts to axios mock

**Read first:** current spec file (has Azure SDK mock)

Replace with axios-based spec (same pattern as plan 05-04):

```typescript
import axios from 'axios';
import FormData = require('form-data');
import { BfaService } from './bfa.service';

jest.mock('axios');
const mockedAxios = axios as jest.Mocked<typeof axios>;

describe('BfaService', () => {
  let service: BfaService;
  const AUDIO = Buffer.from('fake-audio');

  beforeEach(() => {
    service = new BfaService();
    jest.clearAllMocks();
  });

  describe('analyze()', () => {
    const MOCK_RESULT = {
      success: true,
      transcription: { text: 'cat' },
      phonemes: [],
      score: 90,
      feedback: [],
      word: 'cat',
      espeak_fallback: false,
    };

    beforeEach(() => { mockedAxios.post.mockResolvedValue({ data: MOCK_RESULT }); });

    it('posts to /analyze endpoint', async () => {
      await service.analyze(AUDIO, 'audio/webm', 'cat', ['k', 'ae', 't']);
      expect((mockedAxios.post.mock.calls[0][0] as string)).toMatch(/\/analyze$/);
    });

    it('uses timeout=120_000', async () => {
      await service.analyze(AUDIO, 'audio/webm', 'cat', []);
      expect((mockedAxios.post.mock.calls[0][2] as any).timeout).toBe(120_000);
    });

    it('returns response.data', async () => {
      const result = await service.analyze(AUDIO, 'audio/webm', 'cat', []);
      expect(result).toEqual(MOCK_RESULT);
    });
  });

  describe('transcribe()', () => {
    beforeEach(() => { mockedAxios.post.mockResolvedValue({ data: { text: 'cat', words: [] } }); });

    it('posts to /transcribe endpoint', async () => {
      await service.transcribe(AUDIO, 'audio/webm');
      expect((mockedAxios.post.mock.calls[0][0] as string)).toMatch(/\/transcribe$/);
    });

    it('uses timeout=120_000', async () => {
      await service.transcribe(AUDIO, 'audio/webm');
      expect((mockedAxios.post.mock.calls[0][2] as any).timeout).toBe(120_000);
    });
  });

  describe('analyzeSpeaking()', () => {
    const MOCK_SPEAKING = {
      success: true,
      transcription: { text: 'the cat is black' },
      words: [],
      overall_score: 80,
      matched_words: 3,
      total_words: 4,
    };

    beforeEach(() => { mockedAxios.post.mockResolvedValue({ data: MOCK_SPEAKING }); });

    it('posts to /analyze-speaking endpoint', async () => {
      await service.analyzeSpeaking(AUDIO, 'audio/webm', 'the cat is black');
      expect((mockedAxios.post.mock.calls[0][0] as string)).toMatch(/\/analyze-speaking$/);
    });

    it('uses timeout=180_000', async () => {
      await service.analyzeSpeaking(AUDIO, 'audio/webm', 'the cat is black');
      expect((mockedAxios.post.mock.calls[0][2] as any).timeout).toBe(180_000);
    });

    it('defaults mode to SCRIPT_MATCH', async () => {
      await service.analyzeSpeaking(AUDIO, 'audio/webm', 'the cat is black');
      const form = mockedAxios.post.mock.calls[0][1] as FormData;
      const raw: string = (form as any)._streams?.join?.('') ?? '';
      if (raw) expect(raw).toContain('SCRIPT_MATCH');
      else expect(mockedAxios.post).toHaveBeenCalled();
    });
  });
});
```

Run: `cd backend && npx jest src/bfa/bfa.service.spec.ts --no-coverage`

---

## Task 7 — Restore bfa service in docker-compose.yml

**Read first:** `docker-compose.yml` — current state (no bfa block)

Add back bfa service block + update backend:

```yaml
  bfa:
    build:
      context: ./bfa-service
      dockerfile: Dockerfile
    environment:
      GROQ_API_KEY: ${GROQ_API_KEY}
      GROQ_MODEL: ${GROQ_MODEL:-whisper-large-v3-turbo}
      BFA_MIN_WORD_SCORE: '70'
    ports:
      - '3002:8000'
    healthcheck:
      test: ['CMD', 'curl', '-f', 'http://localhost:8000/health']
      interval: 10s
      timeout: 10s
      retries: 10
      start_period: 20s
```

In `backend:` service:
- Remove `AZURE_SPEECH_KEY` and `AZURE_SPEECH_REGION` env vars
- Add `BFA_URL: http://bfa:8000`
- Add back to depends_on: `bfa: condition: service_healthy`

**Acceptance criteria:**
- `docker compose config > /dev/null` passes
- bfa service block present with GROQ_API_KEY
- backend depends_on includes bfa

---

## Verification

```bash
# TypeScript
cd backend && npx tsc --noEmit

# Unit tests
cd backend && npm run test

# Docker config valid
docker compose config > /dev/null

# Smoke test (requires GROQ_API_KEY)
docker compose build bfa
docker compose up bfa -d
curl http://localhost:3002/health
```

---

## Environment Variables Required

```
GROQ_API_KEY=<your-groq-api-key>   # get free at console.groq.com
GROQ_MODEL=whisper-large-v3-turbo  # optional, this is default
```

Get Groq API key: https://console.groq.com — free tier: 7,200 min/month Whisper.

---

## Known Limitations

- Phoneme scoring is text-based (G2P comparison), not acoustic-based → less accurate than Azure PA
- No per-phoneme confidence from acoustic model → timestamps are estimated
- Groq Whisper WER ~30-40% on Vietnamese children without fine-tuning
- Constrained decoding via `prompt` parameter helps for phonics (known word)
- Fine-tuning Whisper on Vietnamese children data recommended after MVP to improve WER
