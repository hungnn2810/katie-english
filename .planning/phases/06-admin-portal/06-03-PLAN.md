---
phase: 06-admin-portal
plan: 03
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - backend/src/admin/admin-teachers.dto.ts
  - backend/src/admin/admin-teachers.service.ts
  - backend/src/admin/admin-teachers.controller.ts
  - backend/src/admin/admin.module.ts
  - backend/src/auth/auth.service.ts
  - frontend/lib/admin-portal-api.ts
  - frontend/app/admin/teachers/page.tsx
autonomous: true
requirements:
  - ADMIN-02
  - ADMIN-03
mvp_mode: true

must_haves:
  truths:
    - "Admin opens /admin/teachers and sees a table of all TEACHER-role users with columns: Name, Email, Phone, Status, Actions"
    - "Admin clicks 'Create Teacher' → fills name/email/phone/password → submit → row appears in the table"
    - "Created teacher account can log in immediately at /login (existing teacher login endpoint accepts the new account)"
    - "Admin clicks 'Edit' on a row → name/phone editable + optional new password → submit → row updates"
    - "Admin clicks 'Disable' on an Active teacher → confirm dialog ('Keep teacher' / 'Disable account') → row Status flips to 'Disabled' badge"
    - "A disabled teacher cannot log in via POST /auth/login (returns 403 'Account disabled')"
    - "Re-enabling a disabled teacher restores login access"
    - "Duplicate email on create returns 409 surfaced as 'An account with this email already exists.'"
  artifacts:
    - path: backend/src/admin/admin-teachers.controller.ts
      provides: "GET/POST/PUT/PATCH endpoints under /admin/teachers"
      contains: "@Controller('admin/teachers')"
    - path: backend/src/admin/admin-teachers.service.ts
      provides: "findAll/create/update/setDisabled on TEACHER-role users"
      exports: ["AdminTeachersService"]
    - path: backend/src/auth/auth.service.ts
      provides: "login() rejects disabled teacher accounts"
      contains: "disabled"
    - path: frontend/app/admin/teachers/page.tsx
      provides: "Teacher CRUD table page with Create/Edit modal and Disable/Enable confirm dialog"
      contains: "Create Teacher"
  key_links:
    - from: frontend/app/admin/teachers/page.tsx
      to: backend/src/admin/admin-teachers.controller.ts
      via: "getTeachers/createTeacher/updateTeacher/disableTeacher/enableTeacher in admin-portal-api"
      pattern: "/admin/teachers"
    - from: backend/src/auth/auth.service.ts (login)
      to: User.disabled column
      via: "added check after approved gate"
      pattern: "user\\.disabled"
---

<objective>
Deliver the teacher-management vertical slice: **admin can create, edit, and disable/enable teacher accounts from `/admin/teachers`**. End-to-end this spans the backend `/admin/teachers` CRUD endpoints, the existing `/auth/login` flow (extended to reject disabled accounts — D-01/D-03 multi-teacher migration), the frontend API client extension, and the `/admin/teachers` page itself.

After this plan ships, a real user story is satisfied: as the platform administrator, I want to provision and revoke teacher accounts so that I can onboard real teachers without manually editing env vars or the database.

Purpose: Replaces the single-teacher env-seed model (D-03: existing seed survives but the DB becomes the source of truth). Once Plan 03 lands, the platform supports multiple teacher accounts.
Output: A live teacher CRUD page wired to live backend endpoints + working soft-delete semantics on login.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-admin-portal/06-CONTEXT.md
@.planning/phases/06-admin-portal/06-UI-SPEC.md
@.planning/phases/06-admin-portal/06-PATTERNS.md
@.planning/phases/06-admin-portal/06-01-SUMMARY.md
@.planning/phases/06-admin-portal/06-02-SUMMARY.md
@backend/src/class/class.controller.ts
@backend/src/class/class.service.ts
@backend/src/student/student.service.ts
@backend/src/auth/auth.service.ts
@backend/src/auth/auth.guard.ts
@frontend/app/teacher/classes/page.tsx
@frontend/lib/admin-portal-api.ts
@frontend/components/AdminShell.tsx

<interfaces>
<!-- Backend response shape used by the admin-teachers list endpoint -->
export interface TeacherItem {
  id: number;
  upn: string;       // also the email (UserModel.upn === email for teachers)
  name: string | null;
  phone: string | null;
  disabled: boolean;
  createdAt: string;
}

<!-- DTOs to expose -->
export class CreateTeacherDto {
  email: string;     // becomes user.upn AND user.email
  password: string;
  name: string;
  phone: string;
}

export class UpdateTeacherDto {
  name?: string;
  phone?: string;
  password?: string;   // optional — only updated if non-empty
}

<!-- Frontend admin-portal-api additions (extends file from Plan 02 Task 2) -->
export interface TeacherItem { id: number; upn: string; name: string | null; phone: string | null; disabled: boolean; createdAt: string; }
export interface CreateTeacherInput { email: string; password: string; name: string; phone: string; }
export interface UpdateTeacherInput { name?: string; phone?: string; password?: string; }

export const getTeachers     = () => req<TeacherItem[]>('/admin/teachers');
export const createTeacher   = (data: CreateTeacherInput) => req<TeacherItem>('/admin/teachers', { method: 'POST', body: JSON.stringify(data) });
export const updateTeacher   = (id: number, data: UpdateTeacherInput) => req<TeacherItem>(`/admin/teachers/${id}`, { method: 'PUT', body: JSON.stringify(data) });
export const disableTeacher  = (id: number) => req<{ id: number; disabled: boolean }>(`/admin/teachers/${id}/disable`, { method: 'PATCH' });
export const enableTeacher   = (id: number) => req<{ id: number; disabled: boolean }>(`/admin/teachers/${id}/enable`, { method: 'PATCH' });

<!-- AdminGuard contract (from Plan 06-01) — used on every admin-teachers route -->
@UseGuards(AdminGuard) @Controller('admin/teachers')
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Backend admin-teachers (CRUD + disable/enable) + AuthService login rejects disabled teachers</name>
  <files>
    backend/src/admin/admin-teachers.dto.ts,
    backend/src/admin/admin-teachers.service.ts,
    backend/src/admin/admin-teachers.controller.ts,
    backend/src/admin/admin.module.ts,
    backend/src/auth/auth.service.ts
  </files>
  <read_first>
    - backend/src/class/class.controller.ts (controller skeleton with Get/Post/Put/Delete + AuthGuard)
    - backend/src/class/class.service.ts (NotFoundException + ConflictException patterns)
    - backend/src/student/student.service.ts lines 24-33 (creating a User row alongside a domain entity — pattern for hashing password + setting role + approved)
    - backend/src/auth/auth.service.ts lines 15-25 (existing login() — extend to check user.disabled)
    - backend/src/auth/auth.guard.ts lines 7-28 (AuthGuard — also extend approved check? NO — login is the single point that returns the error, guard's existing approved check stays as-is for student approval semantics)
    - backend/src/admin/admin.module.ts (created in Plan 02 Task 1 — add new service + controller to providers/controllers)
    - .planning/phases/06-admin-portal/06-PATTERNS.md (sections "backend/src/admin/admin-teachers.controller.ts", "admin-teachers.service.ts" — full code analogs)
    - .planning/phases/06-admin-portal/06-CONTEXT.md (D-02, D-07: teacher fields; disable = soft-delete flag)
    - .planning/phases/06-admin-portal/06-UI-SPEC.md (error copy: "An account with this email already exists.")
  </read_first>
  <behavior>
    - Test 1 (admin-teachers.service.spec): `findAll()` returns only users with `role: 'TEACHER'`, ordered by `createdAt: 'asc'`, selecting `{id, upn, name, phone, disabled, createdAt}` (NO password field in output).
    - Test 2: `create({ email, password, name, phone })` throws ConflictException with message `'An account with this email already exists.'` when a user with that upn already exists.
    - Test 3: `create` hashes the password with bcrypt before storing, creates a User with `role: 'TEACHER'`, `approved: true`, `disabled: false` (default), `upn = email`, `email = email`, and returns the row WITHOUT the password field.
    - Test 4: `update(id, {name, phone})` updates only provided fields; `update(id, {password})` re-hashes and updates the password field.
    - Test 5: `setDisabled(id, true)` flips the `disabled` column to true; `setDisabled(id, false)` flips it to false. Both throw NotFoundException if the teacher doesn't exist.
    - Test 6 (auth.service.spec extension): `login({ upn, password })` for a teacher whose `disabled === true` throws ForbiddenException with message `'Account disabled'`. A teacher with `disabled === false` (default) still logs in normally.
  </behavior>
  <action>
    Apply five edits:

    1. **`backend/src/admin/admin-teachers.dto.ts`** (new) — export `CreateTeacherDto { email; password; name; phone }` and `UpdateTeacherDto { name?; phone?; password? }`. All fields `string` (plain DTOs, no class-validator — match existing project style in class.dto.ts).

    2. **`backend/src/admin/admin-teachers.service.ts`** (new) — `@Injectable() AdminTeachersService` with constructor dep `PrismaService`. Implement:
       - `findAll()` → `prisma.user.findMany({ where: { role: UserRole.TEACHER }, select: { id: true, upn: true, name: true, phone: true, disabled: true, createdAt: true }, orderBy: { createdAt: 'asc' } })`.
       - `async create(dto)`:
         - `prisma.user.findUnique({ where: { upn: dto.email } })` → if exists, throw `ConflictException('An account with this email already exists.')`.
         - `bcrypt.hash(dto.password, 10)`.
         - `prisma.user.create({ data: { upn: dto.email, email: dto.email, name: dto.name, phone: dto.phone, password: hashed, role: UserRole.TEACHER, approved: true }, select: {…same 6 fields as findAll} })`.
       - `async findById(id)`: `prisma.user.findFirst({ where: { id, role: UserRole.TEACHER } })`. Throw `NotFoundException('Teacher ${id} not found')` if null.
       - `async update(id, dto)`: call `findById(id)` first (throws 404 if missing). Build `data` object including only defined fields; if `dto.password` is a non-empty string, set `data.password = await bcrypt.hash(dto.password, 10)`. Return `prisma.user.update({ where: { id }, data, select: same 6 fields })`.
       - `async setDisabled(id, disabled: boolean)`: call `findById(id)` first; `prisma.user.update({ where: { id }, data: { disabled }, select: { id, upn, disabled } })`. Used by both PATCH `/disable` (true) and PATCH `/enable` (false).

    3. **`backend/src/admin/admin-teachers.controller.ts`** (new) — `@UseGuards(AdminGuard) @Controller('admin/teachers')`. Routes:
       - `@Get() findAll()` → `service.findAll()`.
       - `@Post() create(@Body() dto: CreateTeacherDto)` → `service.create(dto)`.
       - `@Put(':id') update(@Param('id', ParseIntPipe) id, @Body() dto: UpdateTeacherDto)` → `service.update(id, dto)`.
       - `@Patch(':id/disable') disable(@Param('id', ParseIntPipe) id)` → `service.setDisabled(id, true)`.
       - `@Patch(':id/enable') enable(@Param('id', ParseIntPipe) id)` → `service.setDisabled(id, false)`.
       Import `AdminGuard` from `../auth/auth.guard`. Do NOT add a Delete route (CONTEXT D-07: disable, not delete).

    4. **`backend/src/admin/admin.module.ts`** (edit) — add `AdminTeachersService` to `providers` and `AdminTeachersController` to `controllers`. Add the imports at the top. Preserve `AdminAuthService`, `AdminStatsService`, and their controllers from Plan 02.

    5. **`backend/src/auth/auth.service.ts`** (edit, login method only) — after the existing `if (!user.approved) throw new ForbiddenException('Account pending approval');` line, add:
       `if (user.disabled) throw new ForbiddenException('Account disabled');`
       Place the disabled check AFTER the approved check (preserves existing semantics for unapproved students). Do NOT modify any other method in auth.service.ts.
  </action>
  <verify>
    <automated>cd backend && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "admin-teachers|auth\\.service\\.ts" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "@Controller('admin/teachers')" backend/src/admin/admin-teachers.controller.ts` returns 1
    - `grep -c "@UseGuards(AdminGuard)" backend/src/admin/admin-teachers.controller.ts` returns 1
    - `grep -c "@Patch(':id/disable')\\|@Patch(':id/enable')" backend/src/admin/admin-teachers.controller.ts` returns 2
    - `grep -c "@Delete" backend/src/admin/admin-teachers.controller.ts` returns 0 (no hard delete per D-07)
    - `grep -c "An account with this email already exists" backend/src/admin/admin-teachers.service.ts` returns 1
    - `grep -c "role: UserRole.TEACHER" backend/src/admin/admin-teachers.service.ts` returns ≥ 2 (findAll where + create data)
    - `grep -c "bcrypt.hash" backend/src/admin/admin-teachers.service.ts` returns ≥ 2 (create + update password branch)
    - `grep -v '^#' backend/src/admin/admin.module.ts | grep -c "AdminTeachersService\\|AdminTeachersController"` returns ≥ 2
    - `grep -c "user.disabled" backend/src/auth/auth.service.ts` returns ≥ 1
    - `grep -c "Account disabled" backend/src/auth/auth.service.ts` returns 1
    - `npx tsc --noEmit` (from backend/) exits 0
    - Manual: `curl -X POST /admin/teachers -H 'Authorization: Bearer <admin>' -H 'Content-Type: application/json' -d '{"email":"t1@x.com","password":"pw123","name":"T1","phone":"123"}'` returns 200 with TeacherItem
    - Manual: same POST repeated returns 409 with message "An account with this email already exists."
    - Manual: `curl -X POST /auth/login -d '{"upn":"t1@x.com","password":"pw123"}'` returns 200; then `PATCH /admin/teachers/{id}/disable` and re-login returns 403 "Account disabled"
  </acceptance_criteria>
  <done>Admin teacher CRUD endpoints live and guarded; disabled flag is enforced at login.</done>
</task>

<task type="auto">
  <name>Task 2: Extend admin-portal-api with teacher CRUD functions; build /admin/teachers page with table, modal, and confirm dialog</name>
  <files>
    frontend/lib/admin-portal-api.ts,
    frontend/app/admin/teachers/page.tsx
  </files>
  <read_first>
    - frontend/lib/admin-portal-api.ts (created in Plan 02 Task 2 — extend with teacher functions; do NOT touch the existing `req` helper or `getAdminStats`)
    - frontend/app/teacher/classes/page.tsx (analog for modal pattern lines 42-212, table-like row layout lines 360-410, toast pattern lines 227+262-266)
    - frontend/components/ui/table.tsx (shadcn Table primitives — confirm exports: Table, TableBody, TableCell, TableHead, TableHeader, TableRow)
    - frontend/components/ui/dialog.tsx (Dialog, DialogContent, DialogHeader, DialogTitle, DialogFooter)
    - frontend/components/ui/badge.tsx (Badge primitive — used for Active/Disabled status)
    - .planning/phases/06-admin-portal/06-UI-SPEC.md (entire /admin/teachers copy table + status badge colors emerald-50/text-emerald-700 vs slate-100/text-slate-500 + "Dismiss label rule" — Cancel is prohibited)
    - .planning/phases/06-admin-portal/06-PATTERNS.md (section "frontend/app/admin/teachers/page.tsx — teacher CRUD table" full pattern)
  </read_first>
  <action>
    Apply two edits:

    1. **`frontend/lib/admin-portal-api.ts`** (extend) — Add the following exports below `getAdminStats`. Do NOT modify the file header or the `req<T>` helper.
       - `export interface TeacherItem { id: number; upn: string; name: string | null; phone: string | null; disabled: boolean; createdAt: string; }`
       - `export interface CreateTeacherInput { email: string; password: string; name: string; phone: string; }`
       - `export interface UpdateTeacherInput { name?: string; phone?: string; password?: string; }`
       - `export const getTeachers = () => req<TeacherItem[]>('/admin/teachers');`
       - `export const createTeacher = (data: CreateTeacherInput) => req<TeacherItem>('/admin/teachers', { method: 'POST', body: JSON.stringify(data) });`
       - `export const updateTeacher = (id: number, data: UpdateTeacherInput) => req<TeacherItem>(\`/admin/teachers/${id}\`, { method: 'PUT', body: JSON.stringify(data) });`
       - `export const disableTeacher = (id: number) => req<{ id: number; disabled: boolean }>(\`/admin/teachers/${id}/disable\`, { method: 'PATCH' });`
       - `export const enableTeacher = (id: number) => req<{ id: number; disabled: boolean }>(\`/admin/teachers/${id}/enable\`, { method: 'PATCH' });`

    2. **`frontend/app/admin/teachers/page.tsx`** (new) — Client component, default export `TeachersPage`. Structure:
       - State: `teachers: TeacherItem[]`, `loading`, `error`, `editing: TeacherItem | null`, `creating: boolean`, `confirmTarget: TeacherItem | null` (the row currently in the disable/enable confirm dialog), `toast: string`.
       - `loadTeachers()` calls `getTeachers()` and populates state; called in `useEffect` on mount and after every CRUD mutation.
       - **Header row**: page title comes from AdminLayout (title="Teachers"). Add a top-right "Create Teacher" button (`bg-[#4F9DFF] text-white` per UI-SPEC accent) that sets `creating = true`.
       - **Table** using shadcn `<Table>` with columns "Name", "Email", "Phone", "Status", "Actions" (exact strings from UI-SPEC). Use sticky header. Row hover: `hover:bg-slate-50`. Render rows from `teachers` state.
         - Status cell: `disabled ? <Badge className="bg-slate-100 text-slate-500">Disabled</Badge> : <Badge className="bg-emerald-50 text-emerald-700">Active</Badge>` (per UI-SPEC).
         - Disabled-row text styling: `disabled ? 'text-slate-400' : ''` on the `<TableRow>`.
         - Actions cell: ghost buttons "Edit" (always) and "Disable" or "Enable" depending on current disabled state.
       - **Empty state** (zero teachers after load): render heading "No teachers yet" and body "Create the first teacher account to get started." per UI-SPEC.
       - **TeacherModal** (inline component): shadcn Dialog with header "Create Teacher" or "Edit Teacher" (UI-SPEC copy). Fields: name (required), email (required, disabled in edit mode — email is immutable), phone (required), password (required on create, optional on edit with placeholder "Leave blank to keep current").
         - Submit: on create call `createTeacher(form)`; on edit call `updateTeacher(editing.id, {name, phone, password: password || undefined})`. On success: showToast("Teacher account created.") or showToast("Changes saved.") per UI-SPEC. On error: setError to `err.message` (parseApiError already produces a user-readable message; if generic, fall back to "Failed to save. Please try again.").
         - Submit button uses loading-state pattern: text "Saving..." + `disabled` + `opacity-50` while pending.
       - **Disable/Enable confirm Dialog**: when `confirmTarget` is non-null, show shadcn Dialog with:
         - Title: "Disable teacher?" or "Enable teacher?" (with the implicit detail in body).
         - Body: `${confirmTarget.disabled ? 'Enable teacher? ' + confirmTarget.name + ' will be able to log in again.' : 'Disable teacher? ' + confirmTarget.name + ' will no longer be able to log in until re-enabled.'}` (UI-SPEC literal copy).
         - Footer: two buttons — destructive ("Disable account" with `bg-destructive text-white`) or constructive ("Enable account" with `bg-[#4F9DFF] text-white`), and a dismiss button labeled "Keep teacher" (NOT "Cancel" — UI-SPEC Dismiss label rule).
         - On confirm: call `disableTeacher(id)` or `enableTeacher(id)` then reload + close + showToast (no specific success toast in UI-SPEC for disable/enable, reuse "Changes saved.").
       - **Toast**: bottom-right `fixed` position, 3s auto-dismiss, same pattern as teacher/classes/page.tsx (`fixed bottom-6 right-6 z-50 bg-textPrimary text-white text-sm font-semibold px-5 py-3 rounded-2xl shadow-2xl`).
       - **Forms must use shadcn Input/Label/Button** from `@/components/ui/*`. Use Inter font (already global). All borders use `border-border` token.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | grep -E "teachers/page|admin-portal-api" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export const getTeachers" frontend/lib/admin-portal-api.ts` returns 1
    - `grep -c "export const createTeacher\\|export const updateTeacher\\|export const disableTeacher\\|export const enableTeacher" frontend/lib/admin-portal-api.ts` returns 4
    - `grep -c "Create Teacher" frontend/app/admin/teachers/page.tsx` returns ≥ 1
    - `grep -c "Edit Teacher" frontend/app/admin/teachers/page.tsx` returns ≥ 1
    - `grep -c "Disable account\\|Enable account" frontend/app/admin/teachers/page.tsx` returns ≥ 2
    - `grep -c "Keep teacher" frontend/app/admin/teachers/page.tsx` returns ≥ 1
    - `grep -v '^#' frontend/app/admin/teachers/page.tsx | grep -c "'Cancel'\\|>Cancel<"` returns 0 (Cancel prohibited)
    - `grep -c "bg-emerald-50 text-emerald-700" frontend/app/admin/teachers/page.tsx` returns ≥ 1
    - `grep -c "bg-slate-100 text-slate-500" frontend/app/admin/teachers/page.tsx` returns ≥ 1
    - `grep -c "No teachers yet" frontend/app/admin/teachers/page.tsx` returns 1
    - `grep -c "An account with this email already exists" frontend/app/admin/teachers/page.tsx` returns 0 (error comes from backend message via parseApiError — do NOT hardcode in UI)
    - `npx tsc --noEmit` (from frontend/) exits 0
    - Behavior: visiting `/admin/teachers` shows table; clicking "Create Teacher" opens modal; submitting creates row + shows toast; the new teacher can log in at `/login`; clicking "Disable" opens confirm dialog with "Keep teacher" / "Disable account" buttons; confirming flips badge to Disabled and the teacher can no longer log in (403 at /auth/login)
  </acceptance_criteria>
  <done>/admin/teachers fully wired to backend CRUD + disable/enable; UI-SPEC copy and badge colors match exactly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /admin/teachers/* | All requests guarded by AdminGuard (server-side, JWT role check) |
| Admin payload → User.password | Plaintext password crosses the boundary in POST/PUT bodies; must be bcrypt-hashed before persistence |
| auth.service.login → User.disabled | New gate added; disabled teachers must not receive tokens |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-01 | Tampering | CreateTeacherDto/UpdateTeacherDto | mitigate | Service ignores any field not in the destructured create/update payload; only `name`, `phone`, `password` are accepted on update (mass-assignment protection) |
| T-06-03-02 | Information Disclosure | Teacher response payloads | mitigate | All findAll/create/update queries use explicit `select` clauses that omit `password` and `registrationData` |
| T-06-03-03 | Spoofing / Privilege Escalation | Disable bypass | mitigate | `auth.service.login` checks `user.disabled` after `user.approved`; rejects with `ForbiddenException('Account disabled')`. AuthGuard does NOT check `disabled` because existing JWTs may outlive a disable action — but the next login attempt will fail. Acceptable per CONTEXT D-07 (soft-delete = re-login blocked, not active-session revocation). |
| T-06-03-04 | IDOR | PUT/PATCH /admin/teachers/:id | mitigate | Service `findById` filters by `role: 'TEACHER'`. An admin attempting to PUT a STUDENT or ADMIN user id receives 404, preventing role-switching via the teacher endpoint. |
| T-06-03-05 | Repudiation | Teacher account changes | accept | Single admin; audit logging deferred. Existing NestJS HTTP log captures method+path+status. |
| T-06-03-06 | Information Disclosure | "An account with this email already exists" | accept | This message intentionally reveals email existence — admin context only (behind AdminGuard); non-admins cannot reach the endpoint, so admin-side user enumeration is acceptable for UX clarity. |
| T-06-03-07 | DoS | Bcrypt cost on create/update | accept | bcrypt cost 10 matches existing project standard; admin endpoint usage is low-frequency. |
| T-06-03-08 | Tampering | Email immutability | mitigate | UpdateTeacherDto omits `email` field; service does not write to `upn` or `email` on update. UI also disables the email input in edit mode. |
</threat_model>

<verification>
- `npx tsc --noEmit` from `backend/` and `frontend/` both exit 0
- Backend smoke: full lifecycle via curl — list 0 teachers; POST creates one; teacher logs in at /auth/login; PATCH /disable returns {disabled:true}; teacher login returns 403 "Account disabled"; PATCH /enable returns {disabled:false}; teacher login succeeds again
- Frontend smoke: `/admin/teachers` shows table; Create modal works; Edit modal updates name+phone, leaves password unchanged when blank; Disable confirm dialog uses "Keep teacher" / "Disable account" labels; Cancel button is nowhere on the page
</verification>

<success_criteria>
- ADMIN-02: admin can create a teacher account from `/admin/teachers` with name, email, phone, password — created teacher can immediately log in at `/login`
- ADMIN-03: admin can edit teacher (name/phone/password) and disable/enable — disabled teachers cannot log in until re-enabled
- D-02 (teacher fields), D-07 (disable=soft-delete), D-11 (no admin-managed class reassign) all respected
- UI-SPEC copy exactly matches: "Create Teacher", "Edit Teacher", "Disable account", "Enable account", "Keep teacher", "An account with this email already exists.", "No teachers yet", Active/Disabled badge colors
- The word "Cancel" appears nowhere on the page (UI-SPEC Dismiss label rule)
</success_criteria>

<output>
Create `.planning/phases/06-admin-portal/06-03-SUMMARY.md` when done with:
- Admin-teachers endpoint inventory (5 routes: GET, POST, PUT, PATCH/disable, PATCH/enable)
- Multi-teacher migration note (auth.service login disabled gate)
- Frontend page structure + screenshot/notes from manual smoke
- Confirmed ADMIN-02 + ADMIN-03 demonstrable
</output>
</content>
</invoke>