---
phase: 6
slug: admin-portal
status: draft
shadcn_initialized: true
preset: base-nova
created: 2026-05-22
---

# Phase 6 — UI Design Contract

> Visual and interaction contract for the Admin Portal. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn (components.json confirmed) |
| Preset | base-nova |
| Style | base-nova (components.json `"style": "base-nova"`) |
| Component library | Radix UI (via shadcn) |
| Icon library | lucide-react |
| Font | Inter (`var(--font-inter)`, `fontFamily.sans`) |

Source: `frontend/components.json`, `frontend/tailwind.config.js`

---

## Design System Tokens (pre-populated from existing codebase)

The following tokens are already declared in `tailwind.config.js` and `globals.css`. The admin portal uses these without modification.

| Token | Value | Role |
|-------|-------|------|
| `background` | `#F7F9FC` | Page background |
| `card` | `#FFFFFF` | Cards, panels |
| `border` | `#E2E8F0` | All borders |
| `textPrimary` | `#0F172A` | Headings, primary text |
| `textSecondary` | `#64748B` | Labels, meta text |
| `primary` | `#4F9DFF` | Primary actions |
| `destructive` | `#FF7B7B` (via `var(--destructive)`) | Destructive actions only |
| `--radius` | `0.75rem` | Border radius for all components |
| Sidebar bg | `#0C1220` | Dark sidebar (matched from TeacherShell) |
| Admin accent | `#4F9DFF` | Admin portal accent (primary blue — not orange, which is teacher portal) |

> **Admin vs Teacher accent distinction:** The teacher portal uses `#F0623A` (orange) as its active-nav accent. The admin portal uses `#4F9DFF` (primary blue) as its active-nav accent to visually distinguish the two portals.

---

## Spacing Scale

Declared values (multiples of 4 only):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline padding |
| sm | 8px | Compact element spacing, nav item gaps |
| md | 16px | Default element spacing, form field gaps |
| lg | 24px | Section padding, card internal padding |
| xl | 32px | Layout gaps between major sections |
| 2xl | 48px | Page header top padding |
| 3xl | 64px | Reserved (unused in this phase) |

Exceptions:
- Sidebar width: 240px (`w-60`) — matched from TeacherShell
- Page header horizontal padding: 32px (`px-8`) — matched from TeacherShell
- Sidebar logo area: `px-5 pt-7 pb-6` — matched from TeacherShell
- `minWidth: 1280` on root container (desktop-only, matched from teacher dashboard)

---

## Typography

Exactly 4 sizes, exactly 2 weights.

| Role | Size | Weight | Line Height | Usage |
|------|------|--------|-------------|-------|
| Label / nav-meta | 12px (`text-xs`) | 400 (regular) | 1.4 | Breadcrumbs, stat card subtitles, nav group labels (`uppercase tracking-widest text-slate-600`) |
| Body | 14px (`text-sm`) | 400 (regular) | 1.5 | Table rows, form labels, nav items, card body |
| Heading | 26px | 700 (bold) | 1.0 (`leading-none`) | Page title (h1) |
| Display | 28px | 700 (bold) | 1.1 | Stat card numbers on dashboard |

Weight contract:
- **400 (regular):** body text, labels, nav-meta, idle nav items
- **700 (bold):** page headings (h1), display stat numbers, active nav items

Notes:
- Nav group labels use the 12px Label role with `uppercase tracking-widest text-slate-600` — no separate 10px size
- Active nav item uses `font-bold` (700), not semibold
- Source: `TeacherShell.tsx` heading pattern adapted; weight reduced from 900 to 700

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#F7F9FC` | Page background, main content area |
| Secondary (30%) | `#0C1220` (sidebar) + `#FFFFFF` (cards) | Sidebar navigation, data cards, table rows |
| Accent (10%) | `#4F9DFF` | Active nav indicator, primary CTA buttons, stat card highlights, focus rings |
| Destructive | `#FF7B7B` (`var(--destructive)`) | Disable teacher button, delete class button, delete homework/session button, destructive confirm dialog trigger |

Accent reserved for:
1. Active sidebar nav item background (`rgba(79, 157, 255, 0.12)`) and left-edge indicator bar
2. Primary action buttons: "Create Teacher", "Save Changes"
3. Stat count highlights on the dashboard (teacher/class/student/submission counts)
4. Input focus ring (`ring-primary/10`)

Accent NOT used for: table row hover states, secondary actions, breadcrumbs, badges for status.

Status badge colors (power-user UI, semantic):
- Active/Enabled teacher: `bg-emerald-50 text-emerald-700` (green)
- Disabled teacher: `bg-slate-100 text-slate-500` (neutral)

---

## Focal Point

**Primary screen (Dashboard):** The primary visual anchor is the 4-column stat card grid. The stat numbers — the largest text on screen at 28px bold — draw the eye first. Accent-colored values (`#4F9DFF`) within the cards reinforce hierarchy and direct attention to platform-wide counts before the user reads the nav or page chrome.

---

## Copywriting Contract

### Global

| Element | Copy |
|---------|------|
| Portal label (sidebar subtitle) | "Admin Portal" |
| Breadcrumb prefix | "Admin Portal › {Page}" |
| User menu role label | "Administrator" |
| Sign out | "Sign out" |

### /admin/login

| Element | Copy |
|---------|------|
| Page heading | "Admin Login" |
| Subheading | "Platform administrator access" |
| Email field label | "Email" |
| Password field label | "Password" |
| Primary CTA | "Sign in" |
| Error: invalid credentials | "Invalid email or password" |
| Error: account not found | "Invalid email or password" (same — do not reveal which field failed) |

### /admin (Dashboard)

| Element | Copy |
|---------|------|
| Page title (h1) | "Dashboard" |
| Stat card: teachers | "Teachers" |
| Stat card: classes | "Classes" |
| Stat card: students | "Students" |
| Stat card: submissions | "Submissions" |
| Empty state (no data) | "No data yet. Create teacher accounts to get started." |

### /admin/teachers

| Element | Copy |
|---------|------|
| Page title | "Teachers" |
| Primary CTA | "Create Teacher" |
| Table column headers | "Name", "Email", "Phone", "Status", "Actions" |
| Empty state heading | "No teachers yet" |
| Empty state body | "Create the first teacher account to get started." |
| Disabled badge | "Disabled" |
| Active badge | "Active" |
| Edit action | "Edit" |
| Disable action | "Disable" |
| Enable action | "Enable" |
| Create modal heading | "Create Teacher" |
| Edit modal heading | "Edit Teacher" |
| Disable confirmation | "Disable teacher? {name} will no longer be able to log in until re-enabled." |
| Disable confirm button | "Disable account" |
| Disable dialog dismiss | "Keep teacher" |
| Enable confirmation | "Enable teacher? {name} will be able to log in again." |
| Enable confirm button | "Enable account" |
| Enable dialog dismiss | "Keep teacher" |
| Error: email already exists | "An account with this email already exists." |
| Error: save failed | "Failed to save. Please try again." |
| Success: teacher created | "Teacher account created." |
| Success: teacher updated | "Changes saved." |

### /admin/classes

| Element | Copy |
|---------|------|
| Page title | "Classes" |
| Filter label | "Filter by teacher" |
| Filter placeholder | "All teachers" |
| Table column headers | "Class Name", "Teacher", "Students", "Status", "Actions" |
| Empty state (no filter) heading | "No classes yet" |
| Empty state (no filter) body | "Classes are created by teachers from their dashboard." |
| Empty state (filtered) heading | "No classes for this teacher" |
| Empty state (filtered) body | "This teacher has not created any classes yet." |
| Edit action | "Edit" |
| Delete action | "Delete" |
| Edit modal heading | "Edit Class" |
| Delete confirmation | "Delete class? All homework and sessions in this class will be permanently deleted." |
| Delete confirm button | "Delete class" |
| Delete dialog dismiss | "Keep class" |
| Error: delete failed | "Failed to delete class. Please try again." |
| Success: class updated | "Class updated." |

### /admin/students

| Element | Copy |
|---------|------|
| Page title | "Students" |
| Table column headers | "Student Name", "Class", "Teacher", "Homeworks", "Actions" |
| Empty state heading | "No students yet" |
| Empty state body | "Students are added to classes by teachers." |
| View results action | "View Results" |
| Results drill-down heading | "{Student Name} — Homework Results" |
| Empty results state | "No homework submissions yet." |
| Back link | "Back to Students" |
| Delete session action | "Delete" |
| Delete session confirmation | "Delete session? This will permanently remove the student's submission and score." |
| Delete session confirm button | "Delete session" |
| Delete session dialog dismiss | "Keep session" |

### Shared / Destructive flow

| Element | Copy |
|---------|------|
| Generic error | "Something went wrong. Please try again." |
| Generic loading | "Loading..." |

> **Dismiss label rule:** Every destructive confirmation dialog uses a context-specific dismiss label (declared per-dialog above). The word "Cancel" is prohibited as a CTA label.

---

## Component Inventory

Existing shadcn components available in `frontend/components/ui/`:
- `button.tsx` — all CTA and action buttons
- `input.tsx` — all form fields
- `label.tsx` — all form labels
- `card.tsx` — stat cards on dashboard
- `badge.tsx` — Active/Disabled status badges
- `table.tsx` — teacher/class/student data tables
- `dialog.tsx` — create/edit modals and destructive confirmations
- `select.tsx` — teacher filter dropdown on /admin/classes
- `avatar.tsx` — user avatar in header
- `separator.tsx` — dividers

New component to build:
- `AdminShell` — mirrors `TeacherShell.tsx` layout pattern (sidebar `#0C1220` + header + content area) with admin nav items and blue accent instead of orange

---

## Layout Contract

### AdminShell structure

```
<div class="flex h-screen bg-background" style="minWidth: 1280">
  <aside class="w-60 flex-shrink-0" style="background: #0C1220">
    <!-- Logo: "K" mark + "Katie English" + "Admin Portal" subtitle -->
    <!-- Nav groups: GENERAL group label at 12px uppercase tracking-widest text-slate-600 -->
    <!-- Nav items: Dashboard, Teachers, Classes, Students -->
    <!-- Active indicator: left 3px bar + bg rgba(79,157,255,0.12) + text #60A5FA + font-bold -->
    <!-- Footer: "© Katie English" -->
  </aside>
  <div class="flex-1 flex flex-col overflow-hidden">
    <main class="flex-1 overflow-y-auto">
      <!-- Page header: px-8 pt-8 pb-6 — breadcrumb + h1 + user avatar -->
      <!-- Content: px-8 pb-8 -->
    </main>
  </div>
</div>
```

### Admin nav items

| Label | Route | Icon |
|-------|-------|------|
| Dashboard | /admin | LayoutDashboard |
| Teachers | /admin/teachers | Users |
| Classes | /admin/classes | School |
| Students | /admin/students | GraduationCap |

### Dashboard stat cards

4 cards in a responsive grid (`grid-cols-4` at desktop):
- Each card: `bg-white rounded-2xl border border-border shadow-card p-6`
- Stat number: 28px bold (700), `textPrimary`
- Stat label: 14px regular (400), `textSecondary`
- Icon: 20px lucide icon, colored with accent or muted

### Data tables

All 3 data pages (teachers, classes, students) use:
- `<Table>` from shadcn
- Sticky header
- Row hover: `hover:bg-slate-50`
- Action buttons in last column: ghost variant, 14px
- Pagination: not required in v1 (load all)

---

## Interaction States

| State | Treatment |
|-------|-----------|
| Form submit loading | Button text changes to "Saving...", `disabled`, `opacity-50` |
| Table row loading (delete) | Row opacity 50%, spinner in action cell |
| Login error | Red inline text below password field, no toast |
| CRUD success | Inline toast (top-right, 3s auto-dismiss) or page-level success banner |
| Destructive confirmation | `<Dialog>` modal with destructive-colored confirm button (`bg-destructive text-white`) |
| Disabled teacher row | Row text `text-slate-400`, "Disabled" badge |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | button, input, label, card, badge, table, dialog, select, avatar, separator | not required |

No third-party registries declared. Registry vetting gate: not applicable.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
