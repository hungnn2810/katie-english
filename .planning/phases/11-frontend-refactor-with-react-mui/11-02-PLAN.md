---
phase: 11-frontend-refactor-with-react-mui
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - frontend/components/TeacherShell.tsx
  - frontend/app/teacher/layout.tsx
  - frontend/app/teacher/page.tsx
  - frontend/app/teacher/classes/page.tsx
  - frontend/app/teacher/students/page.tsx
  - frontend/app/teacher/sessions/page.tsx
  - frontend/app/teacher/homework/page.tsx
  - frontend/app/teacher/homework/[id]/page.tsx
  - frontend/app/teacher/homework/[id]/edit/page.tsx
  - frontend/app/teacher/homework/[id]/try/page.tsx
  - frontend/app/teacher/homework/create/reading/page.tsx
  - frontend/app/teacher/homework/[id]/session/[sessionId]/page.tsx
  - frontend/app/teacher/homework/_components/ReadingCreationPage.tsx
autonomous: true
requirements: [FE-03]

must_haves:
  truths:
    - "A teacher can navigate the dashboard, classes, students, sessions, and homework pages with the MUI sidebar shell and no Tailwind classes remain"
    - "Teacher can create/edit a reading homework — dnd-kit reorder still works; only the draggable visual wrapper became MUI Paper (D-08)"
    - "The shake animation still fires on the homework Try page using the MUI keyframe"
    - "No file under app/teacher imports @/components/ui"
  artifacts:
    - path: "frontend/components/TeacherShell.tsx"
      provides: "MUI Box sidebar shell (no AppBar) with List/ListItemButton nav + Menu user dropdown"
      contains: "@mui/material"
    - path: "frontend/app/teacher/homework/_components/ReadingCreationPage.tsx"
      provides: "Reading creation with dnd-kit intact + MUI Paper draggable wrapper"
      contains: "Paper"
  key_links:
    - from: "frontend/app/teacher/homework/[id]/try/page.tsx"
      to: "frontend/lib/theme.ts"
      via: "import shake keyframe"
      pattern: "shake"
    - from: "frontend/components/TeacherShell.tsx"
      to: "@mui/material/ListItemButton"
      via: "nav link component={Link}"
      pattern: "ListItemButton"
---

<objective>
Migrate the entire teacher area to MUI: the TeacherShell sidebar, the teacher layout loading state, all 10 teacher pages, and the ReadingCreationPage (dnd-kit visual wrapper only). Replace every `@/components/ui` import and Tailwind `className` with MUI components + `sx`, preserving the current visual appearance and all behavior (D-00, FE-03).

Purpose: After this plan a teacher can use the full dashboard/creation/assignment flow rendered entirely in MUI with zero behavior regression. This is the teacher-facing vertical slice — a real teacher can do everything they could before, now on the standardized component system.

Output: Teacher area fully migrated; `npm run build` compiles the teacher routes; dnd-kit reorder and shake animation preserved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-frontend-refactor-with-react-mui/11-CONTEXT.md
@.planning/phases/11-frontend-refactor-with-react-mui/11-RESEARCH.md
@.planning/phases/11-frontend-refactor-with-react-mui/11-PATTERNS.md
@.claude/skills/frontend-nextjs-app.md
@.planning/phases/11-frontend-refactor-with-react-mui/11-01-SUMMARY.md

<interfaces>
<!-- From plan 11-01 (must exist before this plan runs): -->
<!--   @/lib/theme exports: baseTheme, and keyframes shake, fadeIn, slideUp -->
<!--   @/components/ui/* NO LONGER EXISTS — all imports must move to @mui/material/* -->
<!-- Shadcn -> MUI mapping (11-RESEARCH Pattern 5 / 11-PATTERNS Shared Patterns): -->
<!--   Button -> @mui/material/Button; Input -> @mui/material/TextField; Label -> FormLabel; -->
<!--   Dialog(+Content/Header/Title) -> Dialog+DialogTitle+DialogContent+DialogActions; -->
<!--   Badge -> Chip; Table+variants -> @mui/material Table/TableHead/TableBody/TableRow/TableCell(+TableContainer/Paper); -->
<!--   Select -> Select+MenuItem+FormControl+InputLabel; Card -> Card/Paper; DatePicker -> @mui/x-date-pickers/DatePicker -->
<!-- TeacherShell ACCENT = '#F0623A'; sidebar bg '#0C1220'; outer minWidth 1280 -->
<!-- Full copy-ready TeacherShell / Dialog / Table / Form-field / Paper-wrapper patterns are in 11-PATTERNS.md -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate TeacherShell + teacher/layout loading state to MUI</name>
  <files>frontend/components/TeacherShell.tsx, frontend/app/teacher/layout.tsx</files>
  <read_first>
    - frontend/components/TeacherShell.tsx (current: aside sidebar, nav Links, user dropdown, change-password form using shadcn Button/Input)
    - frontend/app/teacher/layout.tsx (loading spinner div lines ~27-31; TeacherUserContext + TeacherShell wiring must stay)
    - .planning/phases/11-frontend-refactor-with-react-mui/11-PATTERNS.md (TeacherShell MUI target, Loading Spinner Pattern)
    - .planning/phases/11-frontend-refactor-with-react-mui/11-RESEARCH.md (Pattern 4 sidebar shell; Anti-Pattern: do NOT use Drawer/AppBar for the CSS-flex sidebar)
  </read_first>
  <action>
    Migrate `frontend/components/TeacherShell.tsx` to MUI: outer `<div className="flex h-screen" style={minWidth:1280}>` becomes `Box sx={{ display:'flex', height:'100vh', minWidth:1280 }}`; `<aside className="w-60...">` becomes `Box sx={{ width:240, flexShrink:0, display:'flex', flexDirection:'column', bgcolor:'#0C1220', boxShadow:'1px 0 0 rgba(255,255,255,0.05)' }}`. Convert nav `<Link>` items to `ListItemButton component={Link} href={item.href} selected={active}` inside `List`/`ListItem`, with the selected state using `'&.Mui-selected': { bgcolor: ACCENT_BG, color: ACCENT_TEXT }` (keep ACCENT `#F0623A`). Use `ListItemIcon`/`ListItemText` with lucide icons. Convert the nav-group label `<p>` to `Typography variant="caption"`. Convert the change-password form: shadcn `Input type="password"` to MUI `TextField type="password" size="small" fullWidth`, shadcn submit `Button` to MUI `Button variant="contained"` with `sx={{ bgcolor: ACCENT }}`. Convert the user dropdown custom absolute div to MUI `Menu`/`MenuItem` anchored to the trigger. Do NOT use MUI `Drawer` or `AppBar` (RESEARCH anti-pattern — keep the CSS-flex sidebar). Preserve `usePathname`/`useRouter`/`clearAuth`/`changePassword` logic unchanged.
    Migrate `frontend/app/teacher/layout.tsx`: replace the loading spinner div with `Box` (sx minHeight '100vh', bgcolor 'grey.50', flex center, minWidth 1280) wrapping `CircularProgress size={32}`. Leave the auth check, `TeacherUserContext.Provider`, and `<TeacherShell>` wrapping UNCHANGED.
  </action>
  <verify>
    <automated>cd frontend && grep -q '@mui/material' components/TeacherShell.tsx && ! grep -q '@/components/ui' components/TeacherShell.tsx && grep -q 'ListItemButton' components/TeacherShell.tsx && ! grep -q 'className=' components/TeacherShell.tsx && grep -q 'CircularProgress' app/teacher/layout.tsx && grep -q 'TeacherUserContext' app/teacher/layout.tsx && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/components/TeacherShell.tsx` imports from `@mui/material/*` and has no `@/components/ui` import and no `className=` attribute
    - Nav links render as `ListItemButton component={Link}` with `selected={active}`; ACCENT stays `#F0623A`; sidebar bg stays `#0C1220`
    - Outer container preserves `minWidth: 1280`
    - No `Drawer` or `AppBar` import is used for the sidebar
    - `frontend/app/teacher/layout.tsx` loading state renders `CircularProgress` and retains `TeacherUserContext.Provider` + `TeacherShell`
  </acceptance_criteria>
  <done>TeacherShell renders as MUI Box/List sidebar with Menu dropdown and MUI password form; teacher layout uses MUI spinner; all auth/nav logic preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate teacher dashboard, classes, students, sessions pages to MUI</name>
  <files>frontend/app/teacher/page.tsx, frontend/app/teacher/classes/page.tsx, frontend/app/teacher/students/page.tsx, frontend/app/teacher/sessions/page.tsx</files>
  <read_first>
    - frontend/app/teacher/page.tsx
    - frontend/app/teacher/classes/page.tsx
    - frontend/app/teacher/students/page.tsx (uses shadcn Select; has ErrorBanner)
    - frontend/app/teacher/sessions/page.tsx
    - frontend/app/admin/teachers/page.tsx (reference CRUD page: Dialog + Table + Button + Input + Label + Badge patterns)
    - .planning/phases/11-frontend-refactor-with-react-mui/11-PATTERNS.md (Dialog pattern, Table pattern, Badge->Chip, Select replacement, Form Field Pattern, Error Banner Pattern, Tailwind->sx table)
  </read_first>
  <action>
    Migrate `frontend/app/teacher/page.tsx`, `classes/page.tsx`, `students/page.tsx`, `sessions/page.tsx`. For each: replace every `@/components/ui` import with the MUI equivalent per the mapping (Button->Button, Input->TextField, Label->FormLabel, Badge->Chip, Card->Card, Dialog->Dialog/DialogTitle/DialogContent/DialogActions, Select->Select+MenuItem+FormControl+InputLabel, Table->Table/TableHead/TableBody/TableRow/TableCell + TableContainer/Paper). Convert all Tailwind `className` strings to MUI `sx` using the Tailwind->sx translation table in 11-PATTERNS.md. Convert ErrorBanner divs to MUI `Alert severity="error"`. Convert form Label+Input pairs to the FormLabel+TextField field pattern. Preserve all data fetching (`lib/*api*.ts` calls), state, handlers, and loading/error/empty states unchanged — visual swap only (D-00). Match existing colors via theme tokens or exact hex in `sx`.
  </action>
  <verify>
    <automated>cd frontend && for f in app/teacher/page.tsx app/teacher/classes/page.tsx app/teacher/students/page.tsx app/teacher/sessions/page.tsx; do grep -q '@/components/ui' "$f" && { echo "FAIL $f"; exit 1; }; grep -q 'className=' "$f" && { echo "FAIL className $f"; exit 1; }; done && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - None of `app/teacher/page.tsx`, `classes/page.tsx`, `students/page.tsx`, `sessions/page.tsx` import from `@/components/ui`
    - None of those four files contain a `className=` attribute
    - `students/page.tsx` uses MUI `Select`+`MenuItem` (no shadcn Select) and `Alert` for its error banner
    - Data-fetch calls, state hooks, and handler functions are unchanged (visual-only diff)
  </acceptance_criteria>
  <done>Teacher dashboard, classes, students, and sessions pages render fully in MUI with no Tailwind/shadcn and unchanged behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate teacher homework pages + ReadingCreationPage (dnd-kit wrapper only)</name>
  <files>frontend/app/teacher/homework/page.tsx, frontend/app/teacher/homework/[id]/page.tsx, frontend/app/teacher/homework/[id]/edit/page.tsx, frontend/app/teacher/homework/[id]/try/page.tsx, frontend/app/teacher/homework/create/reading/page.tsx, frontend/app/teacher/homework/[id]/session/[sessionId]/page.tsx, frontend/app/teacher/homework/_components/ReadingCreationPage.tsx</files>
  <read_first>
    - frontend/app/teacher/homework/page.tsx (HomeworkModal, TypePickerModal, AssignModal, list with Badge/Card)
    - frontend/app/teacher/homework/[id]/page.tsx
    - frontend/app/teacher/homework/[id]/edit/page.tsx
    - frontend/app/teacher/homework/[id]/try/page.tsx (uses animate-shake — must use MUI shake keyframe from @/lib/theme)
    - frontend/app/teacher/homework/create/reading/page.tsx
    - frontend/app/teacher/homework/[id]/session/[sessionId]/page.tsx
    - frontend/app/teacher/homework/_components/ReadingCreationPage.tsx (dnd-kit imports lines ~18-34 — UNCHANGED; only visual wrapper becomes Paper)
    - .planning/phases/11-frontend-refactor-with-react-mui/11-PATTERNS.md (ReadingCreationPage Paper wrapper, shake import usage, Dialog/Table/Form patterns)
    - .planning/phases/11-frontend-refactor-with-react-mui/11-RESEARCH.md (Pattern 6 shake animation; Pitfall 4 shake breaking; D-08 dnd-kit unchanged)
  </read_first>
  <action>
    Migrate all six teacher homework page files plus ReadingCreationPage. Replace every `@/components/ui` import with MUI equivalents and every Tailwind `className` with `sx`. In `homework/page.tsx`, convert HomeworkModal/TypePickerModal/AssignModal to MUI `Dialog` (DialogTitle/DialogContent/DialogActions), Badge to Chip, Card list items to MUI Card/Paper; if a DatePicker is used for the assign due-date, replace shadcn DatePicker with `@mui/x-date-pickers/DatePicker` wrapped in `LocalizationProvider dateAdapter={AdapterDateFns}`, converting the Date object to ISO string at the onChange boundary (RESEARCH Pattern 7).
    In `homework/[id]/try/page.tsx`: replace the `animate-shake` Tailwind class with `sx={{ animation: shaking ? `${shake} 0.4s ease-in-out` : 'none' }}` importing `shake` from `@/lib/theme` (Pitfall 4) — do NOT redefine the keyframe inline. Preserve the interactive matching/fill-in-blank preview logic unchanged.
    In `ReadingCreationPage.tsx` (D-08): keep ALL dnd-kit imports, `DndContext`, `PointerSensor`/`KeyboardSensor`, `useSortable`, `arrayMove`, `SortableContext`, `useSensors` UNCHANGED. Replace ONLY the visual draggable-item container div with MUI `Paper` (keep the `ref={setNodeRef}` + `style={{ transform: CSS.Transform.toString(transform), transition }}` on the Paper), `elevation={isDragging ? 4 : 1}`, sx border + cursor as in PATTERNS.md. Convert the rest of the form (inputs, buttons, chip toggles) to MUI components.
    Preserve all create/edit persistence, scoring preview, and submission logic unchanged across all files (D-00).
  </action>
  <verify>
    <automated>cd frontend && for f in app/teacher/homework/page.tsx app/teacher/homework/[id]/page.tsx app/teacher/homework/[id]/edit/page.tsx app/teacher/homework/[id]/try/page.tsx app/teacher/homework/create/reading/page.tsx app/teacher/homework/[id]/session/[sessionId]/page.tsx app/teacher/homework/_components/ReadingCreationPage.tsx; do grep -q '@/components/ui' "$f" && { echo "FAIL ui $f"; exit 1; }; done && ! grep -q 'animate-shake' 'app/teacher/homework/[id]/try/page.tsx' && grep -q 'shake' 'app/teacher/homework/[id]/try/page.tsx' && grep -q 'DndContext' app/teacher/homework/_components/ReadingCreationPage.tsx && grep -q 'Paper' app/teacher/homework/_components/ReadingCreationPage.tsx && npm run build && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - None of the six teacher homework files import from `@/components/ui`
    - `homework/[id]/try/page.tsx` has no `animate-shake` class and imports the `shake` keyframe from `@/lib/theme`
    - `ReadingCreationPage.tsx` retains all dnd-kit imports + `DndContext` + `useSortable` and uses MUI `Paper` for the draggable wrapper
    - Any due-date DatePicker uses `@mui/x-date-pickers` wrapped in `LocalizationProvider`
    - `cd frontend && npm run build` succeeds (teacher routes compile end-to-end)
  </acceptance_criteria>
  <done>All teacher homework pages and ReadingCreationPage render in MUI; dnd-kit reorder and shake animation preserved; full frontend build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → teacher pages | Teacher form inputs (homework create, assign, password change) cross into client state then to backend API |
| client → backend API | Existing fetch adapters unchanged; no new request shapes introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03 | Input Validation | Teacher form fields (TextField) | mitigate | Preserve existing `required`/validation on migrated fields; MUI `TextField required` retains HTML validation; no validation logic removed (D-00 visual-only) |
| T-11-04 | Tampering | TeacherShell change-password form | accept | Calls existing `changePassword` adapter unchanged; UI swap does not touch auth flow |
| T-11-05 | Elevation of Privilege | teacher/layout auth gate | accept | Auth check + `TeacherUserContext` logic unchanged; only spinner div migrated |
</threat_model>

<verification>
- `cd frontend && grep -rL '@/components/ui' app/teacher` then confirm zero matches of `@/components/ui` across `app/teacher` and `components/TeacherShell.tsx`
- `cd frontend && ! grep -rq 'animate-shake' app/teacher` — shake migrated to MUI keyframe
- `cd frontend && grep -q 'DndContext' app/teacher/homework/_components/ReadingCreationPage.tsx` — dnd-kit intact
- `cd frontend && npm run build` — teacher routes compile
- Manual smoke: visit `/teacher`, `/teacher/homework` (create + assign + edit + try), `/teacher/sessions`, `/teacher/students`, `/teacher/classes`
</verification>

<success_criteria>
- TeacherShell + teacher layout migrated to MUI (CSS-flex sidebar, not Drawer); ACCENT/minWidth preserved
- All 10 teacher pages render in MUI with no `@/components/ui` import and no Tailwind `className`
- ReadingCreationPage dnd-kit reorder works unchanged; draggable wrapper is MUI Paper (D-08)
- Shake animation fires on the Try page via the `@/lib/theme` keyframe (D-00 / Pitfall 4)
- `npm run build` passes for teacher routes; behavior unchanged (D-00)
</success_criteria>

<output>
Create `.planning/phases/11-frontend-refactor-with-react-mui/11-02-SUMMARY.md` when done
</output>
