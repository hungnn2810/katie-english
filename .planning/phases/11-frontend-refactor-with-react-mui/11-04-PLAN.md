---
phase: 11-frontend-refactor-with-react-mui
plan: 04
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - frontend/components/AdminShell.tsx
  - frontend/app/admin/layout.tsx
  - frontend/app/admin/login/page.tsx
  - frontend/app/admin/page.tsx
  - frontend/app/admin/teachers/page.tsx
  - frontend/app/admin/classes/page.tsx
  - frontend/app/admin/students/page.tsx
  - frontend/app/admin/homework/page.tsx
autonomous: true
requirements: [FE-05]

must_haves:
  truths:
    - "An admin can log in, see the dashboard, and manage teachers/classes/students/homework rendered in MUI with the sidebar shell"
    - "Admin CRUD dialogs, tables, and form validation/error states render with MUI components and consistent error display"
    - "No file under app/admin and no AdminShell imports @/components/ui; the full frontend build passes"
  artifacts:
    - path: "frontend/components/AdminShell.tsx"
      provides: "MUI Box sidebar shell (ACCENT #4F9DFF) with List nav + Menu sign-out"
      contains: "@mui/material"
    - path: "frontend/app/admin/teachers/page.tsx"
      provides: "MUI Dialog + Table CRUD with Alert error states"
      contains: "Dialog"
  key_links:
    - from: "frontend/app/admin/teachers/page.tsx"
      to: "@mui/material/Table"
      via: "teacher list table"
      pattern: "TableContainer|Table"
    - from: "frontend/components/AdminShell.tsx"
      to: "@mui/material/ListItemButton"
      via: "nav link component={Link}"
      pattern: "ListItemButton"
---

<objective>
Migrate the admin portal to MUI: the AdminShell sidebar, the admin layout loading state, the admin login page, and all 5 admin pages (dashboard, teachers, classes, students, homework). Normalize data-entry (MUI TextField/Select/DatePicker), tables (MUI Table), and validation/error states (MUI Alert) per FE-05, preserving current behavior (D-00). Then run the full frontend build to confirm the entire migration compiles end-to-end.

Purpose: After this plan an admin can perform every management task rendered in MUI with consistent validation/error patterns. This is the admin vertical slice and the final integration gate — completing it means all four areas (foundation, teacher, student, admin) are migrated and `npm run build` passes for the whole app.

Output: Admin area fully migrated; consistent MUI form/table/error patterns; full `npm run build` green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-frontend-refactor-with-react-mui/11-CONTEXT.md
@.planning/phases/11-frontend-refactor-with-react-mui/11-RESEARCH.md
@.planning/phases/11-frontend-refactor-with-react-mui/11-PATTERNS.md
@.claude/skills/frontend-nextjs-app.md
@.planning/phases/11-frontend-refactor-with-react-mui/11-01-SUMMARY.md

<interfaces>
<!-- From plan 11-01 (must exist before this plan runs): -->
<!--   @/lib/theme exports baseTheme + keyframes; @/components/ui/* NO LONGER EXISTS -->
<!-- AdminShell ACCENT = '#4F9DFF'; sidebar bg '#0C1220'; outer minWidth 1280; simpler user menu (no password form) -->
<!-- admin/teachers/page.tsx is the reference admin CRUD page (Dialog + Table + Button + Input + Label + Badge) -->
<!-- Shadcn -> MUI mapping (11-PATTERNS Shared Patterns / 11-RESEARCH Pattern 5): -->
<!--   Button->Button; Input->TextField; Label->FormLabel; Badge->Chip; -->
<!--   Dialog(+Content/Header/Title)->Dialog+DialogTitle+DialogContent+DialogActions; -->
<!--   Table+variants->Table/TableHead/TableBody/TableRow/TableCell + TableContainer/Paper; -->
<!--   Select->Select+MenuItem+FormControl+InputLabel; error banner->Alert severity="error" -->
<!-- Full copy-ready AdminShell / Dialog / Table / Form-field / Error-banner patterns in 11-PATTERNS.md -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate AdminShell + admin/layout loading state + admin login page to MUI</name>
  <files>frontend/components/AdminShell.tsx, frontend/app/admin/layout.tsx, frontend/app/admin/login/page.tsx</files>
  <read_first>
    - frontend/components/AdminShell.tsx (sidebar identical to TeacherShell pattern; ACCENT #4F9DFF; simpler user menu, no password form)
    - frontend/app/admin/layout.tsx (loading spinner div ~lines 31-36; AdminUserContext + AdminShell wiring must stay)
    - frontend/app/admin/login/page.tsx (admin login form; imports @/components/ui Button/Input)
    - .planning/phases/11-frontend-refactor-with-react-mui/11-PATTERNS.md (AdminShell MUI target, Loading Spinner Pattern, Form Field Pattern)
    - .planning/phases/11-frontend-refactor-with-react-mui/11-RESEARCH.md (Pattern 4 sidebar shell; Anti-Pattern: no Drawer/AppBar for CSS-flex sidebar)
  </read_first>
  <action>
    Migrate `frontend/components/AdminShell.tsx` using the same MUI `Box`/`List`/`ListItemButton` sidebar pattern as TeacherShell (RESEARCH Pattern 4): outer `Box sx={{ display:'flex', height:'100vh', minWidth:1280 }}`; sidebar `Box sx={{ width:240, flexShrink:0, bgcolor:'#0C1220', ... }}`; nav items as `ListItemButton component={Link} href selected` with selected sx using ACCENT `#4F9DFF` (NOT the teacher orange). Convert the user dropdown to MUI `Menu`/`MenuItem` with a single sign-out item (no password form — AdminShell is simpler than TeacherShell). Do NOT use `Drawer`/`AppBar`. Preserve `usePathname`/`useRouter`/`clearAdminAuth` logic.
    Migrate `frontend/app/admin/layout.tsx`: replace the loading spinner div with `Box` + `CircularProgress size={32}`; leave the auth check, `AdminUserContext.Provider`, and `<AdminShell>` wrapping UNCHANGED.
    Migrate `frontend/app/admin/login/page.tsx`: replace shadcn `Button`/`Input` with MUI `Button`/`TextField`, Tailwind `className` with `sx`, and any error message with MUI `Alert severity="error"`. Preserve the admin login submit logic and any `minWidth` constraint present.
  </action>
  <verify>
    <automated>cd frontend && grep -q '@mui/material' components/AdminShell.tsx && ! grep -q '@/components/ui' components/AdminShell.tsx && grep -q 'ListItemButton' components/AdminShell.tsx && ! grep -q 'className=' components/AdminShell.tsx && grep -q '4F9DFF' components/AdminShell.tsx && grep -q 'CircularProgress' app/admin/layout.tsx && grep -q 'AdminUserContext' app/admin/layout.tsx && ! grep -q '@/components/ui' app/admin/login/page.tsx && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `AdminShell.tsx` imports `@mui/material/*`, has no `@/components/ui` import and no `className=`, uses `ListItemButton component={Link}`, and ACCENT is `#4F9DFF`
    - No `Drawer`/`AppBar` used for the sidebar; outer container keeps `minWidth: 1280`
    - `admin/layout.tsx` renders `CircularProgress` for loading and retains `AdminUserContext.Provider` + `AdminShell`
    - `admin/login/page.tsx` has no `@/components/ui` import and uses MUI `TextField`/`Button` (+ `Alert` for errors)
  </acceptance_criteria>
  <done>AdminShell renders as MUI sidebar (admin accent), admin layout uses MUI spinner, admin login uses MUI form; auth logic preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate admin dashboard + teachers + classes pages to MUI</name>
  <files>frontend/app/admin/page.tsx, frontend/app/admin/teachers/page.tsx, frontend/app/admin/classes/page.tsx</files>
  <read_first>
    - frontend/app/admin/page.tsx (dashboard stats)
    - frontend/app/admin/teachers/page.tsx (reference CRUD: Dialog + Table + Button + Input + Label + Badge)
    - frontend/app/admin/classes/page.tsx
    - .planning/phases/11-frontend-refactor-with-react-mui/11-PATTERNS.md (Dialog pattern, Table pattern, Badge->Chip, Form Field Pattern, Error Banner Pattern)
  </read_first>
  <action>
    Migrate `frontend/app/admin/page.tsx`, `teachers/page.tsx`, `classes/page.tsx`. Replace every `@/components/ui` import with MUI equivalents. Convert CRUD modals to MUI `Dialog` (DialogTitle with close `IconButton`, DialogContent, DialogActions with Cancel/Save `Button`s) per the PATTERNS.md Dialog target. Convert list tables to MUI `TableContainer component={Paper}` + `Table`/`TableHead`/`TableBody`/`TableRow`/`TableCell`. Convert Label+Input form pairs to the FormLabel+TextField field pattern; convert status badges to MUI `Chip`. Convert all error/validation banners to MUI `Alert severity="error"` for consistent error states (FE-05). Convert all Tailwind `className` to `sx`. Preserve all CRUD fetch calls (create/edit/disable teacher, edit/delete class), state, and validation logic unchanged (D-00).
  </action>
  <verify>
    <automated>cd frontend && for f in app/admin/page.tsx app/admin/teachers/page.tsx app/admin/classes/page.tsx; do grep -q '@/components/ui' "$f" && { echo "FAIL ui $f"; exit 1; }; grep -q 'className=' "$f" && { echo "FAIL className $f"; exit 1; }; done && grep -q 'Dialog' app/admin/teachers/page.tsx && grep -qE 'TableContainer|Table' app/admin/teachers/page.tsx && grep -q 'Alert' app/admin/teachers/page.tsx && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - None of `admin/page.tsx`, `teachers/page.tsx`, `classes/page.tsx` import `@/components/ui` or contain `className=`
    - `teachers/page.tsx` uses MUI `Dialog`, MUI `Table`(+TableContainer), and `Alert` for error states
    - Status badges render as MUI `Chip`; form fields use FormLabel+TextField pattern
    - CRUD fetch/state/validation logic unchanged (visual-only diff)
  </acceptance_criteria>
  <done>Admin dashboard, teachers, and classes pages render in MUI with consistent Dialog/Table/Chip/Alert patterns and unchanged behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate admin students + homework pages to MUI; run full frontend build</name>
  <files>frontend/app/admin/students/page.tsx, frontend/app/admin/homework/page.tsx</files>
  <read_first>
    - frontend/app/admin/students/page.tsx (student list + drill-in to results; may use Select)
    - frontend/app/admin/homework/page.tsx (homework list + delete)
    - frontend/app/admin/teachers/page.tsx (already migrated in Task 2 — reuse as pattern reference)
    - .planning/phases/11-frontend-refactor-with-react-mui/11-PATTERNS.md (Table pattern, Select replacement, Error Banner Pattern)
    - .planning/phases/11-frontend-refactor-with-react-mui/11-RESEARCH.md (Validation Architecture — build is the primary gate)
  </read_first>
  <action>
    Migrate `frontend/app/admin/students/page.tsx` and `frontend/app/admin/homework/page.tsx`. Replace every `@/components/ui` import with MUI equivalents; convert any shadcn `Select` to MUI `Select`+`MenuItem`+`FormControl`+`InputLabel`; convert tables to MUI `Table`(+TableContainer/Paper); convert error banners to MUI `Alert`; convert all Tailwind `className` to `sx`. Preserve the student drill-in to results navigation, homework/session delete handlers, and all fetch/state logic unchanged (D-00).
    Then run `cd frontend && npm run build`. This is the full-app integration gate — by this point plans 02 and 03 have removed all other `@/components/ui` import sites, so the build must now compile every route with zero errors. Resolve any remaining compile errors. Also run the global grep gates to confirm no `@/components/ui` import and no `@tailwind` directive survive anywhere in `app/` or `components/`.
  </action>
  <verify>
    <automated>cd frontend && for f in app/admin/students/page.tsx app/admin/homework/page.tsx; do grep -q '@/components/ui' "$f" && { echo "FAIL ui $f"; exit 1; }; grep -q 'className=' "$f" && { echo "FAIL className $f"; exit 1; }; done && ( grep -rl '@/components/ui' app components 2>/dev/null | grep -c . | grep -q '^0$' || { echo "FAIL: @/components/ui import sites remain"; grep -rl '@/components/ui' app components; exit 1; } ) && ! grep -rq '@tailwind' app && npm run build && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `admin/students/page.tsx` and `admin/homework/page.tsx` import no `@/components/ui` and contain no `className=`
    - `admin/students/page.tsx` uses MUI `Select`/`MenuItem` (if a dropdown exists) and preserves drill-in navigation
    - Zero files under `frontend/app` or `frontend/components` import from `@/components/ui`
    - No `@tailwind` directive exists anywhere under `frontend/app`
    - `cd frontend && npm run build` succeeds for the entire app
  </acceptance_criteria>
  <done>Admin students + homework pages migrated; no @/components/ui or @tailwind remain anywhere; full frontend build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → admin forms | Admin creates/edits teachers, classes, students — privileged data-entry crossing into client state then backend |
| client → admin API | Existing admin fetch adapters unchanged; delete actions (homework/session) preserved |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-09 | Input Validation | admin teacher/class create forms | mitigate | Migrated MUI `TextField` keeps `required`; validation logic preserved (D-00 visual-only); error states normalized to MUI `Alert` (FE-05) |
| T-11-10 | Elevation of Privilege | admin/layout auth gate | accept | Auth check + `AdminUserContext` logic unchanged; only spinner div migrated |
| T-11-11 | Tampering | admin delete (homework/session) handlers | accept | Delete handlers + confirm flow unchanged; UI swap does not alter the action |
</threat_model>

<verification>
- `cd frontend && grep -rl '@/components/ui' app components` returns nothing (whole-app gate)
- `cd frontend && ! grep -rq '@tailwind' app` — Tailwind fully removed
- `cd frontend && grep -q '4F9DFF' components/AdminShell.tsx && grep -q 'ListItemButton' components/AdminShell.tsx` — admin shell migrated
- `cd frontend && npm run build` — full app compiles
- Manual smoke: `/admin/login`, `/admin`, `/admin/teachers` (create/edit/disable), `/admin/classes` (edit/delete), `/admin/students` (drill-in), `/admin/homework` (delete)
</verification>

<success_criteria>
- AdminShell + admin layout + admin login migrated to MUI (CSS-flex sidebar, admin accent #4F9DFF)
- All 5 admin pages render in MUI with consistent Dialog/Table/Chip/Alert validation-error patterns (FE-05)
- Zero `@/components/ui` imports and zero `@tailwind` directives remain anywhere in the frontend
- Full `cd frontend && npm run build` passes; admin CRUD/delete/auth behavior unchanged (D-00)
</success_criteria>

<output>
Create `.planning/phases/11-frontend-refactor-with-react-mui/11-04-SUMMARY.md` when done
</output>
