---
phase: 11-frontend-refactor-with-react-mui
plan: 03
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - frontend/components/PhonemeButton.tsx
  - frontend/app/game/session/[id]/_components/PhonemeChips.tsx
  - frontend/components/ResultBanner.tsx
  - frontend/components/SelectedPhonemes.tsx
  - frontend/app/game/page.tsx
  - frontend/app/game/homework/page.tsx
  - frontend/app/game/session/[id]/page.tsx
  - frontend/app/game/reading/[id]/page.tsx
  - frontend/app/login/page.tsx
autonomous: true
requirements: [FE-04]

must_haves:
  truths:
    - "A student can open the homework list and play phonics, reading, and session games rendered in MUI under the kid-friendly student theme"
    - "Game gradient backgrounds and the minWidth:1024 constraint are preserved on every game page (Phase 04 D-01)"
    - "PhonemeChips show the 4 color states, ResultBanner shows correct/incorrect, and the shake animation fires on the reading game via the MUI keyframe"
    - "The login page (teacher/student) uses MUI TextField/Button and the MUI DatePicker for date-of-birth"
  artifacts:
    - path: "frontend/app/game/session/[id]/_components/PhonemeChips.tsx"
      provides: "MUI Chip row with 4 variant sx color states"
      contains: "Chip"
    - path: "frontend/app/game/reading/[id]/page.tsx"
      provides: "Reading game in MUI with preserved gradient bg + minWidth 1024 + shake"
      contains: "minWidth: 1024"
  key_links:
    - from: "frontend/app/game/reading/[id]/page.tsx"
      to: "frontend/lib/theme.ts"
      via: "import shake keyframe"
      pattern: "shake"
    - from: "frontend/app/game/reading/[id]/page.tsx"
      to: "frontend/lib/colors.ts"
      via: "gradients.gameBg inline background"
      pattern: "gradients"
---

<objective>
Migrate the student-facing area to MUI: the four game components (PhonemeButton, PhonemeChips, ResultBanner, SelectedPhonemes), all game pages (`/game`, `/game/homework`, `/game/session/[id]`, `/game/reading/[id]`), and the shared login page. All `/game/**` routes already inherit the kid-friendly `studentTheme` from the game layout created in plan 01. Preserve gradient backgrounds, the `minWidth: 1024` constraint, the shake animation, and game feel (D-00, D-05, D-07, FE-04, Phase 04 D-01).

Purpose: After this plan a student can complete the full homework-and-gameplay flow rendered in MUI without any UX regression. This is the student vertical slice — every screen a student touches is migrated and playable.

Output: Student/game area fully migrated; gradients + minWidth + shake intact; phoneme chips/banner/buttons render in MUI; login page uses MUI form + DatePicker.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-frontend-refactor-with-react-mui/11-CONTEXT.md
@.planning/phases/11-frontend-refactor-with-react-mui/11-RESEARCH.md
@.planning/phases/11-frontend-refactor-with-react-mui/11-PATTERNS.md
@.claude/skills/frontend-nextjs-app.md
@.planning/phases/11-frontend-refactor-with-react-mui/11-01-SUMMARY.md
@.planning/phases/04-student-page-ui-ux/04-CONTEXT.md

<interfaces>
<!-- From plan 11-01 (must exist before this plan runs): -->
<!--   @/lib/theme exports baseTheme + keyframes shake, fadeIn, slideUp -->
<!--   @/lib/student-theme exports studentTheme; app/game/layout.tsx already applies it to all /game routes -->
<!--   @/components/ui/* NO LONGER EXISTS -->
<!-- PRESERVE (Phase 04 D-01 + RESEARCH Pitfall 5): -->
<!--   game pages keep inline style={{ background: gradients.gameBg, minWidth: 1024 }} from @/lib/colors -->
<!--   gradients values live in @/lib/colors.ts (UNCHANGED) -->
<!-- PhonemeChips 4-state VARIANT_SX (11-PATTERNS): -->
<!--   correct {bgcolor #dcfce7, color #166534}; similar {#fef9c3,#854d0e}; -->
<!--   wrong {#fee2e2,#991b1b}; missing {border 2px dashed #9ca3af, color #9ca3af, transparent} -->
<!-- login DatePicker: MUI @mui/x-date-pickers + LocalizationProvider/AdapterDateFns; convert Date->ISO at onChange -->
<!-- Full copy-ready bodies for all 4 components + game pages + login in 11-PATTERNS.md -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate the four game components (PhonemeButton, PhonemeChips, ResultBanner, SelectedPhonemes)</name>
  <files>frontend/components/PhonemeButton.tsx, frontend/app/game/session/[id]/_components/PhonemeChips.tsx, frontend/components/ResultBanner.tsx, frontend/components/SelectedPhonemes.tsx</files>
  <read_first>
    - frontend/components/PhonemeButton.tsx (button with selected state + audio sub-button)
    - frontend/app/game/session/[id]/_components/PhonemeChips.tsx (VARIANT_CLASSES 4-state Tailwind map)
    - frontend/components/ResultBanner.tsx (correct/incorrect div)
    - frontend/components/SelectedPhonemes.tsx (wrapper + removable phoneme buttons)
    - .planning/phases/11-frontend-refactor-with-react-mui/11-PATTERNS.md (PhonemeButton/PhonemeChips/ResultBanner/SelectedPhonemes MUI targets)
    - .planning/phases/11-frontend-refactor-with-react-mui/11-RESEARCH.md (Pitfall 6 PhonemeChips color classes)
  </read_first>
  <action>
    Migrate `frontend/components/PhonemeButton.tsx`: outer `<button>` becomes MUI `Button` with sx width/height 80, minWidth 80, borderRadius 4, fontSize '1.5rem', fontWeight 700, border 2px solid; selected vs unselected states via conditional sx (selected: bgcolor 'primary.main', color white, transform scale(0.95); unselected: bgcolor white, color 'primary.main', borderColor 'primary.light', hover). Keep the audio play sub-button as a `Box component="button"` (absolute, warning.main circle) — preserve `playAudio` handler. Keep the `onClick`/`symbol`/`selected` props and logic.
    Migrate `frontend/app/game/session/[id]/_components/PhonemeChips.tsx`: replace `VARIANT_CLASSES` Tailwind map with `VARIANT_SX` object map (correct/similar/wrong/missing exact hexes from interfaces). Replace each `<span>` chip with MUI `Chip label={labelFor(op)} size="small" sx={{ fontFamily:'monospace', fontWeight:700, ...VARIANT_SX[variant] }} data-status={op.status}`. Replace the wrapper `<div>` with `Box sx={{ display:'flex', flexWrap:'wrap', gap:0.5, mt:1 }} data-testid="phoneme-chips"`. Keep the `data-status` attribute (Pitfall 6 — used as a hook).
    Migrate `frontend/components/ResultBanner.tsx`: replace the conditional div with MUI `Alert severity={isCorrect ? 'success' : 'error'}` with sx borderRadius 4, textAlign center, fontSize '1.125rem', fontWeight 600. Keep the correct/incorrect message + `correctAnswer.join(' - ')` content.
    Migrate `frontend/components/SelectedPhonemes.tsx`: wrapper div becomes `Box` (flex, gap 1.5, minHeight 5rem, dashed border via borderColor 'divider'); empty-state text becomes `Typography variant="body2" color="text.secondary"`; each removable phoneme `<button>` becomes MUI `Button` (64x64, primary tint, hover error) preserving the `onRemove(i)` handler.
  </action>
  <verify>
    <automated>cd frontend && grep -q 'Chip' app/game/session/[id]/_components/PhonemeChips.tsx && grep -q 'data-status' app/game/session/[id]/_components/PhonemeChips.tsx && ! grep -q 'bg-green-100' app/game/session/[id]/_components/PhonemeChips.tsx && grep -q 'Alert' components/ResultBanner.tsx && grep -q '@mui/material/Button' components/PhonemeButton.tsx && ! grep -q 'className=' components/SelectedPhonemes.tsx && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `PhonemeChips.tsx` renders MUI `Chip`, defines a `VARIANT_SX` map with the four exact color pairs, keeps `data-status`, and contains no `bg-green-100`/Tailwind color classes
    - `ResultBanner.tsx` uses MUI `Alert` with `severity` driven by `isCorrect`
    - `PhonemeButton.tsx` uses MUI `Button` and preserves `playAudio` + `selected` behavior
    - `SelectedPhonemes.tsx` has no `className=` and preserves the `onRemove` handler
  </acceptance_criteria>
  <done>All four game components render in MUI with preserved color states, audio sub-button, and remove handlers.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate game pages (/game, /game/homework, /game/session/[id], /game/reading/[id])</name>
  <files>frontend/app/game/page.tsx, frontend/app/game/homework/page.tsx, frontend/app/game/session/[id]/page.tsx, frontend/app/game/reading/[id]/page.tsx</files>
  <read_first>
    - frontend/app/game/page.tsx
    - frontend/app/game/homework/page.tsx (student homework list — preserve due-date ordering + badges from Phase 04)
    - frontend/app/game/session/[id]/page.tsx (phonics game; uses PhonemeButton/Chips/ResultBanner/SelectedPhonemes)
    - frontend/app/game/reading/[id]/page.tsx (uses animate-shake, gradients.gameBg, minWidth 1024)
    - frontend/lib/colors.ts (gradients map — UNCHANGED; game pages keep importing it)
    - .planning/phases/11-frontend-refactor-with-react-mui/11-PATTERNS.md (Game pages target, Inline Style Preservation Rule, shake import usage)
    - .planning/phases/11-frontend-refactor-with-react-mui/11-RESEARCH.md (Pitfall 5 gradients survive; Pitfall 4 shake; D-07 game screens in scope)
  </read_first>
  <action>
    Migrate the four game pages. For each: replace Tailwind `className` strings with MUI `Box`/`Typography`/`Button` + `sx`; replace custom loading-spinner divs with MUI `CircularProgress` (e.g. reading page size 48, color 'rgba(255,255,255,0.7)'). PRESERVE the inline `style={{ background: gradients.gameBg, minWidth: 1024 }}` (and `gameBgAlt`) on container elements — these reference `@/lib/colors` and are NOT Tailwind; keep them as inline `style` OR migrate to `sx={{ background: gradients.gameBg, minWidth: 1024 }}` (either is correct, but the gradient value and `minWidth: 1024` MUST remain — Phase 04 D-01 / Pitfall 5).
    In `game/reading/[id]/page.tsx`: replace the `animate-shake` Tailwind class with `sx={{ animation: shaking ? `${shake} 0.4s ease-in-out` : 'none' }}` importing `shake` from `@/lib/theme` (Pitfall 4). Keep the matching/fill-in-blank click-to-pair logic, scoring, and result screen unchanged.
    In `game/homework/page.tsx`: preserve the due-date ascending ordering, Overdue badge, and best-score badge from Phase 04 (convert badges to MUI `Chip`, but keep the ordering/sorting logic and badge variants intact — D-00).
    In `game/session/[id]/page.tsx`: keep the VAD recording flow, `/analyze` call, and state machine unchanged; migrate only layout/text/button styling. Components imported in Task 1 already render in MUI.
    Do not add an extra ThemeProvider to any game page — the student theme is applied by `app/game/layout.tsx` from plan 01.
  </action>
  <verify>
    <automated>cd frontend && for f in app/game/page.tsx app/game/homework/page.tsx app/game/session/[id]/page.tsx app/game/reading/[id]/page.tsx; do grep -q '@/components/ui' "$f" && { echo "FAIL ui $f"; exit 1; }; done && grep -q 'minWidth: 1024' app/game/reading/[id]/page.tsx && grep -q 'gradients' app/game/reading/[id]/page.tsx && ! grep -q 'animate-shake' app/game/reading/[id]/page.tsx && grep -q 'shake' app/game/reading/[id]/page.tsx && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - None of the four game pages import from `@/components/ui`
    - `game/reading/[id]/page.tsx` preserves `minWidth: 1024`, still imports `gradients` from `@/lib/colors`, has no `animate-shake` class, and imports `shake` from `@/lib/theme`
    - `game/homework/page.tsx` retains due-date ordering + Overdue/best-score badges (now as MUI Chip)
    - No game page declares its own `ThemeProvider` (student theme comes from the game layout)
    - VAD/`/analyze`/scoring logic in session and reading pages is unchanged (visual-only diff)
  </acceptance_criteria>
  <done>All four game pages render in MUI under the student theme with gradients, minWidth 1024, shake, and gameplay logic preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate login page (TextField/Button + MUI DatePicker) and build student routes</name>
  <files>frontend/app/login/page.tsx</files>
  <read_first>
    - frontend/app/login/page.tsx (teacher/student toggle; shadcn DatePicker for date-of-birth; raw input/button HTML; minWidth 1024)
    - .planning/phases/11-frontend-refactor-with-react-mui/11-PATTERNS.md (login page target, DatePicker replacement)
    - .planning/phases/11-frontend-refactor-with-react-mui/11-RESEARCH.md (Pattern 7 DatePicker + AdapterDateFns; Pitfall: LocalizationProvider required)
  </read_first>
  <action>
    Migrate `frontend/app/login/page.tsx`: replace the shadcn `DatePicker` (from `@/components/ui/date-picker`) for `dateOfBirth` with MUI `DatePicker` from `@mui/x-date-pickers/DatePicker`, wrapped in `LocalizationProvider dateAdapter={AdapterDateFns}` (RESEARCH Pattern 7). At the onChange boundary convert the `Date | null` to an ISO `YYYY-MM-DD` string (`newValue.toISOString().split('T')[0]`) so the existing `reg.dateOfBirth` string contract is preserved. Convert raw `<input>` elements to MUI `TextField` and raw `<button>` elements to MUI `Button`, mapping Tailwind classes to `sx`. PRESERVE the outer `style={{ minWidth: 1024 }}` (Phase 04 D-01) and the teacher/student login mode toggle + submit logic unchanged.
    Run `cd frontend && npm run build` and resolve compile errors in the student/game/login files this plan owns. Note: the build also compiles admin pages still importing `@/components/ui` (migrated in plan 04); if plan 04 has not run yet, the build may fail only on admin files — confirm the student/game/login routes themselves compile by checking that any remaining error references only `app/admin/**` files. If plan 04 has already merged, the build must pass fully.
  </action>
  <verify>
    <automated>cd frontend && ! grep -q '@/components/ui' app/login/page.tsx && grep -q '@mui/x-date-pickers' app/login/page.tsx && grep -q 'LocalizationProvider' app/login/page.tsx && grep -q 'minWidth: 1024' app/login/page.tsx && grep -q 'TextField' app/login/page.tsx && (npm run build 2>&1 | tee /tmp/build11-03.log; grep -E 'app/(game|login)|components/(Phoneme|ResultBanner|SelectedPhonemes)' /tmp/build11-03.log && exit 1; echo BUILD_OK_FOR_OWNED_FILES) && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `app/login/page.tsx` no longer imports `@/components/ui`, uses MUI `DatePicker` inside `LocalizationProvider`, uses MUI `TextField`/`Button`, and preserves `minWidth: 1024`
    - The DatePicker `onChange` still writes a `YYYY-MM-DD` string into `reg.dateOfBirth` (contract preserved)
    - `npm run build` produces no error against any `app/game/**`, `app/login/page.tsx`, or migrated game component file (errors may remain only on `app/admin/**` until plan 04)
  </acceptance_criteria>
  <done>Login page uses MUI form + DatePicker with preserved minWidth and date-string contract; student/game/login routes compile.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → login form | Credentials + registration (date-of-birth) entered client-side, sent to backend auth |
| microphone → game session | Student audio captured via VAD, uploaded to `/analyze` (unchanged) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-06 | Input Validation | login DatePicker date-of-birth | mitigate | onChange converts to `YYYY-MM-DD` string before storing; preserves the existing string contract so backend validation is unchanged |
| T-11-07 | Tampering | game session VAD + /analyze flow | accept | Recording/upload/scoring logic unchanged; only layout styling migrated |
| T-11-08 | Information Disclosure | student theme gradients/colors | accept | Cosmetic only; no data crosses the boundary |
</threat_model>

<verification>
- `cd frontend && grep -rq '@/components/ui' app/game app/login components/PhonemeButton.tsx components/ResultBanner.tsx components/SelectedPhonemes.tsx app/game/session/[id]/_components/PhonemeChips.tsx` returns nothing
- `cd frontend && grep -q 'minWidth: 1024' app/game/reading/[id]/page.tsx app/login/page.tsx` — constraint preserved
- `cd frontend && ! grep -rq 'animate-shake' app/game` — shake migrated
- `cd frontend && npm run build` — student/game/login routes compile (admin errors acceptable until plan 04)
- Manual smoke: `/login` (teacher + student + register with DOB), `/game/homework`, `/game/session/*`, `/game/reading/*`
</verification>

<success_criteria>
- Four game components render in MUI with 4-state chips, MUI Alert banner, MUI phoneme buttons
- All four game pages migrated under the student theme; gradients + `minWidth: 1024` + shake preserved (D-05, D-07, Phase 04 D-01, Pitfall 4/5)
- Login page uses MUI TextField/Button + `@mui/x-date-pickers` DatePicker with preserved date-string contract and minWidth
- Student/game/login routes compile; gameplay + auth + scoring logic unchanged (D-00)
</success_criteria>

<output>
Create `.planning/phases/11-frontend-refactor-with-react-mui/11-03-SUMMARY.md` when done
</output>
