---
phase: 02
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/prisma/schema.prisma
  - backend/prisma/migrations/
  - backend/src/homework/homework.dto.ts
  - backend/src/homework/homework.repository.ts
  - backend/src/game/game.dto.ts
  - backend/src/game/game.repository.ts
  - backend/src/game/game.service.ts
  - backend/src/game/game.controller.ts
  - backend/src/game/game.service.spec.ts
  - backend/src/homework/homework.service.ts
autonomous: true
requirements: [READ-01, READ-02, READ-03, READ-04, READ-05, READ-06]
tags: [backend, prisma, nestjs, reading-homework, scoring, transaction]

must_haves:
  truths:
    - "POST /homework with type: READING + readingActivities[] persists Homework + ReadingActivity + MatchPair/FillBlank/FillBlankChoice rows atomically"
    - "POST /game/session/:id/reading-result with {correctItems, totalItems} stores or updates a ReadingResult row keyed by sessionId, with score = round(correctItems/totalItems*100)"
    - "POST /game/session/:id/complete on a READING homework reads ReadingResult.score (no video upload required) and writes HomeworkSession.score + completedAt"
    - "GET /game/session/:id returns the homework with readingActivities[] (each including matchPairs or fillBlanks→choices, all ordered by `order` asc) and the readingResult relation (when present)"
    - "Backend rejects saveReadingResult when correctItems > totalItems or either value is negative"
    - "Backend rejects POST /homework READING when any FillBlank item lacks exactly one isCorrect=true choice or has fewer than 2 choices"
    - "Jest suite passes including new tests for READING scoring and completeSession READING branch"
  artifacts:
    - path: backend/prisma/schema.prisma
      provides: "READING enum value, ReadingActivityType enum, ReadingActivity/MatchPair/FillBlank/FillBlankChoice/ReadingResult models, Homework.readingActivities relation, HomeworkSession.readingResult relation"
      contains: "model ReadingResult"
    - path: backend/prisma/migrations/
      provides: "New migration folder named <timestamp>_add_reading_homework containing migration.sql with CREATE TYPE ReadingActivityType and CREATE TABLE statements for all 5 new tables"
      contains: "add_reading_homework"
    - path: backend/src/homework/homework.dto.ts
      provides: "'READING' added to HomeworkType union; ReadingActivityType type; CreateMatchPairDto, CreateFillBlankChoiceDto, CreateFillBlankItemDto, CreateReadingActivityDto classes; readingActivities? on CreateHomeworkDto"
      exports: ["HomeworkType", "ReadingActivityType", "CreateMatchPairDto", "CreateFillBlankChoiceDto", "CreateFillBlankItemDto", "CreateReadingActivityDto", "CreateHomeworkDto"]
    - path: backend/src/homework/homework.repository.ts
      provides: "readingActivitiesInclude constant; createReading(dto) method using prisma.$transaction(async tx => {...}); branch in create() routing READING to createReading"
      contains: "createReading"
    - path: backend/src/game/game.dto.ts
      provides: "SaveReadingResultDto class with correctItems and totalItems numeric fields"
      exports: ["SaveReadingResultDto"]
    - path: backend/src/game/game.repository.ts
      provides: "homeworkInclude extended with readingActivities (matchPairs + fillBlanks.choices, all ordered by order asc); sessionInclude extended with readingResult: true; saveReadingResult() upsert; getReadingResult() finder"
      contains: "saveReadingResult"
    - path: backend/src/game/game.service.ts
      provides: "saveReadingResult(sessionId, dto) method with guard sequence and score computation; READING branch in completeSession that reads ReadingResult.score"
      contains: "saveReadingResult"
    - path: backend/src/game/game.controller.ts
      provides: "@Post('session/:id/reading-result') endpoint that accepts SaveReadingResultDto JSON body and returns the persisted ReadingResult"
      contains: "reading-result"
    - path: backend/src/game/game.service.spec.ts
      provides: "READING fixtures, tests for saveReadingResult happy path, validation failures (correctItems > totalItems, negatives), and completeSession READING branch reading the stored score"
      contains: "READING"
  key_links:
    - from: backend/src/game/game.controller.ts
      to: backend/src/game/game.service.ts
      via: "saveReadingResult(id, dto) call"
      pattern: "service\\.saveReadingResult"
    - from: backend/src/game/game.service.ts
      to: backend/src/game/game.repository.ts
      via: "this.repo.saveReadingResult / this.repo.getReadingResult"
      pattern: "repo\\.(saveReadingResult|getReadingResult)"
    - from: backend/src/homework/homework.repository.ts
      to: backend/prisma/schema.prisma
      via: "prisma.$transaction async callback creating ReadingActivity then MatchPair/FillBlank/FillBlankChoice"
      pattern: "tx\\.(readingActivity|matchPair|fillBlank|fillBlankChoice)"
    - from: backend/src/game/game.service.ts
      to: backend/src/game/game.repository.ts
      via: "completeSession READING branch reading rr.score via getReadingResult"
      pattern: "hw\\.type === 'READING'"
---

<objective>
Establish the complete backend data spine for reading homework: Prisma schema with new enum value + 5 tables + relations, Prisma migration, DTOs for both homework creation and game session results, repository transaction for atomic READING homework creation, repository helpers for reading results, service method for scoring + persistence, REST endpoint, and the READING branch in completeSession. Extends game.service.spec.ts with scoring and completion tests so READ-06 has automated regression coverage.

Purpose: Without the backend, no other plan can be exercised. This plan is the precondition for Plans 03 (teacher creation page) and 04 (student game page).
Output: Prisma migration, six modified backend source files, one extended spec file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-reading-homework/02-CONTEXT.md
@.planning/phases/02-reading-homework/02-RESEARCH.md
@.planning/phases/02-reading-homework/02-PATTERNS.md
@.planning/phases/02-reading-homework/02-VALIDATION.md
@backend/prisma/schema.prisma
@backend/src/homework/homework.dto.ts
@backend/src/homework/homework.repository.ts
@backend/src/game/game.dto.ts
@backend/src/game/game.repository.ts
@backend/src/game/game.service.ts
@backend/src/game/game.controller.ts
@backend/src/game/game.service.spec.ts

<interfaces>
<!-- Contracts the executor will implement against / extend. -->

Existing schema enums to extend:
- HomeworkType: PHONICS | SPEAKING -> add READING (third value, no default change)
- New enum: ReadingActivityType { MATCH FILL_BLANK }

Existing models referenced (do NOT alter their existing fields):
- model Homework { id Int @id; type HomeworkType; name String?; parts HomeworkPart[]; assignments HomeworkAssignment[]; ... @@map("homeworks") }
- model HomeworkSession { id Int @id; speakingResults SpeakingResult[]; phonicsResults PhonicsItemResult[]; ... @@map("homework_sessions") }

Existing speaking result template to mirror (line 191 in schema.prisma):
- model SpeakingResult { id Int; sessionId Int @unique; score Float; ...; @@map("speaking_results") }

Existing DTO type alias (homework.dto.ts):
- export type HomeworkType = 'PHONICS' | 'SPEAKING';   // add 'READING'

Existing controller-level guard (game.controller.ts):
- @UseGuards(AuthGuard) @Controller('game')             // new endpoint inherits this — do NOT add another decorator

Existing service guard sequence to mirror (game.service.ts saveSpeakingResult):
- 1) const session = await this.repo.getSession(sessionId); if (!session) throw NotFoundException
- 2) if (session.completedAt) throw BadRequestException('Session already completed')
- 3) const hw = session.assignment.homework; if (hw.type !== 'READING') throw BadRequestException

Existing repository upsert pattern (game.repository.ts saveSpeakingResult):
- prisma.<model>.upsert({ where: { sessionId }, update: {...}, create: { sessionId, ... } })

Existing test fixture style (game.service.spec.ts mockPhonicsSession / mockSpeakingSession): plain objects with overrides, no real DB.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Prisma schema, migration, and homework module DTO + transaction</name>
  <files>backend/prisma/schema.prisma, backend/prisma/migrations/&lt;new_timestamp&gt;_add_reading_homework/migration.sql, backend/src/homework/homework.dto.ts, backend/src/homework/homework.repository.ts</files>
  <read_first>
    - backend/prisma/schema.prisma (entire file — see existing enum/model conventions on lines 16-19, 111-201)
    - backend/src/homework/homework.dto.ts (entire file — plain-class DTO convention, no class-validator decorators)
    - backend/src/homework/homework.repository.ts (entire file — see partsInclude on lines 5-10, create() on lines 68-80, buildPartsCreate on lines 17-33, assignmentInclude on lines 12-15)
    - backend/prisma/migrations/20260510000001_add_speaking_mode/ (most recent migration — copy its structure / naming for the new migration folder)
    - .planning/phases/02-reading-homework/02-RESEARCH.md (Architecture Patterns - Pattern 1, Code Examples section)
    - .planning/phases/02-reading-homework/02-PATTERNS.md (schema.prisma, homework.dto.ts, homework.repository.ts sections)
  </read_first>
  <behavior>
    - prisma.homework.create with type:READING and 2 readingActivities (1 MATCH with 3 pairs, 1 FILL_BLANK with 2 items each with 3 choices, 1 isCorrect) returns a Homework row joined with its readingActivities ordered by order asc; matchPairs and fillBlanks/choices populated; everything inserted atomically (no orphan rows on failure).
    - prisma.homework.create with type:READING and empty readingActivities array creates a Homework row only (no activities). Loading via findById returns readingActivities: [].
    - Transaction rollback: if any nested insert throws (e.g., choices have invalid blankId), the parent Homework row is also rolled back (no Homework row exists post-failure).
    - HomeworkType.READING is a valid Prisma enum value and a valid TypeScript HomeworkType union member.
  </behavior>
  <action>
    Step 1 — Extend backend/prisma/schema.prisma (do NOT touch any existing model fields):
    - Add `READING` as the third value of enum HomeworkType (after SPEAKING).
    - Add enum ReadingActivityType { MATCH FILL_BLANK }.
    - Add 5 new models with snake_case @@map names: `reading_activities`, `match_pairs`, `fill_blanks`, `fill_blank_choices`, `reading_results`. Use `@id @default(autoincrement())` on every id; `onDelete: Cascade` on every child FK; `@@unique([parentId, order])` on ReadingActivity (homeworkId+order), MatchPair (activityId+order), FillBlank (activityId+order); `sessionId Int @unique` on ReadingResult.
    - ReadingActivity fields: id Int, homeworkId Int, type ReadingActivityType, order Int, homework Homework relation (onDelete: Cascade), matchPairs MatchPair[], fillBlanks FillBlank[].
    - MatchPair fields: id Int, activityId Int, imageUrl String, word String, order Int, activity ReadingActivity relation (onDelete: Cascade).
    - FillBlank fields: id Int, activityId Int, sentence String, order Int, activity ReadingActivity relation (onDelete: Cascade), choices FillBlankChoice[].
    - FillBlankChoice fields: id Int, blankId Int, word String, isCorrect Boolean, blank FillBlank relation (onDelete: Cascade).
    - ReadingResult fields: id Int, sessionId Int @unique, totalItems Int, correctItems Int, score Float, session HomeworkSession relation (onDelete: Cascade).
    - Add to Homework model: `readingActivities ReadingActivity[]` (new relation field alongside parts, assignments).
    - Add to HomeworkSession model: `readingResult ReadingResult?` (one-to-one via @unique on FK side — Open Question Q2 in 02-RESEARCH.md is resolved this way for cleaner service code).

    Step 2 — Create the Prisma migration:
    - Run `cd backend && npx prisma migrate dev --name add_reading_homework --create-only` to generate the migration folder. If Prisma reports drift from prior stale migration folders (20260507000003 / 20260507000004 / 20260507000005 / 20260508000001 / 20260509000001 are present), check `npx prisma migrate status` first; if it reports drift, run `npx prisma migrate resolve --applied <name>` for each name shown as "applied to database but not in local history", per RESEARCH Pitfall 3. Do NOT modify the existing migration folders.
    - Verify the generated migration.sql contains `CREATE TYPE "ReadingActivityType"`, `ALTER TYPE "HomeworkType" ADD VALUE 'READING'`, and `CREATE TABLE "reading_activities"`, `"match_pairs"`, `"fill_blanks"`, `"fill_blank_choices"`, `"reading_results"`. If the migration created any extra ALTER/DROP statements, abort and re-derive schema changes (you accidentally altered an existing model).
    - Run `npx prisma generate` so the client picks up the new types.

    Step 3 — Extend backend/src/homework/homework.dto.ts:
    - Add `'READING'` to the HomeworkType union (becomes `'PHONICS' | 'SPEAKING' | 'READING'`).
    - Add `export type ReadingActivityType = 'MATCH' | 'FILL_BLANK';`
    - Add four new plain DTO classes (no class-validator decorators — matches existing convention): CreateMatchPairDto { imageUrl: string; word: string }, CreateFillBlankChoiceDto { word: string; isCorrect: boolean }, CreateFillBlankItemDto { sentence: string; choices: CreateFillBlankChoiceDto[] }, CreateReadingActivityDto { type: ReadingActivityType; pairs?: CreateMatchPairDto[]; items?: CreateFillBlankItemDto[] }.
    - Extend CreateHomeworkDto with `readingActivities?: CreateReadingActivityDto[];` (no other changes to existing fields).
    - Do NOT modify UpdateHomeworkDto — Phase 2 does not support editing READING homework (deferred to Phase 3).

    Step 4 — Extend backend/src/homework/homework.repository.ts:
    - Add a top-level constant `readingActivitiesInclude` mirroring the shape of `partsInclude`: { readingActivities: { include: { matchPairs: { orderBy: { order: 'asc' as const } }, fillBlanks: { include: { choices: true }, orderBy: { order: 'asc' as const } } }, orderBy: { order: 'asc' as const } } }.
    - Update every existing include that spreads `...partsInclude` to also spread `...readingActivitiesInclude`: in findAll (line 42-46), findById (line 50-66), create (line 78), update (line 95). Same for homework inside createAssignment (line 110) and findAssignmentById (line 117) and updateAssignment (line 135) — `homework: { include: { ...partsInclude, ...readingActivitiesInclude } }`.
    - Add a new async method `createReading(dto: CreateHomeworkDto)` using `this.prisma.$transaction(async (tx) => {...})`. Inside the callback: tx.homework.create({ data: { type: 'READING', name: dto.name ?? null } }), then for each entry in (dto.readingActivities ?? []) create a tx.readingActivity with order=index, then for MATCH branch use tx.matchPair.createMany with the act.pairs array; for FILL_BLANK branch loop the items and for each create a tx.fillBlank then tx.fillBlankChoice.createMany for its choices. Final return: tx.homework.findUnique({ where: { id: hw.id }, include: { ...partsInclude, ...readingActivitiesInclude, assignments: { include: assignmentInclude } } }).
    - Modify the existing create() method (line 68-80) to delegate to createReading when dto.type === 'READING': add `if (dto.type === 'READING') return this.createReading(dto);` as the first line of create(). Leave the PHONICS/SPEAKING flow untouched.

    Step 5 — Validation in service: in backend/src/homework/homework.service.ts (do NOT redesign this file; just add a tiny private validateReadingDto helper at the bottom of the class and call it from create()) — when dto.type === 'READING' loop dto.readingActivities ?? [] and: for each FILL_BLANK activity, for each item in items ?? [], throw BadRequestException('Each fill-blank item must have at least 2 choices') if (item.choices?.length ?? 0) < 2 and throw BadRequestException('Each fill-blank item must have exactly one isCorrect=true choice') unless item.choices.filter(c => c.isCorrect).length === 1. For each MATCH activity, throw BadRequestException('Matching activity must have 2 to 6 pairs') unless 2 <= (act.pairs?.length ?? 0) <= 6 (per D-07). Import BadRequestException from '@nestjs/common' if not already imported.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - File backend/prisma/schema.prisma contains the literal text `READING` inside the HomeworkType enum block AND the text `model ReadingResult` AND `model ReadingActivity` AND `model MatchPair` AND `model FillBlank` AND `model FillBlankChoice`.
    - File backend/prisma/schema.prisma contains the literal text `readingActivities ReadingActivity[]` (Homework relation field) AND `readingResult   ReadingResult?` or `readingResult ReadingResult?` (HomeworkSession relation field).
    - Directory backend/prisma/migrations/ contains exactly one new folder whose name ends with `_add_reading_homework`. Inside that folder, migration.sql contains all of: `CREATE TYPE "ReadingActivityType"`, `ALTER TYPE "HomeworkType" ADD VALUE 'READING'`, `CREATE TABLE "reading_activities"`, `CREATE TABLE "match_pairs"`, `CREATE TABLE "fill_blanks"`, `CREATE TABLE "fill_blank_choices"`, `CREATE TABLE "reading_results"`. Verify with: `ls backend/prisma/migrations/*_add_reading_homework/migration.sql &amp;&amp; grep -l 'CREATE TABLE "reading_results"' backend/prisma/migrations/*_add_reading_homework/migration.sql`.
    - File backend/src/homework/homework.dto.ts grep `-v '^//' | grep "'READING'"` returns a nonzero count, and grep -c "export class CreateReadingActivityDto" returns 1.
    - File backend/src/homework/homework.repository.ts contains the literal text `createReading` (method name appears in two places — declaration and call from create()) and contains the literal text `this.prisma.$transaction(async`.
    - File backend/src/homework/homework.service.ts contains both `Each fill-blank item must have exactly one isCorrect=true choice` and `Matching activity must have 2 to 6 pairs`.
    - `cd backend &amp;&amp; npx tsc --noEmit` exits 0 (no type errors after Prisma generate ran in Step 2).
  </acceptance_criteria>
  <done>Schema, migration, DTOs, repository transaction, and homework-level validation in place; TypeScript compiles; no existing tests broken.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Game module reading-result endpoint, service, repository, includes, and spec</name>
  <files>backend/src/game/game.dto.ts, backend/src/game/game.repository.ts, backend/src/game/game.service.ts, backend/src/game/game.controller.ts, backend/src/game/game.service.spec.ts</files>
  <read_first>
    - backend/src/game/game.dto.ts (entire file — plain DTO convention)
    - backend/src/game/game.repository.ts (entire file — homeworkInclude on lines 4-9, sessionInclude on lines 11-21, saveSpeakingResult upsert on lines 68-74)
    - backend/src/game/game.service.ts (entire file — saveSpeakingResult guard on lines 35-41, completeSession branching on lines 132-159)
    - backend/src/game/game.controller.ts (entire file — class-level AuthGuard on line 13, JSON POST pattern on lines 26-29, file POST pattern on lines 49-57)
    - backend/src/game/game.service.spec.ts (entire file — mock session helpers, jest pattern; this is the file you will extend)
    - backend/src/game/game.repository.ts patterns from 02-PATTERNS.md (game.repository.ts section)
    - backend/src/game/game.service.ts patterns from 02-PATTERNS.md (game.service.ts section, branching code excerpt)
  </read_first>
  <behavior>
    - service.saveReadingResult(sessionId, { correctItems: 5, totalItems: 8 }) on a READING session computes score = Math.round(5/8*100) = 63 and upserts a ReadingResult row with totalItems=8, correctItems=5, score=63.
    - service.saveReadingResult on a session whose homework type !== 'READING' throws BadRequestException('Homework is not a READING type').
    - service.saveReadingResult on a session where completedAt is set throws BadRequestException('Session already completed').
    - service.saveReadingResult with correctItems=10, totalItems=5 throws BadRequestException('correctItems cannot exceed totalItems').
    - service.saveReadingResult with totalItems=0 returns/persists score=0 (avoids divide-by-zero; matches D-17 wording "all items" — if there are no items, score 0).
    - service.saveReadingResult with negative values throws BadRequestException('correctItems and totalItems must be non-negative').
    - service.completeSession on a READING homework calls repo.getReadingResult, uses rr.score (or 0 when missing), passes NO videoUrl to repo.completeSession.
    - GET /game/session/:id returns a session whose assignment.homework includes readingActivities (when type=READING) with nested matchPairs and fillBlanks.choices, ordered by order asc.
    - jest game.service.spec.ts new "READING" describe block passes.
  </behavior>
  <action>
    Step 1 — Extend backend/src/game/game.dto.ts: add `export class SaveReadingResultDto { correctItems: number; totalItems: number; }` after CompleteSessionDto. Plain class, no decorators (matches existing pattern).

    Step 2 — Extend backend/src/game/game.repository.ts:
    - Add a top-level constant before homeworkInclude: `const readingActivitiesInclude = { readingActivities: { include: { matchPairs: { orderBy: { order: 'asc' as const } }, fillBlanks: { include: { choices: true }, orderBy: { order: 'asc' as const } } }, orderBy: { order: 'asc' as const } } };` (same shape as homework module).
    - Change homeworkInclude (lines 4-9) to spread both: `const homeworkInclude = { parts: { include: { words: { orderBy: { order: 'asc' as const } } }, orderBy: { order: 'asc' as const } }, ...readingActivitiesInclude, };`
    - Change sessionInclude (lines 11-21) to add `readingResult: true,` on a new line at the same level as speakingResults and phonicsResults. Do NOT touch the existing relations.
    - Add two methods at the bottom of GameRepository class:
      * `saveReadingResult(sessionId: number, totalItems: number, correctItems: number, score: number) { return this.prisma.readingResult.upsert({ where: { sessionId }, update: { totalItems, correctItems, score }, create: { sessionId, totalItems, correctItems, score } }); }`
      * `getReadingResult(sessionId: number) { return this.prisma.readingResult.findUnique({ where: { sessionId } }); }`

    Step 3 — Extend backend/src/game/game.service.ts:
    - Import SaveReadingResultDto at the top alongside the other DTOs: change `import { StartSessionDto, SavePhonicsResultDto } from './game.dto';` to also include `SaveReadingResultDto`.
    - Add a new async method `saveReadingResult(sessionId: number, dto: SaveReadingResultDto)` placed after savePhonicsResult and before listSessions. Implementation: copy the guard sequence from saveSpeakingResult lines 36-41 but check `hw.type !== 'READING'` and throw `BadRequestException('Homework is not a READING type')`. Then validate: if (dto.correctItems < 0 || dto.totalItems < 0) throw `BadRequestException('correctItems and totalItems must be non-negative')`; if (dto.correctItems > dto.totalItems) throw `BadRequestException('correctItems cannot exceed totalItems')`. Compute `const score = dto.totalItems > 0 ? Math.round((dto.correctItems / dto.totalItems) * 100) : 0;`. Call `this.logger.log(`[session=${sessionId}] reading score=${score} correct=${dto.correctItems}/${dto.totalItems}`);`. Return `this.repo.saveReadingResult(sessionId, dto.totalItems, dto.correctItems, score)`.
    - Modify completeSession (line 132-159): in the branching block starting at line 148, change `if (hw.type === 'SPEAKING') { ... } else { ... phonics ... }` to a three-way `if (hw.type === 'SPEAKING') { ... } else if (hw.type === 'READING') { const rr = await this.repo.getReadingResult(sessionId); avgScore = rr ? rr.score : 0; } else { ... existing phonics branch ... }`.
    - Note: completeSession's video upload block on lines 138-145 already passes `videoBuffer && videoBuffer.length > 0` as a guard, so READING sessions that send no video skip the upload naturally — no changes needed there.

    Step 4 — Extend backend/src/game/game.controller.ts:
    - Import `SaveReadingResultDto` from `./game.dto`.
    - Add a new endpoint between savePhonicsResult and getJobStatus (line 36-47 are phonics; line 49-57 are speaking; line 59-61 are job; place reading-result after speaking-result for symmetry):
      `@Post('session/:id/reading-result')`
      `saveReadingResult(@Param('id', ParseIntPipe) id: number, @Body() dto: SaveReadingResultDto) { return this.service.saveReadingResult(id, dto); }`
    - Use the same @UseGuards class-level guard (already in place — no extra decorator needed).
    - This is a JSON POST (no file upload), no FileInterceptor, no HttpCode(202).

    Step 5 — Extend backend/src/game/game.service.spec.ts:
    - Add a new fixture helper `mockReadingSession` mirroring mockSpeakingSession but with type:'READING', empty speakingText/Picture, readingActivities array with 1 MATCH (3 pairs) + 1 FILL_BLANK (2 items), readingResult:null.
    - Find the existing `describe('GameService'` or root testing block and add nested `describe('saveReadingResult')` block with tests:
      * 'throws NotFoundException when session is missing' — repo.getSession returns null.
      * 'throws BadRequestException when session already completed' — session has completedAt set.
      * 'throws BadRequestException when homework type is not READING' — pass mockPhonicsSession.
      * 'throws BadRequestException when correctItems > totalItems' — pass {correctItems: 5, totalItems: 3}.
      * 'throws BadRequestException when values are negative' — pass {correctItems: -1, totalItems: 5}.
      * 'computes score = round(correctItems/totalItems*100) and calls repo.saveReadingResult with that score' — pass {correctItems: 5, totalItems: 8}, expect repo.saveReadingResult called with (sessionId, 8, 5, 63).
      * 'returns score=0 when totalItems is 0' — pass {correctItems: 0, totalItems: 0}, expect repo.saveReadingResult called with (sessionId, 0, 0, 0).
    - Add nested `describe('completeSession READING')` block with tests:
      * 'reads ReadingResult.score when present' — mockReadingSession with readingResult on session; mock repo.getReadingResult returning { score: 75 }; expect repo.completeSession called with (sessionId, null, 75).
      * 'uses 0 when ReadingResult is missing' — mock repo.getReadingResult returning null; expect repo.completeSession called with (sessionId, null, 0).
    - Wire up the mocks: repo.saveReadingResult, repo.getReadingResult must be present on the GameRepository mock (jest.fn()). If the existing spec uses a `Test.createTestingModule(...).overrideProvider(GameRepository).useValue({ ... })` shape, extend that override object to include the new methods. If the spec instantiates GameService directly with a manual mock object, extend the manual mock.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; npx jest game.service.spec.ts --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - File backend/src/game/game.dto.ts contains `export class SaveReadingResultDto`.
    - File backend/src/game/game.repository.ts contains the literal text `readingResult: true` AND `readingActivitiesInclude` AND `saveReadingResult` AND `getReadingResult` AND `prisma.readingResult.upsert`.
    - File backend/src/game/game.service.ts contains all of: `SaveReadingResultDto`, `hw.type !== 'READING'`, `correctItems cannot exceed totalItems`, `correctItems and totalItems must be non-negative`, `Math.round((dto.correctItems / dto.totalItems) * 100)`, `hw.type === 'READING'`, `this.repo.getReadingResult(sessionId)`.
    - File backend/src/game/game.controller.ts contains `'session/:id/reading-result'` AND `SaveReadingResultDto`.
    - File backend/src/game/game.service.spec.ts contains `describe('saveReadingResult'` AND `describe('completeSession READING'` AND `mockReadingSession`.
    - `cd backend &amp;&amp; npx jest game.service.spec.ts --no-coverage` exits 0 (all existing tests still pass and new tests pass).
    - `cd backend &amp;&amp; npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Reading-result endpoint live, scoring logic deterministic with input validation, completeSession READING branch routed, spec covers all five validation paths plus completeSession behavior, full backend Jest suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (browser) -> /homework POST | Teacher submits arbitrary JSON describing reading activities; backend trusts shape but must validate semantic correctness |
| client (student browser) -> /game/session/:id/reading-result POST | Student submits correctItems/totalItems; backend must not blindly trust these numbers |
| client -> /game/session/:id/complete | Student triggers session finalization; READING branch reads server-stored ReadingResult, not client input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | POST /homework (READING) — teacher submits multiple isCorrect=true choices per FillBlank item | mitigate | homework.service.ts validateReadingDto rejects with BadRequestException('Each fill-blank item must have exactly one isCorrect=true choice') unless exactly one choice has isCorrect=true (per ASVS V5 V5.1.4) |
| T-02-02 | Tampering | POST /homework (READING) — teacher submits 0/1 or >6 matching pairs (D-07 violates) | mitigate | homework.service.ts validateReadingDto rejects with BadRequestException unless 2 <= pairs.length <= 6 (ASVS V5 V5.1.1) |
| T-02-03 | Tampering | POST /game/session/:id/reading-result — student submits correctItems > totalItems to inflate score | mitigate | game.service.ts saveReadingResult throws BadRequestException('correctItems cannot exceed totalItems') (ASVS V5 V5.1.4); server recomputes score from the validated correctItems/totalItems — no client-provided score is trusted |
| T-02-04 | Tampering | POST /game/session/:id/reading-result — student submits negative values to break math | mitigate | game.service.ts rejects when correctItems < 0 or totalItems < 0 (ASVS V5 V5.1.2) |
| T-02-05 | Tampering | POST /game/session/:id/reading-result — student replays endpoint after completeSession | mitigate | guard sequence checks `session.completedAt` and throws BadRequestException('Session already completed') — mirrors existing saveSpeakingResult behavior |
| T-02-06 | Spoofing | Any /game/session/* endpoint — unauthenticated caller | mitigate | inherited from class-level @UseGuards(AuthGuard) on GameController — no new decorator needed but verified |
| T-02-07 | Information disclosure | GET /game/session/:id exposing ReadingResult before completion | accept | low value (own score); same disclosure pattern as SpeakingResult and PhonicsItemResult which are already exposed pre-completion; no PII; ASVS V5 V5.1 not applicable |
| T-02-08 | Denial of Service | Massive readingActivities payload causing OOM in transaction | accept | NestJS default body-parser limit (~100KB) caps payload size; no per-activity limit needed for Phase 2 use case (teacher entering 2-6 pairs and ~3-5 fill-blank items per activity); revisit if abuse seen |
| T-02-09 | Repudiation | Lack of audit log for score submission | accept | Phase 2 logs score via Logger; full audit trail deferred to Phase 3 (READ-07 needs per-activity persistence anyway) |
</threat_model>

<verification>
- Run `cd backend && npx jest --no-coverage` — full Jest suite green.
- Run `cd backend && npx tsc --noEmit` — no type errors.
- Verify migration applied cleanly in a dev DB via `npx prisma migrate status` (executor should confirm during execution).
</verification>

<success_criteria>
- POST /homework with type:READING + activities[] creates Homework + all child rows atomically (one row count check before/after the request confirms +1 on homeworks + N on reading_activities, etc.).
- POST /game/session/:id/reading-result with valid body returns a ReadingResult JSON with score = round(correctItems/totalItems*100).
- POST /game/session/:id/reading-result with correctItems > totalItems returns HTTP 400.
- POST /game/session/:id/complete on a READING homework returns HomeworkSession with score matching the stored ReadingResult.score.
- All backend Jest tests pass (including new READING tests).
</success_criteria>

<output>
After completion, create `.planning/phases/02-reading-homework/02-01-SUMMARY.md` summarizing schema additions, new endpoints, validation behavior, and any deviations from the plan.
</output>
