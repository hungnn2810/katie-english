---
phase: 02
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/lib/admin-api.ts
  - frontend/app/game/homework/page.tsx
  - frontend/app/teacher/homework/page.tsx
autonomous: true
requirements: [READ-01, READ-04, READ-05, READ-06]
tags: [frontend, api-types, routing, teacher-homework, student-homework]

must_haves:
  truths:
    - "Frontend can call POST /game/session/:id/reading-result via saveReadingResult(sessionId, {correctItems, totalItems}) and receive a typed ReadingResult"
    - "Frontend CreateHomeworkInput accepts type:'READING' with readingActivities[] of CreateReadingActivityInput shape, ready for the teacher creation page (Plan 03) to construct"
    - "Student homework list page navigates to /game/reading/{sessionId} when starting a READING assignment, and /game/session/{sessionId} otherwise"
    - "Teacher homework list page displays a 'New Reading' Link button to /teacher/homework/create/reading, a 'Reading' filter tab, and a 'READING' card preview branch showing activity count"
    - "Frontend TypeScript compiles with new types and READING branches (no `any`, no implicit narrowing failures)"
  artifacts:
    - path: frontend/lib/admin-api.ts
      provides: "'READING' added to HomeworkType union; ReadingActivityType type; MatchPair, FillBlankChoice, FillBlank, ReadingActivity, ReadingResult interfaces; readingActivities? on HomeworkItem; readingResult? on GameSession; CreateMatchPairInput, CreateFillBlankChoiceInput, CreateFillBlankItemInput, CreateReadingActivityInput; readingActivities? on CreateHomeworkInput; saveReadingResult(sessionId, {correctItems, totalItems}) function"
      exports: ["HomeworkType", "ReadingActivityType", "MatchPair", "FillBlankChoice", "FillBlank", "ReadingActivity", "ReadingResult", "CreateReadingActivityInput", "CreateMatchPairInput", "CreateFillBlankChoiceInput", "CreateFillBlankItemInput", "saveReadingResult"]
    - path: frontend/app/game/homework/page.tsx
      provides: "READING entry in TYPE_META; handleStart routes READING sessions to /game/reading/{sessionId} via session.assignment.homework.type check"
      contains: "/game/reading/"
    - path: frontend/app/teacher/homework/page.tsx
      provides: "READING entry in TYPE_META (label:'Reading', emoji:'📖', color:'#6ED6C1', bg:'#6ED6C118'); '📖 Reading' filter tab; 'New Reading' Link toolbar button to /teacher/homework/create/reading; READING content preview branch showing '{n} activit(y|ies)' count in homework cards"
      contains: "/teacher/homework/create/reading"
  key_links:
    - from: frontend/lib/admin-api.ts
      to: backend POST /game/session/:id/reading-result
      via: "saveReadingResult() using req<ReadingResult> helper"
      pattern: "saveReadingResult"
    - from: frontend/app/game/homework/page.tsx
      to: frontend/app/game/reading/[id]/page.tsx (created in Plan 04)
      via: "router.push when homework.type === 'READING'"
      pattern: "/game/reading/"
    - from: frontend/app/teacher/homework/page.tsx
      to: frontend/app/teacher/homework/create/reading/page.tsx (created in Plan 03)
      via: "Next.js Link in toolbar"
      pattern: "/teacher/homework/create/reading"
---

<objective>
Add the type vocabulary and routing glue that lets the rest of the frontend speak "READING": API types, the saveReadingResult function, student-app routing branch, and teacher-app entry-point button + filter + card preview. This plan is the contract layer for Plans 03 and 04 — once it ships, both can be implemented purely against typed imports from `@/lib/admin-api`.

Purpose: Eliminate the "scavenger hunt" for executors of Plans 03/04 — they import typed inputs/outputs and call typed functions, never inferring shapes from runtime payloads. Also gives the teacher app a visible "New Reading" button so the click-through path is observable even before Plan 03 ships its destination page (404 until then, but the link exists).
Output: Three modified frontend files; no new pages yet (those are Plans 03 and 04).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02-reading-homework/02-CONTEXT.md
@.planning/phases/02-reading-homework/02-RESEARCH.md
@.planning/phases/02-reading-homework/02-PATTERNS.md
@.planning/phases/02-reading-homework/02-UI-SPEC.md
@frontend/lib/admin-api.ts
@frontend/app/game/homework/page.tsx
@frontend/app/teacher/homework/page.tsx

<interfaces>
<!-- Existing exports and patterns the executor extends, not rewrites. -->

frontend/lib/admin-api.ts existing exports (do not break):
- `export type HomeworkType = 'PHONICS' | 'SPEAKING'`  // add 'READING'
- `export interface HomeworkItem { id; type; speakingMode?; name?; parts; speakingPictureUrl?; speakingText?; assignments; createdAt }`
- `export interface GameSession { id; studentId; assignmentId; videoUrl?; score?; completedAt?; startedAt; assignment? }`
- `export interface CreateHomeworkInput { type; speakingMode?; name?; parts?; speakingPictureUrl?; speakingText? }`
- `req<T>(path, options)` JSON helper (lines 15-22) — use this for saveReadingResult, NOT raw fetch
- `saveSpeakingResult(sessionId, audio?)` (lines 136-153) — pattern for FormData, NOT what we want
- `getSession`, `startSession`, `completeSession` — existing game functions

frontend/app/game/homework/page.tsx (entry-point routing):
- `TYPE_META: Record<HomeworkType, { label; emoji }>` line 9-12
- `handleStart(assignmentId)` line 48-58 always routes to /game/session/{id} — needs branch

frontend/app/teacher/homework/page.tsx (teacher dashboard):
- `TYPE_META: Record<HomeworkType, { label; emoji; color; bg }>` line 12-15
- `setTypeFilter` filter array line 562-576
- "New Homework" button line 578-584
- Card content preview switch by `h.type` line 638-668

UI-SPEC color tokens for READING:
- color: '#6ED6C1' (secondary token); bg: '#6ED6C118'
- Toolbar button gradient: `gradients.greenSecondary` (linear-gradient(135deg, #7BD88F, #6ED6C1)) for "New Reading"
- Filter tab label: '📖 Reading'

UI-SPEC copy:
- Toolbar button: "New Reading"
- Filter tab: "📖 Reading"
- Card empty state: "No activities yet"
- Card with N activities: "{n} activity" (n=1) or "{n} activities" (n>1)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: admin-api.ts type additions and saveReadingResult function</name>
  <files>frontend/lib/admin-api.ts</files>
  <read_first>
    - frontend/lib/admin-api.ts (entire file — req<T> helper on lines 15-22; HomeworkType on line 239; CreateHomeworkInput on 253-260; HomeworkItem on 316-326; GameSession on 341-353; SpeakingResult on 332-339; createHomework call on line 93-94 to confirm the POST /homework path)
    - .planning/phases/02-reading-homework/02-PATTERNS.md (admin-api.ts section — full type and function shapes spelled out)
    - .planning/phases/02-reading-homework/02-CONTEXT.md (D-22 — saveReadingResult target endpoint)
  </read_first>
  <action>
    Step 1 — Extend the HomeworkType union on line 239: change `export type HomeworkType = 'PHONICS' | 'SPEAKING';` to `export type HomeworkType = 'PHONICS' | 'SPEAKING' | 'READING';`.

    Step 2 — Immediately after the HomeworkType line, add a new type alias and five interfaces (place them BEFORE the existing CreateWordInput so they sit alongside the homework type system):
    - `export type ReadingActivityType = 'MATCH' | 'FILL_BLANK';`
    - `export interface MatchPair { id: number; activityId: number; imageUrl: string; word: string; order: number; }`
    - `export interface FillBlankChoice { id: number; blankId: number; word: string; isCorrect: boolean; }`
    - `export interface FillBlank { id: number; activityId: number; sentence: string; order: number; choices: FillBlankChoice[]; }`
    - `export interface ReadingActivity { id: number; homeworkId: number; type: ReadingActivityType; order: number; matchPairs?: MatchPair[]; fillBlanks?: FillBlank[]; }`
    - `export interface ReadingResult { id: number; sessionId: number; totalItems: number; correctItems: number; score: number; }`

    Step 3 — Add Input-side companions for creation (place alongside CreatePartInput/CreateWordInput around lines 242-251):
    - `export interface CreateMatchPairInput { imageUrl: string; word: string; }`
    - `export interface CreateFillBlankChoiceInput { word: string; isCorrect: boolean; }`
    - `export interface CreateFillBlankItemInput { sentence: string; choices: CreateFillBlankChoiceInput[]; }`
    - `export interface CreateReadingActivityInput { type: ReadingActivityType; pairs?: CreateMatchPairInput[]; items?: CreateFillBlankItemInput[]; }`

    Step 4 — Extend CreateHomeworkInput (lines 253-260): add a new optional field `readingActivities?: CreateReadingActivityInput[];`. Do NOT touch the other fields or UpdateHomeworkInput.

    Step 5 — Extend HomeworkItem (lines 316-326): add `readingActivities?: ReadingActivity[];` as an optional field. Do NOT touch the other fields.

    Step 6 — Extend GameSession (lines 341-353): add `readingResult?: ReadingResult;` (singular, NOT an array — matches backend one-to-one relation per Plan 01 schema decision).

    Step 7 — Add the saveReadingResult function alongside the other game helpers (place after completeSession, around line 184). Use the existing `req<T>` JSON helper, NOT raw fetch with authHeaders (this is a plain JSON POST, no FormData):

      `export async function saveReadingResult(sessionId: number, data: { correctItems: number; totalItems: number }): Promise<ReadingResult> {
        return req<ReadingResult>(`/game/session/${sessionId}/reading-result`, { method: 'POST', body: JSON.stringify(data), headers: { 'Content-Type': 'application/json' } });
      }`
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File frontend/lib/admin-api.ts grep `-v '^//' | grep -c "'READING'"` returns &gt;= 1 (in HomeworkType union).
    - File contains all of: `export interface MatchPair`, `export interface FillBlankChoice`, `export interface FillBlank`, `export interface ReadingActivity`, `export interface ReadingResult`, `export interface CreateMatchPairInput`, `export interface CreateFillBlankChoiceInput`, `export interface CreateFillBlankItemInput`, `export interface CreateReadingActivityInput`, `export type ReadingActivityType`.
    - File contains the literal text `readingActivities?: CreateReadingActivityInput[]` (in CreateHomeworkInput).
    - File contains the literal text `readingActivities?: ReadingActivity[]` (in HomeworkItem).
    - File contains the literal text `readingResult?: ReadingResult` (in GameSession).
    - File contains `export async function saveReadingResult` AND `/game/session/${sessionId}/reading-result` AND `req&lt;ReadingResult&gt;`.
    - `cd frontend &amp;&amp; npx tsc --noEmit` exits 0 — no new TS errors.
  </acceptance_criteria>
  <done>All Reading types and the saveReadingResult function are exported and TypeScript-clean; downstream plans can `import { ... } from '@/lib/admin-api'` without further additions.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Student homework list READING routing + Teacher dashboard READING entry, filter, and card preview</name>
  <files>frontend/app/game/homework/page.tsx, frontend/app/teacher/homework/page.tsx</files>
  <read_first>
    - frontend/app/game/homework/page.tsx (entire file — TYPE_META on lines 9-12, handleStart on lines 48-58, card render on lines 159-222)
    - frontend/app/teacher/homework/page.tsx (offset 0 limit 100 for imports + TYPE_META; offset 520 limit 160 for toolbar + filter + card preview)
    - frontend/lib/colors.ts (gradients.greenSecondary on line 26)
    - .planning/phases/02-reading-homework/02-UI-SPEC.md (Screens 1 and 2 — exact copy and color tokens)
    - .planning/phases/02-reading-homework/02-PATTERNS.md (teacher/homework/page.tsx and game/homework/page.tsx sections — exact extension shapes)
  </read_first>
  <action>
    Step 1 — frontend/app/game/homework/page.tsx (student app entry routing per D-05, UI-SPEC Screen 2):
    - On line 9-12, extend TYPE_META: add `READING:  { label: 'Reading', emoji: '📖' },` after the SPEAKING entry. Keep existing entries untouched.
    - In handleStart (lines 48-58), replace the unconditional `router.push(`/game/session/${session.id}`)` (line 53) with a type-branched push. Use the homework type returned in the session payload: `const hwType = session.assignment?.homework?.type; if (hwType === 'READING') { router.push(`/game/reading/${session.id}`); } else { router.push(`/game/session/${session.id}`); }`. Do NOT change the surrounding try/catch/finally structure.
    - The card preview block on lines 195-208 has PHONICS and SPEAKING branches inside a single render. Add a third branch for READING after the SPEAKING block at line 208, BEFORE the closing `</div>` on line 209:
      `{hw.type === 'READING' && (
        <span className="bg-white bg-opacity-20 text-white text-sm px-3 py-1 rounded-lg font-semibold">
          📖 {(hw.readingActivities ?? []).length} activit{(hw.readingActivities ?? []).length !== 1 ? 'ies' : 'y'}
        </span>
      )}`
    - Note: this uses bg-white bg-opacity-20 matching the existing PHONICS/SPEAKING chip styling on the game card (NOT the teacher card styling). UI-SPEC Screen 2 specifies "📖 {activityCount} activities".

    Step 2 — frontend/app/teacher/homework/page.tsx (teacher dashboard per D-18, UI-SPEC Screen 1):
    - Imports (line 1-10): add `Link` from `'next/link'` to the existing `import Link from 'next/link';` — confirm it is already imported (line 3 already has Link). No change if present.
    - Lines 12-15 TYPE_META: extend to three entries by adding `READING:  { label: 'Reading',  emoji: '📖', color: '#6ED6C1', bg: '#6ED6C118' },` after SPEAKING. Color from UI-SPEC Color table (secondary token).
    - Toolbar filter tabs (lines 562-576): extend the `({ key, label } as const)[]` array literal to add `{ key: 'READING', label: '📖 Reading' }` after the SPEAKING entry. The existing keyed map already handles the new key — no other change needed.
    - Toolbar CTA area: locate the existing `<button onClick={openCreate} className="btn-primary ...">New Homework</button>` block (lines 578-584). Insert a new `<Link>` element IMMEDIATELY BEFORE it (so "New Reading" sits to the left of "New Homework"; the toolbar uses gap, no order concerns):
      `<Link href="/teacher/homework/create/reading"
        className="btn-primary flex items-center gap-2 shrink-0"
        style={{ background: gradients.greenSecondary }}>
        <svg className="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M12 4v16m8-8H4" />
        </svg>
        New Reading
      </Link>`
    - Card content preview (lines 638-668): the existing structure has `{h.type === 'PHONICS' && (...)} {h.type === 'SPEAKING' && (...)}` inside the `<div className="mb-3">` wrapper. Add a third branch immediately after the SPEAKING block, BEFORE the closing `</div>` on line 668:
      `{h.type === 'READING' && (
        <div className="space-y-1">
          {h.name && (<p className="text-xs font-bold" style={{ color: meta.color }}>{h.name}</p>)}
          {(h.readingActivities ?? []).length === 0 ? (
            <span className="text-xs text-textSecondary/60 italic">No activities yet</span>
          ) : (
            <span className="text-xs px-2 py-0.5 rounded-lg font-bold" style={{ background: meta.bg, color: meta.color }}>
              {(h.readingActivities ?? []).length} activit{(h.readingActivities ?? []).length !== 1 ? 'ies' : 'y'}
            </span>
          )}
        </div>
      )}`
    - Do NOT modify the openEdit function (line 535-549) — it constructs the form for PHONICS/SPEAKING editing only, and READING editing is deferred to Phase 3. The existing form modal already short-circuits via TYPE_META[form.type] so a READING entry showing up in TYPE_META does not enable editing through the modal (the modal is opened only via openEdit/openCreate which default to PHONICS); however, to be safe, in openEdit ensure that when h.type === 'READING' the function navigates the teacher to a Phase 3 placeholder OR simply does nothing — implement as: `function openEdit(h: HomeworkItem) { if (h.type === 'READING') return; /* editing deferred to Phase 3 */ ...existing body }`. This is a tiny guard that prevents a teacher from accidentally opening a half-broken modal on a READING card if they click an edit affordance later.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File frontend/app/game/homework/page.tsx contains all of: `READING:  { label: 'Reading', emoji: '📖' }`, `'READING'` inside an `if (hwType === 'READING')` line, `/game/reading/${session.id}`, and `{hw.type === 'READING'`.
    - File frontend/app/teacher/homework/page.tsx contains all of: `READING:  { label: 'Reading',  emoji: '📖', color: '#6ED6C1', bg: '#6ED6C118' }` (whitespace-insensitive grep also OK), `{ key: 'READING', label: '📖 Reading' }`, `/teacher/homework/create/reading`, `New Reading`, `gradients.greenSecondary`, `{h.type === 'READING'`, `No activities yet`.
    - `cd frontend &amp;&amp; npx tsc --noEmit` exits 0.
    - Visual smoke (executor manual): `cd frontend &amp;&amp; npm run dev` then visit `/teacher/homework` — three filter tabs visible (ALL, PHONICS, SPEAKING, READING), two CTA buttons visible (New Reading, New Homework), clicking New Reading 404s (Plan 03 not yet shipped — expected).
  </acceptance_criteria>
  <done>Student app branches routing for READING sessions; teacher app shows the READING entry point button + filter tab + card preview branch; TypeScript clean. Plan 03 destination page is the only remaining piece for the teacher-side click-through.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser localStorage -> fetch Authorization header | Existing pattern (lines 126, 175 in admin-api.ts); no change |
| backend JSON response -> typed TS interface | TS interfaces document the wire shape; runtime trust comes from backend validation (Plan 01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-10 | Tampering | saveReadingResult body could be tampered in transit | accept | HTTPS in production; backend re-validates correctItems vs totalItems (Plan 01 T-02-03); frontend is not the trust enforcer |
| T-02-11 | Information disclosure | TYPE_META exposes color hex values | accept | not sensitive; existing pattern |
| T-02-12 | Spoofing | A non-student session ID passed to saveReadingResult | mitigate | backend AuthGuard + saveReadingResult service guard (Plan 01) — frontend has no responsibility |
| T-02-13 | XSS | Homework name displayed unescaped in teacher card READING branch | mitigate | React's default JSX escaping handles this; no dangerouslySetInnerHTML introduced — matches existing PHONICS/SPEAKING branches |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` exits 0.
- `cd frontend && npm run dev`, browse `/teacher/homework`, observe three filter tabs and a "New Reading" green-gradient button; click it -> 404 (Plan 03 outstanding).
- Browse `/game/homework` as a student with at least one PHONICS or SPEAKING assignment — start the session and confirm routing still goes to `/game/session/{id}` (regression check).
</verification>

<success_criteria>
- `saveReadingResult` exported and importable from `@/lib/admin-api`.
- `CreateHomeworkInput`, `HomeworkItem`, `GameSession` accept their new READING fields without breaking existing PHONICS/SPEAKING code paths (TypeScript compiles).
- "New Reading" button visible on `/teacher/homework`; READING filter tab functions (filters list to empty array — nothing exists yet — without console errors).
- Student app starting a READING assignment routes to `/game/reading/{id}` (404 until Plan 04 ships); PHONICS/SPEAKING routing unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/02-reading-homework/02-02-SUMMARY.md` listing exported types, the saveReadingResult signature, and any minor deviations.
</output>
