---
phase: 02
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - frontend/package.json
  - frontend/package-lock.json
  - frontend/app/teacher/homework/create/reading/page.tsx
autonomous: true
requirements: [READ-01, READ-02, READ-03]
tags: [frontend, teacher, reading-homework, dnd-kit, file-upload]

must_haves:
  truths:
    - "Teacher can navigate from /teacher/homework via 'New Reading' button to /teacher/homework/create/reading and see an empty creation form"
    - "Teacher can add a matching activity, bulk-upload 2-6 images at once, see each image become a pair card with the filename (without extension) pre-filled as the word label, and edit the label"
    - "Teacher can add a fill-in-blank activity, add multiple sentences each containing the literal ___ placeholder, add 2+ choices per sentence, mark exactly one as correct via a radio control"
    - "Teacher can reorder activities via drag-and-drop on a dedicated drag handle (≡), with reorder persisting in client state"
    - "Form blocks Save while invalid (no activities; any MATCH with <2 or >6 pairs; any FILL_BLANK with <1 sentence; any sentence missing literal ___; any item with <2 choices; any item with isCorrect count != 1); shows inline error per problem"
    - "On Save, POST /homework fires with type:'READING', name, and readingActivities[] reflecting current order; on success the page navigates back to /teacher/homework"
  artifacts:
    - path: frontend/package.json
      provides: "@dnd-kit/core@6.3.1, @dnd-kit/sortable@10.0.0, @dnd-kit/utilities@3.2.2 in dependencies"
      contains: "@dnd-kit/core"
    - path: frontend/package-lock.json
      provides: "Lockfile resolutions for the three @dnd-kit packages"
      contains: "@dnd-kit/core"
    - path: frontend/app/teacher/homework/create/reading/page.tsx
      provides: "Teacher reading homework creation page (NEW). Default-exported React component that renders the layout in UI-SPEC Screen 3: page header (Back link, title, Save button), homework name input, activities list (DnD with SortableContext), Add Matching/Fill-in-blank buttons, per-activity editors (MatchingActivityEditor with bulk image upload + 2-6 pair limit; FillBlankActivityEditor with sentence/choice authoring + radio for correct + inline validation), and on-save POST to createHomework() with router.push('/teacher/homework') redirect."
      min_lines: 250
  key_links:
    - from: frontend/app/teacher/homework/create/reading/page.tsx
      to: frontend/lib/admin-api.ts
      via: "named imports of createHomework, uploadSpeakingImage, CreateReadingActivityInput, CreateMatchPairInput, CreateFillBlankItemInput, CreateFillBlankChoiceInput"
      pattern: "from '@/lib/admin-api'"
    - from: frontend/app/teacher/homework/create/reading/page.tsx
      to: backend POST /homework
      via: "createHomework({ type: 'READING', name, readingActivities }) call on Save"
      pattern: "createHomework"
    - from: frontend/app/teacher/homework/create/reading/page.tsx
      to: backend POST /homework/image
      via: "uploadSpeakingImage(file) per selected image during bulk upload"
      pattern: "uploadSpeakingImage"
    - from: frontend/app/teacher/homework/create/reading/page.tsx
      to: '@dnd-kit/core' + '@dnd-kit/sortable'
      via: "DndContext + SortableContext + useSortable for activity card reordering"
      pattern: "useSortable|DndContext|SortableContext"
---

<objective>
Build the new teacher creation page at `/teacher/homework/create/reading` that satisfies READ-01 (image-word matching with bulk image upload), READ-02 (fill-in-blank with explicit choices + one correct), and READ-03 (sequence reorder via @dnd-kit drag-and-drop). On save the page POSTs to the existing /homework endpoint using the Plan 01 backend transaction.

Purpose: Deliver the teacher's entire vertical slice for creating reading homework — by the end of this plan a teacher can click the "New Reading" button from Plan 02, create a complete homework, save it, and see it in their list. The student-side game page is a separate vertical slice (Plan 04).
Output: One new page, one updated package.json + lockfile.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02-reading-homework/02-CONTEXT.md
@.planning/phases/02-reading-homework/02-RESEARCH.md
@.planning/phases/02-reading-homework/02-PATTERNS.md
@.planning/phases/02-reading-homework/02-UI-SPEC.md
@.planning/phases/02-reading-homework/02-01-PLAN.md
@.planning/phases/02-reading-homework/02-02-PLAN.md
@frontend/app/teacher/homework/page.tsx
@frontend/lib/admin-api.ts
@frontend/lib/colors.ts
@frontend/app/globals.css

<interfaces>
<!-- Contracts the executor consumes (from Plan 02 frontend types and existing helpers). -->

From @/lib/admin-api (post-Plan-02):
- `export type ReadingActivityType = 'MATCH' | 'FILL_BLANK';`
- `export interface CreateMatchPairInput { imageUrl: string; word: string; }`
- `export interface CreateFillBlankChoiceInput { word: string; isCorrect: boolean; }`
- `export interface CreateFillBlankItemInput { sentence: string; choices: CreateFillBlankChoiceInput[]; }`
- `export interface CreateReadingActivityInput { type: ReadingActivityType; pairs?: CreateMatchPairInput[]; items?: CreateFillBlankItemInput[]; }`
- `export interface CreateHomeworkInput { type: HomeworkType; name?: string; readingActivities?: CreateReadingActivityInput[]; ... }`
- `export const createHomework = (data: CreateHomeworkInput) => req<HomeworkItem>('/homework', { method: 'POST', body: JSON.stringify(data) })`
- `export async function uploadSpeakingImage(file: File): Promise<string>` — returns full URL

From @/lib/colors:
- `gradients.greenSecondary` (for Save button) — linear-gradient(135deg, #7BD88F, #6ED6C1)
- `colors`: primary #4F9DFF, secondary #6ED6C1, textPrimary, textSecondary, border, highlight
- `cardGradients` array — for card stripe variety

From @/app/globals.css utility classes:
- `.input-base` — standard input styling
- `.btn-primary` — standard CTA button base
- `.card` — white rounded card with shadow

From @dnd-kit (newly installed):
- `import { DndContext, DragEndEvent, closestCenter, PointerSensor, useSensor, useSensors } from '@dnd-kit/core'`
- `import { SortableContext, arrayMove, useSortable, verticalListSortingStrategy } from '@dnd-kit/sortable'`
- `import { CSS } from '@dnd-kit/utilities'`
- Pattern: useSortable returns { attributes, listeners, setNodeRef, setActivatorNodeRef, transform, transition }
- PointerSensor activationConstraint: { distance: 8 } prevents drag-on-typing (RESEARCH Pitfall 4)

UI-SPEC Screen 3 (read it in full):
- Page container: `animate-fade-in max-w-3xl mx-auto px-8 py-8`
- Save button gradient: `gradients.greenSecondary`
- Activity card: bg-white rounded-2xl border border-border shadow-card overflow-hidden
- Match activity body: bulk upload trigger (when 0 pairs), pair cards grid `grid-cols-3 gap-3`, max 6 pairs
- Fill-blank body: per-item card with sentence textarea, choices list with radio (single select per item)
- Drag handle: `≡` icon on `setActivatorNodeRef` element with `p-3 rounded-lg hover:bg-gray-100`
- Color tokens: MATCH badge `bg-secondary/15 text-secondary`; FILL_BLANK badge `bg-accent/20 text-amber-700`

All copy listed in UI-SPEC "Copywriting Contract" section — use the literal strings shown there (Save Homework, Saving…, Add Matching Activity, Add Fill-in-blank Activity, etc).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Install @dnd-kit dependencies and scaffold creation page with header, name field, activities list, save flow</name>
  <files>frontend/package.json, frontend/package-lock.json, frontend/app/teacher/homework/create/reading/page.tsx</files>
  <read_first>
    - frontend/package.json (current deps — add @dnd-kit/core@6.3.1, @dnd-kit/sortable@10.0.0, @dnd-kit/utilities@3.2.2)
    - frontend/app/teacher/homework/page.tsx (HomeworkModal pattern lines 19-510 — copy error display, loading button, input-base usage)
    - frontend/lib/admin-api.ts (createHomework signature line 93-94; CreateReadingActivityInput from Plan 02)
    - frontend/lib/colors.ts (gradients.greenSecondary, cardGradients)
    - frontend/app/globals.css (input-base, btn-primary, card utility classes — lines 32-50)
    - .planning/phases/02-reading-homework/02-UI-SPEC.md (Screen 3 entire section)
    - .planning/phases/02-reading-homework/02-RESEARCH.md (Pattern 2 - SortableContext example; Pitfall 4 - PointerSensor activationConstraint)
    - .planning/phases/02-reading-homework/02-PATTERNS.md (teacher/homework/create/reading/page.tsx section — full @dnd-kit + bulk upload patterns)
  </read_first>
  <action>
    Step 1 — Install @dnd-kit packages. Run from repo root: `cd frontend && npm install @dnd-kit/core@6.3.1 @dnd-kit/sortable@10.0.0 @dnd-kit/utilities@3.2.2 --save`. The exact versions are locked per RESEARCH (npm verified 2026-05-14). Verify package.json now lists all three under `dependencies`. Verify package-lock.json updated.

    Step 2 — Create the new page directory and file: `frontend/app/teacher/homework/create/reading/page.tsx`. Mark `'use client'` on line 1.

    Step 3 — Top-of-file imports. Use named imports only (no default-import re-exports). Required:
    - React: `useState` from 'react' (no useEffect needed in the basic shell — activities are pure client state)
    - Next: `Link` from 'next/link', `useRouter` from 'next/navigation'
    - admin-api types: `createHomework`, `uploadSpeakingImage`, type-only imports for `CreateReadingActivityInput`, `CreateMatchPairInput`, `CreateFillBlankItemInput`, `CreateFillBlankChoiceInput`, `ReadingActivityType`
    - colors: `gradients`, `colors`
    - dnd-kit core: `DndContext`, `closestCenter`, `PointerSensor`, `useSensor`, `useSensors`, type `DragEndEvent`
    - dnd-kit sortable: `SortableContext`, `arrayMove`, `useSortable`, `verticalListSortingStrategy`
    - dnd-kit utilities: `CSS`

    Step 4 — Define a local Activity draft type that wraps CreateReadingActivityInput with a stable client-side id (needed for SortableContext key + drag identity). Since CreateReadingActivityInput from the API has no id, declare:
    `type DraftActivity = CreateReadingActivityInput & { clientId: string };`
    and generate clientId with `crypto.randomUUID()` (browser-native; this page is 'use client' so it's available). If a TS target issue arises, fall back to `String(Date.now()) + String(Math.random())`.

    Step 5 — Default-exported page component `CreateReadingHomeworkPage`. State:
    - `const [name, setName] = useState('')` — homework name input
    - `const [activities, setActivities] = useState<DraftActivity[]>([])`
    - `const [error, setError] = useState('')`
    - `const [loading, setLoading] = useState(false)`
    - `const [uploadError, setUploadError] = useState('')`
    - PointerSensor: `const sensors = useSensors(useSensor(PointerSensor, { activationConstraint: { distance: 8 } }))` — RESEARCH Pitfall 4.

    Step 6 — Helper functions (Task 2 will fill MATCH and FILL_BLANK editors; this task wires the skeleton):
    - `function addMatchingActivity() { setActivities(prev => [...prev, { clientId: crypto.randomUUID(), type: 'MATCH', pairs: [] }]); }`
    - `function addFillBlankActivity() { setActivities(prev => [...prev, { clientId: crypto.randomUUID(), type: 'FILL_BLANK', items: [] }]); }`
    - `function removeActivity(clientId: string) { setActivities(prev => prev.filter(a =&gt; a.clientId !== clientId)); }`
    - `function updateActivity(clientId: string, patch: Partial<DraftActivity>) { setActivities(prev => prev.map(a =&gt; a.clientId === clientId ? { ...a, ...patch } : a)); }`
    - `function handleDragEnd(event: DragEndEvent) { const { active, over } = event; if (over &amp;&amp; active.id !== over.id) { setActivities(prev =&gt; { const oldIdx = prev.findIndex(a =&gt; a.clientId === active.id); const newIdx = prev.findIndex(a =&gt; a.clientId === over.id); return arrayMove(prev, oldIdx, newIdx); }); } }`

    Step 7 — `function validate(): string | null` — returns first error message or null:
    - If activities.length === 0: return 'Add at least one activity.'
    - For each activity: if type === 'MATCH', validate (pairs?.length ?? 0) between 2 and 6 and every pair has imageUrl and a non-empty word (after trim) — return specific message like `Matching activity ${idx+1}: add 2 to 6 image-word pairs.` or `Matching activity ${idx+1}: every pair needs a word label.`
    - If type === 'FILL_BLANK', validate (items?.length ?? 0) >= 1 and every item has a sentence containing the literal substring `___`, and (choices?.length ?? 0) >= 2, and exactly one choice has isCorrect === true. Returns messages: `Fill-in-blank activity ${idx+1}: add at least 1 sentence.`, `Fill-in-blank activity ${idx+1} sentence ${j+1}: must contain ___ for the blank.`, `Fill-in-blank activity ${idx+1} sentence ${j+1}: add at least 2 word choices.`, `Fill-in-blank activity ${idx+1} sentence ${j+1}: mark one choice as correct.`

    Step 8 — `async function handleSave()` flow:
    - `e.preventDefault()` when invoked from a form submit (or directly when invoked from button onClick).
    - Validate; if returned message, setError(message); return.
    - setError(''); setLoading(true)
    - Strip clientId before sending: `const payload: CreateHomeworkInput = { type: 'READING', name: name.trim() || undefined, readingActivities: activities.map(({ clientId, ...rest }) =&gt; rest) }`
    - try { await createHomework(payload); router.push('/teacher/homework'); } catch (err: unknown) { setError(err instanceof Error ? err.message : 'Failed to save.'); } finally { setLoading(false); }

    Step 9 — JSX skeleton (UI-SPEC Screen 3, page-level layout). Apply this structure exactly:
    - Outer wrapper: `<div className="animate-fade-in max-w-3xl mx-auto px-8 py-8">`
    - Header row: `<div className="flex items-center justify-between mb-8">` containing
      - Left: `<Link href="/teacher/homework" className="text-sm text-textSecondary hover:text-textPrimary">← Back</Link>` AND `<h1 className="text-2xl font-black text-textPrimary mt-1">New Reading Homework</h1>` (wrap these two in a left `<div>`).
      - Right: Save button. Use `<button type="button" onClick={handleSave} disabled={loading} className="btn-primary flex items-center gap-2 shrink-0 disabled:opacity-60" style={{ background: gradients.greenSecondary }}>` with the existing spinner SVG (copied from teacher/homework/page.tsx HomeworkModal save button) when loading, and label `'Saving…'` while loading else `'Save Homework'`.
    - Name field block: `<div className="mb-6">` containing label `<label className="block text-xs font-bold text-textSecondary uppercase tracking-wide mb-2">Homework Name</label>` and `<input className="input-base" placeholder="e.g. Animals – Unit 3 Reading" value={name} onChange={e =&gt; setName(e.target.value)} />`.
    - Activities section: `<div>` with header `<div className="text-xs font-bold text-textSecondary uppercase tracking-wide mb-3">Activities</div>` then the DnD container.
    - DnD container: `<DndContext sensors={sensors} collisionDetection={closestCenter} onDragEnd={handleDragEnd}>` wrapping `<SortableContext items={activities.map(a =&gt; a.clientId)} strategy={verticalListSortingStrategy}>` wrapping the rendered activity list with `space-y-4` on the parent ul/div.
    - Each activity rendered via `<SortableActivityCard key={a.clientId} id={a.clientId} index={idx} activity={a} onRemove={() =&gt; removeActivity(a.clientId)} onUpdate={(patch) =&gt; updateActivity(a.clientId, patch)} />` — the SortableActivityCard component is defined in this same file (Task 2 builds its editors, Task 1 stubs the shell so the page is renderable).
    - After SortableContext: `<div className="mt-4 flex gap-3">` with two buttons:
      - "Add Matching Activity": `<button type="button" onClick={addMatchingActivity} className="flex items-center gap-2 px-4 py-2 rounded-xl text-sm font-bold border-2 border-dashed border-secondary text-secondary hover:bg-secondary/10">+ Add Matching Activity</button>`
      - "Add Fill-in-blank Activity": `<button type="button" onClick={addFillBlankActivity} className="flex items-center gap-2 px-4 py-2 rounded-xl text-sm font-bold border-2 border-dashed border-accent text-amber-600 hover:bg-accent/10">+ Add Fill-in-blank Activity</button>`
    - Error block above Save (or below activities): `{error &amp;&amp; <div className="text-sm bg-highlight/10 border border-highlight/25 text-highlight px-4 py-3 rounded-xl mt-4">{error}</div>}` and same shape for `{uploadError && ...}`.

    Step 10 — `SortableActivityCard` component inside the same file (above the page component to keep declaration order natural). Signature: `function SortableActivityCard({ id, index, activity, onRemove, onUpdate }: { id: string; index: number; activity: DraftActivity; onRemove: () =&gt; void; onUpdate: (patch: Partial<DraftActivity>) =&gt; void })`. Implementation:
    - `const { attributes, listeners, setNodeRef, setActivatorNodeRef, transform, transition } = useSortable({ id });`
    - `const style = { transform: CSS.Transform.toString(transform), transition };`
    - Outer: `<div ref={setNodeRef} style={style} className="card overflow-hidden bg-white rounded-2xl border border-border shadow-card">`
    - Header row: `<div className="flex items-center gap-3 px-5 py-3 border-b border-border bg-background/50">` containing:
      - Drag handle: `<button ref={setActivatorNodeRef} {...attributes} {...listeners} type="button" aria-label="Drag to reorder" className="cursor-grab active:cursor-grabbing p-3 rounded-lg hover:bg-gray-100"><svg className="w-5 h-5 text-textSecondary" viewBox="0 0 24 24" fill="none" stroke="currentColor"><path d="M4 6h16M4 12h16M4 18h16" strokeLinecap="round" strokeWidth="2"/></svg></button>`
      - Type badge: if activity.type === 'MATCH' `<span className="px-2.5 py-1 rounded-full text-xs font-bold bg-secondary/15 text-secondary">📷 Matching</span>` else `<span className="px-2.5 py-1 rounded-full text-xs font-bold bg-accent/20 text-amber-700">✏️ Fill in the Blank</span>`
      - Index: `<span className="text-xs font-bold text-textSecondary ml-auto">Activity {index + 1}</span>`
      - Remove: `<button type="button" onClick={onRemove} aria-label="Remove activity" className="text-xs font-bold text-highlight hover:text-red-600 px-2 py-1 rounded-lg hover:bg-red-50">Remove</button>`
    - Body: `<div className="px-5 py-4">` rendering either `<MatchingActivityEditor activity={activity} onUpdate={onUpdate} onUploadError={setUploadError} />` or `<FillBlankActivityEditor activity={activity} onUpdate={onUpdate} />` (these will be implemented in Task 2; in Task 1 stub them as one-line return TODO placeholders so the page compiles and the shell is reviewable: `function MatchingActivityEditor({ activity }: any) { return <div className="text-xs text-textSecondary italic">Matching editor coming in Task 2 ({(activity.pairs ?? []).length} pairs)</div>; }` and the same for FillBlankActivityEditor showing `{(activity.items ?? []).length} sentences`.

    Step 11 — Confirm: the page compiles, the route works, clicking "Add Matching Activity" creates a draggable placeholder card, dragging reorders them, clicking Remove removes a card. Save attempts will fail validation (placeholder editors don't actually wire pairs/items) — that's expected at end of Task 1.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File frontend/package.json contains `"@dnd-kit/core"`, `"@dnd-kit/sortable"`, `"@dnd-kit/utilities"` under dependencies. Verify: `grep -c '@dnd-kit/' frontend/package.json` returns 3.
    - File frontend/package-lock.json contains `@dnd-kit/core` (executor run `grep -c '@dnd-kit/core' frontend/package-lock.json` returns &gt;= 1).
    - File frontend/app/teacher/homework/create/reading/page.tsx exists. Contains all of: `'use client'`, `useSortable`, `DndContext`, `SortableContext`, `arrayMove`, `closestCenter`, `PointerSensor`, `activationConstraint: { distance: 8 }`, `createHomework`, `router.push('/teacher/homework')`, `New Reading Homework`, `Save Homework`, `Add Matching Activity`, `Add Fill-in-blank Activity`, `Add at least one activity.`
    - File contains both `MATCH` and `FILL_BLANK` as type values (in addMatchingActivity / addFillBlankActivity bodies).
    - `cd frontend &amp;&amp; npx tsc --noEmit` exits 0.
    - `cd frontend &amp;&amp; npm run build` succeeds — Next.js compiles the new route without errors.
    - Manual: `npm run dev` → click "New Reading" from /teacher/homework → reach /teacher/homework/create/reading with empty form. Click "+ Add Matching Activity" twice — two cards appear; drag one above the other (≡ handle) — order persists.
  </acceptance_criteria>
  <done>Creation page shell live, drag-and-drop functional, validation framework in place, Save flow wired (will fail validation until Task 2 ships editors).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: MatchingActivityEditor (bulk upload, pair cards, 2-6 limit) and FillBlankActivityEditor (sentence + choices + radio + inline validation)</name>
  <files>frontend/app/teacher/homework/create/reading/page.tsx</files>
  <read_first>
    - frontend/app/teacher/homework/create/reading/page.tsx (the shell created in Task 1 — extend, do not rewrite)
    - frontend/app/teacher/homework/page.tsx (HomeworkModal lines 200-510 — bulk file input pattern, error/upload state, pair-style word/image card layout)
    - frontend/lib/admin-api.ts (uploadSpeakingImage signature line 155-167)
    - .planning/phases/02-reading-homework/02-UI-SPEC.md (Screen 3 "Matching activity creation body" and "Fill-in-blank activity creation body" subsections — exact copy + classnames + inline validation messages)
    - .planning/phases/02-reading-homework/02-CONTEXT.md (D-07 max 6 pairs; D-11/D-12 fill-blank rules; D-20 filename-prefill rule)
    - .planning/phases/02-reading-homework/02-PATTERNS.md (bulk image upload pattern showing filename-strip extension regex)
  </read_first>
  <action>
    Replace the Task-1 stub `MatchingActivityEditor` with the full implementation:

    `function MatchingActivityEditor({ activity, onUpdate, onUploadError }: { activity: DraftActivity; onUpdate: (patch: Partial<DraftActivity>) =&gt; void; onUploadError: (msg: string) =&gt; void; })`

    State (local to component):
    - `const [uploading, setUploading] = useState(false)`
    Pairs accessor: `const pairs = activity.pairs ?? [];`

    Handlers:
    - `function setPair(idx: number, patch: Partial<CreateMatchPairInput>) { const next = pairs.map((p, i) =&gt; i === idx ? { ...p, ...patch } : p); onUpdate({ pairs: next }); }`
    - `function removePair(idx: number) { onUpdate({ pairs: pairs.filter((_, i) =&gt; i !== idx) }); }`
    - `async function handleFiles(files: FileList | null) { if (!files) return; const remaining = 6 - pairs.length; const toUpload = Array.from(files).slice(0, remaining); if (toUpload.length === 0) return; setUploading(true); onUploadError(''); const newPairs: CreateMatchPairInput[] = [...pairs]; for (const file of toUpload) { try { const url = await uploadSpeakingImage(file); const word = file.name.replace(/\\.[^.]+$/, ''); newPairs.push({ imageUrl: url, word }); } catch (err: unknown) { onUploadError(err instanceof Error ? err.message : 'Image upload failed'); break; } } onUpdate({ pairs: newPairs }); setUploading(false); }`
    - Filename-strip regex `/\\.[^.]+$/` removes everything after (and including) the last dot.

    JSX (UI-SPEC Screen 3 "Matching activity creation body"):
    - Pair count indicator: `<div className="flex items-center justify-between mb-3"><span className="text-xs font-bold text-textSecondary">{pairs.length} / 6 pairs</span></div>`
    - When pairs.length === 0: render bulk upload trigger:
      - `<label className="block rounded-xl border-2 border-dashed border-secondary/50 bg-secondary/5 py-8 flex flex-col items-center gap-2 cursor-pointer">`
      - `<span className="text-2xl">🖼️</span>`
      - `<span className="text-sm font-bold text-secondary">Click to upload images</span>`
      - `<span className="text-xs text-textSecondary">Each image becomes a matching pair</span>`
      - `<input type="file" accept="image/*" multiple hidden onChange={e =&gt; { handleFiles(e.target.files); e.currentTarget.value = ''; }} />`
      - `</label>`
    - When pairs.length > 0: render grid + add-more button:
      - `<div className="grid grid-cols-3 gap-3 mb-4">` containing each pair:
        - `<div key={i} className="bg-background rounded-xl border border-border p-3">`
        - `<img src={pair.imageUrl} alt={pair.word} className="w-20 h-20 rounded-lg object-cover mb-2 mx-auto" />`
        - `<input className="input-base text-sm py-2" placeholder="Word label" value={pair.word} onChange={e =&gt; setPair(i, { word: e.target.value })} />`
        - `<button type="button" onClick={() =&gt; removePair(i)} aria-label="Remove pair" className="block text-center mt-1 text-xs text-highlight hover:text-red-600">✕</button>`
        - `</div>`
      - Below grid: if pairs.length &lt; 6: `<label className="text-xs font-bold text-secondary hover:underline cursor-pointer">+ Add images<input type="file" accept="image/*" multiple hidden onChange={e =&gt; { handleFiles(e.target.files); e.currentTarget.value = ''; }} /></label>`
      - Else: `<span className="text-xs font-bold text-textSecondary opacity-60">Maximum 6 pairs reached</span>`
    - Inline validation under the grid: `{pairs.length === 1 &amp;&amp; <p className="text-xs text-highlight mt-2">Add at least 2 image-word pairs.</p>}` (UI-SPEC says "Add at least 2 image-word pairs." — only shown when count is &gt;0 but &lt;2; the validate() function on save covers length===0).

    Replace the Task-1 stub `FillBlankActivityEditor` with the full implementation:

    `function FillBlankActivityEditor({ activity, onUpdate }: { activity: DraftActivity; onUpdate: (patch: Partial<DraftActivity>) =&gt; void; })`

    Items accessor: `const items = activity.items ?? [];`

    Handlers:
    - `function addItem() { onUpdate({ items: [...items, { sentence: '', choices: [{ word: '', isCorrect: true }, { word: '', isCorrect: false }] }] }); }` — start with 2 choices, first marked correct, so the radio state is valid immediately.
    - `function removeItem(idx: number) { onUpdate({ items: items.filter((_, i) =&gt; i !== idx) }); }`
    - `function setItem(idx: number, patch: Partial<CreateFillBlankItemInput>) { onUpdate({ items: items.map((it, i) =&gt; i === idx ? { ...it, ...patch } : it) }); }`
    - `function setChoice(itemIdx: number, choiceIdx: number, patch: Partial<CreateFillBlankChoiceInput>) { const it = items[itemIdx]; setItem(itemIdx, { choices: it.choices.map((c, j) =&gt; j === choiceIdx ? { ...c, ...patch } : c) }); }`
    - `function markCorrect(itemIdx: number, choiceIdx: number) { const it = items[itemIdx]; setItem(itemIdx, { choices: it.choices.map((c, j) =&gt; ({ ...c, isCorrect: j === choiceIdx })) }); }` — radio behavior: marking one un-marks all others.
    - `function addChoice(itemIdx: number) { const it = items[itemIdx]; setItem(itemIdx, { choices: [...it.choices, { word: '', isCorrect: false }] }); }`
    - `function removeChoice(itemIdx: number, choiceIdx: number) { const it = items[itemIdx]; const next = it.choices.filter((_, j) =&gt; j !== choiceIdx); if (next.length &gt; 0 &amp;&amp; !next.some(c =&gt; c.isCorrect)) next[0].isCorrect = true; setItem(itemIdx, { choices: next }); }` — keep at least one correct after removal.

    JSX (UI-SPEC Screen 3 "Fill-in-blank activity creation body"):
    - Items list: `<div className="space-y-4">` per item:
      - `<div key={i} className="rounded-xl border border-border p-4 bg-background/50">`
      - `<div className="text-xs font-bold text-textSecondary mb-2">Sentence {i + 1}</div>`
      - Sentence textarea: `<textarea rows={2} className="input-base resize-none" placeholder="Type the sentence, use ___ for the blank (e.g. The cat sat on the ___)" value={item.sentence} onChange={e =&gt; setItem(i, { sentence: e.target.value })} />`
      - Inline sentence error: `{item.sentence.length &gt; 0 &amp;&amp; !item.sentence.includes('___') &amp;&amp; <p className="text-xs text-highlight mt-1">Each sentence must contain ___ for the blank.</p>}`
      - Choices block: `<div className="mt-3"><div className="text-xs font-bold text-textSecondary uppercase tracking-wide mb-2">Word choices</div>` then for each choice:
        - `<div key={j} className="flex items-center gap-2 mb-2">`
        - Radio: `<input type="radio" name={`item-${item}-correct-${i}`} aria-label="Mark as correct answer" checked={c.isCorrect} onChange={() =&gt; markCorrect(i, j)} className="w-4 h-4 accent-primary" />` — note the `name` attribute MUST be unique per item so the radios within ONE item act as a group but radios across items are independent. Use a stable key: e.g. `name={`correct-${i}`}` — i is the item index in the activity, which is stable per render of this editor.
        - Choice input: `<input className="input-base flex-1 py-2" placeholder="Word option" value={c.word} onChange={e =&gt; setChoice(i, j, { word: e.target.value })} />`
        - Remove: `<button type="button" onClick={() =&gt; removeChoice(i, j)} aria-label="Remove choice" className="text-highlight text-sm hover:text-red-600">✕</button>`
        - `</div>`
      - Add choice button: `<button type="button" onClick={() =&gt; addChoice(i)} className="mt-2 text-xs font-bold text-primary hover:underline">+ Add choice</button>`
      - Inline choice errors: `{item.choices.length &lt; 2 &amp;&amp; <p className="text-xs text-highlight mt-2">Add at least 2 word choices.</p>}` AND `{item.choices.length &gt;= 2 &amp;&amp; item.choices.filter(c =&gt; c.isCorrect).length !== 1 &amp;&amp; <p className="text-xs text-highlight mt-1">Mark one choice as correct.</p>}`
      - `</div>` close choices block
      - Remove sentence: `<button type="button" onClick={() =&gt; removeItem(i)} className="text-xs font-bold text-highlight hover:text-red-600 mt-2">Remove sentence</button>`
      - `</div>` close item card
    - Below items list: `<button type="button" onClick={addItem} className="mt-3 text-xs font-bold text-amber-700 hover:underline">+ Add sentence</button>`
    - If items.length === 0: `<p className="text-xs text-textSecondary italic">No sentences yet — click "+ Add sentence" to start.</p>`

    Final wiring: ensure the placeholder onUploadError prop in SortableActivityCard (Task 1 left a `setUploadError` reference) is passed through. The page component's `uploadError` state should be displayed in the top-level error block alongside `error`.

    Polish: when activities.length === 0, show a helper empty state inside the DnD container: `<div className="rounded-xl border-2 border-dashed border-border bg-background/50 py-12 text-center text-sm text-textSecondary">No activities yet. Use the buttons below to add a Matching or Fill-in-blank activity.</div>`.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File frontend/app/teacher/homework/create/reading/page.tsx contains all of: `function MatchingActivityEditor`, `function FillBlankActivityEditor`, `uploadSpeakingImage`, `file.name.replace(/\\.[^.]+$/`, `Each image becomes a matching pair`, `Maximum 6 pairs reached`, `+ Add images`, `+ Add choice`, `+ Add sentence`, `Type the sentence, use ___ for the blank`, `Each sentence must contain ___ for the blank.`, `Add at least 2 word choices.`, `Mark one choice as correct.`, `Remove sentence`.
    - File contains `aria-label="Remove pair"` AND `aria-label="Remove choice"` AND `aria-label="Mark as correct answer"` AND `aria-label="Remove activity"` AND `aria-label="Drag to reorder"` (UI-SPEC requires aria-labels on icon-only remove + radio + drag-handle).
    - File contains `accept="image/*"` AND `multiple` (bulk upload input).
    - File contains `type="radio"` AND `markCorrect` (radio + handler).
    - `cd frontend &amp;&amp; npx tsc --noEmit` exits 0.
    - `cd frontend &amp;&amp; npm run build` succeeds.
    - Manual: with backend running, create a complete homework end-to-end: name "Smoke test", one MATCH activity with 3 images (upload from any local jpgs/pngs), one FILL_BLANK activity with one sentence "The cat sat on the ___" with 3 choices (mat/cat/bat with mat marked correct), drag the FILL_BLANK above the MATCH, click Save Homework → should redirect to /teacher/homework with the new homework in the list.
  </acceptance_criteria>
  <done>Teacher can create a complete reading homework end-to-end: bulk image upload populates pairs with filename labels, fill-blank sentences with explicit choices + radio, drag-reorder, inline validation, Save persists via POST /homework and redirects.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| teacher browser file picker -> uploadSpeakingImage -> POST /homework/image | Image files of arbitrary content type/size; backend already enforces 10 MB limit + auth |
| teacher browser form state -> POST /homework | Free-form text in sentence/word fields; backend validates the structural rules (Plan 01 T-02-01/T-02-02) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-14 | Tampering | Teacher could submit a malformed sentence missing `___` | mitigate | Inline validate() in handleSave returns specific error and blocks Save (UI-SPEC contract); backend re-validates (Plan 01) |
| T-02-15 | Tampering | Teacher could submit MATCH with 0 or 7+ pairs by editing client state directly | mitigate | Frontend bulk uploader slices to `6 - pairs.length`; "Add images" hidden when pairs.length === 6; backend re-validates pair count between 2 and 6 (Plan 01 T-02-02) |
| T-02-16 | Tampering | Teacher could mark multiple choices isCorrect by directly editing state | mitigate | Radio onChange uses markCorrect which forces exactly-one; backend re-validates one-correct rule (Plan 01 T-02-01) |
| T-02-17 | Information disclosure | uploadSpeakingImage URLs are predictable | accept | Existing pattern from Phase 1; URLs are public-readable image paths in MinIO; no PII; acceptable for teacher-uploaded matching images |
| T-02-18 | XSS | Sentence text rendered unescaped | mitigate | React JSX escapes by default; no dangerouslySetInnerHTML introduced |
| T-02-19 | Denial of Service | Teacher uploads many large images at once | accept | Backend's per-image 10 MB limit + sequential upload (loop, not Promise.all) bounds memory; teacher is authenticated so abuse vector is teacher account misuse, not anonymous |
| T-02-20 | Repudiation | No record of who created which reading homework | accept | Phase 2 inherits existing Homework table (no createdBy column); deferred to Phase 3 multi-teacher work |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` exits 0.
- `cd frontend && npm run build` succeeds.
- Manual end-to-end (with backend + DB + MinIO running): navigate /teacher/homework → New Reading → fill name "E2E Reading" → add MATCH activity → bulk-upload 3 images → edit labels → add FILL_BLANK activity → add 2 sentences each with 3 choices + 1 marked correct → drag FILL_BLANK above MATCH → click Save Homework → land on /teacher/homework with "E2E Reading" card visible showing "2 activities".
- Validation negative cases: empty form → Save shows 'Add at least one activity.'; MATCH with 1 pair → Save shows 'Matching activity 1: add 2 to 6 image-word pairs.'; FILL_BLANK sentence missing `___` → Save shows the appropriate inline error and blocks.
</verification>

<success_criteria>
- READ-01: Teacher creates an image-word matching activity via bulk upload, 2-6 pairs limit enforced, filename pre-fills label. ✓
- READ-02: Teacher creates fill-in-blank activity, types sentences with `___`, adds explicit choices, marks exactly one correct. ✓
- READ-03: Teacher reorders matching and fill-in-blank activities via drag-and-drop; order persists in submitted payload. ✓
- New homework appears in /teacher/homework list after save; backend round-trips it correctly (verifies Plan 01 transaction works end-to-end).
</success_criteria>

<output>
After completion, create `.planning/phases/02-reading-homework/02-03-SUMMARY.md` covering dependency versions installed, page structure, validation rules implemented, and any UI-SPEC deviations.
</output>
