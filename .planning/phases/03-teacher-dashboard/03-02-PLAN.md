---
phase: 03-teacher-dashboard
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - backend/src/homework/homework.repository.ts
  - frontend/app/teacher/homework/page.tsx
  - frontend/app/teacher/homework/[id]/page.tsx
autonomous: true
requirements:
  - TEACH-02
  - TEACH-03
  - TEACH-04
user_setup: []

must_haves:
  truths:
    - "Teacher sees an X/Y submitted pill on every assignment card in the homework list page where Y is the total enrolled students across that assignment's classes"
    - "Teacher sees an X/Y submitted pill on the homework detail page for each assignment"
    - "Teacher sees a list of students who have NOT submitted under each assignment on the detail page, with the student's display name"
    - "Backend assignmentInclude returns class._count.students alongside the existing _count.sessions"
    - "Backend findById on Homework returns each class's enrolled students (id + name) so the frontend can compute the non-submitted set"
    - "Existing AssignModal multi-class assignment flow remains functional end-to-end (TEACH-02 regression check): teacher can select 2+ classes, set a due date, and the assignment row created has classIds = the selected set"
  artifacts:
    - path: "backend/src/homework/homework.repository.ts"
      provides: "assignmentInclude extended with class._count.students (D-13); homework findById extended with class.students enrollment list (for non-submitted list)"
      contains: "_count: { select: { students: true } }"
    - path: "frontend/app/teacher/homework/page.tsx"
      provides: "Per-assignment submission count badge on list cards (X/Y submitted style pill); reads totalEnrolled via assignment.classes.reduce sum"
    - path: "frontend/app/teacher/homework/[id]/page.tsx"
      provides: "Submission count pill near assignment header; non-submitted student list section under each assignment"
  key_links:
    - from: "backend/src/homework/homework.repository.ts assignmentInclude"
      to: "frontend/app/teacher/homework/page.tsx + [id]/page.tsx"
      via: "GET /homework -> assignment.classes[].class._count.students"
      pattern: "_count\\?\\.students"
    - from: "frontend non-submitted list"
      to: "backend findById homeworkInclude"
      via: "class.students enrollment join"
      pattern: "students: \\{ select:"
    - from: "Existing AssignModal (frontend/app/teacher/homework/page.tsx lines 409-446)"
      to: "POST /homework/assignment with classIds[] payload"
      via: "createAssignment({ homeworkId, classIds: [...], endDate })"
      pattern: "classIds: selectedClassIds"
---

<objective>
Deliver the submission-count vertical slice end-to-end: backend returns enrolled-student counts and student lists; frontend displays "X / Y submitted" pills on both the homework list and detail pages and a non-submitted student list under each assignment on the detail page. This slice is independent of READING homework creation and ships TEACH-03 and TEACH-04 immediately for ALL existing homework types. As a no-code-change side effect, this plan also formally verifies that TEACH-02 (multi-class assignment via the existing AssignModal) continues to function — Task 4 is a regression-style assertion that the AssignModal pathway still routes a `classIds[]` array to `POST /homework/assignment`.

Purpose: Per D-13/D-14, teachers need to know how many of the assigned students have actually completed each homework, and who has not. This data already exists in the database; only the include shape and two display surfaces need to change. TEACH-02 is satisfied by the existing AssignModal — this plan owns its verification.

Output: After this slice, a teacher loading `/teacher/homework` sees a green "5 / 12 submitted" pill on each assignment card; opening any assignment shows the same pill plus a divider list "Not submitted: Alice, Bob, Charlie". The TEACH-02 multi-class assignment flow remains intact.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-teacher-dashboard/03-CONTEXT.md
@.planning/phases/03-teacher-dashboard/03-RESEARCH.md
@.planning/phases/03-teacher-dashboard/03-PATTERNS.md
@.planning/phases/03-teacher-dashboard/03-01-SUMMARY.md

<interfaces>
<!-- Existing line anchors -->

backend/src/homework/homework.repository.ts:
- Lines 12-15: existing `assignmentInclude` constant
- Existing pattern for class includes: `classes: { include: { class: true } }`

frontend/app/teacher/homework/page.tsx:
- Lines 670-674: existing `totalSessions` submission display block
- Lines 627-633: existing Open/Closed pill style (use as visual template)
- Lines 562-585: filter tabs (untouched in this plan)
- Lines 409-446: existing AssignModal component (multi-class assignment — TEACH-02 owner)
- Line 433: `const input: CreateAssignmentInput = { homeworkId: homework.id, classIds: selectedClassIds, endDate };` (TEACH-02 evidence anchor)

frontend/app/teacher/homework/[id]/page.tsx:
- Lines 96-166: assignment list rendering
- Lines 97-99: existing `isOpen`, `classNames`, `sessions` derivation
- Lines 128-155: existing per-session list with divide-y pattern (template for non-submitted list)

frontend/lib/admin-api.ts:
- AssignmentClass.class already extended with `_count?: { students: number }` in Plan 01 — consume here.

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend backend assignmentInclude with class student count and findById with class students enrollment list</name>
  <files>backend/src/homework/homework.repository.ts</files>
  <read_first>
    - backend/src/homework/homework.repository.ts (full file)
    - .planning/phases/03-teacher-dashboard/03-PATTERNS.md (`backend/src/homework/homework.repository.ts` section — D-13 extension snippet)
    - .planning/phases/03-teacher-dashboard/03-RESEARCH.md (Pattern 5: assignmentInclude Extension for Student Counts; "Open Questions" item 2 — recommended approach is option (b): extend findById to include class students)
    - .planning/phases/03-teacher-dashboard/03-CONTEXT.md (D-13, D-14)
  </read_first>
  <action>
    Per D-13 and D-14:

    1. Locate the existing `assignmentInclude` constant (currently around lines 12-15). Replace its `classes` shape so that the nested `class` carries the new `_count` aggregate:
       - Before: `classes: { include: { class: true } }, _count: { select: { sessions: true } }`
       - After: `classes: { include: { class: { include: { _count: { select: { students: true } } } } } }, _count: { select: { sessions: true } }`
    2. For the non-submitted student list (D-14 / Open Question 2 recommendation option b): also extend the class include used by `findById` (the homework detail query). Two acceptable approaches — pick (a) if `findById` already references the same `assignmentInclude`, otherwise add a parallel detail-level include:
       - Add a new `assignmentDetailInclude` constant near `assignmentInclude` that extends it with `students: { select: { id: true, name: true } }` on each class. Shape: `classes: { include: { class: { include: { _count: { select: { students: true } } }, students: { select: { id: true, name: true } } } } } }`. Use the deeper shape only where the detail page needs it. If clarification needed: the detail page already loads `assignments` via `findById`, so the simplest route is to make `findById` use the deeper include while `findAll` keeps the lighter `assignmentInclude`. Choose this route.
    3. Confirm `findAll` continues to use the lighter `assignmentInclude` (with `_count` only, no `students` join) to keep the list endpoint cheap.
    4. Do NOT modify any other method (this plan deliberately avoids `createReadingHomework` body work — that lives in Plan 04).

    No new endpoint is added; only the existing GET /homework and GET /homework/:id payload shapes are enriched.
  </action>
  <verify>
    <automated>cd backend && npx tsc --noEmit && npx jest --testPathPattern=homework --no-coverage --passWithNoTests</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "_count: { select: { students: true } }" backend/src/homework/homework.repository.ts` returns at least 1.
    - `grep -c "students: { select: { id: true" backend/src/homework/homework.repository.ts` returns at least 1 (or equivalent shape: `students: { select: { id: true, name: true } }`).
    - The unchanged `_count: { select: { sessions: true } }` line is still present (grep returns >= 1).
    - `cd backend && npx tsc --noEmit` exits 0.
    - When the app boots, a `GET /homework` response includes `assignments[i].classes[j].class._count.students` as a number (manual probe; tested again at phase verification).
    - `findById(id)` response includes `assignments[i].classes[j].class.students` as an array of `{ id, name }` (manual probe).
  </acceptance_criteria>
  <done>Repository returns both the aggregate student count and the enrollment list; tsc clean; existing homework tests still pass.</done>
</task>

<task type="auto">
  <name>Task 2: Show "X / Y submitted" pill on homework list page assignment cards</name>
  <files>frontend/app/teacher/homework/page.tsx</files>
  <read_first>
    - frontend/app/teacher/homework/page.tsx (full file; in particular the assignment card rendering and the existing totalSessions block around lines 670-674; Open/Closed pill style around lines 627-633)
    - frontend/lib/admin-api.ts (line 281-286 AssignmentClass — already includes `_count?: { students: number }` from Plan 01)
    - .planning/phases/03-teacher-dashboard/03-PATTERNS.md (`frontend/app/teacher/homework/page.tsx` section, "Submission count badge on list card" snippet)
    - .planning/phases/03-teacher-dashboard/03-CONTEXT.md (D-13, D-14)
  </read_first>
  <action>
    Per D-13 / D-14 / TEACH-03 on the homework LIST page only:

    1. In the assignment card rendering area where `totalSessions` is currently displayed (around line 670-674), add a computed `totalEnrolled` value per assignment using the D-13 sum formula: `const totalEnrolled = assignment.classes.reduce((sum, ac) => sum + (ac.class._count?.students ?? 0), 0);`. Place this near the existing per-assignment computed values, before the JSX that renders the card.
    2. Compute `completedSessions = assignment._count?.sessions ?? assignment.sessions?.length ?? 0;` using whichever field is already exposed on the AssignmentItem in the list payload. (`_count.sessions` is already present per assignmentInclude.)
    3. Render a pill near the existing card metadata. Use the same Open/Closed pill style template (lines 627-633): rounded-full, text-xs font-semibold, px-2.5 py-1. Use `bg-emerald-50 text-emerald-600` when `completedSessions > 0`, otherwise `bg-gray-50 text-gray-500`. Pill content: `{completedSessions} / {totalEnrolled} submitted`.
    4. Keep the existing "N submission(s) total" text OR replace it — choose ONE display per assignment to avoid duplication. Recommended: REPLACE the old `totalSessions` line with the new pill. Document in SUMMARY which line numbers changed.
    5. Do NOT modify TYPE_META, filter tabs, openCreate flow, or the type-picker — those are owned by Plan 03.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npx next lint --dir app/teacher/homework --max-warnings 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "totalEnrolled" frontend/app/teacher/homework/page.tsx` returns at least 1.
    - `grep -c "submitted" frontend/app/teacher/homework/page.tsx` returns at least 1 (the new pill text).
    - `grep -cE "ac\\.class\\._count\\?\\.students \\?\\? 0" frontend/app/teacher/homework/page.tsx` returns 1 (D-13 sum formula present verbatim).
    - `cd frontend && npx tsc --noEmit` exits 0.
    - When logged in as teacher and visiting `/teacher/homework`, every assignment card renders a pill with the format `<num> / <num> submitted` (manual verification at phase end).
  </acceptance_criteria>
  <done>Homework list cards show the X/Y submitted pill; pill uses emerald style; type-checks clean.</done>
</task>

<task type="auto">
  <name>Task 3: Show "X / Y submitted" pill and non-submitted student list on homework detail page</name>
  <files>frontend/app/teacher/homework/[id]/page.tsx</files>
  <read_first>
    - frontend/app/teacher/homework/[id]/page.tsx (full file; in particular lines 96-166 assignment list rendering; lines 97-99 derivation pattern; lines 128-155 session list divide-y template)
    - frontend/lib/admin-api.ts (AssignmentClass and HomeworkDetail; class.students from extended include)
    - .planning/phases/03-teacher-dashboard/03-PATTERNS.md (`frontend/app/teacher/homework/[id]/page.tsx` section — submission count + non-submitted list snippets)
    - .planning/phases/03-teacher-dashboard/03-CONTEXT.md (D-13, D-14)
  </read_first>
  <action>
    Per D-13 / D-14 / TEACH-04 on the homework DETAIL page:

    1. Inside the existing per-assignment block (around line 97-99 where `isOpen`, `classNames`, `sessions`, `completed` are derived), add:
       - `const totalEnrolled = a.classes.reduce((sum, ac) => sum + (ac.class._count?.students ?? 0), 0);`
       - `const submittedStudentIds = new Set(sessions.map((s) => s.studentId));`
       - `const enrolledStudents = a.classes.flatMap((ac) => ac.class.students ?? []);`
       - `const dedupedEnrolled = Array.from(new Map(enrolledStudents.map((s) => [s.id, s])).values());`
       - `const notSubmitted = dedupedEnrolled.filter((s) => !submittedStudentIds.has(s.id));`
    2. Near the existing Open/Closed status badge (around line 106-109), add a new pill: `<span className="text-xs font-semibold px-2.5 py-1 rounded-full bg-blue-50 text-blue-600">{completed.length} / {totalEnrolled} submitted</span>`.
    3. After the existing sessions divide-y list (after lines 128-155), add a new section visible only when `notSubmitted.length > 0`:
       - Heading: `<h4 className="text-xs font-semibold text-textSecondary mt-3 mb-1">Not submitted ({notSubmitted.length})</h4>`.
       - List: same divide-y pattern as sessions but no link — each row renders `<div key={s.id} className="py-2 text-sm text-textPrimary">{s.name}</div>`.
    4. If `dedupedEnrolled.length === 0` (no class enrollment data returned), skip the not-submitted section silently (defensive — old data may pre-date the include extension).
    5. Do NOT touch the existing TYPE_META in this plan — TYPE_META READING entry is added in Plan 03 alongside the type-picker work. The detail page already renders PHONICS/SPEAKING types; READING items are not yet creatable in Wave 2 so this is safe.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npx next lint --dir app/teacher/homework --max-warnings 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "totalEnrolled" frontend/app/teacher/homework/\\[id\\]/page.tsx` returns at least 1.
    - `grep -c "notSubmitted" frontend/app/teacher/homework/\\[id\\]/page.tsx` returns at least 1.
    - `grep -c "submittedStudentIds" frontend/app/teacher/homework/\\[id\\]/page.tsx` returns 1.
    - `grep -c "Not submitted" frontend/app/teacher/homework/\\[id\\]/page.tsx` returns at least 1.
    - `cd frontend && npx tsc --noEmit` exits 0.
    - When a teacher opens any existing assignment detail page with at least one class, the page renders both the X/Y submitted pill and (when applicable) a "Not submitted (N)" section listing students who do not yet have a session record (manual verification at phase end).
  </acceptance_criteria>
  <done>Detail page renders submission pill + non-submitted student list under each assignment; tsc clean; visual style mirrors existing pills and divide-y list patterns.</done>
</task>

<task type="auto">
  <name>Task 4: Regression-verify TEACH-02 (existing AssignModal multi-class assignment flow)</name>
  <files>frontend/app/teacher/homework/page.tsx</files>
  <read_first>
    - frontend/app/teacher/homework/page.tsx (lines 409-446 — AssignModal component; line 433 — the `classIds: selectedClassIds` payload; lines 555-560 — AssignModal mount site `{assigningHw && <AssignModal …/>}`)
    - frontend/lib/admin-api.ts (CreateAssignmentInput type, createAssignment function)
    - .planning/phases/03-teacher-dashboard/03-CONTEXT.md (TEACH-02 already considered "no code change required — AssignModal handles multi-class")
    - .planning/phases/03-teacher-dashboard/03-RESEARCH.md (Architectural Responsibility Map — TEACH-02 currently satisfied by existing AssignModal)
  </read_first>
  <action>
    Per TEACH-02 — TEACH-02 is satisfied by the pre-existing AssignModal which already accepts a `classIds[]` array (line 433: `classIds: selectedClassIds`). This task does NOT modify any source code. Its sole purpose is to record verifiable assertions that this slice's other tasks (which touch the same page.tsx file) did NOT break the multi-class assignment pathway.

    Procedure:
    1. After Tasks 1–3 are committed, run the grep assertions in the acceptance_criteria block below to confirm the multi-class wiring is still in place verbatim. If any assertion fails, restore the AssignModal section from git and re-run.
    2. Do a manual end-to-end probe: open `/teacher/homework`, click Assign on any homework, select 2 different classes in the modal, set an end-date 7 days in the future, click Save. Then open the homework detail page and confirm the new assignment shows both class names in `classNames` (line 98) and the X/Y submitted pill from Task 3 renders with `Y > 0` (because the new assignment now has class enrollment counts).
    3. Record the result of the probe in the SUMMARY: pass/fail plus any UI regressions observed.

    No file is modified by this task. The "files" field above refers to the file being asserted-on, not modified.
  </action>
  <verify>
    <automated>grep -c "classIds: selectedClassIds" frontend/app/teacher/homework/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "function AssignModal" frontend/app/teacher/homework/page.tsx` returns 1 (AssignModal component still defined).
    - `grep -c "classIds: selectedClassIds" frontend/app/teacher/homework/page.tsx` returns 1 (the multi-class payload assembly line is intact).
    - `grep -c "selectedClassIds" frontend/app/teacher/homework/page.tsx` returns at least 3 (state, toggle handler, payload).
    - `grep -c "<AssignModal" frontend/app/teacher/homework/page.tsx` returns at least 1 (the modal is still mounted somewhere on the page).
    - `cd frontend && npx tsc --noEmit` exits 0 (no incidental type damage from Tasks 2–3 affecting AssignModal).
    - Manual probe documented in SUMMARY: assigning a homework to 2+ classes succeeds and renders correctly on the detail page; the X/Y submitted pill shows non-zero Y because the new assignment has class enrollment.
  </acceptance_criteria>
  <done>Existing AssignModal multi-class assignment flow verified intact; TEACH-02 evidence recorded in SUMMARY; no code changes required.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Teacher browser → NestJS API | Existing AuthGuard already protects `/homework`; this plan only enriches response shapes |
| Backend → Postgres | Larger query payload (class.students join) — read-only, no new write surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Information Disclosure | New `class.students` include exposes enrolled student id + name | accept | This data is ALREADY shown on `/teacher/classes/[id]`; same teacher role consumes it. Single-teacher system per PROJECT.md "Constraints". |
| T-03-07 | Denial of Service | `findById` payload grows by O(students-per-class * classes-per-assignment) | mitigate | Restrict the new include to `select: { id: true, name: true }` only — NOT `include: true`; avoids pulling parent/email/etc fields. Add to acceptance gate via grep on `select` keyword. |
| T-03-08 | Tampering | Frontend reduce formula trusts `_count?.students` from server | accept | `_count` is computed server-side by Prisma; client cannot influence it. Fallback `?? 0` prevents NaN if absent. |
| T-03-09 | Tampering | AssignModal accepts `classIds[]` array from client | accept | Existing AuthGuard restricts to TEACHER role; existing assignment service already validates referenced class ids belong to the teacher (pre-existing behavior, not introduced by this plan). |
</threat_model>

<verification>
- `assignmentInclude` carries `_count: { select: { students: true } }` on each class.
- `findById` returns class.students as `{ id, name }[]` (select narrowed).
- Both list and detail pages render the X/Y pill.
- Detail page renders a "Not submitted (N)" section when there are enrolled-but-no-session students.
- AssignModal multi-class wiring intact (TEACH-02 regression).
- All TS files type-check cleanly.
</verification>

<success_criteria>
1. `GET /homework` response carries `assignments[].classes[].class._count.students` (numeric).
2. `GET /homework/:id` response carries `assignments[].classes[].class.students` as `{ id, name }[]`.
3. The homework list and detail pages each render at least one `* / * submitted` pill per assignment.
4. The detail page renders "Not submitted" list for assignments with at least one enrolled student lacking a session.
5. The existing AssignModal continues to send `classIds[]` to the assignment endpoint (TEACH-02 regression).
</success_criteria>

<output>
After completion, create `.planning/phases/03-teacher-dashboard/03-02-SUMMARY.md` describing: which exact lines were modified in repository.ts, page.tsx, and [id]/page.tsx; any places where the old `totalSessions` display was replaced by the new pill; any defensive paths added for empty student data; and the result of the TEACH-02 manual multi-class assignment probe.
</output>
