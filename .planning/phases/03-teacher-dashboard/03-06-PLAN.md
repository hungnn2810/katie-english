---
phase: 03-teacher-dashboard
plan: 06
type: execute
wave: 6
depends_on: [04, 05]
files_modified:
  - frontend/app/teacher/homework/create/reading/page.tsx
  - frontend/app/teacher/homework/[id]/edit/page.tsx
  - frontend/app/teacher/homework/[id]/try/page.tsx
autonomous: true
requirements:
  - TEACH-01
user_setup: []

must_haves:
  truths:
    - "Teacher clicking Edit on a READING homework card on /teacher/homework lands on /teacher/homework/[id]/edit which renders the same ReadingCreationPage prefilled with the existing name and activities"
    - "Updating a reading homework on the edit page calls PUT /homework/reading/:id, redirects to /teacher/homework, and the DB row reflects the changes (delete-and-recreate of activities is server-side, already implemented in Plan 04)"
    - "Reading creation page (both create and edit modes) renders a Try button (D-05) that navigates to /teacher/homework/[id]/try; in create mode the button is hidden until the homework is saved (or grayed-out)"
    - "/teacher/homework/[id]/try detects READING homework type and renders an interactive reading preview (matching click-to-pair + fill-in-blank word selection); no API call on completion (preview only)"
    - "Try page READING preview displays each activity sequentially using the same data shape returned by getReadingHomework; teacher can click through all activities and see the simulated student experience"
  artifacts:
    - path: "frontend/app/teacher/homework/create/reading/page.tsx"
      provides: "Edit-mode support: a prop or route-derived editId; on mount in edit mode, fetch getReadingHomework(editId) and prefill name + activities (mapping ReadingActivity → ReadingActivityDraft with freshly generated draftIds); Save button calls updateReadingHomework; Try button visible when not loading"
    - path: "frontend/app/teacher/homework/[id]/edit/page.tsx"
      provides: "Route page that reads params.id and renders ReadingCreationPage in edit mode (passes editId); thin wrapper"
    - path: "frontend/app/teacher/homework/[id]/try/page.tsx"
      provides: "READING branch: when hw.type === 'READING', render a ReadingPreview component (inline) that walks through activities — matching: click-image then click-word to pair; fill-in-blank: click distractor chips per blank; final summary shows a calculated preview score, no API save"
  key_links:
    - from: "Edit button on homework list READING items"
      to: "/teacher/homework/[id]/edit"
      via: "router.push (already wired in Plan 03)"
      pattern: "router\\.push\\(\\`/teacher/homework/.*\\/edit\\`\\)"
    - from: "Edit page"
      to: "ReadingCreationPage in edit mode"
      via: "thin wrapper renders the same component"
      pattern: "ReadingCreationPage"
    - from: "Try button on creation/edit page"
      to: "/teacher/homework/[id]/try"
      via: "router.push"
      pattern: "/try"
---

<objective>
Deliver the fifth vertical slice: edit-mode reuse and the Try/Preview functionality (D-04, D-05). After this plan, a teacher can fully manage the lifecycle of a Reading homework — create, edit, preview before assigning. The save-side endpoints already exist from Plan 04; this plan only adds the data-fetch-on-mount, the route shell at /[id]/edit, and the READING branch in the existing /[id]/try page.

Purpose: Without edit-mode, teachers cannot correct mistakes; without Try, they cannot preview what students will see. Both come "for free" once the creation page accepts prefill data and the try page learns the new homework type. This slice is independent of Plan 07 (session results) so they may execute in parallel within Wave 6.

Output: /teacher/homework/[id]/edit renders the prefilled creation page; Save updates the homework via PUT; Try button on creation/edit and on the homework detail page (existing) navigates to a working READING preview on /try.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-teacher-dashboard/03-CONTEXT.md
@.planning/phases/03-teacher-dashboard/03-RESEARCH.md
@.planning/phases/03-teacher-dashboard/03-PATTERNS.md
@.planning/phases/03-teacher-dashboard/03-04-SUMMARY.md
@.planning/phases/03-teacher-dashboard/03-05-SUMMARY.md

<interfaces>
<!-- Line anchors -->

frontend/app/teacher/homework/page.tsx (lines 535-549): existing openEdit prefill pattern (PHONICS/SPEAKING). For READING, the same data-shape mapping happens in the dedicated edit page.

frontend/app/teacher/homework/[id]/try/page.tsx (lines 99-111): existing PHONICS/SPEAKING item-list construction. Add a READING branch.
- Lines 300-394: full-screen game layout pattern (AuthGate wrapper + game background + camera panel) — the READING preview is simpler (no camera) but can use the same outer shell.

frontend/lib/admin-api.ts:
- getReadingHomework(id) declared in Plan 04
- updateReadingHomework(id, data) declared in Plan 04

ReadingCreationPage:
- Plan 03 created the skeleton; Plan 04 wired Save; Plan 05 added FILL_IN_BLANK + DnD. This plan adds the edit-mode prefill path.

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add edit-mode support to ReadingCreationPage (data fetch + prefill + PUT)</name>
  <files>frontend/app/teacher/homework/create/reading/page.tsx</files>
  <read_first>
    - frontend/app/teacher/homework/create/reading/page.tsx (current state after Plan 05)
    - frontend/app/teacher/homework/page.tsx (lines 535-549 openEdit pattern — for shape mapping reference)
    - frontend/lib/admin-api.ts (getReadingHomework, updateReadingHomework, ReadingHomeworkDetail, ReadingActivity, SentenceSegment)
    - .planning/phases/03-teacher-dashboard/03-PATTERNS.md (`frontend/app/teacher/homework/[id]/edit/page.tsx` section — exact same component, prefill discussion)
    - .planning/phases/03-teacher-dashboard/03-CONTEXT.md (D-04, D-05)
  </read_first>
  <action>
    Per D-04 / D-05 / TEACH-01:

    1. Refactor `ReadingCreationPage` to accept an OPTIONAL prop `editId?: number`. The Plan 03 default export is a function with no props; change it so the function takes `{ editId }: { editId?: number }` and pass undefined in the create route.
    2. After the existing state declarations, add an `editMode` flag: `const editMode = typeof editId === 'number';` and an `initialLoading` state initialized to `editMode`.
    3. Add a `useEffect` that runs on mount when `editMode` is true:
       ```
       useEffect(() => {
         if (!editMode || !editId) return;
         (async () => {
           try {
             const hw = await getReadingHomework(editId);
             setName(hw.name ?? '');
             const drafts: ReadingActivityDraft[] = (hw.activities ?? []).map(a => ({
               draftId: crypto.randomUUID(),  // fresh DnD ids
               type: a.type,
               pairs: a.type === 'MATCHING' ? (a.matchingPairs ?? []).map(p => ({ imageUrl: p.imageUrl, word: p.word })) : undefined,
               segments: a.type === 'FILL_IN_BLANK'
                 ? (a.fillInBlank?.sentenceSegments ?? []) as SentenceSegment[]  // server returns JSON in canonical D-12 shape
                 : undefined,
             }));
             setActivities(drafts);
           } catch (err: unknown) {
             setError(err instanceof Error ? err.message : 'Failed to load homework.');
           } finally {
             setInitialLoading(false);
           }
         })();
       }, [editId, editMode]);
       ```
    4. While `initialLoading`, render a small "Loading…" placeholder INSTEAD of the form (so a flicker of empty fields does not happen).
    5. In `handleSubmit`, branch on `editMode`:
       ```
       if (editMode && editId) {
         await updateReadingHomework(editId, payload);
       } else {
         await createReadingHomework(payload);
       }
       router.push('/teacher/homework');
       ```
    6. Update the page heading to reflect mode: `<h1>{editMode ? 'Edit Reading Homework' : 'Create Reading Homework'}</h1>`.
    7. Update the Save button label: `{loading ? 'Saving…' : editMode ? 'Update' : 'Create'}`.
    8. Add a Try button (D-05) in the header area, visible when `editMode && editId`:
       ```
       {editMode && editId && (
         <button type="button" onClick={() => router.push(`/teacher/homework/${editId}/try`)} className="flex items-center gap-1.5 px-4 py-2 rounded-xl text-sm font-semibold text-white" style={{ background: gradients.primaryPurple }}>
           <span aria-hidden>👁️</span> Try
         </button>
       )}
       ```
       In CREATE mode the Try button is hidden (no DB row exists to preview yet). Document this UX in the SUMMARY.

    Note: The matching pair `imageUrl` returned from the backend is the full URL (it was stored as such because uploadSpeakingImage already returns a full URL — see admin-api.ts line 167 in the patterns map). Edit mode does NOT need to re-upload images; the URLs remain valid.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npx next lint --dir app/teacher/homework/create --max-warnings 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "editId" frontend/app/teacher/homework/create/reading/page.tsx` returns at least 4 (prop + editMode + useEffect dep + handleSubmit branch).
    - `grep -c "getReadingHomework" frontend/app/teacher/homework/create/reading/page.tsx` returns at least 1.
    - `grep -c "updateReadingHomework" frontend/app/teacher/homework/create/reading/page.tsx` returns at least 1.
    - `grep -c "editMode \\? 'Update' : 'Create'" frontend/app/teacher/homework/create/reading/page.tsx` returns 1.
    - `grep -c "Edit Reading Homework" frontend/app/teacher/homework/create/reading/page.tsx` returns 1.
    - `grep -c "👁️" frontend/app/teacher/homework/create/reading/page.tsx` returns 1 (Try button).
    - `cd frontend && npx tsc --noEmit` exits 0.
    - Manual probe: Plan 04 created a homework with id X. Navigating to /teacher/homework/X/edit (when Task 2 ships) renders the form prefilled with that homework's name and activities. Clicking Update saves and redirects.
  </acceptance_criteria>
  <done>ReadingCreationPage supports both create and edit modes; prefill works; Try button visible in edit mode; tsc/lint clean.</done>
</task>

<task type="auto">
  <name>Task 2: Create thin /teacher/homework/[id]/edit/page.tsx route that renders ReadingCreationPage in edit mode</name>
  <files>frontend/app/teacher/homework/[id]/edit/page.tsx</files>
  <read_first>
    - frontend/app/teacher/homework/create/reading/page.tsx (after Task 1 refactor)
    - frontend/app/teacher/homework/[id]/page.tsx (for params reading pattern reference — confirms `useParams()` style used elsewhere)
    - .planning/phases/03-teacher-dashboard/03-PATTERNS.md (`frontend/app/teacher/homework/[id]/edit/page.tsx` section)
    - .planning/phases/03-teacher-dashboard/03-CONTEXT.md (D-04)
  </read_first>
  <action>
    Create a new file `frontend/app/teacher/homework/[id]/edit/page.tsx` as a client component that imports ReadingCreationPage from the create route and renders it in edit mode.

    Required surface:

    1. `'use client'` directive.
    2. Import the default export from the create route. Since Next.js does not allow importing a default-exported route component cleanly across route boundaries via a relative path that crosses the `/[id]/` segment, the cleanest approach is to EXTRACT the `ReadingCreationPage` component into a shared module:
       - Create `frontend/app/teacher/homework/_components/ReadingCreationPage.tsx` and move the component body there as a named export `export function ReadingCreationPage({ editId }: { editId?: number }) { ... }`.
       - In `frontend/app/teacher/homework/create/reading/page.tsx`, replace the inline body with: `'use client'; import { ReadingCreationPage } from '../../_components/ReadingCreationPage'; export default function Page() { return <ReadingCreationPage />; }`.
       - In the new edit route, write: `'use client'; import { useParams } from 'next/navigation'; import { ReadingCreationPage } from '../../_components/ReadingCreationPage'; export default function Page() { const params = useParams<{ id: string }>(); const editId = Number(params.id); if (!Number.isFinite(editId)) return null; return <ReadingCreationPage editId={editId} />; }`.
       - The `_components` underscore-prefixed folder name follows the Next.js App Router convention for non-route folders that share code (verified via Next.js docs convention; folders starting with `_` are skipped by the router).
    3. Confirm the extracted ReadingCreationPage still works as a client component: it must retain the `'use client'` directive at the top of the extracted file (or be sure the importer is a client component and the extracted file does not contain server-only imports).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npx next lint --dir app/teacher/homework --max-warnings 0</automated>
  </verify>
  <acceptance_criteria>
    - `test -f frontend/app/teacher/homework/[id]/edit/page.tsx`.
    - `test -f frontend/app/teacher/homework/_components/ReadingCreationPage.tsx`.
    - `grep -c "export function ReadingCreationPage\\|export default function ReadingCreationPage" frontend/app/teacher/homework/_components/ReadingCreationPage.tsx` returns 1.
    - `grep -c "ReadingCreationPage" frontend/app/teacher/homework/\\[id\\]/edit/page.tsx` returns at least 1.
    - `grep -c "editId={editId}\\|editId=" frontend/app/teacher/homework/\\[id\\]/edit/page.tsx` returns at least 1.
    - `cd frontend && npx tsc --noEmit` exits 0.
    - Manual probe: clicking Edit on a READING homework card on /teacher/homework navigates to /teacher/homework/[id]/edit and renders the prefilled form within ~1 second.
  </acceptance_criteria>
  <done>Edit route exists, mounts ReadingCreationPage in edit mode, no route-boundary import issues; tsc/lint clean.</done>
</task>

<task type="auto">
  <name>Task 3: Add READING branch to /teacher/homework/[id]/try/page.tsx (interactive client-side preview, no API save)</name>
  <files>frontend/app/teacher/homework/[id]/try/page.tsx</files>
  <read_first>
    - frontend/app/teacher/homework/[id]/try/page.tsx (full file — lines 99-111 for item-list construction pattern, lines 300-394 for game shell layout, lines 8-9 for state machine)
    - frontend/lib/admin-api.ts (ReadingHomeworkDetail, ReadingActivity, SentenceSegment, ReadingMatchingPair, FillInBlankBlank, getReadingHomework)
    - .planning/phases/03-teacher-dashboard/03-PATTERNS.md (`frontend/app/teacher/homework/[id]/try/page.tsx` section)
    - .planning/phases/03-teacher-dashboard/03-CONTEXT.md (D-05)
    - .planning/phases/03-teacher-dashboard/03-RESEARCH.md (Open Question 1 — Reading Try recommended: fully interactive client-side, no API call)
  </read_first>
  <action>
    Per D-05 / TEACH-01 / Open Question 1's recommended interpretation:

    1. Near the top of the file where homework is loaded (currently around lines 99-111), branch on homework type EARLY. If `hw.type === 'READING'`, fetch reading detail via `getReadingHomework(hw.id)` (or `getReadingHomework(homeworkId)` depending on the existing variable naming) to get the full activity/pair/blank data — the existing `getHomework` does not include reading_activities, but `getReadingHomework` from Plan 04 does.
    2. Render a NEW preview component `ReadingPreview` (declared inline in the same file, in the spirit of HomeworkModal being inline in page.tsx):
       - Props: `{ homework: ReadingHomeworkDetail }`.
       - Local state: `const [activityIdx, setActivityIdx] = useState(0);` and per-activity progress state (matching: a map pairId → studentChosenWord, plus highlighted-correct/wrong; fill-in-blank: a map blankIndex → chosen word).
       - For MATCHING activities: render the activity's `matchingPairs` as two columns — left column lists images (clickable, shows selected ring), right column lists words (the union of all `pair.word` values, shuffled with `useMemo`). Click image → store activeImage; click word → record the pair, mark correct if `word === pair.word` (animate green) else mark wrong (shake animation). When all pairs answered, show "Activity complete" with `pairsCorrect / pairsTotal` score and a Next button.
       - For FILL_IN_BLANK activities: render the sentence by mapping segments — non-blank segments rendered as plain text spans; blank segments rendered as inline dropdown-like chips showing either "___" or the student-chosen word. Below the sentence, render the choice palette: for each blank not yet answered, show `[correctWord, ...distractors].sort(deterministicShuffleByBlankIndex)` as clickable chips. Clicking a chip fills the active blank. When all blanks are filled, compute score = `blanksCorrect / blanksTotal` and show Next.
       - After the final activity, show an overall preview-only summary card showing average score across activities (per D-18 formula). Include a "This was a preview — no result was saved" notice and a Done button returning to /teacher/homework/[id].
    3. Use the existing full-screen game shell (lines 300-394 pattern) without the camera panel — the reading preview does not need the camera. Wrap content in the existing `AuthGate requiredRole="TEACHER"` and the gameBgAlt background. The progress bar chips at the top (lines 307-313) can be reused to show "Activity 1/N".
    4. The state machine constant union (lines 8-9) currently looks like `'loading' | 'cam-check' | ... | 'results'`. Add a `'reading-preview'` variant if the existing machine cannot accommodate the new branch cleanly — but inspect first; a parallel `if (hw.type === 'READING') { return <ReadingPreview homework={readingHw} />; }` branch placed BEFORE the existing PHONICS/SPEAKING render path may avoid touching the state machine entirely. Prefer this minimal approach.
    5. Per D-05: this preview is "scored, not saved to DB" — no API call to /game/session/* should be made for READING in this branch. Add an inline comment documenting this.

    Implementation hint: the deterministic shuffle for distractor presentation order should be seeded by `blank.blankIndex` so the order is stable across re-renders within the same session (avoids React key churn).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npx next lint --dir app/teacher/homework --max-warnings 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "getReadingHomework" frontend/app/teacher/homework/\\[id\\]/try/page.tsx` returns at least 1.
    - `grep -c "ReadingPreview\\|hw.type === 'READING'\\|homework.type === 'READING'" frontend/app/teacher/homework/\\[id\\]/try/page.tsx` returns at least 2.
    - `grep -c "preview" frontend/app/teacher/homework/\\[id\\]/try/page.tsx` returns at least 1 (the no-save notice).
    - `cd frontend && npx tsc --noEmit` exits 0.
    - Manual probe: open a READING homework, click Try on the detail page → preview screen appears, walk through one matching + one fill-in-blank activity → final summary appears with a numeric score, Done button returns to /teacher/homework/[id].
    - No network calls to /game/session/* during the preview (verifiable via DevTools network tab; non-automated, validated at phase verification).
  </acceptance_criteria>
  <done>Try page handles READING type with a fully interactive client-side preview; no DB writes; tsc/lint clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Teacher browser → PUT /homework/reading/:id | Existing AuthGuard inherited |
| Try preview rendering teacher-authored JSON | Same trust boundary as the editor — no escalation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-21 | Tampering | PUT /homework/reading/:id with malicious body | mitigate | Same service-level validation as create (from Plan 04) — name required, ≥2 pairs per MATCHING, blankIndex contiguous, ≥1 distractor per blank, array length caps. Acceptance: Plan 04's grep on `BadRequestException` remains valid. |
| T-03-22 | Information Disclosure | Edit route exposes another teacher's homework id via URL guessing | accept | Single-teacher per PROJECT.md "Constraints"; no cross-tenant data. |
| T-03-23 | Cross-Site Scripting | Try preview renders teacher-authored `segment.text` and `blank.correctWord`/`distractors` | mitigate | Rendered exclusively as React text nodes (`{seg.text}`, `{w}`) — never via dangerouslySetInnerHTML. Acceptance: `grep -c "dangerouslySetInnerHTML" frontend/app/teacher/homework/\\[id\\]/try/page.tsx` returns 0 (or no greater than current count). |
| T-03-24 | Spoofing | Anonymous user navigates to /[id]/edit | mitigate | TeacherShell layout already wraps `/teacher/*` with auth; route inherits. |
</threat_model>

<verification>
- Edit route prefills correctly and PUT round-trips.
- Try preview navigates through all activities and shows a final summary without any /game/session/* network calls.
- No dangerouslySetInnerHTML usage introduced.
- All TS/lint checks clean.
</verification>

<success_criteria>
1. Click Edit on a READING card → /teacher/homework/[id]/edit renders the prefilled form; Update saves the change; redirect to /teacher/homework.
2. Click Try on the homework detail page for a READING homework → fully interactive preview; final score shown; Done returns to detail.
3. tsc, lint clean across the three modified files plus the new `_components/ReadingCreationPage.tsx` extraction.
4. No network calls to /game/session/* during the READING try preview.
</success_criteria>

<output>
After completion, create `.planning/phases/03-teacher-dashboard/03-06-SUMMARY.md` describing: the path of the extracted ReadingCreationPage component, how editMode is detected, and the inline ReadingPreview component's shape (which game-shell elements it reuses vs omits).
</output>
