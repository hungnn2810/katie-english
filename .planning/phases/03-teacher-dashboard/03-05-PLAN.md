---
phase: 03-teacher-dashboard
plan: 05
type: execute
wave: 5
depends_on: [04]
files_modified:
  - frontend/package.json
  - frontend/app/teacher/homework/create/reading/page.tsx
autonomous: true
requirements:
  - TEACH-01
user_setup: []

must_haves:
  truths:
    - "@dnd-kit/core, @dnd-kit/sortable, @dnd-kit/utilities are installed in frontend/package.json"
    - "Teacher can add a FILL_IN_BLANK activity, type a sentence into the textarea, see it tokenized into word chips, click any word chip to toggle it between normal and blank state, and per-blank distractor input fields appear"
    - "Toggling a word to blank inserts a sequentially-numbered blank into the activity's segments; un-toggling re-indexes remaining blanks 0..n-1 (Pitfall 3)"
    - "Teacher can drag-and-drop activity cards to reorder them; the order is preserved in local state via @dnd-kit/sortable arrayMove and persisted as order index on save"
    - "Saving a Reading homework with mixed MATCHING + FILL_IN_BLANK activities round-trips through the existing /homework/reading endpoint and lands in the database with both activity types and contiguous blankIndex sequences"
  artifacts:
    - path: "frontend/package.json"
      provides: "@dnd-kit/core ^6.3.1, @dnd-kit/sortable ^10.0.0, @dnd-kit/utilities ^3.2.2 dependencies"
    - path: "frontend/app/teacher/homework/create/reading/page.tsx"
      provides: "FillInBlankActivityEditor inline subcomponent (textarea + tokenized chip toggle + per-blank distractor input row); SortableActivityCard wrapper using useSortable; DndContext + SortableContext at the activity-list level; tokenize/toggleBlank/reindexBlanks helpers"
  key_links:
    - from: "FillInBlankActivityEditor word chips"
      to: "activity.segments array"
      via: "toggleBlank helper with reindexBlanks pass"
      pattern: "blankIndex"
    - from: "Activity card drag handle"
      to: "activities array order"
      via: "@dnd-kit/sortable arrayMove in handleDragEnd"
      pattern: "arrayMove"
---

<objective>
Deliver the fourth vertical slice: full Reading authoring with FILL_IN_BLANK activities and drag-and-drop activity reordering. After this plan, a teacher can compose any sequence of MATCHING and FILL_IN_BLANK activities, type a sentence and click individual words to convert them into blanks with manual distractors (D-07, D-08), reorder activities by drag-and-drop (D-09), and save the entire homework end-to-end.

Purpose: This is the heaviest UI slice in the phase. The save endpoint already exists from Plan 04; this plan only adds the FILL_IN_BLANK editor and the DnD layer on the same creation page.

Output: The /teacher/homework/create/reading page supports the full D-06 through D-12 authoring contract; saved homeworks pass the existing server-side validation (min blanks, contiguous blankIndex); the @dnd-kit dependency is installed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-teacher-dashboard/03-CONTEXT.md
@.planning/phases/03-teacher-dashboard/03-RESEARCH.md
@.planning/phases/03-teacher-dashboard/03-PATTERNS.md
@.planning/phases/03-teacher-dashboard/03-04-SUMMARY.md

<interfaces>
<!-- Pattern references -->

RESEARCH.md "Pattern 2: @dnd-kit/sortable Vertical List" — full canonical snippet for DndContext + SortableContext + useSortable + arrayMove + handleDragEnd. The executor copies this pattern directly with one change: id is `activity.draftId` (stable string), NOT array index (Pitfall 2).

RESEARCH.md "Pattern 3: Fill-in-Blank Sentence Tokenizer" — `match(/\S+|\s+/g)` split + chip toggle + reindexBlanks helper.

CONTEXT.md "Claude's Discretion" — already-blank words show as chip with × remove button; word clicks toggle blank state; whitespace tokens skipped from toggle.

03-04-SUMMARY.md — ReadingActivityDraft shape and segment shape as used by save logic.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install @dnd-kit packages and verify version pinning</name>
  <files>frontend/package.json, frontend/package-lock.json (generated)</files>
  <read_first>
    - frontend/package.json (entire file — particularly the dependencies section)
    - .planning/phases/03-teacher-dashboard/03-RESEARCH.md (section "Standard Stack" — verified versions 6.3.1 / 10.0.0 / 3.2.2)
  </read_first>
  <action>
    Per D-09 and RESEARCH.md "Environment Availability":

    1. From the project root, run `cd frontend && npm install @dnd-kit/core@^6.3.1 @dnd-kit/sortable@^10.0.0 @dnd-kit/utilities@^3.2.2`. This adds all three packages and updates package-lock.json.
    2. Verify the three entries land in `dependencies` (not devDependencies) in `frontend/package.json`.
    3. Do NOT add `--legacy-peer-deps` — `@dnd-kit/sortable@10.0.0` peer-requires `@dnd-kit/core ^6.3.0` which 6.3.1 satisfies (RESEARCH.md verified).
    4. After install completes, run `cd frontend && npx tsc --noEmit` to confirm types are resolvable.
  </action>
  <verify>
    <automated>cd frontend && node -e "const p=require('./package.json'); const d=p.dependencies||{}; if(!d['@dnd-kit/core']||!d['@dnd-kit/sortable']||!d['@dnd-kit/utilities']){console.error('MISSING');process.exit(1)} console.log('OK',d['@dnd-kit/core'],d['@dnd-kit/sortable'],d['@dnd-kit/utilities'])"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "\"@dnd-kit/core\":" frontend/package.json` returns 1.
    - `grep -c "\"@dnd-kit/sortable\":" frontend/package.json` returns 1.
    - `grep -c "\"@dnd-kit/utilities\":" frontend/package.json` returns 1.
    - The installed version of `@dnd-kit/core` resolves to `>=6.3.0 <7` (verified by reading the version string in package.json after install).
    - `cd frontend && npx tsc --noEmit` exits 0.
    - `cd frontend && node -e "require('@dnd-kit/core'); require('@dnd-kit/sortable'); require('@dnd-kit/utilities'); console.log('OK')"` exits 0 (runtime resolution works).
  </acceptance_criteria>
  <done>Three @dnd-kit packages installed with the verified versions; tsc clean; runtime require resolves.</done>
</task>

<task type="auto">
  <name>Task 2: Implement FillInBlankActivityEditor with sentence tokenizer, chip toggle, distractor inputs, and reindexBlanks safety</name>
  <files>frontend/app/teacher/homework/create/reading/page.tsx</files>
  <read_first>
    - frontend/app/teacher/homework/create/reading/page.tsx (current state from Plan 04 — note the placeholder block for activity.type === 'FILL_IN_BLANK')
    - frontend/lib/admin-api.ts (SentenceSegment interface from Plan 01 — canonical D-12 shape)
    - .planning/phases/03-teacher-dashboard/03-RESEARCH.md ("Pattern 3" tokenizer; "Pitfall 3" blank-index drift)
    - .planning/phases/03-teacher-dashboard/03-CONTEXT.md (D-07, D-08, D-12; "Claude's Discretion": chip × remove)
    - .planning/phases/03-teacher-dashboard/03-PATTERNS.md (`frontend/app/teacher/homework/create/reading/page.tsx` section)
  </read_first>
  <action>
    Per D-07 / D-08 / D-12, replace the FILL_IN_BLANK placeholder block in the creation page with a real editor.

    1. Add three pure helper functions at module scope (not inside the component — they have no dependencies on React state):
       ```
       function tokenizeSentence(sentence: string): SentenceSegment[] {
         const tokens = sentence.match(/\S+|\s+/g) ?? [];
         return tokens.map(text => ({ text, blank: false }));
       }
       function reindexBlanks(segments: SentenceSegment[]): SentenceSegment[] {
         let idx = 0;
         return segments.map(s => s.blank ? { ...s, blankIndex: idx++ } : s);
       }
       function toggleBlankAt(segments: SentenceSegment[], i: number): SentenceSegment[] {
         const seg = segments[i];
         if (!seg || seg.text.trim() === '') return segments; // skip whitespace tokens
         const next = segments.map((s, j) => {
           if (j !== i) return s;
           if (s.blank) return { text: s.text, blank: false };
           return { text: s.text, blank: true, correctWord: s.text, distractors: [] };
         });
         return reindexBlanks(next);
       }
       ```
       Note: when un-blanking, drop `blankIndex`, `correctWord`, `distractors` (they will be reassigned only if/when the word is re-blanked); we accomplish this by replacing the whole segment with the minimal `{ text, blank: false }` object.
    2. Add per-activity helpers inside the component (using setActivities):
       - `setSentence(actId, sentence)` → recompute segments via `tokenizeSentence(sentence)`. This DISCARDS existing blanks if the teacher edits the sentence text; document this in the editor UI with a subtle warning when blanks exist and the textarea changes (acceptable UX cost — the alternative requires diffing tokens which is out of scope).
       - `toggleSegmentBlank(actId, segIdx)` → apply `toggleBlankAt` to that activity's segments.
       - `updateDistractors(actId, blankIndex, distractors: string[])` → find segment where `segments[i].blank && segments[i].blankIndex === blankIndex` and set `distractors`.
    3. Replace the FILL_IN_BLANK card body in the activity-list map with:
       - A header row matching the MATCHING editor style: `<span>Fill in the Blank</span>` + Remove activity button (reuses `removeActivity` from Plan 04).
       - A `<textarea>` for sentence entry: `value={(activity.segments ?? []).map(s => s.text).join('')}`, `onChange={(e) => setSentence(activity.draftId, e.target.value)}`. Reconstructing the value from segments ensures the textarea and chip view stay in sync (whitespace preserved by the tokenizer).
       - Below the textarea: a chip row. For each segment `s` (skipping pure-whitespace tokens visually but preserving them in the array):
         - If `s.text.trim() === ''`: render a small spacer (e.g. one non-breaking space) without an onClick.
         - Else if `s.blank`: chip styled `bg-primary text-white px-2.5 py-1 rounded-lg text-sm font-semibold` with the text replaced by `___` and a `×` button that calls `toggleSegmentBlank(activity.draftId, i)`.
         - Else: chip styled `bg-gray-100 text-textPrimary px-2.5 py-1 rounded-lg text-sm hover:bg-primary/10 cursor-pointer` and onClick `toggleSegmentBlank(activity.draftId, i)`.
       - Below the chip row: distractor inputs — one row per blank (iterate `activity.segments?.filter(s => s.blank).sort((a,b) => a.blankIndex! - b.blankIndex!)`). Each row: `<span className="text-xs font-mono">Blank {b.blankIndex! + 1}: "{b.correctWord}"</span>` + an input `<input value={(b.distractors ?? []).join(', ')} onChange={(e) => updateDistractors(activity.draftId, b.blankIndex!, e.target.value.split(',').map(x => x.trim()).filter(Boolean))} placeholder="Comma-separated distractors (e.g. dog, bird)" className="flex-1 px-3 py-1.5 rounded-lg border border-border text-sm" />`. D-08 mandates per-blank distractor input.
    4. Update the save-time validation in `handleSubmit` (from Plan 04) to add the FILL_IN_BLANK branch:
       ```
       if (act.type === 'FILL_IN_BLANK') {
         const segs = act.segments ?? [];
         if (!segs.length || !segs.some(s => s.blank)) {
           setError('Each fill-in-blank activity needs at least one blank.'); return;
         }
         // Verify contiguous blankIndex sequence 0..n-1 (Pitfall 3 safety on the client side)
         const blanks = segs.filter(s => s.blank).sort((a,b) => a.blankIndex! - b.blankIndex!);
         for (let i = 0; i < blanks.length; i++) {
           if (blanks[i].blankIndex !== i) {
             setError('Internal blank index out of sequence — try toggling one blank again.'); return;
           }
         }
         // distractors per blank: at least 1 distractor recommended; require >= 1 (D-08 minimum)
         if (blanks.some(b => !(b.distractors?.length ?? 0))) {
           setError('Each blank needs at least one distractor.'); return;
         }
       }
       ```
    5. In the save payload, the FILL_IN_BLANK activity already serializes correctly because `act.segments` carries the canonical D-12 shape (text, blank, blankIndex, correctWord, distractors).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npx next lint --dir app/teacher/homework/create --max-warnings 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "tokenizeSentence" frontend/app/teacher/homework/create/reading/page.tsx` returns at least 2 (declaration + usage).
    - `grep -c "reindexBlanks" frontend/app/teacher/homework/create/reading/page.tsx` returns at least 2.
    - `grep -c "toggleBlankAt\\|toggleSegmentBlank" frontend/app/teacher/homework/create/reading/page.tsx` returns at least 2.
    - `grep -c "updateDistractors" frontend/app/teacher/homework/create/reading/page.tsx` returns at least 2.
    - `grep -c "Fill in the Blank" frontend/app/teacher/homework/create/reading/page.tsx` returns 1.
    - `grep -c "blankIndex" frontend/app/teacher/homework/create/reading/page.tsx` returns at least 3.
    - `cd frontend && npx tsc --noEmit` exits 0.
    - Manual probe: add a Fill-in-Blank activity, type "The cat sits on the mat", click on "cat" → chip shows ___, distractor input appears labeled `Blank 1: "cat"`; click "mat" → second chip appears, second distractor input shows `Blank 2: "mat"`; click "cat" again → only `Blank 1: "mat"` remains (re-indexed).
    - Save a homework with one matching activity (≥2 pairs) and one fill-in-blank activity (≥1 blank with distractors) → backend returns HTTP 201 and DB row is created.
  </acceptance_criteria>
  <done>Fill-in-blank editor functional with contiguous blank indices; chip toggle works in both directions; distractor inputs persist; saved end-to-end with mixed activities.</done>
</task>

<task type="auto">
  <name>Task 3: Wrap activity list in DndContext + SortableContext; convert activity cards to SortableActivityCard with drag handle</name>
  <files>frontend/app/teacher/homework/create/reading/page.tsx</files>
  <read_first>
    - frontend/app/teacher/homework/create/reading/page.tsx (current state after Task 2)
    - .planning/phases/03-teacher-dashboard/03-RESEARCH.md ("Pattern 2: @dnd-kit/sortable Vertical List" canonical snippet; "Pitfall 2" stable id requirement)
    - .planning/phases/03-teacher-dashboard/03-CONTEXT.md (D-09)
    - .planning/phases/03-teacher-dashboard/03-PATTERNS.md (`frontend/app/teacher/homework/create/reading/page.tsx` section — DnD activity list snippet)
  </read_first>
  <action>
    Per D-09 / TEACH-01, integrate @dnd-kit/sortable into the activity list.

    1. Imports at the top of the file:
       ```
       import { DndContext, closestCenter, PointerSensor, KeyboardSensor, useSensor, useSensors, DragEndEvent } from '@dnd-kit/core';
       import { arrayMove, SortableContext, sortableKeyboardCoordinates, verticalListSortingStrategy, useSortable } from '@dnd-kit/sortable';
       import { CSS } from '@dnd-kit/utilities';
       ```
    2. Wrap the activity-list `<div className="space-y-3">` in `<DndContext sensors={sensors} collisionDetection={closestCenter} onDragEnd={handleDragEnd}><SortableContext items={activities.map(a => a.draftId)} strategy={verticalListSortingStrategy}>...</SortableContext></DndContext>`.
    3. Define sensors inside the component:
       ```
       const sensors = useSensors(
         useSensor(PointerSensor, { activationConstraint: { distance: 4 } }),
         useSensor(KeyboardSensor, { coordinateGetter: sortableKeyboardCoordinates }),
       );
       ```
       The `distance: 4` activation constraint prevents accidental drags on simple clicks (important since the activity card contains many click-targets like word chips and × buttons).
    4. Define `handleDragEnd`:
       ```
       function handleDragEnd(event: DragEndEvent) {
         const { active, over } = event;
         if (over && active.id !== over.id) {
           setActivities(prev => {
             const oldIdx = prev.findIndex(a => a.draftId === active.id);
             const newIdx = prev.findIndex(a => a.draftId === over.id);
             if (oldIdx < 0 || newIdx < 0) return prev;
             return arrayMove(prev, oldIdx, newIdx);
           });
         }
       }
       ```
    5. Refactor the activity card into a SortableActivityCard inline subcomponent that takes the activity, the editor JSX (matching or fill-in-blank), and the removeActivity callback as props. Internals:
       ```
       function SortableActivityCard({ activity, children, onRemove }: { activity: ReadingActivityDraft; children: React.ReactNode; onRemove: () => void; }) {
         const { attributes, listeners, setNodeRef, transform, transition, isDragging } = useSortable({ id: activity.draftId });
         const style: React.CSSProperties = {
           transform: CSS.Transform.toString(transform),
           transition,
           opacity: isDragging ? 0.6 : 1,
         };
         return (
           <div ref={setNodeRef} style={style} {...attributes} className="bg-white border border-border rounded-2xl p-4 shadow-sm">
             <div className="flex items-center gap-3 mb-2">
               <button {...listeners} type="button" className="cursor-grab active:cursor-grabbing text-textSecondary hover:text-textPrimary px-2 py-1" aria-label="Drag to reorder">≡</button>
               <span className="flex-1 text-xs font-semibold uppercase tracking-wide text-textSecondary">
                 {activity.type === 'MATCHING' ? 'Matching' : 'Fill in the Blank'}
               </span>
               <button type="button" onClick={onRemove} className="text-xs text-textSecondary hover:text-highlight">Remove</button>
             </div>
             {children}
           </div>
         );
       }
       ```
    6. Replace the activity map in the page body so each activity is rendered as `<SortableActivityCard key={a.draftId} activity={a} onRemove={() => removeActivity(a.draftId)}>{a.type === 'MATCHING' ? <MatchingEditorBody a={a} /> : <FillInBlankEditorBody a={a} />}</SortableActivityCard>`. (Inline the editor body or extract small subcomponents — both acceptable; whichever keeps the file readable.)
    7. The `id` passed to `useSortable` MUST be `activity.draftId` (a stable string) — never the array index (Pitfall 2). This is already what the Plan 03 skeleton generates via `crypto.randomUUID()`.
    8. On save, the local activity array's order IS the persisted order (handleSubmit already maps activities in array order to `payload.activities`, and the backend assigns `order: idx` per the buildReadingActivitiesCreate helper from Plan 04). No additional persistence logic needed.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npx next lint --dir app/teacher/homework/create --max-warnings 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "DndContext" frontend/app/teacher/homework/create/reading/page.tsx` returns at least 2 (import + JSX).
    - `grep -c "SortableContext" frontend/app/teacher/homework/create/reading/page.tsx` returns at least 2.
    - `grep -c "useSortable" frontend/app/teacher/homework/create/reading/page.tsx` returns at least 2.
    - `grep -c "arrayMove" frontend/app/teacher/homework/create/reading/page.tsx` returns at least 1.
    - `grep -c "activity.draftId" frontend/app/teacher/homework/create/reading/page.tsx` returns at least 2 (sortable id + items mapping).
    - `cd frontend && npx tsc --noEmit` exits 0.
    - Manual probe: add three activities, drag the second one to the top → save → reload `/teacher/homework/[id]/edit` (when Plan 06 ships) — for now confirm via DB inspection that `reading_activities.order` reflects the dragged order.
  </acceptance_criteria>
  <done>Activity cards are draggable; order persisted via array index on save; no DnD id collisions; tsc/lint clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Teacher browser → existing POST /homework/reading | No new endpoints; same trust boundary as Plan 04 |
| @dnd-kit npm packages → frontend bundle | Supply-chain trust extends to verified versions documented in RESEARCH.md |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-17 | Tampering | Client-side blankIndex sequencing in segments | mitigate | Both client (handleSubmit validation) and server (homework.service createReadingHomework from Plan 04) verify contiguous blankIndex 0..n-1. Defense in depth — same property checked twice. |
| T-03-18 | Tampering | Drag reorder mutates only client state; server only sees final array order | accept | Server assigns `order: idx` deterministically; teacher cannot persist a partial state. |
| T-03-19 | Supply-chain | New transitive deps from @dnd-kit | mitigate | All three packages pinned to RESEARCH.md verified versions (^6.3.1 / ^10.0.0 / ^3.2.2). No `--legacy-peer-deps`. Lockfile updated. |
| T-03-20 | Information Disclosure | `sentenceSegments` and `distractors` are user-authored text stored as JSON; rendered as text on teacher and student sides | accept | Teacher-authored content; teachers are the only authors (single-teacher per PROJECT.md). When the student game UI is built (deferred — Phase 2), it must render this text via React text nodes (not dangerouslySetInnerHTML). This plan only writes the data, not renders it on student side. |
</threat_model>

<verification>
- @dnd-kit packages installed and resolve at runtime.
- Fill-in-blank editor maintains contiguous blankIndex through arbitrary toggle/un-toggle sequences.
- DnD reorder persists across save → DB `reading_activities.order` reflects the dragged order.
- Mixed homework (≥1 matching + ≥1 fill-in-blank) saves end-to-end with no server-side 400.
- tsc and lint clean.
</verification>

<success_criteria>
1. Add a matching activity with 2 pairs + a fill-in-blank activity with a 4-word sentence and 2 blanks (each with 1 distractor); save → HTTP 201; DB shows both activity types, correctly ordered.
2. Toggle the same word three times: blank → normal → blank — final blankIndex sequence remains contiguous.
3. Drag a fill-in-blank activity above a matching activity → save → DB ordering reflects new sequence.
4. tsc/lint clean across all touched files.
</success_criteria>

<output>
After completion, create `.planning/phases/03-teacher-dashboard/03-05-SUMMARY.md` describing: the helper functions added (tokenizeSentence / reindexBlanks / toggleBlankAt), how SortableActivityCard is structured (props), and any DnD-specific UX choices (sensor activation distance, etc.).
</output>
