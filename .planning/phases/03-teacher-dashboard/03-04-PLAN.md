---
phase: 03-teacher-dashboard
plan: 04
type: execute
wave: 4
depends_on: [01, 03]
files_modified:
  - backend/src/homework/homework.repository.ts
  - backend/src/homework/homework.service.ts
  - frontend/lib/admin-api.ts
  - frontend/app/teacher/homework/create/reading/page.tsx
autonomous: true
requirements:
  - TEACH-01
  - READ-07
user_setup: []

must_haves:
  truths:
    - "Teacher on /teacher/homework/create/reading can click + Add Matching, fill in at least 2 image-word pairs, name the homework, click Save, and the homework persists to the database with HomeworkType=READING"
    - "After save, teacher is redirected to /teacher/homework (no AssignModal auto-open, per D-03)"
    - "Matching activity images upload via the existing /homework/image endpoint (reuse uploadSpeakingImage; no new upload route)"
    - "Backend createReadingHomework persists Homework + ReadingActivity + MatchingPair rows in a single Prisma nested create"
    - "Backend findReadingById returns the full reading homework with activities, matchingPairs, and (later) fillInBlank.blanks for the edit page to consume"
    - "Frontend createReadingHomework / getReadingHomework / updateReadingHomework API client functions exist in admin-api.ts and are wired to /homework/reading endpoints"
  artifacts:
    - path: "backend/src/homework/homework.repository.ts"
      provides: "buildReadingActivitiesCreate helper; createReadingHomework / findReadingById / updateReadingHomework with real Prisma create + nested include (readingActivitiesInclude); reading delete-and-recreate update pattern"
    - path: "backend/src/homework/homework.service.ts"
      provides: "Validation: name required, min 2 pairs per MATCHING activity, blankIndex contiguous per FILL_IN_BLANK activity"
    - path: "frontend/lib/admin-api.ts"
      provides: "createReadingHomework, getReadingHomework, updateReadingHomework, CreateReadingHomeworkInput, UpdateReadingHomeworkInput types"
    - path: "frontend/app/teacher/homework/create/reading/page.tsx"
      provides: "MatchingActivityEditor inline subcomponent rendering inside the activity card; + Add pair, image upload via uploadSpeakingImage, per-pair word input, × remove pair; real handleSubmit calls createReadingHomework and redirects to /teacher/homework"
  key_links:
    - from: "MatchingActivityEditor + Add pair button"
      to: "uploadSpeakingImage (POST /homework/image)"
      via: "uploadPairImage helper"
      pattern: "uploadSpeakingImage\\("
    - from: "handleSubmit on creation page"
      to: "createReadingHomework -> POST /homework/reading"
      via: "admin-api req<>"
      pattern: "createReadingHomework\\("
    - from: "POST /homework/reading body"
      to: "Prisma nested create"
      via: "buildReadingActivitiesCreate"
      pattern: "buildReadingActivitiesCreate"
---

<objective>
Deliver the third vertical slice: matching-activity authoring end-to-end. Replace the stub backend handlers with real Prisma nested creates, ship the MatchingActivityEditor inline editor with image upload via the existing /homework/image endpoint, wire the Save button on the creation page to actually call POST /homework/reading and redirect to /teacher/homework. After this plan, a teacher can create and save a MATCHING-only reading homework end-to-end through the production UI; FILL_IN_BLANK editor remains a placeholder (lands in Plan 05).

Purpose: Establishes the canonical save round-trip. Plan 05 only adds the second activity-type editor and DnD reorder; Plan 06 adds edit-mode reuse and Try preview; Plan 07 adds session results display. All depend on this slice's backend persistence shape.

Output: A teacher can create and save a Reading homework consisting of one or more MATCHING activities, each with ≥ 2 image-word pairs, and the homework appears in the homework list with TYPE_META.READING rendering.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-teacher-dashboard/03-CONTEXT.md
@.planning/phases/03-teacher-dashboard/03-RESEARCH.md
@.planning/phases/03-teacher-dashboard/03-PATTERNS.md
@.planning/phases/03-teacher-dashboard/03-01-SUMMARY.md
@.planning/phases/03-teacher-dashboard/03-03-SUMMARY.md

<interfaces>
<!-- Line anchors -->

backend/src/homework/homework.repository.ts:
- Lines 12-15: existing assignmentInclude (extended in Plan 02)
- Lines 17-33: buildPartsCreate function (template for buildReadingActivitiesCreate)
- Lines 39-101: existing findAll/findById/create/update method bodies
- Lines 82-95: delete-and-recreate update pattern for HomeworkPart

backend/src/homework/homework.service.ts:
- Lines 11-14: NotFoundException pattern (already used by the Plan 01 stubs)

frontend/lib/admin-api.ts:
- Line 91-98: existing req<HomeworkItem> patterns for createHomework / updateHomework / deleteHomework
- Line 155-167: uploadSpeakingImage helper (reuse, per D-11)
- Plan 01 already declared ReadingHomeworkDetail, ReadingActivity, ReadingMatchingPair, SentenceSegment, FillInBlankBlank.

frontend/app/teacher/homework/create/reading/page.tsx:
- ReadingActivityDraft type defined in Plan 03; in this plan we add the per-pair UI for MATCHING activities and the real handleSubmit.

frontend/app/teacher/homework/page.tsx:
- Lines 72-89: existing uploadImage helper inside HomeworkModal — same usage pattern for reading

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace stub repository + service methods with real Prisma create / update / findById including readingActivitiesInclude</name>
  <files>backend/src/homework/homework.repository.ts, backend/src/homework/homework.service.ts</files>
  <read_first>
    - backend/src/homework/homework.repository.ts (full file — focus on lines 12-15 for assignmentInclude, 17-33 for buildPartsCreate pattern, 39-101 for method shapes, 82-95 for delete-and-recreate update pattern)
    - backend/src/homework/homework.service.ts (full file — service shape and NotFoundException pattern)
    - backend/src/homework/homework.dto.ts (DTOs declared in Plan 01)
    - .planning/phases/03-teacher-dashboard/03-PATTERNS.md (sections `backend/src/homework/homework.repository.ts` and `backend/src/homework/homework.service.ts` — buildReadingActivitiesCreate, update delete-and-recreate, mirror of service findById/create/update)
    - .planning/phases/03-teacher-dashboard/03-RESEARCH.md (sections "Pattern 1: Prisma Nested Create for Reading Activities", "Pitfall 4: Prisma update deletes child records on reading homework edit", "Pitfall 2 / 3" for blank index integrity, "Pitfall 5" for session include — irrelevant here, relevant in Plan 07)
    - .planning/phases/03-teacher-dashboard/03-01-SUMMARY.md (placeholder return shape from Plan 01)
  </read_first>
  <action>
    Per TEACH-01 / READ-07 / D-06 / D-07 / D-08 / D-12, replace the Plan 01 stubs.

    In `backend/src/homework/homework.repository.ts`:

    1. Define a new constant `readingActivitiesInclude` near `assignmentInclude`:
       ```
       const readingActivitiesInclude = {
         readingActivities: {
           orderBy: { order: 'asc' as const },
           include: {
             matchingPairs: { orderBy: { order: 'asc' as const } },
             fillInBlank: { include: { blanks: { orderBy: { blankIndex: 'asc' as const } } } },
           },
         },
       };
       ```
    2. Add helper `buildReadingActivitiesCreate(activities: CreateReadingActivityDto[] | undefined)` mirroring `buildPartsCreate` (lines 17-33). Implementation per RESEARCH.md Pattern 1: returns `undefined` for empty/missing input; otherwise returns `{ create: [...] }` where each entry has `type, order: idx,` plus the type-conditional branch — `matchingPairs.create` for MATCHING (mapping `act.pairs!` → `{ imageUrl, word, order: pIdx }`), or `fillInBlank.create` for FILL_IN_BLANK (with nested `blanks.create` mapping segments where `s.blank` to `{ blankIndex: s.blankIndex!, correctWord: s.text, distractors: s.distractors ?? [] }` and `sentenceSegments: act.segments` stored as JSON).
    3. Replace the body of `createReadingHomework(dto)`:
       ```
       return this.prisma.homework.create({
         data: {
           type: 'READING',
           name: dto.name,
           readingActivities: buildReadingActivitiesCreate(dto.activities),
         },
         include: { ...readingActivitiesInclude, assignments: { include: assignmentInclude } },
       });
       ```
    4. Replace the body of `findReadingById(id)`:
       ```
       return this.prisma.homework.findUnique({
         where: { id },
         include: { ...readingActivitiesInclude, assignments: { include: assignmentInclude } },
       });
       ```
    5. Replace the body of `updateReadingHomework(id, dto)` using the delete-and-recreate pattern (mirroring lines 82-95) — for reading we delete all `readingActivity` rows first (cascade onDelete handles matchingPairs, fillInBlank, blanks):
       ```
       if (dto.activities !== undefined) {
         await this.prisma.readingActivity.deleteMany({ where: { homeworkId: id } });
       }
       return this.prisma.homework.update({
         where: { id },
         data: {
           ...(dto.name !== undefined ? { name: dto.name } : {}),
           ...(dto.activities !== undefined ? { readingActivities: buildReadingActivitiesCreate(dto.activities) } : {}),
         },
         include: { ...readingActivitiesInclude, assignments: { include: assignmentInclude } },
       });
       ```

    In `backend/src/homework/homework.service.ts`:

    1. Replace the body of `createReadingHomework(dto)` with validation FIRST then delegation:
       - Validate `dto.name?.trim()` is non-empty; else `throw new BadRequestException('Name is required')`.
       - Validate `dto.activities` is an array of length ≥ 1; else `throw new BadRequestException('At least one activity is required')`.
       - For each activity: if `type === 'MATCHING'` require `pairs && pairs.length >= 2` (D-06); else `throw new BadRequestException('Matching activities require at least 2 pairs')`.
       - For each activity: if `type === 'FILL_IN_BLANK'` require `segments && segments.length > 0` AND at least one segment with `blank: true`; else `throw new BadRequestException('Fill-in-blank activities require at least one blank')`. Additionally verify blankIndex sequence is contiguous starting at 0 (Pitfall 3): collect all `s.blankIndex` where `s.blank`, sort, ensure the resulting array equals `[0, 1, ..., n-1]`.
       - After validation passes, `return this.repo.createReadingHomework(dto);`.
    2. Apply the SAME validation in `updateReadingHomework(id, dto)` but only when `dto.activities !== undefined`. Still call `await this.findReadingById(id)` first to throw NotFoundException if the homework doesn't exist.
    3. Import `BadRequestException` from `@nestjs/common`.

    No changes to the controller in this task — Plan 01 already registered POST/GET/PUT under `/homework/reading` and they now flow through the real service.
  </action>
  <verify>
    <automated>cd backend && npx tsc --noEmit && npx jest --testPathPattern="homework" --no-coverage --passWithNoTests</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "buildReadingActivitiesCreate" backend/src/homework/homework.repository.ts` returns at least 4 (declaration + 2 usages in create/update + maybe export).
    - `grep -c "readingActivitiesInclude" backend/src/homework/homework.repository.ts` returns at least 3 (declaration + create include + findById include + update include = 4).
    - `grep -c "readingActivity.deleteMany" backend/src/homework/homework.repository.ts` returns 1.
    - `grep -c "BadRequestException" backend/src/homework/homework.service.ts` returns at least 4 (import + 3+ throw statements).
    - `grep -c "pairs.length >= 2" backend/src/homework/homework.service.ts` returns at least 1 (D-06 enforcement).
    - `cd backend && npx tsc --noEmit` exits 0.
    - `cd backend && npx jest --testPathPattern="homework" --no-coverage --passWithNoTests` exits 0.
    - End-to-end probe (manual or via curl in verification stage): `POST /homework/reading` with `{ name: 't1', activities: [{ type: 'MATCHING', pairs: [{ imageUrl: 'http://x/a', word: 'cat' }, { imageUrl: 'http://x/b', word: 'dog' }] }] }` returns HTTP 201 with `id > 0` and `readingActivities[0].matchingPairs.length === 2`.
    - `POST /homework/reading` with only 1 pair returns HTTP 400.
  </acceptance_criteria>
  <done>Real Prisma persistence wired with cascade-friendly delete-and-recreate update; service validates D-06 and D-12 blank-index integrity; type-checks clean.</done>
</task>

<task type="auto">
  <name>Task 2: Add reading API client functions in admin-api and define matching-activity input types</name>
  <files>frontend/lib/admin-api.ts</files>
  <read_first>
    - frontend/lib/admin-api.ts (full file — lines 91-98 for existing CRUD function pattern, line 155-167 for uploadSpeakingImage)
    - .planning/phases/03-teacher-dashboard/03-PATTERNS.md (`frontend/lib/admin-api.ts` section — req<T> pattern and new function signatures)
    - .planning/phases/03-teacher-dashboard/03-CONTEXT.md (D-11)
    - .planning/phases/03-teacher-dashboard/03-01-SUMMARY.md (existing interface shapes from Plan 01)
  </read_first>
  <action>
    Add to `frontend/lib/admin-api.ts` after the existing homework CRUD functions:

    1. Input type aliases (paralleling backend DTO classes):
       ```
       export interface CreateReadingPairInput { imageUrl: string; word: string; }
       export interface CreateReadingActivityInput {
         type: ReadingActivityType;
         pairs?: CreateReadingPairInput[];
         segments?: SentenceSegment[];
       }
       export interface CreateReadingHomeworkInput {
         name: string;
         activities: CreateReadingActivityInput[];
       }
       export interface UpdateReadingHomeworkInput {
         name?: string;
         activities?: CreateReadingActivityInput[];
       }
       ```
    2. Three API client functions using the existing `req<T>` helper:
       ```
       export const createReadingHomework = (data: CreateReadingHomeworkInput) =>
         req<ReadingHomeworkDetail>('/homework/reading', { method: 'POST', body: JSON.stringify(data) });
       export const getReadingHomework = (id: number) =>
         req<ReadingHomeworkDetail>(`/homework/reading/${id}`);
       export const updateReadingHomework = (id: number, data: UpdateReadingHomeworkInput) =>
         req<ReadingHomeworkDetail>(`/homework/reading/${id}`, { method: 'PUT', body: JSON.stringify(data) });
       ```
    3. Do NOT add a separate upload function — `uploadSpeakingImage` is reused (D-11).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npx next lint --dir lib --max-warnings 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export const createReadingHomework" frontend/lib/admin-api.ts` returns 1.
    - `grep -c "export const getReadingHomework" frontend/lib/admin-api.ts` returns 1.
    - `grep -c "export const updateReadingHomework" frontend/lib/admin-api.ts` returns 1.
    - `grep -cE "export interface (CreateReadingPairInput|CreateReadingActivityInput|CreateReadingHomeworkInput|UpdateReadingHomeworkInput)" frontend/lib/admin-api.ts` returns 4.
    - `cd frontend && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Three API client functions plus four input interfaces present; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 3: Implement MatchingActivityEditor + image upload + real Save handler on the reading creation page</name>
  <files>frontend/app/teacher/homework/create/reading/page.tsx</files>
  <read_first>
    - frontend/app/teacher/homework/create/reading/page.tsx (skeleton produced by Plan 03)
    - frontend/app/teacher/homework/page.tsx (lines 72-89 for uploadImage helper template, lines 241-249 for image thumbnail with × remove pattern, lines 107-138 for form submission pattern)
    - frontend/lib/admin-api.ts (uploadSpeakingImage, createReadingHomework, CreateReadingHomeworkInput, ReadingActivityType)
    - .planning/phases/03-teacher-dashboard/03-PATTERNS.md (`frontend/app/teacher/homework/create/reading/page.tsx` section — image upload helper, form submission, Save/Cancel button row, error display)
    - .planning/phases/03-teacher-dashboard/03-CONTEXT.md (D-03, D-06, D-10, D-11; "Claude's Discretion" on image thumbnail size)
    - .planning/phases/03-teacher-dashboard/03-RESEARCH.md (Pitfall 1 — TYPE_META synced; Pitfall 4 — backend handles cascade; not relevant here directly)
    - .planning/phases/03-teacher-dashboard/03-03-SUMMARY.md (ReadingActivityDraft type signature)
  </read_first>
  <action>
    Extend the skeleton page produced by Plan 03 with real matching-editor behavior and a working save handler. Fill-in-blank editor remains a placeholder (Plan 05 owns it).

    1. Import additions at the top: `createReadingHomework, uploadSpeakingImage, CreateReadingHomeworkInput, CreateReadingActivityInput` from '@/lib/admin-api'.
    2. Local state additions: `const [uploading, setUploading] = useState<string | null>(null);` (key: `${activityDraftId}-${pairIdx}` while that pair's image is uploading). `const [uploadError, setUploadError] = useState('');`.
    3. Pair mutation helpers (declared inside the component):
       - `addPair(actId: string)` → set activities; map matching pair list to push `{ imageUrl: '', word: '' }`.
       - `removePair(actId: string, pairIdx: number)` → filter out that pair index.
       - `updatePairWord(actId: string, pairIdx: number, word: string)` → update the word at that index.
       - `uploadPairImage(actId: string, pairIdx: number, file: File)` → mirror the HomeworkModal pattern (lines 72-89):
         ```
         setUploading(`${actId}-${pairIdx}`); setUploadError('');
         try {
           const url = await uploadSpeakingImage(file);
           setActivities(prev => prev.map(a => a.draftId !== actId ? a : {
             ...a, pairs: (a.pairs ?? []).map((p, i) => i !== pairIdx ? p : { ...p, imageUrl: url }),
           }));
         } catch (err: unknown) {
           setUploadError(err instanceof Error ? err.message : 'Upload failed');
         } finally {
           setUploading(null);
         }
         ```
    4. Render MatchingActivityEditor inline inside the activity-card map when `activity.type === 'MATCHING'`:
       - Heading: `<div className="flex items-center justify-between mb-3"><span className="text-sm font-semibold">Image–Word Matching</span><button onClick={() => removeActivity(activity.draftId)} className="text-xs text-textSecondary hover:text-highlight">Remove activity</button></div>`. (`removeActivity` is a small helper that filters activities by draftId.)
       - For each `pair` in `activity.pairs ?? []`, render a row containing:
         - A 40×40 image preview if `pair.imageUrl` is set, else a small dashed-border drop placeholder. Use `<img src={pair.imageUrl} alt={pair.word} className="w-10 h-10 rounded-lg object-cover border border-border" />`. Include `eslint-disable-next-line @next/next/no-img-element` if needed.
         - A hidden file input wired to a label/button: clicking the placeholder triggers a file picker; `onChange` calls `uploadPairImage(activity.draftId, idx, file)`.
         - Word input: `<input value={pair.word} onChange={(e) => updatePairWord(activity.draftId, idx, e.target.value)} placeholder="Word label" className="flex-1 px-3 py-1.5 rounded-lg border border-border text-sm" />`.
         - × remove button calling `removePair(activity.draftId, idx)`.
       - Below the pair list: `<button type="button" onClick={() => addPair(activity.draftId)} className="text-sm text-primary font-semibold hover:underline">+ Add pair</button>`.
       - If `uploading === \`${activity.draftId}-${idx}\`` for any pair, show a small spinner near that pair's image.
    5. For `activity.type === 'FILL_IN_BLANK'` keep the existing Plan 03 placeholder card text (e.g. "Fill-in-blank editor — coming in Plan 05"). Plan 05 replaces this block with the real editor.
    6. `removeActivity(draftId: string)` helper: `setActivities(prev => prev.filter(a => a.draftId !== draftId));`.
    7. Replace the disabled Save button with a real handler:
       ```
       async function handleSubmit() {
         setError('');
         if (!name.trim()) { setError('Homework name is required.'); nameRef.current?.focus(); return; }
         if (activities.length === 0) { setError('Add at least one activity.'); return; }
         for (const act of activities) {
           if (act.type === 'MATCHING' && (act.pairs?.length ?? 0) < 2) {
             setError('Each matching activity needs at least 2 pairs.'); return;
           }
           if (act.type === 'MATCHING' && act.pairs!.some(p => !p.imageUrl || !p.word.trim())) {
             setError('Each matching pair needs an image and a word.'); return;
           }
           // FILL_IN_BLANK validation is added in Plan 05; for now skip those activities.
         }
         setLoading(true);
         try {
           const payload: CreateReadingHomeworkInput = {
             name: name.trim(),
             activities: activities.map(a => ({
               type: a.type,
               ...(a.type === 'MATCHING' ? { pairs: a.pairs ?? [] } : { segments: a.segments ?? [] }),
             })),
           };
           await createReadingHomework(payload);
           router.push('/teacher/homework');  // D-03: no AssignModal auto-open
         } catch (err: unknown) {
           setError(err instanceof Error ? err.message : 'Failed to save.');
         } finally {
           setLoading(false);
         }
       }
       ```
    8. Save button: `<button type="button" onClick={handleSubmit} disabled={loading} className="flex-1 py-2.5 rounded-xl text-sm font-bold text-white disabled:opacity-60 flex items-center justify-center gap-2" style={{ background: gradients.primarySecondary }}>{loading ? 'Saving…' : 'Create'}</button>`.
    9. Upload error banner: `{uploadError && <div className="text-xs text-highlight">{uploadError}</div>}` near the activity list.

    Do NOT touch the FILL_IN_BLANK branch or add @dnd-kit — those belong to Plan 05.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npx next lint --dir app/teacher/homework/create --max-warnings 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "uploadSpeakingImage" frontend/app/teacher/homework/create/reading/page.tsx` returns at least 1.
    - `grep -c "createReadingHomework" frontend/app/teacher/homework/create/reading/page.tsx` returns at least 1.
    - `grep -c "addPair\\|updatePairWord\\|removePair\\|uploadPairImage" frontend/app/teacher/homework/create/reading/page.tsx` returns at least 4.
    - `grep -c "+ Add pair" frontend/app/teacher/homework/create/reading/page.tsx` returns 1.
    - `grep -c "router.push('/teacher/homework')" frontend/app/teacher/homework/create/reading/page.tsx` returns at least 1.
    - `cd frontend && npx tsc --noEmit` exits 0.
    - End-to-end probe: logged in as teacher → + Create → Reading card → name="Animals" → + Add Matching → upload 2 images, type 2 words → Create → page redirects to /teacher/homework and the new homework appears in the list with the 📖 emoji (per TYPE_META.READING from Plan 03).
  </acceptance_criteria>
  <done>Matching-only reading homework can be created and saved through the production UI; image upload reuses the existing endpoint; tsc / lint clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Teacher browser → POST /homework/reading | New write surface; AuthGuard inherited from class decorator (Plan 01) |
| Teacher browser → POST /homework/image | Existing image upload endpoint; reused unchanged |
| NestJS service → Prisma | New write path with nested create; cascade rules guard child rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-12 | Tampering | `dto.activities[].pairs[].imageUrl` is a free-form string set by the client | mitigate | The frontend only ever uses URLs returned by `uploadSpeakingImage` (server-issued). Backend validation: in service, assert each `pair.imageUrl` starts with the configured API URL or storage prefix — accept as low priority since the existing speaking-homework flow has the same pattern (single-teacher trust boundary per PROJECT.md). Document but do not add new validation in this plan; revisit if multi-tenant comes online. |
| T-03-13 | Input Validation | `dto.activities` array length / `pairs` length unbounded | mitigate | Service enforces `activities.length >= 1` and `pairs.length >= 2`; add upper bound `activities.length <= 50` and `pairs.length <= 30` and `segments.length <= 200` as defensive caps (BadRequestException). Acceptance criterion: grep for `.length > 50`, `.length > 30` in service. |
| T-03-14 | Tampering | Validation logic duplicated on client and server | accept | Client validation is UX-only; server is the authority. Mismatches return HTTP 400 which the page surfaces via error banner. |
| T-03-15 | Information Disclosure | New endpoint returns homework regardless of which teacher created it | accept (this phase) | Same single-teacher constraint as existing /homework/:id endpoint per PROJECT.md "Single teacher role". |
| T-03-16 | Denial of Service | Image upload reuse without new size limit | accept | Existing `/homework/image` endpoint already enforces 10MB cap and TEACHER role per RESEARCH.md "Security Domain". No new mitigation needed. |
</threat_model>

<verification>
- Backend POST /homework/reading creates rows in `homeworks`, `reading_activities`, and `matching_pairs` (raw SQL probe at phase verification stage).
- Backend rejects payloads violating D-06 (min 2 pairs) and D-12 (blankIndex contiguity).
- Frontend uploads images via existing /homework/image endpoint and persists URLs in the activity draft state.
- Frontend save round-trip ends at /teacher/homework with the new reading homework visible.
- All tsc/lint commands exit 0.
</verification>

<success_criteria>
1. A matching-only Reading homework with name "Animals" and ≥ 2 pairs saves successfully and appears in the homework list.
2. Backend rejects payloads with <2 pairs per matching activity (HTTP 400).
3. Image upload uses the existing /homework/image endpoint (no new upload route).
4. tsc and lint exit 0 across frontend and backend.
</success_criteria>

<output>
After completion, create `.planning/phases/03-teacher-dashboard/03-04-SUMMARY.md` describing: the exact CreateReadingHomeworkInput shape, the validation rules now enforced server-side, and any deviations from the patterns file in MatchingActivityEditor markup.
</output>
