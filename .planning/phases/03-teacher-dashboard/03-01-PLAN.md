---
phase: 03-teacher-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/prisma/schema.prisma
  - backend/src/homework/homework.dto.ts
  - frontend/lib/admin-api.ts
  - backend/src/homework/homework.repository.ts
  - backend/src/homework/homework.service.ts
  - backend/src/homework/homework.controller.ts
autonomous: true
requirements:
  - READ-07
  - TEACH-01
user_setup: []

must_haves:
  truths:
    - "HomeworkType enum includes READING in Prisma schema, backend DTO, and frontend admin-api"
    - "Live Postgres database has the 7 new reading tables and READING enum value (verified by prisma migrate status / db query)"
    - "Frontend TypeScript compiles with new SentenceSegment, ReadingActivity, ReadingActivityResult, MatchingItemResult, FillInBlankItemResult, GameSession.readingActivityResults type extensions"
    - "Backend builds with stub createReadingHomework / findReadingById / updateReadingHomework methods returning placeholder values"
    - "POST /homework/reading endpoint exists and returns HTTP 201 with a placeholder body when called with a valid teacher JWT"
  artifacts:
    - path: "backend/prisma/schema.prisma"
      provides: "READING enum value + 7 new models (ReadingActivity, MatchingPair, FillInBlankActivity, FillInBlankBlank, ReadingActivityResult, MatchingItemResult, FillInBlankItemResult) with onDelete: Cascade"
      contains: "READING\nmodel ReadingActivity\nmodel MatchingPair\nmodel FillInBlankActivity\nmodel FillInBlankBlank\nmodel ReadingActivityResult\nmodel MatchingItemResult\nmodel FillInBlankItemResult"
    - path: "backend/src/homework/homework.dto.ts"
      provides: "HomeworkType union extended with 'READING'; ReadingActivityType union; CreateReadingHomeworkDto, UpdateReadingHomeworkDto, CreateReadingActivityDto, CreateReadingPairDto, SentenceSegmentDto"
    - path: "frontend/lib/admin-api.ts"
      provides: "HomeworkType union extended with 'READING'; SentenceSegment, ReadingMatchingPair, FillInBlankBlank, ReadingActivity, ReadingHomeworkDetail, MatchingItemResult, FillInBlankItemResult, ReadingActivityResult interfaces; GameSession.readingActivityResults?; AssignmentClass.class._count?.students"
    - path: "backend/src/homework/homework.repository.ts"
      provides: "Stub findReadingById, createReadingHomework, updateReadingHomework methods (return placeholder; full implementation in Plan 04)"
    - path: "backend/src/homework/homework.service.ts"
      provides: "Stub findReadingById, createReadingHomework, updateReadingHomework methods delegating to repo"
    - path: "backend/src/homework/homework.controller.ts"
      provides: "POST /homework/reading, GET /homework/reading/:id, PUT /homework/reading/:id endpoints (handlers delegate to service stubs)"
  key_links:
    - from: "backend/prisma/schema.prisma"
      to: "live Postgres database"
      via: "npx prisma migrate dev --name add_reading_homework"
      pattern: "READING enum + 7 new tables present in database"
    - from: "backend/src/homework/homework.dto.ts HomeworkType"
      to: "frontend/lib/admin-api.ts HomeworkType"
      via: "manual mirror"
      pattern: "'PHONICS' \\| 'SPEAKING' \\| 'READING'"
    - from: "backend/src/homework/homework.controller.ts"
      to: "backend/src/homework/homework.service.ts"
      via: "POST /homework/reading -> createReadingHomework"
      pattern: "@Post\\('reading'\\)"
---

<objective>
Establish the foundational vertical slice for Phase 3 Teacher Dashboard READING homework: extend the database schema with the READING enum value and 7 new tables, push that schema to the live database, mirror the type changes across the backend DTO and frontend admin-api, and create stub backend service/controller wiring so a POST /homework/reading endpoint is reachable end-to-end. This slice unblocks every downstream plan.

Purpose: Without the live database schema and synchronized HomeworkType enum across all three locations, every later slice would fail at type-check, runtime, or DB-write time. This is the single foundational slice all later vertical slices build on.

Output: Live Postgres schema with 7 reading tables; HomeworkType='PHONICS'|'SPEAKING'|'READING' in three locations; reading-related TypeScript interfaces declared once in admin-api; backend stubs returning placeholder JSON so subsequent plans can wire real logic.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-teacher-dashboard/03-CONTEXT.md
@.planning/phases/03-teacher-dashboard/03-RESEARCH.md
@.planning/phases/03-teacher-dashboard/03-PATTERNS.md
@./CLAUDE.md

<interfaces>
<!-- Existing line anchors to mirror. Executor reads source files; lines below are starting points. -->

backend/prisma/schema.prisma:
- Line 16-19: enum HomeworkType { PHONICS SPEAKING } → add READING value
- Line 126-136: model HomeworkPart — cascade pattern to mirror for new models
- Line 175-189: model HomeworkSession — add `readingActivityResults ReadingActivityResult[]` back-relation
- Line 203-214: model PhonicsItemResult — result-table pattern to mirror

backend/src/homework/homework.dto.ts:
- Line 1: `export type HomeworkType = 'PHONICS' | 'SPEAKING';` → extend with 'READING'

frontend/lib/admin-api.ts:
- Line 239: `export type HomeworkType = 'PHONICS' | 'SPEAKING';` → extend with 'READING'
- Line 281-286: AssignmentClass — add `class: ClassItem & { _count?: { students: number } }`
- Line 341-353: GameSession — add `readingActivityResults?: ReadingActivityResult[]`

backend/src/homework/homework.controller.ts:
- Line 8-9: `@UseGuards(AuthGuard)` and `@Controller('homework')` already protect all routes
- Line 26-30: existing inline route handler pattern (`@Get() findAll() { return this.service.findAll(); }`)

backend/src/homework/homework.repository.ts:
- Line 12-15: existing assignmentInclude (kept as-is in this plan; extension moves to Plan 02)
- Line 17-33: buildPartsCreate function (pattern only — implementation deferred to Plan 04)

backend/src/homework/homework.service.ts:
- Line 11-14: NotFoundException pattern for findById

03-RESEARCH.md "New Prisma Schema" section (lines 470-580): full canonical Prisma DDL for the 7 new models — copy as the source of truth.

03-RESEARCH.md "Pitfall 1": HomeworkType enum out of sync — the THREE sync locations are schema.prisma, homework.dto.ts, admin-api.ts.

03-RESEARCH.md "Pitfall 4": Set `onDelete: Cascade` on EVERY new child relation.

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Prisma schema with READING enum and 7 new reading tables</name>
  <files>backend/prisma/schema.prisma</files>
  <read_first>
    - backend/prisma/schema.prisma (full file; especially lines 16-19 for HomeworkType enum, lines 126-150 for HomeworkPart/HomeworkWord cascade pattern, lines 175-214 for HomeworkSession + result-table patterns)
    - .planning/phases/03-teacher-dashboard/03-RESEARCH.md (section "New Prisma Schema" — canonical DDL for all 7 new models; section "Pitfall 4" cascade requirement)
    - .planning/phases/03-teacher-dashboard/03-PATTERNS.md (`backend/prisma/schema.prisma` section — cascade and result-table patterns with line citations)
    - .planning/phases/03-teacher-dashboard/03-CONTEXT.md (D-12 sentenceSegments JSON shape; D-16/D-17 result-table fields)
  </read_first>
  <action>
    Per D-12 / D-16 / D-17 / D-18, extend `backend/prisma/schema.prisma` as follows. Do not write any Prisma migration SQL by hand — the next task runs `prisma migrate dev` which generates SQL from the schema.

    1. In the `HomeworkType` enum (currently lines 16-19), add a new value `READING` after `SPEAKING`.
    2. Add a new enum `ReadingActivityType` with values `MATCHING` and `FILL_IN_BLANK`.
    3. Add 7 new models exactly as specified in 03-RESEARCH.md "New Prisma Schema (D-12 + Context.md backend schema decision)":
       - `ReadingActivity` (fields: id, homeworkId, type ReadingActivityType, order; relations: homework → Homework with onDelete: Cascade, matchingPairs MatchingPair[], fillInBlank FillInBlankActivity?, activityResults ReadingActivityResult[]; constraint: @@unique([homeworkId, order]); @@map("reading_activities"))
       - `MatchingPair` (id, activityId, imageUrl, word, order; activity → ReadingActivity onDelete: Cascade; itemResults MatchingItemResult[]; @@unique([activityId, order]); @@map("matching_pairs"))
       - `FillInBlankActivity` (id, activityId @unique, sentenceSegments Json; activity → ReadingActivity onDelete: Cascade; blanks FillInBlankBlank[]; @@map("fill_in_blank_activities"))
       - `FillInBlankBlank` (id, activityId, blankIndex, correctWord, distractors Json; fillInBlank → FillInBlankActivity onDelete: Cascade; itemResults FillInBlankItemResult[]; @@unique([activityId, blankIndex]); @@map("fill_in_blank_blanks"))
       - `ReadingActivityResult` (id, sessionId, activityId, score Float; session → HomeworkSession onDelete: Cascade; activity → ReadingActivity onDelete: Cascade; matchingResults MatchingItemResult[]; fillInBlankResults FillInBlankItemResult[]; @@unique([sessionId, activityId]); @@map("reading_activity_results"))
       - `MatchingItemResult` (id, activityResultId, pairId, studentChosenWord, isCorrect; activityResult → ReadingActivityResult onDelete: Cascade; pair → MatchingPair onDelete: Cascade; @@unique([activityResultId, pairId]); @@map("matching_item_results"))
       - `FillInBlankItemResult` (id, activityResultId, blankId, studentChosenWord, isCorrect; activityResult → ReadingActivityResult onDelete: Cascade; blank → FillInBlankBlank onDelete: Cascade; @@unique([activityResultId, blankId]); @@map("fill_in_blank_item_results"))
    4. Extend `Homework` model: add back-relation `readingActivities ReadingActivity[]`.
    5. Extend `HomeworkSession` model (currently lines 175-189): add back-relation `readingActivityResults ReadingActivityResult[]`.

    All new child relations MUST carry `onDelete: Cascade` per Pitfall 4. Use `Json` type (no Prisma typed-array column) for `sentenceSegments` and `distractors`. Do NOT add default values to the Json columns (the dto layer enforces presence).
  </action>
  <verify>
    <automated>cd backend && npx prisma format && npx prisma validate</automated>
  </verify>
  <acceptance_criteria>
    - `npx prisma validate` exits 0 (no schema errors).
    - `grep -c "^enum HomeworkType" backend/prisma/schema.prisma` returns 1 and `grep -A4 "^enum HomeworkType" backend/prisma/schema.prisma | grep -c "READING"` returns 1.
    - `grep -c "^enum ReadingActivityType" backend/prisma/schema.prisma` returns 1.
    - `grep -cE "^model (ReadingActivity|MatchingPair|FillInBlankActivity|FillInBlankBlank|ReadingActivityResult|MatchingItemResult|FillInBlankItemResult) " backend/prisma/schema.prisma` returns 7.
    - `grep -c "onDelete: Cascade" backend/prisma/schema.prisma` increased by at least 9 (one cascade per new child relation across the 7 new models — homework→ReadingActivity, activity→MatchingPair, activity→FillInBlankActivity, activity→FillInBlankBlank, session→ReadingActivityResult, activity→ReadingActivityResult, activityResult→MatchingItemResult, pair→MatchingItemResult, activityResult→FillInBlankItemResult, blank→FillInBlankItemResult = 10 new cascades).
    - `grep -c "readingActivityResults ReadingActivityResult\\[\\]" backend/prisma/schema.prisma` returns 1 (inside HomeworkSession).
    - `grep -c "readingActivities ReadingActivity\\[\\]" backend/prisma/schema.prisma` returns 1 (inside Homework).
  </acceptance_criteria>
  <done>Schema validates; READING enum, ReadingActivityType enum, and all 7 new models present with required cascade rules and back-relations.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Push reading schema to live database</name>
  <files>backend/prisma/migrations/(generated)</files>
  <read_first>
    - backend/prisma/schema.prisma (must be the version produced by Task 1)
    - .planning/phases/03-teacher-dashboard/03-RESEARCH.md (section "Environment Availability" — PostgreSQL available via Docker)
  </read_first>
  <action>
    BLOCKING — every downstream plan needs a live database matching the schema. Build and type checks would pass without this because Prisma client types are generated from `schema.prisma`, not the database — creating a false-positive verification state.

    Run `cd backend && npx prisma migrate dev --name add_reading_homework`. This generates a new migration directory under `backend/prisma/migrations/<timestamp>_add_reading_homework/` containing `migration.sql`, applies it to the dev DB, and regenerates the Prisma client.

    If `migrate dev` fails because the database connection is unavailable or because of an unrelated drift issue, fall back to `cd backend && npx prisma db push --accept-data-loss` to sync the schema (no migration files created — acceptable for dev). Document which command succeeded in the SUMMARY.

    After whichever command succeeded, run `cd backend && npx prisma generate` to ensure the Prisma client is in sync.
  </action>
  <verify>
    <automated>cd backend && npx prisma migrate status</automated>
  </verify>
  <acceptance_criteria>
    - Either `backend/prisma/migrations/*_add_reading_homework/migration.sql` exists, OR `db push` was used (the executor must note this in SUMMARY).
    - `cd backend && npx prisma migrate status` reports the database is in sync (no pending migrations and no drift), OR `db push` left the DB matching schema.
    - `cd backend && node -e "const {PrismaClient}=require('@prisma/client'); const p=new PrismaClient(); p.\$queryRawUnsafe('SELECT 1 FROM reading_activities LIMIT 0').then(()=>{console.log('OK');process.exit(0)}).catch(e=>{console.error(e.message);process.exit(1)})"` exits 0 (table reading_activities exists at the DB level).
    - The same SELECT-1-FROM check passes for tables: matching_pairs, fill_in_blank_activities, fill_in_blank_blanks, reading_activity_results, matching_item_results, fill_in_blank_item_results (run them all; all must exit 0).
    - The Prisma client exports `ReadingActivity` type — verified by `cd backend && node -e "const t=require('@prisma/client'); if(!('ReadingActivityType' in t)) process.exit(1); console.log('OK')"` exits 0 (the generated enum is exported on the @prisma/client root).
  </acceptance_criteria>
  <done>Live Postgres database contains all 7 new reading tables and the READING enum value; Prisma client regenerated; `npx prisma migrate status` reports in-sync.</done>
</task>

<task type="auto">
  <name>Task 3: Sync HomeworkType across backend DTO and frontend admin-api; declare reading TypeScript interfaces in admin-api</name>
  <files>backend/src/homework/homework.dto.ts, frontend/lib/admin-api.ts</files>
  <read_first>
    - backend/src/homework/homework.dto.ts (entire file — currently ~41 lines; line 1 is `export type HomeworkType = 'PHONICS' | 'SPEAKING';`)
    - frontend/lib/admin-api.ts (lines 1-50 for req helper; line 239 for HomeworkType; lines 281-294 for AssignmentClass/AssignmentItem; lines 316-330 for HomeworkItem/HomeworkDetail; lines 332-389 for result types; lines 341-353 for GameSession)
    - .planning/phases/03-teacher-dashboard/03-PATTERNS.md (`frontend/lib/admin-api.ts` section — all type extensions with exact shape; `backend/src/homework/homework.dto.ts` section)
    - .planning/phases/03-teacher-dashboard/03-RESEARCH.md ("Pitfall 1" — three sync locations)
  </read_first>
  <action>
    Update both files in the same commit per D-01..D-18 and Pitfall 1.

    In `backend/src/homework/homework.dto.ts`:
    - Replace the line `export type HomeworkType = 'PHONICS' | 'SPEAKING';` with `export type HomeworkType = 'PHONICS' | 'SPEAKING' | 'READING';`.
    - Add `export type ReadingActivityType = 'MATCHING' | 'FILL_IN_BLANK';`.
    - Add DTO classes (plain classes, no class-validator decorators — match existing file style):
      - `SentenceSegmentDto` with fields `text: string; blank: boolean; blankIndex?: number; correctWord?: string; distractors?: string[];` (canonical D-12 shape).
      - `CreateReadingPairDto` with `imageUrl: string; word: string;`.
      - `CreateReadingActivityDto` with `type: ReadingActivityType; pairs?: CreateReadingPairDto[]; segments?: SentenceSegmentDto[];`.
      - `CreateReadingHomeworkDto` with `name: string; activities: CreateReadingActivityDto[];`.
      - `UpdateReadingHomeworkDto` with `name?: string; activities?: CreateReadingActivityDto[];`.

    In `frontend/lib/admin-api.ts`:
    - Update line 239: `export type HomeworkType = 'PHONICS' | 'SPEAKING' | 'READING';`.
    - Add type alias `export type ReadingActivityType = 'MATCHING' | 'FILL_IN_BLANK';`.
    - Add interfaces in the existing types region (after the `HomeworkDetail` interface, before `SpeakingResult`):
      - `SentenceSegment { text: string; blank: boolean; blankIndex?: number; correctWord?: string; distractors?: string[]; }` (D-12 canonical).
      - `ReadingMatchingPair { id: number; activityId: number; imageUrl: string; word: string; order: number; }`.
      - `FillInBlankBlank { id: number; activityId: number; blankIndex: number; correctWord: string; distractors: string[]; }`.
      - `ReadingActivity { id: number; homeworkId: number; type: ReadingActivityType; order: number; matchingPairs?: ReadingMatchingPair[]; fillInBlank?: { id: number; sentenceSegments: SentenceSegment[]; blanks: FillInBlankBlank[] } | null; }`.
      - `ReadingHomeworkDetail { id: number; name: string | null; type: 'READING'; activities: ReadingActivity[]; assignments: AssignmentItem[]; createdAt: string; }`.
      - `MatchingItemResult { id: number; activityResultId: number; pairId: number; studentChosenWord: string; isCorrect: boolean; pair?: ReadingMatchingPair; }`.
      - `FillInBlankItemResult { id: number; activityResultId: number; blankId: number; studentChosenWord: string; isCorrect: boolean; blank?: FillInBlankBlank; }`.
      - `ReadingActivityResult { id: number; sessionId: number; activityId: number; score: number; activity?: ReadingActivity; matchingResults?: MatchingItemResult[]; fillInBlankResults?: FillInBlankItemResult[]; }`.
    - Extend the existing `GameSession` interface (currently lines 341-353): add optional field `readingActivityResults?: ReadingActivityResult[];` adjacent to the existing `speakingResults?` / `phonicsResults?` fields.
    - Extend the existing `AssignmentClass` interface (currently lines 281-286): change the `class: ClassItem;` field to `class: ClassItem & { _count?: { students: number } };` (this prepares for D-13 frontend consumption in Plan 02; do NOT add any backend-include logic in this plan).

    Do NOT add `createReadingHomework` / `getReadingHomework` / `updateReadingHomework` API functions yet — those move to Plan 04 to keep the conflict surface minimal.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && cd ../backend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "'PHONICS' | 'SPEAKING' | 'READING'" backend/src/homework/homework.dto.ts` returns 1.
    - `grep -c "'PHONICS' | 'SPEAKING' | 'READING'" frontend/lib/admin-api.ts` returns 1.
    - `grep -c "export type ReadingActivityType" backend/src/homework/homework.dto.ts` returns 1.
    - `grep -c "export type ReadingActivityType" frontend/lib/admin-api.ts` returns 1.
    - `grep -cE "export (class|interface) (SentenceSegmentDto|CreateReadingPairDto|CreateReadingActivityDto|CreateReadingHomeworkDto|UpdateReadingHomeworkDto)" backend/src/homework/homework.dto.ts` returns 5.
    - `grep -cE "export interface (SentenceSegment|ReadingMatchingPair|FillInBlankBlank|ReadingActivity|ReadingHomeworkDetail|MatchingItemResult|FillInBlankItemResult|ReadingActivityResult)" frontend/lib/admin-api.ts` returns 8.
    - `grep -c "readingActivityResults?: ReadingActivityResult\\[\\]" frontend/lib/admin-api.ts` returns 1.
    - `grep -c "_count?: { students: number }" frontend/lib/admin-api.ts` returns 1.
    - `cd frontend && npx tsc --noEmit` exits 0.
    - `cd backend && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>HomeworkType enum string is identical in all three locations (schema.prisma, homework.dto.ts, admin-api.ts); all reading-related interfaces declared in admin-api.ts; both projects type-check cleanly.</done>
</task>

<task type="auto">
  <name>Task 4: Wire backend reading-homework stubs (repository + service + controller)</name>
  <files>backend/src/homework/homework.repository.ts, backend/src/homework/homework.service.ts, backend/src/homework/homework.controller.ts</files>
  <read_first>
    - backend/src/homework/homework.controller.ts (full file — line 4 for DTO imports, lines 8-9 for class-level guards, lines 26-30 for existing inline route pattern)
    - backend/src/homework/homework.service.ts (full file — lines 11-27 for findById + NotFoundException pattern)
    - backend/src/homework/homework.repository.ts (lines 1-50 for imports and assignmentInclude; lines 39-101 for existing findAll/findById/create/update method shape)
    - .planning/phases/03-teacher-dashboard/03-PATTERNS.md (sections for homework.repository.ts, homework.service.ts, homework.controller.ts)
  </read_first>
  <action>
    Per D-01 / TEACH-01 / READ-07, add stub methods so the endpoint contract is reachable. Real Prisma queries are deferred to Plan 04 to keep this slice focused on the foundational wiring.

    In `backend/src/homework/homework.repository.ts`:
    - Import `CreateReadingHomeworkDto`, `UpdateReadingHomeworkDto` from `./homework.dto`.
    - Add three async methods on the `HomeworkRepository` class:
      - `async findReadingById(id: number) { return this.prisma.homework.findUnique({ where: { id }, include: { assignments: { include: assignmentInclude } } }); }` — uses the existing `assignmentInclude` constant. Returns null if not found (real `readingActivities` include is added in Plan 04 alongside the dedicated reading include constant).
      - `async createReadingHomework(_dto: CreateReadingHomeworkDto): Promise<{ id: number; placeholder: true }> { return { id: -1, placeholder: true }; }` — explicit placeholder return shape; Plan 04 replaces the body with a real `prisma.homework.create` call.
      - `async updateReadingHomework(_id: number, _dto: UpdateReadingHomeworkDto): Promise<{ id: number; placeholder: true }> { return { id: _id, placeholder: true }; }` — same placeholder pattern.

    In `backend/src/homework/homework.service.ts`:
    - Import `CreateReadingHomeworkDto`, `UpdateReadingHomeworkDto`.
    - Add three methods that mirror the existing service shape (NotFoundException on missing):
      - `async findReadingById(id: number) { const hw = await this.repo.findReadingById(id); if (!hw) throw new NotFoundException(\`Reading homework ${id} not found\`); return hw; }`.
      - `createReadingHomework(dto: CreateReadingHomeworkDto) { return this.repo.createReadingHomework(dto); }`.
      - `async updateReadingHomework(id: number, dto: UpdateReadingHomeworkDto) { await this.findReadingById(id); return this.repo.updateReadingHomework(id, dto); }`.

    In `backend/src/homework/homework.controller.ts`:
    - Extend the DTO import at the top of the file to also import `CreateReadingHomeworkDto, UpdateReadingHomeworkDto` from `./homework.dto`.
    - Add three handler methods on the existing `HomeworkController` class (the class is already decorated with `@UseGuards(AuthGuard)` so all new routes inherit auth):
      - `@Post('reading') createReading(@Body() dto: CreateReadingHomeworkDto) { return this.service.createReadingHomework(dto); }`.
      - `@Get('reading/:id') findReading(@Param('id', ParseIntPipe) id: number) { return this.service.findReadingById(id); }`.
      - `@Put('reading/:id') updateReading(@Param('id', ParseIntPipe) id: number, @Body() dto: UpdateReadingHomeworkDto) { return this.service.updateReadingHomework(id, dto); }`.
    - Ensure the existing parameterized routes (e.g. `@Get(':id')`, `@Put(':id')`) come AFTER the `reading/:id` routes in the route order, OR that NestJS route specificity correctly dispatches the literal `reading` segment. If a routing conflict exists (`@Get(':id')` defined before `@Get('reading/:id')`), reorder so the specific `reading/...` route is registered first.
  </action>
  <verify>
    <automated>cd backend && npx tsc --noEmit && npx jest --testPathPattern=homework --no-coverage --passWithNoTests</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "(createReadingHomework|findReadingById|updateReadingHomework)" backend/src/homework/homework.repository.ts` returns at least 3.
    - `grep -cE "(createReadingHomework|findReadingById|updateReadingHomework)" backend/src/homework/homework.service.ts` returns at least 3.
    - `grep -c "@Post('reading')" backend/src/homework/homework.controller.ts` returns 1.
    - `grep -c "@Get('reading/:id')" backend/src/homework/homework.controller.ts` returns 1.
    - `grep -c "@Put('reading/:id')" backend/src/homework/homework.controller.ts` returns 1.
    - `cd backend && npx tsc --noEmit` exits 0.
    - `cd backend && npx jest --testPathPattern=homework --no-coverage --passWithNoTests` exits 0.
    - When the NestJS app boots, `POST /homework/reading` with an authenticated request body `{ name: 'test', activities: [] }` returns HTTP 201 with `{ id: -1, placeholder: true }` (smoke; full behavior in Plan 04). Manual curl verification deferred to `/gsd-verify-work`.
  </acceptance_criteria>
  <done>Backend builds and type-checks; three reading endpoints registered under `/homework/reading`; service+repo stubs return placeholder shape; no existing route handlers regressed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Teacher browser → NestJS API | New `/homework/reading` endpoints accept untrusted JSON bodies; JWT verified by AuthGuard |
| NestJS API → PostgreSQL | Prisma client mediates; JSON columns (sentenceSegments, distractors) hold untrusted teacher input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Spoofing | New `/homework/reading` POST/GET/PUT routes | mitigate | `HomeworkController` is class-decorated with `@UseGuards(AuthGuard)`; new routes inherit it automatically (see lines 8-9). Acceptance criterion: `grep -c "@UseGuards(AuthGuard)" backend/src/homework/homework.controller.ts` >= 1. |
| T-03-02 | Tampering | `sentenceSegments` JSON in `FillInBlankActivity` | accept (in this plan) | DTO shape `CreateReadingActivityDto.segments?: SentenceSegmentDto[]` is structurally typed but not class-validator decorated in this plan; full payload validation (length limits, blankIndex sequential integrity) is added in Plan 04 alongside the real persistence logic. Rationale: stubs return placeholder; no DB write happens here, so malformed JSON cannot persist. |
| T-03-03 | Information Disclosure | `findReadingById` does not yet verify teacher tenancy of the homework | accept (this phase, pre-existing risk) | Existing `findById` endpoint has the same gap (one teacher in the system per PROJECT.md "Single teacher role"); Plan 04 keeps parity. Documented in RESEARCH.md "Security Domain". |
| T-03-04 | Elevation of Privilege | Cascade `onDelete` on result tables | mitigate | All new child relations carry `onDelete: Cascade` (Pitfall 4); validated by grep gate in Task 1. Prevents orphaned records when a homework/session is deleted. |
| T-03-05 | Denial of Service | Unbounded `activities[]` or `pairs[]` array size | accept (this plan) | DTO shape currently has no max length; mitigated in Plan 04 with validation. Stubs ignore body so payload size cannot exhaust DB in this slice. |
</threat_model>

<verification>
Phase-level reachability for this plan:
- Live database has all 7 new tables (raw SQL probe).
- `npx prisma validate` and `prisma migrate status` both clean.
- `npx tsc --noEmit` clean on both backend and frontend.
- HomeworkType enum is identical string union in three locations (grep gates).
- Three new HTTP routes registered with class-level AuthGuard inheritance.
- No new linter/eslint regressions in modified files.
</verification>

<success_criteria>
1. `cd backend && npx prisma migrate status` reports in-sync; raw SQL probe confirms all 7 new tables exist.
2. `cd frontend && npx tsc --noEmit` exits 0; `cd backend && npx tsc --noEmit` exits 0.
3. `POST /homework/reading` with valid JWT returns HTTP 201 + `{ id: -1, placeholder: true }`.
4. The HomeworkType union string `'PHONICS' | 'SPEAKING' | 'READING'` is present in all three sync locations.
</success_criteria>

<output>
After completion, create `.planning/phases/03-teacher-dashboard/03-01-SUMMARY.md` describing: which DB sync command succeeded (`migrate dev` vs `db push`), any drift surprises, and the exact shapes of the new admin-api interfaces (so downstream planners have an authoritative reference).
</output>
