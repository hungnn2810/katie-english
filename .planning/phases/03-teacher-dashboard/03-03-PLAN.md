---
phase: 03-teacher-dashboard
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - frontend/app/teacher/homework/page.tsx
  - frontend/app/teacher/homework/create/reading/page.tsx
autonomous: true
requirements:
  - TEACH-01
user_setup: []

must_haves:
  truths:
    - "Teacher clicking the existing + Create button opens a TypePickerModal with three cards: Phonics, Speaking, Reading"
    - "Clicking Phonics or Speaking in the TypePickerModal opens the existing HomeworkModal preset to that type (no regression to current behavior)"
    - "Clicking Reading in the TypePickerModal navigates to /teacher/homework/create/reading"
    - "The /teacher/homework/create/reading route renders a page with a required Name input, an empty 'No activities yet' state, and an + Add Activity button stub (Save button disabled until backend wiring lands in Plan 04)"
    - "TYPE_META gains a READING entry so the homework list filter tabs include a 📖 Reading tab"
    - "Filter tabs include a Reading tab so READING items (once created) will be filterable"
  artifacts:
    - path: "frontend/app/teacher/homework/page.tsx"
      provides: "TYPE_META.READING entry; openCreate now opens TypePickerModal instead of HomeworkModal; TypePickerModal component routes PHONICS/SPEAKING → openHomeworkModal, READING → router.push; Reading filter tab"
    - path: "frontend/app/teacher/homework/create/reading/page.tsx"
      provides: "ReadingCreationPage skeleton (client component) with: name input, empty activity list state, Add Activity stub button, Cancel button (router.push back), disabled Save placeholder, error display container"
  key_links:
    - from: "+ Create button"
      to: "TypePickerModal"
      via: "openCreate handler"
      pattern: "setShowTypePicker"
    - from: "TypePickerModal Reading card"
      to: "/teacher/homework/create/reading"
      via: "router.push"
      pattern: "router\\.push\\('/teacher/homework/create/reading'\\)"
---

<objective>
Deliver the second vertical slice: the unified-entry flow from D-02 (TypePickerModal) plus the skeleton of the Reading creation page (D-01, D-10). After this plan, a teacher can land on the empty creation page through the production entry point even though Save does not yet persist anything — the actual save endpoint and editors arrive in Plan 04.

Purpose: Decouples the routing/entry refactor from the heavy editor work. Plan 04 can then assume the page already exists and route in place.

Output: Working type-picker on the homework list page; standalone /create/reading route reachable; TYPE_META extended for downstream list/detail pages; no behavior regression for phonics/speaking flows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-teacher-dashboard/03-CONTEXT.md
@.planning/phases/03-teacher-dashboard/03-RESEARCH.md
@.planning/phases/03-teacher-dashboard/03-PATTERNS.md
@.planning/phases/03-teacher-dashboard/03-01-SUMMARY.md
@.planning/phases/03-teacher-dashboard/03-02-SUMMARY.md

<interfaces>
<!-- Line anchors in the existing file -->

frontend/app/teacher/homework/page.tsx:
- Lines 12-15: TYPE_META Record<HomeworkType, ...> — extend with READING
- Lines 163-183: type-selector buttons inside HomeworkModal (template for TypePickerModal)
- Lines 534-549: openCreate / openEdit handlers — split openCreate to open TypePickerModal
- Lines 562-585: filter tabs — add Reading tab
- Lines 683-685: Edit button per assignment card — for READING items, route to /[id]/edit (full edit wiring lands in Plan 06; for now this branch is fine to add)

frontend/lib/admin-api.ts:
- HomeworkType already includes 'READING' (Plan 01).

frontend/lib/colors.ts:
- gradients.primaryPurple, colors.primary, colors.textSecondary, colors.border — reuse for TypePickerModal style.

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add TYPE_META READING entry, READING filter tab, and TypePickerModal entry split</name>
  <files>frontend/app/teacher/homework/page.tsx</files>
  <read_first>
    - frontend/app/teacher/homework/page.tsx (entire file)
    - .planning/phases/03-teacher-dashboard/03-PATTERNS.md (`frontend/app/teacher/homework/page.tsx` section — TYPE_META extension, filter tab snippet, openCreate split snippet)
    - .planning/phases/03-teacher-dashboard/03-CONTEXT.md (D-01, D-02, D-04)
    - .planning/phases/03-teacher-dashboard/03-RESEARCH.md (Pitfall 1 — TYPE_META[h.type] cannot be undefined)
  </read_first>
  <action>
    Per D-02 / D-04 / TEACH-01:

    1. Extend `TYPE_META` (currently lines 12-15) with a new entry: `READING:  { label: 'Reading',  emoji: '📖', color: '#34D399', bg: '#34D39918' }` so `TYPE_META[h.type]` is defined for every value of HomeworkType (Pitfall 1).
    2. Extend filter tabs (currently lines 562-585) by adding `{ key: 'READING', label: '📖 Reading' }` to the tab list literal, keeping the same `as const` pattern and the same `typeFilter` state machine.
    3. Add a new state variable `const [showTypePicker, setShowTypePicker] = useState(false);` near the existing modal state (showModal, showAssign, etc.).
    4. Replace the body of `openCreate` (currently around line 534): instead of `setShowModal(true)`, do `setShowTypePicker(true)`. Do not change `openEdit` in this plan — the READING-edit branch on the Edit button is added in step 6.
    5. Add a new component `TypePickerModal` declared inline in this file (consistent with the existing HomeworkModal pattern of being declared in the same file). Component contract:
       - Props: `{ onClose: () => void; onPickInline: (type: 'PHONICS' | 'SPEAKING') => void; onPickReading: () => void; }`.
       - Renders three large card buttons in a 3-column grid (or stacked on narrow widths). Each card shows the emoji + label from TYPE_META, uses the same border-2 / rounded-xl / hover style as the existing type-selector buttons (lines 163-183 template). Inactive style: `background: 'white', color: m.color, borderColor: m.color + '55'`. Active hover style: scale or shadow only — no preset active state needed since it's a single-click dispatch.
       - Card 1 (Phonics) onClick: `onPickInline('PHONICS'); onClose();`.
       - Card 2 (Speaking) onClick: `onPickInline('SPEAKING'); onClose();`.
       - Card 3 (Reading) onClick: `onPickReading();`. (Do not close the modal here — the navigation will unmount the page; if closing is required for accessibility, call `onClose()` first.)
       - Backdrop: same pattern as HomeworkModal — fixed inset-0 black/30 with click-to-close.
    6. Render `{showTypePicker && <TypePickerModal onClose={() => setShowTypePicker(false)} onPickInline={(t) => { setForm({ ...emptyForm(), type: t }); setShowModal(true); }} onPickReading={() => router.push('/teacher/homework/create/reading')} />}` near where `<HomeworkModal />` is conditionally rendered. (Import `useRouter` from `next/navigation` if not already imported.)
    7. Modify the per-card Edit button (currently lines 683-685): wrap the onClick so that `h.type === 'READING' ? router.push(\`/teacher/homework/${h.id}/edit\`) : openEdit(h)`. This makes the button forward-compatible — even if no READING items exist yet (Plan 04 creates the first), the branch is harmless because the condition is type-gated.

    Do NOT modify the existing HomeworkModal's type selector (lines 163-183) — the inline selector remains for phonics/speaking edit mode. (Per D-02, phonics/speaking creation is via TypePickerModal → HomeworkModal preset to the chosen type; the type selector inside HomeworkModal therefore becomes mostly redundant for create, but is still useful for edit mode and as a safety net. Keep it.)
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npx next lint --dir app/teacher/homework --max-warnings 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "READING:\\s*\\{ label: 'Reading'" frontend/app/teacher/homework/page.tsx` returns 1.
    - `grep -c "'📖 Reading'" frontend/app/teacher/homework/page.tsx` returns 1.
    - `grep -c "showTypePicker" frontend/app/teacher/homework/page.tsx` returns at least 3 (state, setter call, conditional render).
    - `grep -c "function TypePickerModal" frontend/app/teacher/homework/page.tsx` returns 1.
    - `grep -c "router.push('/teacher/homework/create/reading')" frontend/app/teacher/homework/page.tsx` returns 1.
    - `grep -c "router.push(\`/teacher/homework/\\${h\\.id}/edit\`)" frontend/app/teacher/homework/page.tsx` returns 1.
    - `cd frontend && npx tsc --noEmit` exits 0.
    - When clicking + Create, the TypePickerModal appears with three cards (manual verification).
    - Clicking Phonics card opens the existing HomeworkModal preset to PHONICS; Speaking card opens HomeworkModal preset to SPEAKING; Reading card navigates the browser to /teacher/homework/create/reading.
  </acceptance_criteria>
  <done>Type picker entry point operational; TYPE_META extended; filter tab added; Edit-button READING branch wired; no regression for existing phonics/speaking create or edit flow.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold /teacher/homework/create/reading page (skeleton, no save yet)</name>
  <files>frontend/app/teacher/homework/create/reading/page.tsx</files>
  <read_first>
    - frontend/app/teacher/homework/page.tsx (existing HomeworkModal lines 19-405 for form pattern, lines 383-389 for error display, lines 390-403 for Save/Cancel button row)
    - frontend/lib/colors.ts (gradients, colors)
    - frontend/lib/admin-api.ts (lines 239-260 for HomeworkType / SentenceSegment etc. — needed for activity type union in local state)
    - .planning/phases/03-teacher-dashboard/03-PATTERNS.md (`frontend/app/teacher/homework/create/reading/page.tsx` section — page shell, form submission pattern, Save/Cancel row, error display)
    - .planning/phases/03-teacher-dashboard/03-CONTEXT.md (D-01, D-03, D-05, D-10; "Claude's Discretion": empty-state display)
  </read_first>
  <action>
    Create a NEW file at `frontend/app/teacher/homework/create/reading/page.tsx` as a client component. The page is a SKELETON in this plan — no save endpoint, no real editors. Plan 04 fills in matching, Plan 05 fills in fill-in-blank + DnD, Plan 06 adds edit-mode reuse.

    Required surface (D-01, D-03, D-05, D-10):

    1. `'use client'` directive at the top.
    2. Imports: `useState, useRef` from 'react'; `useRouter` from 'next/navigation'; `gradients, colors` from '@/lib/colors'; `ReadingActivityType, SentenceSegment` from '@/lib/admin-api' (types only).
    3. Define a local draft activity type at the top of the file (not exported): `type ReadingActivityDraft = { draftId: string; type: ReadingActivityType; pairs?: { imageUrl: string; word: string }[]; segments?: SentenceSegment[] };`. This shape is final — Plans 04/05 fill in the editor UIs that mutate this shape.
    4. Default-export an async-free React function component `ReadingCreationPage`. State:
       - `const [name, setName] = useState('');`
       - `const [activities, setActivities] = useState<ReadingActivityDraft[]>([]);`
       - `const [error, setError] = useState('');`
       - `const [loading, setLoading] = useState(false);`
       - `const nameRef = useRef<HTMLInputElement>(null);`
       - `const router = useRouter();`
    5. Layout: outer `<div className="min-h-screen bg-background">` then a `<div className="max-w-3xl mx-auto px-6 py-8">`. (The TeacherShell wrapper is already provided by the layout for `/teacher/*` routes — do NOT wrap in AuthGate; see RESEARCH.md anti-pattern note.)
    6. Header section: `<h1 className="text-2xl font-bold text-textPrimary mb-1">Create Reading Homework</h1>` and a subtitle `<p className="text-sm text-textSecondary mb-6">Compose matching and fill-in-blank activities in any order.</p>`.
    7. Name input (D-10 required):
       - Label `<label className="block text-sm font-semibold mb-1.5">Name <span className="text-highlight">*</span></label>`.
       - Input `<input ref={nameRef} value={name} onChange={(e) => setName(e.target.value)} className="w-full px-4 py-2.5 rounded-xl border border-border bg-white text-sm focus:outline-none focus:ring-2 focus:ring-primary/30" placeholder="e.g. Animals Reading Set 1" />`.
    8. Activity list section: heading `Activities`. Below the heading:
       - If `activities.length === 0`: an empty-state placeholder card `<div className="border-2 border-dashed border-border rounded-2xl p-8 text-center text-sm text-textSecondary">No activities yet — click "+ Add Activity" below to start.</div>`.
       - Otherwise (no-op for now since activities array stays empty until Plan 04): a `<div className="space-y-3">` that maps activities and renders a placeholder card per activity with `draftId` as key. The card renders `{activity.type === 'MATCHING' ? 'Matching activity (editor coming in Plan 04)' : 'Fill-in-blank activity (editor coming in Plan 05)'}`. This stub does NOT need real editors yet.
    9. Add Activity button row: two buttons side-by-side `+ Add Matching` and `+ Add Fill-in-Blank`. Each button onClick: append a new draft with `draftId: crypto.randomUUID()` and the corresponding `type`, initializing `pairs: []` or `segments: []` respectively. Style: secondary outlined buttons matching existing add-row buttons in HomeworkModal.
    10. Error banner (template from HomeworkModal lines 383-389): `{error && <div className="text-sm bg-highlight/8 border border-highlight/25 text-highlight px-4 py-3 rounded-xl mb-4">{error}</div>}`.
    11. Action row (template from HomeworkModal lines 390-403):
        - Cancel button: `<button type="button" onClick={() => router.push('/teacher/homework')} className="flex-1 py-2.5 rounded-xl text-sm font-semibold text-textSecondary border border-border hover:bg-gray-50">Cancel</button>`.
        - Save button STUB (disabled until Plan 04 wires the API): `<button type="button" disabled className="flex-1 py-2.5 rounded-xl text-sm font-bold text-white opacity-60" style={{ background: gradients.primarySecondary }}>Save (coming in Plan 04)</button>`. When Plan 04 implements save, this button's disabled / onClick / label changes — do not pre-implement an inert handler that sets `loading` since it would be dead code.
        - Use `setName`, `setError`, `setLoading`, `nameRef` as the named placeholders for Plan 04's `handleSubmit`. The unused-variable warning is acceptable; alternatively prefix with underscore or wire them into a noop `handleSubmit` that only validates name (`if (!name.trim()) { setError('Homework name is required.'); nameRef.current?.focus(); return; }`) and set the button onClick to that noop. Prefer the noop approach so the variables are not unused.
    12. The Try/Preview button (D-05) is added in Plan 06 alongside the actual save flow — not in this skeleton plan.

    File must compile with no TypeScript errors and no ESLint errors. The page must render at `/teacher/homework/create/reading` when a teacher is authenticated (the layout already enforces auth).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npx next lint --dir app/teacher/homework/create --max-warnings 0</automated>
  </verify>
  <acceptance_criteria>
    - `test -f frontend/app/teacher/homework/create/reading/page.tsx` (file exists).
    - `grep -c "'use client'" frontend/app/teacher/homework/create/reading/page.tsx` returns 1.
    - `grep -c "export default function ReadingCreationPage" frontend/app/teacher/homework/create/reading/page.tsx` returns 1.
    - `grep -c "ReadingActivityDraft" frontend/app/teacher/homework/create/reading/page.tsx` returns at least 2 (type definition + usage in useState).
    - `grep -c "crypto.randomUUID" frontend/app/teacher/homework/create/reading/page.tsx` returns at least 1.
    - `grep -c "+ Add Matching\\|Add Fill-in-Blank" frontend/app/teacher/homework/create/reading/page.tsx` returns at least 1.
    - `grep -c "No activities yet" frontend/app/teacher/homework/create/reading/page.tsx` returns 1.
    - `cd frontend && npx tsc --noEmit` exits 0.
    - `cd frontend && npx next lint --dir app/teacher/homework/create --max-warnings 0` exits 0.
    - Navigating to /teacher/homework/create/reading after logging in renders the page with the name input, the empty-state card, the two Add Activity buttons, and a disabled Save button (manual verification).
  </acceptance_criteria>
  <done>Skeleton page exists at the production route, ready for Plan 04 to fill in the matching editor and save logic; lint and tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Teacher browser → app routes | Existing teacher layout enforces AuthGate for `/teacher/*`; new page inherits |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-09 | Spoofing | /teacher/homework/create/reading route | mitigate | Route lives under `app/teacher/` which is already wrapped by TeacherShell + AuthGate at the layout level (existing). New page does not add its own AuthGate — RESEARCH.md anti-pattern: pages should not wrap themselves; the layout handles auth. |
| T-03-10 | Tampering | Local draft state on the creation page | accept | All draft mutations are client-side; no persistence in this plan. Plan 04 adds backend validation when save lands. |
| T-03-11 | Spoofing | TypePickerModal navigates without parameters | accept | Anonymous users would already be redirected by AuthGate before reaching the homework page; the new route is no riskier than `/teacher/homework`. |
</threat_model>

<verification>
- TYPE_META has 3 entries; filter tabs has 4 entries (ALL + PHONICS + SPEAKING + READING).
- TypePickerModal renders three cards and dispatches correctly to HomeworkModal vs router.push.
- /teacher/homework/create/reading is reachable and renders the empty-state page.
- No regression in existing PHONICS/SPEAKING create or edit flow.
</verification>

<success_criteria>
1. Clicking + Create opens the TypePickerModal (three cards, no prior crash).
2. Reading card → URL changes to /teacher/homework/create/reading.
3. The new page renders with name input, empty-state card, two Add Activity buttons.
4. tsc and next lint exit 0.
</success_criteria>

<output>
After completion, create `.planning/phases/03-teacher-dashboard/03-03-SUMMARY.md` describing: where TypePickerModal lives in page.tsx, the exact ReadingActivityDraft type signature (so Plans 04 and 05 do not redefine it differently), and any layout deviations from the patterns file.
</output>
