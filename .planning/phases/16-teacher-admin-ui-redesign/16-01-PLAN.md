---
phase: "16-teacher-admin-ui-redesign"
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/lib/colors.ts
  - frontend/lib/theme.ts
  - frontend/components/TeacherShell.tsx
  - frontend/components/AdminShell.tsx
  - frontend/app/teacher/homework/_components/ListenCreationPage.tsx
  - frontend/app/teacher/homework/_components/ReadingCreationPage.tsx
  - frontend/app/teacher/homework/create/page.tsx
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-07

must_haves:
  truths:
    - "TeacherShell sidebar background is white (#FFFFFF), not dark (#0C1220)"
    - "TeacherShell active nav item has blue (#3B82F6) background tint (#EFF6FF) and blue text (#1D4ED8)"
    - "AdminShell sidebar background is white (#FFFFFF), not dark (#0C1220)"
    - "AdminShell active nav item has indigo (#6366F1) background tint (#EEF2FF)"
    - "No #F0623A orange literal exists in TeacherShell.tsx, AdminShell.tsx, or teacher homework sub-files"
    - "colors.ts exports teacherAccent as #3B82F6, adminAccent as #6366F1, teacherAccentBg as #EFF6FF, adminAccentBg as #EEF2FF"
    - "theme.ts teacherTheme.primary.main is #3B82F6, adminTheme.primary.main is #6366F1"
  artifacts:
    - path: "frontend/lib/colors.ts"
      provides: "Updated color constants"
      contains: "teacherAccent: '#3B82F6'"
    - path: "frontend/lib/theme.ts"
      provides: "Updated MUI theme palettes"
      contains: "primary: { main: '#3B82F6'"
    - path: "frontend/components/TeacherShell.tsx"
      provides: "Light sidebar teacher shell"
      contains: "bgcolor: '#FFFFFF'"
    - path: "frontend/components/AdminShell.tsx"
      provides: "Light sidebar admin shell"
      contains: "bgcolor: '#FFFFFF'"
  key_links:
    - from: "frontend/lib/colors.ts"
      to: "frontend/app/teacher/page.tsx"
      via: "import { colors } from '@/lib/colors'"
      pattern: "colors\\.teacherAccent"
    - from: "frontend/lib/theme.ts"
      to: "frontend/app/teacher/layout.tsx"
      via: "ThemeProvider theme={teacherTheme}"
      pattern: "teacherTheme"
---

<objective>
Establish the color foundation and redesign both shell sidebars from dark to light.

Purpose: All subsequent plans (16-02, 16-03, 16-04) depend on the color system being correct first. The ThemeProvider cascade in teacher/layout.tsx and admin/layout.tsx means updating theme.ts primary.main automatically applies the new accent to all contained buttons sitewide — this must land before page-level work.

Output: colors.ts with 4 new/updated exports, theme.ts with correct palette mains, TeacherShell.tsx with white sidebar, AdminShell.tsx with white sidebar, and 3 teacher-portal sub-files with orange literals replaced.
</objective>

<execution_context>
@C:\Users\hungnn\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\hungnn\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\sources\katie-english\.planning\PROJECT.md
@C:\sources\katie-english\.planning\ROADMAP.md
@C:\sources\katie-english\.planning\phases\16-teacher-admin-ui-redesign\16-CONTEXT.md
@C:\sources\katie-english\.planning\phases\16-teacher-admin-ui-redesign\16-RESEARCH.md

<interfaces>
From frontend/lib/colors.ts (current state):
```typescript
export const colors = {
  teacherAccent: '#F0623A',   // CHANGE TO '#3B82F6'
  // other entries unchanged
} as const;
// gradients.sidebar is NOT used by TeacherShell — do not update it
```

From frontend/lib/theme.ts (current state):
```typescript
export const teacherTheme = createTheme(baseTheme, {
  palette: { primary: { main: '#F0623A', contrastText: '#ffffff' } }, // CHANGE main to '#3B82F6'
});
export const adminTheme = createTheme(baseTheme, {
  palette: { primary: { main: '#4F9DFF', contrastText: '#ffffff' } }, // CHANGE main to '#6366F1'
});
```

From frontend/components/TeacherShell.tsx (current constants to replace):
```typescript
const ACCENT = '#F0623A';           // → '#3B82F6'
const ACCENT_BG = 'rgba(240, 98, 58, 0.12)';  // → '#EFF6FF'
const ACCENT_TEXT = '#FDA087';      // → '#1D4ED8'
// Sidebar Box sx: bgcolor: '#0C1220' → '#FFFFFF'
// Sidebar Box sx: boxShadow: '1px 0 0 rgba(255,255,255,0.05)' → '1px 0 0 #E2E8F0'
```

From frontend/components/AdminShell.tsx (current constants to replace):
```typescript
const ACCENT = '#4F9DFF';           // → '#6366F1'
const ACCENT_BG = 'rgba(79,157,255,0.12)';    // → '#EEF2FF'
const ACCENT_TEXT = '#60A5FA';      // → '#818CF8'
// Sidebar Box sx: bgcolor: '#0C1220' → '#FFFFFF'
// Sidebar Box sx: boxShadow: '1px 0 0 rgba(255,255,255,0.05)' → '1px 0 0 #E2E8F0'
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update colors.ts and theme.ts — color system foundation (per D-07, D-02, D-06)</name>
  <read_first>
    frontend/lib/colors.ts
    frontend/lib/theme.ts
  </read_first>
  <files>frontend/lib/colors.ts, frontend/lib/theme.ts</files>
  <action>
In frontend/lib/colors.ts:
- Change `teacherAccent: '#F0623A'` to `teacherAccent: '#3B82F6'`
- Add `adminAccent: '#6366F1',` after teacherAccent
- Add `teacherAccentBg: '#EFF6FF',` after adminAccent
- Add `adminAccentBg: '#EEF2FF',` after teacherAccentBg
- Leave all other entries (primary, secondary, accent, gradients, cardGradients, etc.) unchanged
- Do NOT update gradients.sidebar — it is unused by the shells (confirmed by research Pitfall 6)

In frontend/lib/theme.ts:
- Change teacherTheme palette.primary.main from '#F0623A' to '#3B82F6'
- Change adminTheme palette.primary.main from '#4F9DFF' to '#6366F1'
- Leave baseTheme, keyframes, and all other theme entries unchanged

After saving, run: cd frontend && npm run build

The ThemeProvider cascade: teacher/layout.tsx wraps all teacher pages in teacherTheme, so this change automatically updates all Button variant="contained" across teacher portal to blue. This is intentional.
  </action>
  <verify>
    <automated>cd C:\sources\katie-english\frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
- frontend/lib/colors.ts contains `teacherAccent: '#3B82F6'` (not '#F0623A')
- frontend/lib/colors.ts contains `adminAccent: '#6366F1'`
- frontend/lib/colors.ts contains `teacherAccentBg: '#EFF6FF'`
- frontend/lib/colors.ts contains `adminAccentBg: '#EEF2FF'`
- frontend/lib/theme.ts teacherTheme primary.main is '#3B82F6'
- frontend/lib/theme.ts adminTheme primary.main is '#6366F1'
- npm run build exits 0 with no TypeScript errors
  </acceptance_criteria>
  <done>colors.ts has 4 updated/new color entries; theme.ts has both themes updated; build passes</done>
</task>

<task type="auto">
  <name>Task 2: Redesign TeacherShell sidebar — dark to light + blue accent (per D-01, D-02)</name>
  <read_first>
    frontend/components/TeacherShell.tsx
  </read_first>
  <files>frontend/components/TeacherShell.tsx</files>
  <action>
At the top of the file, update the three color constants:
- `const ACCENT = '#F0623A'` → `const ACCENT = '#3B82F6'`
- `const ACCENT_BG = 'rgba(240, 98, 58, 0.12)'` → `const ACCENT_BG = '#EFF6FF'`
- `const ACCENT_TEXT = '#FDA087'` → `const ACCENT_TEXT = '#1D4ED8'`

Update the outer sidebar Box (line ~99): change
  `bgcolor: '#0C1220', boxShadow: '1px 0 0 rgba(255,255,255,0.05)'`
to
  `bgcolor: '#FFFFFF', boxShadow: '1px 0 0 #E2E8F0'`

Update Logo section: change logo text color and subtitle text:
- `color: 'white'` (on "Katie English" Typography) → `color: '#0F172A'`
- `color: '#64748B'` (on "Teacher Portal" Typography) → keep as-is (already correct for light)

Update nav group label Typography:
- `color: '#475569'` → `color: '#6B7280'`

Update ListItemButton sx for each nav item:
- `'&:not(.Mui-selected)': { color: '#94A3B8' }` → `'&:not(.Mui-selected)': { color: '#374151' }`
- `'&:not(.Mui-selected):hover': { bgcolor: 'rgba(255,255,255,0.05)', color: '#E2E8F0' }` → `'&:not(.Mui-selected):hover': { bgcolor: '#F1F5F9', color: '#1F2937' }`
- The `'&.Mui-selected'` and `'&.Mui-selected:hover'` already reference ACCENT_BG and ACCENT_TEXT variables — no direct value change needed there, the constants above handle it

Update sidebar footer Divider:
- `borderColor: 'rgba(255,255,255,0.07)'` → `borderColor: '#E2E8F0'`

Update footer copyright Typography:
- `color: '#475569'` → `color: '#94A3B8'`

All ACCENT usages in the main header (user avatar IconButton bgcolor, Menu user avatar Box bgcolor, change-password Button bgcolor) now correctly read '#3B82F6' because ACCENT constant is updated — no separate changes needed for those.

Run: cd frontend && npm run build
  </action>
  <verify>
    <automated>cd C:\sources\katie-english\frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
- TeacherShell.tsx line for sidebar outer Box contains `bgcolor: '#FFFFFF'`
- TeacherShell.tsx contains `const ACCENT = '#3B82F6'` (not '#F0623A')
- TeacherShell.tsx contains `const ACCENT_BG = '#EFF6FF'`
- TeacherShell.tsx contains `const ACCENT_TEXT = '#1D4ED8'`
- TeacherShell.tsx inactive nav color is '#374151' (not '#94A3B8')
- TeacherShell.tsx inactive hover bgcolor is '#F1F5F9' (not 'rgba(255,255,255,0.05)')
- Sidebar footer Divider borderColor is '#E2E8F0' (not 'rgba(255,255,255,0.07)')
- npm run build exits 0
  </acceptance_criteria>
  <done>TeacherShell sidebar is fully light: white bg, blue accent, dark-on-light text, build passes</done>
</task>

<task type="auto">
  <name>Task 3: Redesign AdminShell sidebar + fix raw #F0623A in teacher homework sub-files (per D-06, D-02, UI-07)</name>
  <read_first>
    frontend/components/AdminShell.tsx
    frontend/app/teacher/homework/_components/ListenCreationPage.tsx
    frontend/app/teacher/homework/_components/ReadingCreationPage.tsx
    frontend/app/teacher/homework/create/page.tsx
  </read_first>
  <files>
    frontend/components/AdminShell.tsx,
    frontend/app/teacher/homework/_components/ListenCreationPage.tsx,
    frontend/app/teacher/homework/_components/ReadingCreationPage.tsx,
    frontend/app/teacher/homework/create/page.tsx
  </files>
  <action>
In frontend/components/AdminShell.tsx:

Update the three constants at the top:
- `const ACCENT = '#4F9DFF'` → `const ACCENT = '#6366F1'`
- `const ACCENT_BG = 'rgba(79,157,255,0.12)'` → `const ACCENT_BG = '#EEF2FF'`
- `const ACCENT_TEXT = '#60A5FA'` → `const ACCENT_TEXT = '#818CF8'`

Update the sidebar outer Box:
- `bgcolor: '#0C1220'` → `bgcolor: '#FFFFFF'`
- `boxShadow: '1px 0 0 rgba(255,255,255,0.05)'` → `boxShadow: '1px 0 0 #E2E8F0'`

Update logo text colors:
- `color: 'white'` (on "Katie English" Typography) → `color: '#0F172A'`
- `color: '#475569'` (on "Admin Portal" Typography) → `color: '#6B7280'`

Update nav group label Typography:
- `color: '#475569'` → `color: '#6B7280'`

Update nav ListItemButton inactive states:
- `'&:not(.Mui-selected)': { color: '#94A3B8' }` → `'&:not(.Mui-selected)': { color: '#374151' }`
- `'&:not(.Mui-selected):hover': { bgcolor: 'rgba(255,255,255,0.05)', color: '#E2E8F0' }` → `'&:not(.Mui-selected):hover': { bgcolor: '#F1F5F9', color: '#1F2937' }`

Update sidebar footer:
- Divider `borderColor: 'rgba(255,255,255,0.07)'` → `borderColor: '#E2E8F0'`
- Footer Typography `color: '#475569'` → `color: '#94A3B8'`

AdminShell has no change-password form — user avatar and menu ACCENT usages are handled by the constant update above.

---

In frontend/app/teacher/homework/_components/ListenCreationPage.tsx:
- Line 533: `bgcolor: '#F0623A'` → `bgcolor: '#3B82F6'`
- Line 534: `'&:hover': { bgcolor: '#F0623A', opacity: 0.9 }` → `'&:hover': { bgcolor: '#3B82F6', opacity: 0.9 }`

In frontend/app/teacher/homework/_components/ReadingCreationPage.tsx:
- Line 666: `bgcolor: '#F0623A', '&:hover': { bgcolor: '#F0623A', opacity: 0.9 }` → `bgcolor: '#3B82F6', '&:hover': { bgcolor: '#3B82F6', opacity: 0.9 }`

In frontend/app/teacher/homework/create/page.tsx:
- Line 167: `bgcolor: '#F0623A', '&:hover': { bgcolor: '#F0623A', opacity: 0.9 }` → `bgcolor: '#3B82F6', '&:hover': { bgcolor: '#3B82F6', opacity: 0.9 }`
- Line 181: `bgcolor: '#F0623A', '&:hover': { bgcolor: '#F0623A', opacity: 0.9 }` → `bgcolor: '#3B82F6', '&:hover': { bgcolor: '#3B82F6', opacity: 0.9 }`

Run: cd frontend && npm run build
Then verify no orange remains:
grep -r "F0623A" frontend/app/teacher frontend/components/TeacherShell.tsx frontend/components/AdminShell.tsx

Do NOT change frontend/app/login/page.tsx, frontend/app/not-found.tsx, or frontend/app/403/page.tsx — those are out of scope per D-08.
  </action>
  <verify>
    <automated>cd C:\sources\katie-english\frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
- AdminShell.tsx sidebar outer Box contains `bgcolor: '#FFFFFF'`
- AdminShell.tsx contains `const ACCENT = '#6366F1'`
- AdminShell.tsx contains `const ACCENT_BG = '#EEF2FF'`
- AdminShell.tsx inactive nav color is '#374151'
- frontend/app/teacher/homework/_components/ListenCreationPage.tsx contains '#3B82F6' where '#F0623A' was
- frontend/app/teacher/homework/_components/ReadingCreationPage.tsx contains '#3B82F6' where '#F0623A' was
- frontend/app/teacher/homework/create/page.tsx contains '#3B82F6' where '#F0623A' was (both lines)
- grep -r "F0623A" frontend/app/teacher frontend/components/TeacherShell.tsx frontend/components/AdminShell.tsx returns 0 matches
- npm run build exits 0
  </acceptance_criteria>
  <done>AdminShell is light sidebar with indigo accent; all teacher-portal orange literals replaced; no #F0623A in teacher/admin shell files</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | This plan modifies only CSS constants and inline sx style values — no auth, no data, no API surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01-SC | Tampering | npm/pip/cargo installs | accept | No packages installed in this phase |
</threat_model>

<verification>
After all 3 tasks complete:

1. Build gate: `cd frontend && npm run build` exits 0
2. Orange-free gate: `grep -r "F0623A" frontend/app/teacher frontend/components/TeacherShell.tsx frontend/components/AdminShell.tsx` returns 0 lines
3. Color constants gate: grep colors.ts for 'teacherAccent' shows '#3B82F6'; grep theme.ts for 'teacherTheme' primary.main shows '#3B82F6'
4. Sidebar gate (visual): visit /teacher and /admin — both sidebars show white background, blue/indigo active state
</verification>

<success_criteria>
- colors.ts has teacherAccent=#3B82F6, adminAccent=#6366F1, teacherAccentBg=#EFF6FF, adminAccentBg=#EEF2FF
- theme.ts teacherTheme.primary.main=#3B82F6, adminTheme.primary.main=#6366F1
- TeacherShell sidebar bgcolor=#FFFFFF, active item uses #EFF6FF bg and #1D4ED8 text
- AdminShell sidebar bgcolor=#FFFFFF, active item uses #EEF2FF bg and #818CF8 text
- Zero #F0623A in teacher portal files and both shell components
- npm run build exits 0
</success_criteria>

<output>
Create `.planning/phases/16-teacher-admin-ui-redesign/16-01-SUMMARY.md` when done
</output>
