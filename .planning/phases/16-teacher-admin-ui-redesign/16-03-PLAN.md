---
phase: "16-teacher-admin-ui-redesign"
plan: 3
type: execute
wave: 2
depends_on:
  - "16-01"
files_modified:
  - frontend/app/teacher/homework/page.tsx
  - frontend/app/teacher/classes/page.tsx
  - frontend/app/teacher/students/page.tsx
  - frontend/app/teacher/sessions/page.tsx
autonomous: true
requirements:
  - UI-03
  - UI-05

must_haves:
  truths:
    - "Homework page shows a CSS grid of cards (3 cols desktop) instead of pure TableShell"
    - "Each homework card shows: HwTypeChip, homework name, class names, due date (red if overdue), LinearProgress bar, action icons"
    - "Filter tabs are pill-style with transparent/blue-50 active state and count badge"
    - "A view toggle (grid icon / table icon) allows switching between grid and table views"
    - "Table view still works using the existing TableShell render (no regression)"
    - "No #F0623A remains in this file; all ACCENT usages read from colors.teacherAccent (now #3B82F6)"
  artifacts:
    - path: "frontend/app/teacher/homework/page.tsx"
      provides: "Homework card grid + pill filter tabs + view toggle"
      contains: "display: 'grid'"
  key_links:
    - from: "frontend/app/teacher/homework/page.tsx"
      to: "frontend/components/ui/HwTypeChip.tsx"
      via: "HwTypeChip component"
      pattern: "HwTypeChip"
    - from: "frontend/app/teacher/homework/page.tsx"
      to: "@mui/material/LinearProgress"
      via: "import LinearProgress from '@mui/material/LinearProgress'"
      pattern: "LinearProgress"
---

<objective>
Replace the homework page TableShell render with a card-grid layout and update filter tabs to pill style.

Purpose: The homework library is the most-visited teacher page. A card grid (inspired by HeyWordVocab) makes homework browsable at a glance. The existing table view is preserved as a fallback behind a toggle.

Output: Updated app/teacher/homework/page.tsx with:
- `const ACCENT = colors.teacherAccent` (sourced from colors.ts, no raw orange)
- LinearProgress import added
- View toggle state (grid | table), defaulting to grid
- Inline HwCard component (replacing the TableShellRow render)
- Pill filter tabs with count badges
- TableShell render preserved for table view
</objective>

<execution_context>
@C:\Users\hungnn\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\hungnn\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\sources\katie-english\.planning\PROJECT.md
@C:\sources\katie-english\.planning\phases\16-teacher-admin-ui-redesign\16-CONTEXT.md
@C:\sources\katie-english\.planning\phases\16-teacher-admin-ui-redesign\16-RESEARCH.md
@C:\sources\katie-english\.planning\phases\16-teacher-admin-ui-redesign\16-01-SUMMARY.md

<interfaces>
From frontend/app/teacher/homework/page.tsx (current state — critical details):

Imports already present:
- LinearProgress NOT imported yet — must add: `import LinearProgress from '@mui/material/LinearProgress';`
- Card NOT imported — must add: `import Card from '@mui/material/Card';`
- Divider NOT imported — must add: `import Divider from '@mui/material/Divider';`
- LayoutGrid, List (lucide) NOT imported — must add for view toggle icons

Current ACCENT declaration (line 33):
  `const ACCENT = '#F0623A';`
  Change to: `const ACCENT = colors.teacherAccent;`
  Note: colors is already imported on line 11: `import { colors } from '@/lib/colors';`

Existing computed values per homework item (lines 768-792):
  - submittedCount: number (deduplicated student IDs)
  - totalEnrolled: number (sum of students across active assignments)
  - classNames: string (comma-joined unique class names, '—' if none)
  - nearestDue: Date | null
  - dueText: string ('Overdue' | date string | '—')
  - isOverdue: boolean
  - hwName: string (h.name fallback to speakingText slice fallback to meta.label)

Existing action callbacks (for use in card action buttons):
  - setAssigningHw(h) → opens AssignModal (assign icon: CheckCircle2, color '#059669')
  - openEdit(h) / router.push for READING/VOCABULARY/LISTEN (edit icon: Pencil, color ACCENT)
  - Link href={`/teacher/homework/${h.id}/try`} (preview icon: Eye, color '#A855F7')
  - setDeletingId(h.id) (delete icon: Trash2, color 'error.main')
  - deletingId === h.id triggers inline delete confirmation (No button + Yes button)

Current filter tab render (lines 715-735):
  Button variant="outlined" per tab, replace with pill pattern.

HwTypeChip component signature (from frontend/components/ui/HwTypeChip.tsx):
  `export default function HwTypeChip({ type }: { type: 'PHONICS' | 'SPEAKING' | 'VOCABULARY' | 'LISTEN' | 'READING' })`
  Do NOT change HwTypeChip colors — per-type colors are intentional (D-03).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix ACCENT constant, add missing imports, update filter tabs to pill style (per D-02, D-04)</name>
  <read_first>
    frontend/app/teacher/homework/page.tsx
  </read_first>
  <files>frontend/app/teacher/homework/page.tsx</files>
  <action>
In frontend/app/teacher/homework/page.tsx:

1. Fix the ACCENT constant (line 33):
   Change `const ACCENT = '#F0623A';` to `const ACCENT = colors.teacherAccent;`
   (colors is already imported — no new import needed)

2. Add missing imports at the top of the file (after existing imports):
   - `import LinearProgress from '@mui/material/LinearProgress';`
   - `import Card from '@mui/material/Card';`
   - `import Divider from '@mui/material/Divider';`
   - Add `LayoutGrid, List as ListIcon` to the lucide-react import line (for view toggle)
   The existing lucide import is: `import { Plus, X, Loader2, Mic, Hash, BookOpen, ImageIcon, Search, CheckCircle2, Headphones, Pencil, Trash2, Eye } from 'lucide-react';`
   Append `, LayoutGrid, List as ListIcon` to that destructure.

3. Add view toggle state in the HomeworkPage component function body (after existing useState declarations):
   `const [viewMode, setViewMode] = useState<'grid' | 'table'>('grid');`

4. Update filter tabs (the Button group inside the action row, lines ~715-735):
   Replace the current `Button variant="outlined"` pattern with pill buttons:

   Each Button: remove `variant="outlined"`, add `border: 'none'`, change `borderRadius` to `'999px'`, change `px` to `1.75`, `py` to `0.5`.
   Active state: `bgcolor: '#EFF6FF', color: '#3B82F6'` (no border)
   Inactive state: `bgcolor: 'transparent', color: '#64748B', '&:hover': { bgcolor: '#F1F5F9' }`

   Count badge: the existing `<Box component="span" sx={{ ml: 0.5, opacity: 0.6 }}>({counts[t.key]})</Box>` replace with:
   ```
   <Box component="span" sx={{
     ml: 0.75, px: 0.75, py: 0.125, borderRadius: '999px',
     fontSize: 10, fontWeight: 700,
     bgcolor: typeFilter === t.key ? '#DBEAFE' : '#F1F5F9',
     color: typeFilter === t.key ? '#3B82F6' : '#94A3B8',
   }}>{counts[t.key]}</Box>
   ```

5. Add view toggle buttons in the action row toolbar, to the right of the filter pill group and left of the "Create Homework" button:
   ```
   <Box sx={{ display: 'flex', gap: 0.5, ml: 1 }}>
     <IconButton size="small" onClick={() => setViewMode('grid')}
       sx={{ borderRadius: 1.5, color: viewMode === 'grid' ? '#3B82F6' : '#94A3B8',
             bgcolor: viewMode === 'grid' ? '#EFF6FF' : 'transparent', width: 32, height: 32 }}>
       <LayoutGrid size={15} />
     </IconButton>
     <IconButton size="small" onClick={() => setViewMode('table')}
       sx={{ borderRadius: 1.5, color: viewMode === 'table' ? '#3B82F6' : '#94A3B8',
             bgcolor: viewMode === 'table' ? '#EFF6FF' : 'transparent', width: 32, height: 32 }}>
       <ListIcon size={15} />
     </IconButton>
   </Box>
   ```

Run: cd frontend && npm run build
  </action>
  <verify>
    <automated>cd C:\sources\katie-english\frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
  - homework/page.tsx line 33 (ACCENT) reads `const ACCENT = colors.teacherAccent;` not '#F0623A'
  - LinearProgress, Card, Divider are imported
  - LayoutGrid and List (as ListIcon) are imported from lucide-react
  - viewMode state is declared with default 'grid'
  - Filter tab buttons use borderRadius '999px' and border: 'none'
  - Active tab has bgcolor '#EFF6FF' and color '#3B82F6'
  - Count badge is a rounded Box with '#DBEAFE' / '#F1F5F9' conditional background
  - View toggle icons (LayoutGrid, ListIcon) appear in toolbar
  - npm run build exits 0
  </acceptance_criteria>
  <done>ACCENT is sourced from colors.ts (now blue); imports added; pill tabs and view toggle in place; build passes</done>
</task>

<task type="auto">
  <name>Task 2: Implement HwCard grid component and wire conditional render (per D-03)</name>
  <read_first>
    frontend/app/teacher/homework/page.tsx
  </read_first>
  <files>frontend/app/teacher/homework/page.tsx</files>
  <action>
In frontend/app/teacher/homework/page.tsx, add an inline HwCard component and replace the TableShell render with a conditional grid/table toggle.

Add the HwCard function component above the HomeworkPage export (after AssignModal function, around line 629):

HwCard receives these props:
```
interface HwCardProps {
  h: HomeworkItem;
  now: Date;
  deletingId: number | null;
  onAssign: () => void;
  onEdit: () => void;
  onDelete: () => void;
  onDeleteCancel: () => void;
  onDeleteConfirm: () => Promise<void>;
}
```

HwCard implementation structure:
- Compute per-card derived values inside HwCard:
  - submittedCount (same logic as current table: deduplicated studentIds via sessions)
  - totalEnrolled (sum of students across all assignment classes)
  - classNames (unique class names joined, '—' if none)
  - nearestDue, dueText, isOverdue (same logic as current table)
  - hwName (h.name fallback to speakingText slice fallback to meta.label via TYPE_META[h.type])

- Outer Card sx:
  ```
  { borderRadius: '16px', border: '1px solid #E2E8F0', bgcolor: 'white',
    transition: 'all 0.2s', '&:hover': { boxShadow: '0 8px 24px rgba(15,23,42,0.12)', transform: 'translateY(-1px)' } }
  ```

- Card header Box (px 2.5, pt 2.5, pb 1.5, flex row, justifyContent space-between, alignItems center):
  - Left: `<HwTypeChip type={h.type as ...} />`
  - Right (only if isOverdue): `<Chip label="Overdue" size="small" sx={{ bgcolor: '#FEE2E2', color: '#DC2626', fontWeight: 700, fontSize: 10 }} />`

- Title section Box (px 2.5, pb 1.5):
  - hwName Typography: fontWeight 700, fontSize 15, color '#0F172A', lineHeight 1.3
  - classNames Typography: fontSize 12, color '#64748B', mt 0.5

- Divider (mx: 2.5)

- Footer Box (px 2.5, py 1.5):
  - Row 1 (flex, justifyContent space-between, mb 0.75):
    - Left Typography: fontSize 11, color '#64748B': "{submittedCount}/{totalEnrolled} submitted"
    - Right Typography: fontSize 11, color isOverdue ? '#DC2626' : '#64748B': dueText
  - LinearProgress: variant="determinate", value={totalEnrolled > 0 ? (submittedCount/totalEnrolled)*100 : 0},
    sx={{ height: 6, borderRadius: 99, bgcolor: '#E2E8F0', '& .MuiLinearProgress-bar': { bgcolor: '#3B82F6', borderRadius: 99 } }}
  - Actions row (flex, justifyContent flex-end, gap 0.25, mt 1.5):
    - If deletingId === h.id: show inline "Delete?" confirm with No button (color text.secondary) and Yes button (bgcolor error.main)
    - Else: show 4 IconButtons (assign CheckCircle2 #059669, edit Pencil ACCENT, preview Eye #A855F7, delete Trash2 error.main)
    - Preview IconButton uses `component={Link} href={'/teacher/homework/' + h.id + '/try'}` so import Link must be available

In HomeworkPage, replace the current conditional render block (the block starting with `{list.length === 0 ? (` through the closing `}`) with:

```
{list.length === 0 ? (
  <Box sx={{ textAlign: 'center', py: 10, ... }}> ... empty state ... </Box>
) : viewMode === 'grid' ? (
  filtered.length === 0 ? (
    <Box sx={{ textAlign: 'center', py: 6, ... }}> ... no filter match ... </Box>
  ) : (
    <Box sx={{
      display: 'grid',
      gridTemplateColumns: { xs: '1fr', sm: 'repeat(2, 1fr)', lg: 'repeat(3, 1fr)' },
      gap: '18px',
    }}>
      {filtered.map((h) => (
        <HwCard
          key={h.id}
          h={h}
          now={now}
          deletingId={deletingId}
          onAssign={() => setAssigningHw(h)}
          onEdit={() => {
            if (h.type === 'READING') router.push(`/teacher/homework/${h.id}/edit`);
            else if (h.type === 'VOCABULARY') router.push(`/teacher/homework/${h.id}/edit/vocabulary`);
            else if (h.type === 'LISTEN') router.push(`/teacher/homework/${h.id}/edit/listen`);
            else openEdit(h);
          }}
          onDelete={() => setDeletingId(h.id)}
          onDeleteCancel={() => setDeletingId(null)}
          onDeleteConfirm={async () => {
            try { await deleteHomework(h.id); setDeletingId(null); load(); showToast('Deleted.', 'success'); }
            catch (err) { setDeletingId(null); showToast(err instanceof Error ? err.message : 'Delete failed.', 'error'); }
          }}
        />
      ))}
    </Box>
  )
) : (
  <TableShell columns={[...existing columns...]}>{...existing table rows...}</TableShell>
)}
```

Keep the existing TableShell + TableShellRow render block unchanged as the table-view branch. Move it under the `viewMode === 'table'` else branch.

The empty state Box (for list.length === 0) remains unchanged.

Run: cd frontend && npm run build
  </action>
  <verify>
    <automated>cd C:\sources\katie-english\frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
  - HwCard function component exists in homework/page.tsx above HomeworkPage export
  - HomeworkPage render conditionally shows grid or table based on viewMode state
  - Grid view uses `display: 'grid'` with `gridTemplateColumns` responsive values (1fr / repeat(2,1fr) / repeat(3,1fr))
  - Each HwCard has LinearProgress with blue bar (bgcolor '#3B82F6' in MuiLinearProgress-bar)
  - HwCard footer shows "{N}/{M} submitted" text
  - Table view (viewMode === 'table') still renders TableShell correctly
  - grep -n "F0623A" frontend/app/teacher/homework/page.tsx returns 0 matches
  - npm run build exits 0
  </acceptance_criteria>
  <done>Card grid renders homework with per-card progress, actions, and type chip; table view preserved; build passes; no orange literals</done>
</task>

<task type="auto">
  <name>Task 3: Replace #FFF2EF accent tint with blue-50 in teacher/classes, teacher/students, teacher/sessions (per UI-07, D-02)</name>
  <read_first>
    frontend/app/teacher/classes/page.tsx
    frontend/app/teacher/students/page.tsx
    frontend/app/teacher/sessions/page.tsx
  </read_first>
  <files>frontend/app/teacher/classes/page.tsx, frontend/app/teacher/students/page.tsx, frontend/app/teacher/sessions/page.tsx</files>
  <action>
These three teacher portal pages retain raw '#FFF2EF' (orange-50) background tint literals that must be replaced with '#EFF6FF' (blue-50) per D-02 (accent orange → blue, full palette).

In frontend/app/teacher/classes/page.tsx:
- Search for all occurrences of '#FFF2EF' (lines 169, 282, 287 per static analysis — verify by reading the file)
- Replace each occurrence with '#EFF6FF'
- Do not change any other color literals; only target #FFF2EF

In frontend/app/teacher/students/page.tsx:
- Search for all occurrences of '#FFF2EF'
- Replace each occurrence with '#EFF6FF'

In frontend/app/teacher/sessions/page.tsx:
- Search for all occurrences of '#FFF2EF'
- Replace each occurrence with '#EFF6FF'

After saving all three files:
Run: cd frontend && npm run build

Then verify no orange-50 tint remains:
grep -rn "FFF2EF" frontend/app/teacher/classes/page.tsx frontend/app/teacher/students/page.tsx frontend/app/teacher/sessions/page.tsx
  </action>
  <verify>
    <automated>cd C:\sources\katie-english\frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
  - grep -n "FFF2EF" frontend/app/teacher/classes/page.tsx returns 0 matches
  - grep -n "FFF2EF" frontend/app/teacher/students/page.tsx returns 0 matches
  - grep -n "FFF2EF" frontend/app/teacher/sessions/page.tsx returns 0 matches
  - '#EFF6FF' (blue-50) is used in place of '#FFF2EF' wherever background tints appeared
  - npm run build exits 0
  </acceptance_criteria>
  <done>All #FFF2EF orange-50 tints in the three teacher pages replaced with #EFF6FF blue-50; build passes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Visual layout change to client component; existing data-fetch and mutation logic unchanged |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-03-SC | Tampering | npm/pip/cargo installs | accept | No packages installed in this phase |
</threat_model>

<verification>
After both tasks complete:

1. Build gate: `cd frontend && npm run build` exits 0
2. Orange-free: `grep -n "F0623A" frontend/app/teacher/homework/page.tsx` returns 0 matches
3. Grid structure: `grep -n "display: 'grid'" frontend/app/teacher/homework/page.tsx` returns at least 1 match
4. LinearProgress: `grep -n "LinearProgress" frontend/app/teacher/homework/page.tsx` shows both import and usage
5. View toggle: `grep -n "viewMode" frontend/app/teacher/homework/page.tsx` shows state declaration and usage
6. Visual: visit /teacher/homework — grid view shows 3-column card layout on wide screen; toggle switches to table view without error
</verification>

<success_criteria>
- Homework page default view is card grid (3 cols desktop, 2 cols tablet, 1 col mobile)
- Each card: HwTypeChip, hw name (700 weight), class names, overdue Chip if applicable, LinearProgress (blue), submitted count, 4 action icon buttons
- Filter tabs are pill-style (borderRadius 999px, no border, count badge)
- View toggle buttons present in toolbar
- Table view works correctly as fallback
- No #F0623A in this file
- npm run build exits 0
</success_criteria>

<output>
Create `.planning/phases/16-teacher-admin-ui-redesign/16-03-SUMMARY.md` when done
</output>
