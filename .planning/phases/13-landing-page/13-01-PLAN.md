---
phase: 13-landing-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/middleware.ts
  - frontend/app/(marketing)/layout.tsx
  - frontend/app/(marketing)/page.tsx
  - frontend/app/sitemap.ts
  - frontend/app/robots.ts
  - frontend/public/og-image.jpg
autonomous: true
requirements: [LAND-01, LAND-09, LAND-10]
tags: [nextjs, middleware, seo, landing-page, routing]

must_haves:
  truths:
    - "Visiting katie.vn (host header = 'katie.vn' or 'www.katie.vn') serves the marketing landing page — not a redirect to app.katie.vn"
    - "Visiting localhost with NEXT_PUBLIC_SUBDOMAIN=marketing renders the /marketing route"
    - "marketing layout.tsx exports metadata with title, description, openGraph, twitter card per D-15 through D-17"
    - "marketing page.tsx renders a valid semantic page with <h1>, <main>, JSON-LD EducationalOrganization script tag"
    - "sitemap.ts includes '/' and excludes /teacher, /admin, /game"
    - "robots.ts disallows /teacher, /admin, /game, /api"
    - "/sitemap.xml and /robots.txt are accessible at katie.vn (not intercepted by middleware rewrite)"
    - "frontend/public/og-image.jpg exists (1200x630 solid #4F9DFF placeholder) so og:image preview returns 200"
  artifacts:
    - path: "frontend/middleware.ts"
      provides: "Subdomain routing — rewrites katie.vn requests to /marketing; passes /sitemap.xml and /robots.txt through unchanged"
      contains: "NextResponse.rewrite.*marketing"
    - path: "frontend/app/(marketing)/layout.tsx"
      provides: "Marketing route group root layout with SEO metadata (title, openGraph, twitter), html lang=vi, Inter font"
      exports: ["metadata", "default (layout function)"]
    - path: "frontend/app/(marketing)/page.tsx"
      provides: "Marketing page scaffold with <main>, placeholder <h1>, and JSON-LD EducationalOrganization script tag"
      contains: "application/ld+json"
    - path: "frontend/app/sitemap.ts"
      provides: "Next.js sitemap generator returning katie.vn root URL only"
      exports: ["default sitemap()"]
    - path: "frontend/app/robots.ts"
      provides: "Next.js robots generator disallowing /teacher, /admin, /game, /api"
      exports: ["default robots()"]
    - path: "frontend/public/og-image.jpg"
      provides: "Placeholder og:image (1200x630 solid blue #4F9DFF) so Facebook/Zalo link previews return 200"
  key_links:
    - from: "frontend/middleware.ts"
      to: "/marketing route"
      via: "NextResponse.rewrite"
      pattern: "NextResponse\\.rewrite.*marketing"
    - from: "frontend/middleware.ts"
      to: "sitemap.xml / robots.txt passthrough"
      via: "NextResponse.next() pathname guard before rewrite"
      pattern: "sitemap\\.xml.*robots\\.txt"
---

<objective>
Extend Phase 12 subdomain middleware to serve the marketing landing page at katie.vn (root domain). Create the marketing route scaffold with full SEO metadata (Next.js metadata API, OpenGraph, JSON-LD, sitemap, robots). After this plan, visiting katie.vn returns a valid HTML page — thin but complete end-to-end slice.

Purpose: LAND-01 (routing at katie.vn) + LAND-09 (SEO metadata) + LAND-10 (semantic HTML, JSON-LD).
Output: Updated middleware.ts, marketing layout with metadata, page scaffold with JSON-LD, sitemap.ts, robots.ts.

Note: The marketing route is structured as a Next.js route group: `app/(marketing)/`. Route groups are transparent to URL resolution — `app/(marketing)/page.tsx` resolves to the `/marketing` URL path, and `app/(marketing)/layout.tsx` is a proper root-level layout for that group, meaning `<html>/<body>` tags are valid there.
</objective>

<execution_context>
@/Users/hung/Documents/katie-english/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hung/Documents/katie-english/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/hung/Documents/katie-english/.planning/PROJECT.md
@/Users/hung/Documents/katie-english/.planning/ROADMAP.md
@/Users/hung/Documents/katie-english/.planning/phases/13-landing-page/13-CONTEXT.md
@/Users/hung/Documents/katie-english/.planning/phases/13-landing-page/13-UI-SPEC.md
</context>

## Phase Goal

**As a** phụ huynh student, **I want to** visit katie.vn and see an informative marketing page about cô Katie's English class, **so that** I can learn about the class and contact cô Katie directly.

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend middleware to serve marketing route at katie.vn</name>
  <files>frontend/middleware.ts</files>
  <read_first>
    - frontend/middleware.ts — Current subdomain routing logic; read fully before editing. The `detectSubdomain` function already returns 'root' for `katie.vn` and `www.katie.vn`. The existing handler at line 76 does a 301 redirect to app.katie.vn — this must be replaced.
    - .planning/phases/13-landing-page/13-CONTEXT.md — D-01, D-02 decisions for routing.
  </read_first>
  <action>
    In `frontend/middleware.ts`:

    1. Extend the `Subdomain` type to add `'marketing'` as a valid value, OR handle `'root'` directly (prefer the latter — no new type needed).

    2. Replace the existing `root` handler block (currently the `if (subdomain === 'root')` block that does a 301 redirect to app.katie.vn) with a rewrite to `/marketing`. The new handler MUST do the following in order:
       a. First, add passthrough guards for special Next.js-generated paths. Before performing any rewrite, check: if `pathname === '/sitemap.xml' || pathname === '/robots.txt'`, return `NextResponse.next()` immediately. This allows Next.js to serve these files directly from `app/sitemap.ts` and `app/robots.ts` without interference.
       b. After the passthrough guard, rewrite `req.nextUrl` pathname to `/marketing` + original pathname suffix, then return `NextResponse.rewrite(...)`. The rewrite maps `katie.vn/` → `/marketing`, `katie.vn/anything` → `/marketing` (all root domain paths serve marketing page, since it is a single-page marketing site).
       c. No auth cookie check for marketing routes — marketing is fully public.

    3. Add `'marketing'` to the `NEXT_PUBLIC_SUBDOMAIN` local dev check so that `NEXT_PUBLIC_SUBDOMAIN=marketing` env var triggers the same rewrite. Insert the check before the existing `envSubdomain === 'admin'` condition in `detectSubdomain`. When `envSubdomain === 'marketing'`, return `'root'` so the same root handler fires.

    4. Do NOT alter any existing admin/app/student subdomain logic.

    The passthrough guard is critical: without it, requests to `katie.vn/sitemap.xml` and `katie.vn/robots.txt` would be rewritten to `/marketing` and return 404 instead of the generated sitemap/robots files.
  </action>
  <verify>
    <automated>cd /Users/hung/Documents/katie-english/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - middleware.ts compiles without TypeScript errors
    - The `detectSubdomain` function returns 'root' when `NEXT_PUBLIC_SUBDOMAIN=marketing`
    - The `if (subdomain === 'root')` block contains pathname guards for '/sitemap.xml' and '/robots.txt' BEFORE the rewrite
    - The `if (subdomain === 'root')` block calls `NextResponse.rewrite(new URL('/marketing', req.url))` for all other paths instead of redirect
    - Lines touching admin/app/student logic are unchanged (git diff confirms no modifications to SUBDOMAIN_CONFIG or those blocks)
  </acceptance_criteria>
  <done>middleware.ts rewrites katie.vn requests to /marketing instead of redirecting to app.katie.vn. /sitemap.xml and /robots.txt pass through without rewrite. TypeScript compiles clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create marketing route group scaffold with SEO metadata and JSON-LD</name>
  <files>
    frontend/app/(marketing)/layout.tsx,
    frontend/app/(marketing)/page.tsx,
    frontend/app/sitemap.ts,
    frontend/app/robots.ts,
    frontend/public/og-image.jpg
  </files>
  <read_first>
    - frontend/app/layout.tsx — Root layout pattern: AppRouterCacheProvider, ThemeProvider, CssBaseline, Inter font variable. The (marketing) route group layout uses the same pattern and CAN include html/body tags because route group layouts are treated as independent root-level layouts by Next.js App Router.
    - frontend/lib/theme.ts — baseTheme export. Marketing layout uses baseTheme (not teacherTheme or adminTheme).
    - .planning/phases/13-landing-page/13-UI-SPEC.md — SEO Contract section (D-15 through D-22), Copywriting Contract (og:title, og:description exact strings).
    - .planning/phases/13-landing-page/13-CONTEXT.md — D-15 through D-22 locked decisions.
  </read_first>
  <action>
    Create directory `frontend/app/(marketing)/` (note the parentheses — this is a Next.js route group). Route groups are transparent to URL paths: `app/(marketing)/page.tsx` serves at `/marketing`, and `app/(marketing)/layout.tsx` is a valid independent layout that can declare `<html>/<body>` without conflicting with the root layout.

    Create `frontend/app/(marketing)/layout.tsx`:
    - `export const metadata: Metadata` object with:
      - `title`: "Lớp Tiếng Anh Cô Katie | Dạy Tiếng Anh Trẻ Em"
      - `description`: 150-160 char Vietnamese string including "tiếng Anh trẻ em" and location (use "Hà Nội" as placeholder location)
      - `openGraph`: `{ title, description, type: 'website', url: 'https://katie.vn', images: [{ url: 'https://katie.vn/og-image.jpg', width: 1200, height: 630, alt: 'Lớp tiếng Anh cô Katie' }] }`
      - `twitter`: `{ card: 'summary_large_image', title, description, images: ['https://katie.vn/og-image.jpg'] }`
      - `keywords`: ['tiếng Anh trẻ em', 'lớp tiếng Anh', 'cô Katie', 'dạy tiếng Anh', 'Hà Nội']
    - The layout function renders `<html lang="vi">` and `<body>` wrapping AppRouterCacheProvider + ThemeProvider(baseTheme) + CssBaseline + children. Because this is a route group layout, it is an independent root layout for the (marketing) group — `<html>/<body>` are valid and required here to set lang="vi" without affecting other routes.
    - Import Inter from 'next/font/google' with `subsets: ['latin'], variable: '--font-inter', display: 'swap'` and apply `inter.variable` className on the body element.

    Create `frontend/app/(marketing)/page.tsx`:
    - Server component (no 'use client').
    - Renders a `<main>` element as the page shell.
    - Inside main: a single placeholder `<Box>` with `sx={{ minHeight: '100vh', display: 'flex', alignItems: 'center', justifyContent: 'center' }}` containing `<Typography variant="h1">Lớp Tiếng Anh Cô Katie</Typography>`. This is the scaffold; sections are added in Plans 02 and 03.
    - Include JSON-LD structured data as an inline `<script type="application/ld+json">` tag via `dangerouslySetInnerHTML`. The JSON-LD must use EducationalOrganization schema with fields: `@context: "https://schema.org"`, `@type: "EducationalOrganization"`, `name: "Lớp Tiếng Anh Cô Katie"`, `url: "https://katie.vn"`, `telephone: "+84-xxx-xxx-xxxx"` (placeholder), `address: { @type: "PostalAddress", addressLocality: "Hà Nội", addressCountry: "VN" }`, `description: "Lớp tiếng Anh cho trẻ 6-12 tuổi"`.

    Create `frontend/app/sitemap.ts`:
    - Next.js `MetadataRoute.Sitemap` export from the `sitemap` function.
    - Returns array with one entry: `{ url: 'https://katie.vn', lastModified: new Date(), changeFrequency: 'monthly', priority: 1 }`.
    - Do NOT include /teacher, /admin, /game, /api paths.

    Create `frontend/app/robots.ts`:
    - Next.js `MetadataRoute.Robots` export from the `robots` function.
    - Returns `{ rules: [{ userAgent: '*', allow: '/', disallow: ['/teacher', '/admin', '/game', '/api'] }], sitemap: 'https://katie.vn/sitemap.xml' }`.

    Create `frontend/public/og-image.jpg` — placeholder og:image required so Facebook and Zalo link previews return 200 instead of 404. Run the following command to generate a 1200x630 solid-color JPEG in the correct dimensions:

      convert -size 1200x630 xc:#4F9DFF frontend/public/og-image.jpg

    If ImageMagick (`convert`) is not available, fall back to any available method that produces a valid JPEG at 1200x630: Python Pillow (`python3 -c "from PIL import Image; img = Image.new('RGB', (1200, 630), color=(79,157,255)); img.save('frontend/public/og-image.jpg')"`), or ffmpeg (`ffmpeg -f lavfi -i color=c=0x4F9DFF:size=1200x630:rate=1 -frames:v 1 frontend/public/og-image.jpg`). The file MUST be a valid JPEG served as a static asset — Next.js serves all files in `public/` as-is at the root URL path, so `/public/og-image.jpg` becomes `https://katie.vn/og-image.jpg`.
  </action>
  <verify>
    <automated>cd /Users/hung/Documents/katie-english/frontend && npx tsc --noEmit 2>&1 | head -20 && npm run build 2>&1 | tail -20 && test -f /Users/hung/Documents/katie-english/frontend/public/og-image.jpg && echo "og-image.jpg exists" || echo "ERROR: og-image.jpg missing"</automated>
  </verify>
  <acceptance_criteria>
    - frontend/app/(marketing)/layout.tsx exists and exports `metadata` with title "Lớp Tiếng Anh Cô Katie | Dạy Tiếng Anh Trẻ Em"
    - frontend/app/(marketing)/layout.tsx renders html element with lang="vi" and body element
    - frontend/app/(marketing)/page.tsx exists, is a server component, contains a script tag with type="application/ld+json"
    - The JSON-LD contains "@type": "EducationalOrganization"
    - frontend/app/sitemap.ts exists and returns an array including url 'https://katie.vn'
    - frontend/app/robots.ts exists and its disallow array includes '/teacher', '/admin', '/game', '/api'
    - `npm run build` exits 0 (no build errors)
    - frontend/public/og-image.jpg exists (file size > 0, valid JPEG at 1200x630)
  </acceptance_criteria>
  <done>Marketing route group scaffold live at /marketing. SEO metadata complete. sitemap.ts and robots.ts created. Build passes. Visiting katie.vn (via middleware rewrite) serves the scaffold page. sitemap.xml and robots.txt accessible directly. frontend/public/og-image.jpg placeholder (1200x630 solid #4F9DFF) created so og:image preview URLs return 200.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public internet → Next.js middleware | Host header can be spoofed by reverse proxy; trust is established upstream |
| Browser → /marketing route | Fully public, no auth, no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Spoofing | middleware.ts detectSubdomain | accept | Root domain detection uses Host header set by CDN/reverse proxy (Vercel/Nginx) — cannot be spoofed by end users in production |
| T-13-02 | Information Disclosure | middleware.ts rewrite | mitigate | Rewrite exposes /marketing route name internally; acceptable since /marketing is public and has no auth-gated content |
| T-13-03 | Tampering | JSON-LD script tag | accept | dangerouslySetInnerHTML used on server-generated static JSON — no user input involved, no XSS risk |
| T-13-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan — all dependencies already in package.json |
</threat_model>

<verification>
1. TypeScript compiles: `cd frontend && npx tsc --noEmit` exits 0
2. Build passes: `npm run build` exits 0
3. middleware.ts contains pathname passthrough guards for '/sitemap.xml' and '/robots.txt' before the rewrite
4. middleware.ts contains `NextResponse.rewrite` for root domain (not redirect) for all other paths
5. (marketing)/layout.tsx exists with metadata export containing OpenGraph fields and renders html lang="vi"
6. (marketing)/page.tsx contains JSON-LD script with EducationalOrganization type
7. sitemap.ts and robots.ts both exist and return correct Next.js MetadataRoute types
8. frontend/public/og-image.jpg exists (valid JPEG, 1200x630) — `file frontend/public/og-image.jpg` confirms JPEG format
</verification>

<success_criteria>
- katie.vn requests are rewritten to /marketing (not redirected to app.katie.vn)
- /sitemap.xml and /robots.txt are NOT rewritten — they pass through to Next.js file generators
- NEXT_PUBLIC_SUBDOMAIN=marketing works for local dev
- Full SEO metadata present: title, description, OpenGraph, Twitter card, JSON-LD
- sitemap.xml includes katie.vn root only
- robots.txt disallows /teacher /admin /game /api
- npm run build passes
- frontend/public/og-image.jpg exists and is served at /og-image.jpg (Next.js public/ static serving)
</success_criteria>

<output>
Create `.planning/phases/13-landing-page/13-01-SUMMARY.md` when done
</output>
