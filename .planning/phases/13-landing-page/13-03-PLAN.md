---
phase: 13-landing-page
plan: 03
type: execute
wave: 3
depends_on: [13-02]
files_modified:
  - frontend/app/(marketing)/components/SoftwareSection.tsx
  - frontend/app/(marketing)/components/ContactCTASection.tsx
  - frontend/app/(marketing)/page.tsx
  - frontend/app/(marketing)/data/content.ts
  - frontend/package.json
autonomous: false
requirements: [LAND-06, LAND-07, LAND-08, LAND-10]
tags: [nextjs, landing-page, mui, cta, seo, performance, responsive]

must_haves:
  truths:
    - "Software section renders a feature list (quản lý lớp học, giao bài tập số, chấm điểm tự động, theo dõi tiến độ) with CheckCircle2 icons colored #4F9DFF"
    - "Software section h2 heading uses AnimatedSection scroll-triggered slideUp animation"
    - "Software section renders platform screenshots using next/image with declared width, height, and priority={true} on the first image for LCP"
    - "CTA section is a full-width banner with #4F9DFF background, Zalo button (min-height 44px) and phone link"
    - "Zalo button href is https://zalo.me/{number} (opens Zalo chat)"
    - "Phone link is <a href='tel:{number}'> with Vietnamese display text"
    - "page.tsx renders all 6 sections in correct order: Hero → Teacher Profile → Student Results → Testimonials → Software → CTA"
    - "All images use next/image with alt text in Vietnamese"
  artifacts:
    - path: "frontend/app/(marketing)/components/SoftwareSection.tsx"
      provides: "Feature list + screenshot layout, h2 wrapped in AnimatedSection"
      exports: ["SoftwareSection"]
    - path: "frontend/app/(marketing)/components/ContactCTASection.tsx"
      provides: "Full-width blue banner with Zalo + phone CTA"
      exports: ["ContactCTASection"]
  key_links:
    - from: "frontend/app/(marketing)/page.tsx"
      to: "SoftwareSection, ContactCTASection"
      via: "named imports from ./components/"
      pattern: "import.*ContactCTASection"
---

<objective>
Complete the landing page by adding the Software intro section (feature list + screenshots, with scroll-animated heading) and the CTA/Contact section (full-width Zalo + phone banner). Wire all 6 sections into page.tsx and add a local dev script for the marketing subdomain. After this plan, the landing page is fully complete and passes the build.

Purpose: LAND-06 (Software section), LAND-07 (CTA/Contact), LAND-08 (responsive confirmed full page), LAND-10 (next/image LCP optimization).
Output: SoftwareSection with AnimatedSection h2, ContactCTASection, updated page.tsx with all 6 sections, dev:marketing npm script.
</objective>

<execution_context>
@/Users/hung/Documents/katie-english/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hung/Documents/katie-english/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/hung/Documents/katie-english/.planning/phases/13-landing-page/13-CONTEXT.md
@/Users/hung/Documents/katie-english/.planning/phases/13-landing-page/13-UI-SPEC.md
</context>

<interfaces>
From content.ts (created in Plan 02):
- heroContent.ctaHref — Zalo URL (reuse in ContactCTASection)
- heroContent.ctaLabel — "Nhắn tin Zalo ngay" (reuse)
- heroContent.phoneHref — tel: link
- heroContent.phoneLabel — "Gọi điện tư vấn"

From AnimatedSection.tsx (created in Plan 02):
- Import path: './components/AnimatedSection' (from page.tsx level) or './AnimatedSection' (from within components/)
- Usage: wrap h2 Typography in AnimatedSection for scroll-triggered slideUp

From UI-SPEC software section contract:
- Features to highlight (D-10): quản lý lớp học, giao bài tập số, chấm điểm tự động, theo dõi tiến độ
- Feature icons: CheckCircle2 from 'lucide-react' colored '#4F9DFF'
- Screenshot images: next/image, declared width/height, first image has priority={true}
- Section heading: "Phần mềm hỗ trợ học tập"

From UI-SPEC CTA section contract:
- Full-width banner, background #4F9DFF, white text
- Zalo button: MUI Button variant="contained" white bg, #4F9DFF text, MessageCircle icon
- Phone: Typography variant="h4" white, href="tel:{number}"
- Section heading: "Đăng ký học thử miễn phí"
- min-height 44px on Zalo button (accessibility)
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Build Software section (with AnimatedSection h2) and CTA/Contact section</name>
  <files>
    frontend/app/(marketing)/components/SoftwareSection.tsx,
    frontend/app/(marketing)/components/ContactCTASection.tsx,
    frontend/app/(marketing)/data/content.ts
  </files>
  <read_first>
    - .planning/phases/13-landing-page/13-UI-SPEC.md — Software Section contract (feature list left + screenshot right, two-column on desktop), CTA/Contact section contract (full-width banner, Zalo button specs, phone link specs, heading text), Animation Contract (slideUp on section headings via IntersectionObserver).
    - .planning/phases/13-landing-page/13-CONTEXT.md — D-09 (feature list + screenshots), D-10 (4 features to highlight), D-11 (Zalo CTA).
    - frontend/app/(marketing)/components/HeroSection.tsx — established server component pattern, MUI sx conventions, import structure.
    - frontend/app/(marketing)/components/AnimatedSection.tsx — AnimatedSection import and usage; SoftwareSection h2 heading must be wrapped in AnimatedSection for scroll-triggered slideUp.
    - frontend/app/(marketing)/data/content.ts — heroContent already has ctaHref/ctaLabel/phoneHref/phoneLabel for reuse. Read actual exported field names before coding ContactCTASection.
    - frontend/lib/theme.ts — slideUp keyframe (already handled by AnimatedSection; no direct sx usage needed in SoftwareSection).
  </read_first>
  <action>
    Add to `frontend/app/(marketing)/data/content.ts`:
    - Export `softwareFeatures`: array of 4 strings: ["Quản lý lớp học và học sinh", "Giao bài tập số cho học sinh", "Chấm điểm tự động bằng AI", "Theo dõi tiến độ học tập"].
    - Export `softwareScreenshots`: array of 2 objects with shape `{ src: string, alt: string, width: number, height: number }`. Values: `[{ src: '/images/screenshot-teacher.png', alt: 'Giao diện dashboard giáo viên', width: 1200, height: 750 }, { src: '/images/screenshot-student.png', alt: 'Giao diện bài tập học sinh', width: 1200, height: 750 }]`.

    Create `frontend/app/(marketing)/components/SoftwareSection.tsx` (server component — no 'use client'):
    - MUI Box component="section" sx={{ backgroundColor: 'background.paper', py: { xs: 4, md: 8 } }}.
    - MUI Container maxWidth="lg": wrap the section h2 heading in AnimatedSection — import AnimatedSection from './AnimatedSection'. Typography variant="h2" sx={{ mb: 4, textAlign: 'center' }} text="Phần mềm hỗ trợ học tập" must be wrapped inside AnimatedSection for scroll-triggered slideUp (per UI-SPEC Animation Contract).
    - MUI Grid2 container spacing={4} alignItems="center":
      - Left column (xs=12, md=5): MUI List disablePadding. Map softwareFeatures: each MUI ListItem disablePadding sx={{ mb: 1 }} containing ListItemIcon (CheckCircle2 size={20} color='#4F9DFF' style={{ minWidth: 32 }}) + ListItemText primary={feature} primaryTypographyProps={{ variant: 'body1', fontWeight: 700 }}.
      - Right column (xs=12, md=7): Map softwareScreenshots, each wrapped in MUI Paper elevation={2} sx={{ borderRadius: 3, overflow: 'hidden', mb: 2 }} containing next/image with src, alt, width, height props. The FIRST image (index 0) must have `priority={true}` for LCP optimization (per D-21 and LAND-10). The second image has no priority prop (lazy load default).
    - Import softwareFeatures, softwareScreenshots from '../data/content'.
    - Import Image from 'next/image'.
    - Import CheckCircle2 from 'lucide-react'.
    - Import AnimatedSection from './AnimatedSection'.

    Create `frontend/app/(marketing)/components/ContactCTASection.tsx` (server component — no 'use client'):
    - MUI Box component="footer" sx={{ backgroundColor: '#4F9DFF', py: { xs: 6, md: 8 }, px: { xs: 2, md: 6 }, textAlign: 'center' }}.
    - MUI Container maxWidth="md":
      - Typography variant="h2" sx={{ color: '#FFFFFF', mb: 1 }} text="Đăng ký học thử miễn phí".
      - Typography variant="body1" sx={{ color: 'rgba(255,255,255,0.85)', mb: 4 }} text="Liên hệ cô Katie để được tư vấn miễn phí và sắp xếp buổi học thử.".
      - Box sx={{ display: 'flex', flexDirection: { xs: 'column', sm: 'row' }, gap: 2, justifyContent: 'center', alignItems: 'center' }} containing:
        1. MUI Button variant="contained" component="a" href={heroContent.ctaHref} sx={{ backgroundColor: '#FFFFFF', color: '#4F9DFF', minHeight: 44, fontWeight: 700, '&:hover': { backgroundColor: '#F0F4FF' } }} startIcon={MessageCircle from 'lucide-react' size={18}} label text "Nhắn tin Zalo ngay".
        2. MUI Typography variant="h4" component="a" href={heroContent.phoneHref} sx={{ color: '#FFFFFF', textDecoration: 'none', '&:hover': { textDecoration: 'underline' } }} text heroContent.phoneLabel.
    - Import heroContent from '../data/content'.
    - Import MessageCircle from 'lucide-react'.
    - Note: ContactCTASection uses component="footer" to satisfy the `<footer>` landmark requirement from UI-SPEC accessibility contract.
    - ContactCTASection does NOT use AnimatedSection — the CTA banner is always visible and should not be hidden by opacity:0 before scroll.
  </action>
  <verify>
    <automated>cd /Users/hung/Documents/katie-english/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - frontend/app/(marketing)/components/SoftwareSection.tsx exists, no 'use client'
    - SoftwareSection.tsx imports AnimatedSection from './AnimatedSection' and wraps the h2 heading in it
    - SoftwareSection.tsx contains `priority={true}` on the first screenshot Image
    - SoftwareSection.tsx imports CheckCircle2 from 'lucide-react'
    - softwareFeatures array in content.ts has length 4
    - softwareScreenshots array in content.ts has length 2
    - frontend/app/(marketing)/components/ContactCTASection.tsx exists, no 'use client'
    - ContactCTASection.tsx contains MessageCircle import from 'lucide-react'
    - ContactCTASection.tsx contains minHeight: 44 on Zalo button
    - ContactCTASection.tsx uses component="footer" on root Box
    - TypeScript compiles without errors
    - No Tailwind class strings in either file
  </acceptance_criteria>
  <done>SoftwareSection with scroll-animated h2 (AnimatedSection), feature list, and screenshots (priority LCP image) built. ContactCTASection full-width blue banner with Zalo button (44px) and phone link built. Both are server components.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire all 6 sections into page.tsx, add dev:marketing npm script, confirm full build</name>
  <files>
    frontend/app/(marketing)/page.tsx,
    frontend/package.json
  </files>
  <read_first>
    - frontend/app/(marketing)/page.tsx — Current state after Plan 02: renders HeroSection, TeacherProfileSection, StudentResultsSection, TestimonialCarousel. Retain JSON-LD script tag. Add SoftwareSection and ContactCTASection.
    - frontend/package.json — Current scripts object. Add dev:marketing script following the same pattern as dev:admin/dev:teacher/dev:student (using cross-env NEXT_PUBLIC_SUBDOMAIN=marketing next dev -p 3004). IMPORTANT: Plan 13-01 must already be executed — `detectSubdomain` must handle NEXT_PUBLIC_SUBDOMAIN=marketing returning 'root' for the middleware rewrite to work. Verify Plan 13-01 is complete before running this task.
    - .planning/phases/13-landing-page/13-UI-SPEC.md — Section order: Hero → Teacher Profile → Student Results → Testimonials → Software → CTA.
  </read_first>
  <action>
    Update `frontend/app/(marketing)/page.tsx`:
    - The page is a server component (no 'use client' — keep as-is).
    - Add imports for SoftwareSection from './components/SoftwareSection' and ContactCTASection from './components/ContactCTASection'.
    - Update the main JSX to render sections in this exact order inside `<main>`:
      1. HeroSection
      2. TeacherProfileSection
      3. StudentResultsSection
      4. TestimonialCarousel
      5. SoftwareSection
      6. ContactCTASection
    - The JSON-LD script tag (from Plan 01) must remain present — place it as the first child of the page function return, before `<main>`, as a React Fragment wrapping both the script and main.
    - Ensure the page export is `export default function LandingPage()` (or `Page` — match existing) and returns valid JSX. No props needed.

    Update `frontend/package.json`:
    - In the "scripts" object, add `"dev:marketing": "cross-env NEXT_PUBLIC_SUBDOMAIN=marketing next dev -p 3004"` after the existing `"dev:student"` entry.
    - Update the `"dev:all"` concurrently command to include the marketing server: change `-n admin,teacher,student` to `-n admin,teacher,student,marketing` and add `"npm run dev:marketing"` as a fourth parallel command. Also update the `-c` color list to `blue,green,yellow,cyan`.
    - No other changes to package.json.
  </action>
  <verify>
    <automated>cd /Users/hung/Documents/katie-english/frontend && npm run build 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - frontend/app/(marketing)/page.tsx imports and renders all 6 section components in correct order
    - page.tsx renders SoftwareSection before ContactCTASection
    - page.tsx retains the JSON-LD script tag with EducationalOrganization schema
    - page.tsx has no 'use client' directive
    - frontend/package.json contains "dev:marketing" script with NEXT_PUBLIC_SUBDOMAIN=marketing and port 3004
    - npm run build exits 0 with no TypeScript or Next.js errors
    - Build output includes /marketing route in the static pages list
  </acceptance_criteria>
  <done>All 6 sections wired into page.tsx in correct order. dev:marketing script added for local development. Full build passes. Landing page is complete and deployable.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete 6-section landing page at katie-english.com.vn via middleware rewrite:
    1. Hero: tagline, blue gradient visual, Zalo CTA (fadeIn on mount)
    2. Teacher profile: avatar, credentials, bio (h2 animates slideUp on scroll)
    3. Student results: 2-3 before/after cards (h2 animates slideUp on scroll)
    4. Testimonials: auto-sliding carousel (5s), prev/next arrows, dot indicators
    5. Software: feature list with icons + screenshot images (h2 animates slideUp on scroll)
    6. CTA: full-width blue banner with Zalo button and phone link

    Local dev script: `npm run dev:marketing` starts on port 3004 with NEXT_PUBLIC_SUBDOMAIN=marketing
  </what-built>
  <how-to-verify>
    1. Run: `cd frontend && npm run dev:marketing` — starts Next.js on http://localhost:3004
    2. Open http://localhost:3004/marketing in a browser
    3. Verify all 6 sections are visible on desktop width (1200px+)
    4. Scroll down — verify section headings (Teacher Profile, Student Results, Software) slide up into view as they enter the viewport (AnimatedSection scroll animation)
    5. Verify Testimonials carousel auto-advances after 5 seconds
    6. Click prev/next arrows on testimonials — verify slide changes
    7. Hover over carousel — verify auto-advance pauses
    8. Resize browser to mobile width (375px) — verify all sections stack to single column
    9. Click "Nhắn tin Zalo ngay" button — verify it opens zalo.me link (or navigates to placeholder)
    10. View page source — verify JSON-LD script tag with "@type":"EducationalOrganization" is present
    11. Verify og:title meta tag = "Lớp Tiếng Anh Cô Katie | Dạy Tiếng Anh Trẻ Em" in &lt;head&gt;
    12. Check no Tailwind classes (className="flex", "text-sm", etc.) visible in page source for marketing components
    13. Verify page background is light blue (#E8F2FF) in hero, white in teacher/software sections
    14. Navigate to http://localhost:3004/sitemap.xml — verify it returns the sitemap XML (not 404)
    15. Navigate to http://localhost:3004/robots.txt — verify it returns robots content (not 404)
    16. Navigate to http://localhost:3004/og-image.jpg — verify it returns 200 with an image (not 404); this confirms Facebook/Zalo link previews will resolve the og:image URL correctly
  </how-to-verify>
  <resume-signal>Type "approved" if the landing page looks correct, or describe any visual/functional issues to fix</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Visitor browser → ContactCTASection links | External navigations to Zalo and tel: — no data submitted to our backend |
| next/image optimization | Images served via Next.js image CDN; external image URLs would require remotePatterns config — all images are local (/images/*.png) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-08 | Information Disclosure | ContactCTASection phone number | accept | Phone number is intentionally public marketing information |
| T-13-09 | Tampering | next/image local images (/images/*.png) | accept | Images are static public/ directory assets; no user upload path |
| T-13-10 | Spoofing | Zalo link href | accept | Link destination is hardcoded by developer, not from user input; placeholder updated by cô Katie before launch |
| T-13-11 | Denial of Service | npm run build + dev:marketing | accept | Adds one more Next.js dev server to dev:all — existing pattern, no new risk |
| T-13-SC | Tampering | npm/pip/cargo installs | accept | No new packages — cross-env and concurrently already in devDependencies; lucide-react and MUI in dependencies |
</threat_model>

<verification>
1. `npm run build` exits 0 — no TypeScript or Next.js build errors
2. page.tsx renders sections in order: Hero → TeacherProfile → StudentResults → Testimonials → Software → CTA
3. SoftwareSection.tsx has priority={true} on first image
4. SoftwareSection.tsx imports and uses AnimatedSection for h2 heading
5. ContactCTASection.tsx root is component="footer"
6. package.json scripts includes "dev:marketing" with port 3004
7. JSON-LD script in page.tsx contains "@type": "EducationalOrganization"
8. Human verification: scroll animations fire on h2 headings, carousel auto-advances, responsive layout works, Zalo link present, /sitemap.xml and /robots.txt accessible, /og-image.jpg returns 200
</verification>

<success_criteria>
- All 6 sections visible and fully rendered at /marketing
- Section h2 headings in TeacherProfile, StudentResults, Software sections animate slideUp on scroll into viewport
- Testimonial carousel functional (auto-advance, manual controls, pause on hover)
- Software screenshots use next/image with priority on hero image (LCP optimization)
- CTA section Zalo button min-height 44px
- Fully responsive desktop + mobile
- npm run build exits 0
- dev:marketing script works on port 3004
- /sitemap.xml and /robots.txt accessible (not intercepted by middleware)
- /og-image.jpg returns 200 (not 404) — Facebook/Zalo og:image preview resolves correctly
- Human verifies visual appearance, scroll animations, and carousel interaction
</success_criteria>

<output>
Create `.planning/phases/13-landing-page/13-03-SUMMARY.md` when done
</output>
