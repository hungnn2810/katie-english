---
phase: 13-landing-page
plan: 02
type: execute
wave: 2
depends_on: [13-01]
files_modified:
  - frontend/app/marketing/page.tsx
  - frontend/app/marketing/_components/HeroSection.tsx
  - frontend/app/marketing/_components/TeacherProfileSection.tsx
  - frontend/app/marketing/_components/StudentResultsSection.tsx
  - frontend/app/marketing/_components/StudentResultCard.tsx
  - frontend/app/marketing/_components/TestimonialCarousel.tsx
  - frontend/app/marketing/_components/TestimonialCard.tsx
  - frontend/app/marketing/data/content.ts
autonomous: true
requirements: [LAND-02, LAND-03, LAND-04, LAND-05, LAND-08]
tags: [nextjs, landing-page, mui, components, carousel, responsive]

must_haves:
  truths:
    - "Hero section renders with Vietnamese tagline 'Tiếng Anh tự tin — bắt đầu từ đây', a blue gradient visual placeholder, and Zalo CTA button"
    - "Teacher profile section renders with circular avatar placeholder, 'Về cô Katie' h2 heading, and bio/credentials paragraphs"
    - "Student results section renders 2-3 StudentResultCard components with before/after score + achievement badge"
    - "Testimonial carousel renders 5-6 quotes that auto-advance every 5 seconds with prev/next controls"
    - "All sections are fully responsive: single-column on mobile (xs), two-column on desktop (md+)"
    - "No Tailwind utility classes anywhere in the marketing components — all styling via MUI sx prop"
  artifacts:
    - path: "frontend/app/marketing/_components/HeroSection.tsx"
      provides: "Two-column hero with tagline, gradient visual, Zalo CTA"
    - path: "frontend/app/marketing/_components/TeacherProfileSection.tsx"
      provides: "Centered card layout for teacher bio"
    - path: "frontend/app/marketing/_components/StudentResultsSection.tsx"
      provides: "Grid of student result cards"
    - path: "frontend/app/marketing/_components/TestimonialCarousel.tsx"
      provides: "Auto-advancing testimonial slider"
    - path: "frontend/app/marketing/data/content.ts"
      provides: "Hardcoded Vietnamese content for all sections"
  key_links:
    - from: "frontend/app/marketing/page.tsx"
      to: "_components/*.tsx"
      via: "named imports"
      pattern: "import.*from.*_components"
    - from: "TestimonialCarousel.tsx"
      to: "content.ts testimonials array"
      via: "prop or direct import"
      pattern: "testimonials"
---

<objective>
Build the top half of the landing page: Hero, Teacher Profile, Student Results, and Testimonials carousel. After this plan, visiting katie.vn shows a real, content-filled marketing page with working carousel.

Purpose: LAND-02 (Hero), LAND-03 (Teacher Profile), LAND-04 (Student Results), LAND-05 (Testimonials), LAND-08 (Responsive).
Output: 7 new component files + content.ts data file + updated page.tsx.
</objective>

<execution_context>
@/Users/hung/Documents/katie-english/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hung/Documents/katie-english/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/hung/Documents/katie-english/.planning/phases/13-landing-page/13-CONTEXT.md
@/Users/hung/Documents/katie-english/.planning/phases/13-landing-page/13-UI-SPEC.md
</context>

<interfaces>
From frontend/lib/theme.ts:
- `export const fadeIn` — keyframes for opacity 0→1, use in hero section sx animation
- `export const slideUp` — keyframes for opacity+translateY, use on section headings
- `export const baseTheme` — palette: primary.main #4F9DFF, secondary.main #6ED6C1, background.default #F7F9FC, background.paper #FFFFFF, text.primary #0F172A, text.secondary #64748B, divider #E2E8F0

MUI Typography variants mapped in UI-SPEC:
- h1: 48px/32px mobile, weight 700 — hero tagline only
- h2: 32px/24px mobile, weight 700 — one per section
- h3: 20px/18px mobile, weight 700 — card titles
- body1: 16px, weight 400 — paragraphs and quotes
- caption: 16px, weight 400, letter-spacing 0.05em — meta/attribution text

MUI responsive breakpoints: xs (0-599px), sm (600-899px), md (900px+)
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create content data file and Hero + Teacher Profile sections</name>
  <files>
    frontend/app/marketing/data/content.ts,
    frontend/app/marketing/_components/HeroSection.tsx,
    frontend/app/marketing/_components/TeacherProfileSection.tsx
  </files>
  <read_first>
    - .planning/phases/13-landing-page/13-UI-SPEC.md — Hero section contract (two-column, gradient placeholder, fadeIn animation, Zalo button specs), Teacher Profile contract (single-column centered card, avatar 200x200 circular), Copywriting Contract (exact copy strings), Color contract (#4F9DFF, #E8F2FF), Animation contract (fadeIn 400ms on mount for hero, slideUp 300ms on scroll for sections).
    - .planning/phases/13-landing-page/13-CONTEXT.md — D-11 (Zalo CTA), D-12 (minimal animation), D-13 (blue theme), D-14 (responsive).
    - frontend/lib/theme.ts — fadeIn and slideUp keyframe exports; baseTheme palette values.
  </read_first>
  <action>
    Create `frontend/app/marketing/data/content.ts` as a TypeScript module (no 'use client'):
    - Export `heroContent`: `{ tagline: string, subheading: string, ctaLabel: string, ctaHref: string, phoneLabel: string, phoneHref: string }` with values: tagline="Tiếng Anh tự tin — bắt đầu từ đây", subheading="Lớp tiếng Anh cô Katie — dành cho trẻ em 6–12 tuổi tại Hà Nội", ctaLabel="Nhắn tin Zalo ngay", ctaHref="https://zalo.me/0000000000" (placeholder), phoneLabel="Gọi điện tư vấn", phoneHref="tel:+84-000-000-0000" (placeholder).
    - Export `teacherContent`: `{ name: string, heading: string, credentials: string[], bio: string, imageSrc: string, imageAlt: string }` with values: heading="Về cô Katie", name="Cô Katie", credentials=["Bằng cử nhân Sư phạm tiếng Anh", "10+ năm kinh nghiệm giảng dạy", "Chứng chỉ IELTS 8.0"], bio="Cô Katie chuyên dạy tiếng Anh cho trẻ em 6–12 tuổi với phương pháp học qua trò chơi và công nghệ. Mỗi buổi học là một hành trình khám phá ngôn ngữ thú vị.", imageSrc="/images/katie-avatar.jpg" (placeholder), imageAlt="Cô Katie — giáo viên tiếng Anh".
    - Export `studentResults`: array of 2-3 objects with shape `{ id: number, studentName: string, before: string, after: string, achievement: string, period: string }`. Example entries: `{ id: 1, studentName: "Bé Minh", before: "Điểm học kì: 6.5/10", after: "Điểm học kì: 9.0/10", achievement: "Học sinh giỏi cấp trường", period: "6 tháng" }`.
    - Export `testimonials`: array of 5-6 objects with shape `{ id: number, parentName: string, relation: string, quote: string, rating: number, initials: string }`. Example: `{ id: 1, parentName: "Chị Lan", relation: "Phụ huynh bé Minh", quote: "Con tôi tiến bộ rõ rệt sau 3 tháng. Cô Katie rất tận tâm và biết cách khơi dậy hứng thú học tiếng Anh cho trẻ.", rating: 5, initials: "L" }`.

    Create `frontend/app/marketing/_components/HeroSection.tsx` (server component — no 'use client'):
    - MUI Box as the section root with `component="section"` and `sx={{ backgroundColor: '#E8F2FF', py: { xs: 4, md: 12 }, px: { xs: 2, md: 6 } }}`.
    - MUI Container `maxWidth="lg"` (1200px) wrapping an MUI Grid2 container with two columns: `{ xs: 12, md: 6 }` each.
    - Left column: Box with `sx={{ animation: \`\${fadeIn} 0.4s ease-out\` }}`. Contains: Typography variant="h1" with heroContent.tagline; Typography variant="body1" sx={{ mt: 2, color: 'text.secondary' }} with heroContent.subheading; Box sx={{ mt: 4, display: 'flex', gap: 2, flexWrap: 'wrap' }} containing: Button variant="contained" href={heroContent.ctaHref} component="a" with MessageCircle lucide icon from 'lucide-react' and label heroContent.ctaLabel (sx: minHeight: 44px, backgroundColor: '#4F9DFF'); Button variant="outlined" href={heroContent.phoneHref} component="a" label heroContent.phoneLabel.
    - Right column: Box sx={{ background: 'linear-gradient(135deg, #4F9DFF 0%, #6ED6C1 100%)', borderRadius: 4, minHeight: { xs: 200, md: 360 }, display: 'flex', alignItems: 'center', justifyContent: 'center' }} with placeholder Typography variant="body2" color="white" text="Ảnh lớp học".
    - Import heroContent from '../data/content'.

    Create `frontend/app/marketing/_components/TeacherProfileSection.tsx` (server component):
    - MUI Box as section root with `component="section"` and `sx={{ backgroundColor: 'background.paper', py: { xs: 4, md: 8 } }}`.
    - MUI Container maxWidth="sm" (800px effective centered) containing MUI Card with sx={{ p: { xs: 3, md: 4 } }}.
    - Inside Card: Box sx={{ display: 'flex', flexDirection: { xs: 'column', md: 'row' }, gap: 4, alignItems: { xs: 'center', md: 'flex-start' } }}.
    - Left: Box with next/image (or MUI Avatar if image missing) — use next/image with src=teacherContent.imageSrc, width=200, height=200, alt=teacherContent.imageAlt, style={{ borderRadius: '50%', objectFit: 'cover' }}. Wrap in an error boundary or use a fallback Avatar (MUI Avatar sx={{ width: 200, height: 200, fontSize: 64 }}) displaying "K" if image not available.
    - Right: Box with Typography variant="h2" (teacherContent.heading), Typography variant="h3" (teacherContent.name), Typography variant="body1" sx={{ mt: 2 }} (teacherContent.bio), and a List component mapping teacherContent.credentials: each ListItem with ListItemIcon (CheckCircle2 from lucide-react, color '#4F9DFF', size 18) and ListItemText primary={cred}.
    - Import teacherContent from '../data/content'.
  </action>
  <verify>
    <automated>cd /Users/hung/Documents/katie-english/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - frontend/app/marketing/data/content.ts exists and exports heroContent, teacherContent, studentResults, testimonials
    - heroContent.ctaLabel === "Nhắn tin Zalo ngay"
    - testimonials.length >= 5
    - HeroSection.tsx exists, is a server component (no 'use client'), imports fadeIn from '@/lib/theme'
    - HeroSection.tsx contains MessageCircle import from 'lucide-react'
    - TeacherProfileSection.tsx exists, uses next/image (import from 'next/image')
    - TeacherProfileSection.tsx contains CheckCircle2 import from 'lucide-react'
    - TypeScript compiles without errors
    - No Tailwind class strings (className="...") in any of the 3 files
  </acceptance_criteria>
  <done>content.ts data module created. HeroSection and TeacherProfileSection components built as server components with full MUI sx styling, responsive layout, and Vietnamese content.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Build Student Results grid and Testimonial Carousel, wire into page.tsx</name>
  <files>
    frontend/app/marketing/_components/StudentResultCard.tsx,
    frontend/app/marketing/_components/StudentResultsSection.tsx,
    frontend/app/marketing/_components/TestimonialCard.tsx,
    frontend/app/marketing/_components/TestimonialCarousel.tsx,
    frontend/app/marketing/page.tsx
  </files>
  <read_first>
    - .planning/phases/13-landing-page/13-UI-SPEC.md — Student Results + Testimonials section spec: results in 2-3 MUI Cards grid, Testimonials carousel with auto-advance 5s, manual prev/next arrows, dot indicators, fallback shows first testimonial if JS disabled, pause on hover/focus. Accessibility: aria-label on carousel region, prev/next buttons aria-labels in Vietnamese.
    - .planning/phases/13-landing-page/13-CONTEXT.md — D-06 (before/after stories), D-07 (điểm thi + danh hiệu), D-08 (carousel 5-6 quotes auto-slider).
    - frontend/lib/theme.ts — slideUp keyframe, secondary.main #6ED6C1 for achievement badges.
    - frontend/app/marketing/_components/HeroSection.tsx — Reference for server component pattern and MUI sx styling conventions used in Task 1.
  </read_first>
  <action>
    Create `frontend/app/marketing/_components/StudentResultCard.tsx` (server component):
    - Props: `{ studentName: string, before: string, after: string, achievement: string, period: string }`.
    - MUI Card with sx={{ p: 3, height: '100%' }}.
    - Top: Typography variant="h3" (studentName).
    - Period badge: MUI Chip label={`Sau ${period}`} size="small" sx={{ mt: 1, mb: 2 }}.
    - Two rows side by side (Box sx={{ display: 'flex', gap: 2, mb: 2 }}): Box "Trước" (Typography variant="caption" uppercase letter-spacing 0.05em + Typography variant="body1" color="text.secondary" showing before value) and Box "Sau" (same structure with after value, color="text.primary" fontWeight=700).
    - Achievement: Box sx={{ backgroundColor: 'secondary.main', borderRadius: 2, px: 2, py: 1 }} with Typography variant="body2" fontWeight=700 (achievement text).

    Create `frontend/app/marketing/_components/StudentResultsSection.tsx` (server component):
    - MUI Box component="section" sx={{ backgroundColor: 'background.default', py: { xs: 4, md: 8 } }}.
    - MUI Container maxWidth="lg" with Typography variant="h2" sx={{ mb: 4, textAlign: 'center' }} text="Học sinh nói gì sau khi học".
    - MUI Grid2 container spacing={3}: map studentResults from '../data/content', each in Grid2 xs={12} md={4} wrapping StudentResultCard with spread props.

    Create `frontend/app/marketing/_components/TestimonialCard.tsx` (server component):
    - Props: `{ parentName: string, relation: string, quote: string, rating: number, initials: string }`.
    - MUI Box sx={{ px: { xs: 2, md: 6 }, textAlign: 'center' }}.
    - MUI Avatar sx={{ width: 64, height: 64, mx: 'auto', mb: 2, backgroundColor: 'primary.main', fontSize: 24, fontWeight: 700 }} displaying initials.
    - Row of 5 star icons: map Array(5), each Typography component="span" color={i < rating ? '#4F9DFF' : 'divider'} text="★".
    - Typography variant="body1" sx={{ mt: 2, fontStyle: 'italic', color: 'text.primary' }} with `"${quote}"` (curly quotes).
    - Typography variant="caption" sx={{ mt: 2, display: 'block', color: 'text.secondary', letterSpacing: '0.05em' }} with `${parentName} — ${relation}`.

    Create `frontend/app/marketing/_components/TestimonialCarousel.tsx` (CLIENT component — 'use client' required):
    - Props: none (imports testimonials directly from '../data/content').
    - State: `const [current, setCurrent] = useState(0)` and `const [paused, setPaused] = useState(false)`.
    - Auto-advance: `useEffect(() => { if (paused) return; const t = setInterval(() => setCurrent(c => (c + 1) % testimonials.length), 5000); return () => clearInterval(t); }, [paused])`.
    - Root: MUI Box component="section" role="region" aria-label="Đánh giá từ phụ huynh" sx={{ backgroundColor: 'background.default', py: { xs: 4, md: 8 } }} with onMouseEnter={() => setPaused(true)} onMouseLeave={() => setPaused(false)} onFocus={() => setPaused(true)} onBlur={() => setPaused(false)}.
    - MUI Container maxWidth="md": Typography variant="h2" textAlign="center" mb={4} text="Phụ huynh chia sẻ".
    - Carousel body: Box sx={{ position: 'relative', overflow: 'hidden' }} containing a Box sx={{ transform: `translateX(${-current * 100}%)`, transition: 'transform 0.5s ease-in-out', display: 'flex' }} mapping all testimonials as non-wrapping flex children (each Box sx={{ minWidth: '100%', flexShrink: 0 }}) rendering TestimonialCard.
    - Prev/Next buttons: MUI IconButton aria-label="Testimonial trước" / "Testimonial tiếp theo" (sx={{ minWidth: 44, minHeight: 44 }}) positioned absolute left/right using ChevronLeft / ChevronRight from lucide-react.
    - Dot indicators: Box sx={{ display: 'flex', justifyContent: 'center', gap: 1, mt: 3 }} mapping testimonials, each Box onClick={() => setCurrent(i)} sx={{ width: 10, height: 10, borderRadius: '50%', backgroundColor: i === current ? '#4F9DFF' : '#E2E8F0', cursor: 'pointer', minWidth: 32, minHeight: 32 }} (32px touch area per UI-SPEC accessibility).
    - Import ChevronLeft, ChevronRight from 'lucide-react'.

    Update `frontend/app/marketing/page.tsx`:
    - Replace the placeholder scaffold Box+Typography from Plan 01 with the full section composition:
      ```
      <main>
        <HeroSection />
        <TeacherProfileSection />
        <StudentResultsSection />
        <TestimonialCarousel />
      </main>
      ```
    - Keep the JSON-LD script tag from Plan 01 — do not remove it.
    - No 'use client' (server component). Import all section components from './_components/'.
  </action>
  <verify>
    <automated>cd /Users/hung/Documents/katie-english/frontend && npx tsc --noEmit 2>&1 | head -30 && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - TestimonialCarousel.tsx has 'use client' as first line
    - TestimonialCarousel.tsx contains useState and useEffect imports from 'react'
    - TestimonialCarousel.tsx contains role="region" and aria-label="Đánh giá từ phụ huynh"
    - TestimonialCarousel.tsx contains aria-label="Testimonial trước" and aria-label="Testimonial tiếp theo"
    - TestimonialCarousel.tsx onMouseEnter sets paused=true
    - StudentResultCard.tsx is a server component (no 'use client')
    - page.tsx imports and renders HeroSection, TeacherProfileSection, StudentResultsSection, TestimonialCarousel
    - page.tsx retains the JSON-LD script tag
    - npm run build exits 0
    - No Tailwind class strings in any of the 5 files
  </acceptance_criteria>
  <done>Student results grid, testimonial carousel, and all section components complete. page.tsx wires them together. Landing page top half fully functional with responsive layout and accessible carousel.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Static React components | No user input accepted; all content is hardcoded |
| External links (Zalo, tel:) | Clicks navigate away; no data sent to our server |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-04 | Spoofing | Zalo link (href=https://zalo.me/...) | accept | Link target is user-controlled content (phone number); planner notes this is hardcoded by cô Katie and verified manually before launch |
| T-13-05 | Information Disclosure | content.ts (phone number) | accept | Phone number is intentionally public — the marketing objective requires it |
| T-13-06 | Denial of Service | TestimonialCarousel setInterval | mitigate | clearInterval called on unmount (useEffect cleanup) — no timer leak on route change |
| T-13-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed — lucide-react and MUI already in package.json |
</threat_model>

<verification>
1. TypeScript compiles: `cd frontend && npx tsc --noEmit` exits 0
2. Build passes: `npm run build` exits 0
3. HeroSection.tsx, TeacherProfileSection.tsx, StudentResultsSection.tsx, StudentResultCard.tsx, TestimonialCard.tsx are server components (no 'use client')
4. TestimonialCarousel.tsx has 'use client', uses useState + useEffect, has paused state on hover/focus
5. content.ts exports all four data objects: heroContent, teacherContent, studentResults (length >= 2), testimonials (length >= 5)
6. page.tsx renders all four section components with JSON-LD intact
7. No Tailwind class strings in any component
</verification>

<success_criteria>
- Hero section visible with Vietnamese tagline and blue gradient visual
- Teacher profile card renders with credentials list
- 2-3 student result cards show before/after scores
- Testimonial carousel auto-advances, manual controls work, pauses on hover
- Fully responsive: stacked on mobile, two-column on desktop
- npm run build exits 0
</success_criteria>

<output>
Create `.planning/phases/13-landing-page/13-02-SUMMARY.md` when done
</output>
