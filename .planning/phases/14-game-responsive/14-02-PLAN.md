---
phase: 14-game-responsive
plan: 02
type: execute
wave: 2
depends_on:
  - 14-01
files_modified:
  - frontend/app/game/session/[id]/page.tsx
  - frontend/app/game/session/[id]/_components/RecordButton.tsx
  - frontend/app/game/session/[id]/_components/PhonemeChips.tsx
  - frontend/app/game/vocab/[id]/page.tsx
  - frontend/app/game/listen/[id]/page.tsx
autonomous: true
requirements:
  - RESP-03
  - RESP-05
  - RESP-06
  - RESP-08

must_haves:
  truths:
    - "Phonics/speaking session page has no horizontal scroll at 320px"
    - "Cam-denied and mic-denied permission screens are centered with maxWidth 480 on all viewports"
    - "CircleTimer is 110px on xs and 140px on sm+"
    - "RecordButton circle (104px) meets 44px touch target minimum"
    - "Results page for session has responsive padding and maxWidth 560 container"
    - "Vocab game image is viewport-relative: maxWidth 90vw on xs, 320px on sm+"
    - "Vocab mic-denied screen has px: 3 and maxWidth: 480"
    - "Listen game audio player panel is full-width within maxWidth 480 container"
    - "Listen results page has responsive padding"
    - "All inline record buttons across vocab and listen pages are at least 104px (touch safe)"
  artifacts:
    - path: "frontend/app/game/session/[id]/page.tsx"
      provides: "Responsive phonics/speaking game"
      contains: "maxWidth: { sm: 600, md: 640 }"
    - path: "frontend/app/game/session/[id]/_components/RecordButton.tsx"
      provides: "Touch-safe record button (already 104px — verified)"
    - path: "frontend/app/game/vocab/[id]/page.tsx"
      provides: "Responsive vocab game with viewport-relative image"
      contains: "maxWidth: { xs: '90vw'"
    - path: "frontend/app/game/listen/[id]/page.tsx"
      provides: "Responsive listen game"
      contains: "maxWidth: 480"
  key_links:
    - from: "frontend/app/game/session/[id]/page.tsx"
      to: "CircleTimer SVG"
      via: "width/height props changed to responsive"
      pattern: "width.*\\{.*xs.*110"
    - from: "frontend/app/game/vocab/[id]/page.tsx"
      to: "vocab image Box"
      via: "width/height changed from fixed 240/200 to responsive"
      pattern: "maxWidth.*90vw"
---

<objective>
Make the three game pages that require microphone access — session (phonics+speaking), vocab, and listen — fully responsive. Key changes: permission screens get maxWidth containers, CircleTimer scales on mobile, vocab image uses viewport-relative dimensions, and all pages have the standard D-02 maxWidth/mx centering with D-03 padding scale. RecordButton is verified at 104px (already meets D-04 touch target — document, no change needed unless a defect is found).

Purpose: Students use these pages on iPads and phones in classroom settings. Touch targets and layout overflow directly affect the learning experience.

Output: Modified session/page.tsx, RecordButton.tsx (verification only), PhonemeChips.tsx (flexWrap verification), vocab/page.tsx, listen/page.tsx — all with responsive sx props.
</objective>

<execution_context>
@C:/Users/hungnn/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/hungnn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/14-game-responsive/14-CONTEXT.md
@.planning/phases/14-game-responsive/14-01-SUMMARY.md

<interfaces>
<!-- Key patterns from existing source files. Read before touching anything. -->

From frontend/app/game/session/[id]/page.tsx (current issues):

1. `cam-denied` state: outer Box uses `px: 4` hardcoded — change to `px: { xs: 3, sm: 4 }` and
   add `maxWidth: 480, mx: 'auto'` on the inner content Box (per D-09).

2. `record` state (speaking): inner content Box already has `maxWidth: 384` — upgrade to
   `maxWidth: { xs: '100%', sm: 384 }` and add `px: { xs: 2, sm: 0 }` to the outer Box.
   The outer Box uses `px: 3, py: 5` — change to `px: { xs: 2, sm: 3 }, py: { xs: 4, sm: 5 }`.

3. `CircleTimer`: the SVG is rendered with `width="140" height="140"`. Change to:
   `width={typeof window !== 'undefined' ? undefined : 140}` is complex — instead pass
   `size` prop from the call site. Add `size?: number` prop to `CircleTimer` with default 140.
   At the call site in the playing state, render `<CircleTimer seconds={timeLeft} total={totalTime} size={undefined} />`.
   Add `sx={{ width: { xs: 110, sm: 140 }, height: { xs: 110, sm: 140 } }}` to the SVG Box
   inside `CircleTimer` and remove the hardcoded `width="140" height="140"` attributes,
   using a `viewBox="0 0 120 120"` that already exists. The `svg` element should use
   `style={{ transform: 'rotate(-90deg)' }}` and get width/height from parent Box sx.

4. `results` state: outer Box uses `py: 6, px: 4` — change to `py: { xs: 4, sm: 6 }, px: { xs: 2, sm: 4 }`.

5. `playing` state header: outer Box uses `px: 4` — change to `px: { xs: 2, sm: 4 }`.

6. `ready` state: playing shell outer Box uses `px: 4, pb: 4` — change to `px: { xs: 2, sm: 4 }, pb: { xs: 3, sm: 4 }`.

7. Add `maxWidth: { sm: 600, md: 640 }, mx: 'auto'` wrapper around the main content column
   in the `ready` and `playing` states.

From frontend/app/game/session/[id]/_components/RecordButton.tsx:
- All states already render a 104×104px circle button. 104px > 44px minimum. No size change needed.
- Verify `PhonemeChips` already has `flexWrap: 'wrap'` via `display: 'flex', flexWrap: 'wrap'`.

From frontend/app/game/vocab/[id]/page.tsx (current issues):

1. Image area `width: 240, height: 200` — change to
   `width: { xs: '90vw', sm: 240 }, maxWidth: { xs: '90vw', sm: 320 }, height: { xs: '35vh', sm: 200 }, maxHeight: { xs: '35vh', sm: 280 }` per D-10.
   The `img` inside uses `objectFit: 'cover'` — keep as-is.

2. `mic-denied` outer Box uses `px: 4` — change to `px: { xs: 3, sm: 4 }` and add
   `maxWidth: 480, mx: 'auto'` to the inner content container Box per D-09.

3. `results` state: outer Box uses `py: 6, px: 4` — change to `py: { xs: 4, sm: 6 }, px: { xs: 2, sm: 4 }`.

4. `playing` state: outer Box uses `px: 3, py: 4` — change to `px: { xs: 2, sm: 3 }, py: { xs: 3, sm: 4 }`.
   Inner content Box already uses `maxWidth: 480, width: '100%'` — keep it.

5. Inline record buttons (idle/recording states): already 104×104px — meets D-04.

From frontend/app/game/listen/[id]/page.tsx (current issues):

1. `mic-denied` outer Box uses `px: 4` — same fix as vocab: `px: { xs: 3, sm: 4 }` and add
   `maxWidth: 480, mx: 'auto'` to the inner content Box.

2. `results` state: outer Box uses `py: 6, px: 4` — change to `py: { xs: 4, sm: 6 }, px: { xs: 2, sm: 4 }`.

3. `playing` state: outer Box uses `px: 3, py: 4` — change to `px: { xs: 2, sm: 3 }, py: { xs: 3, sm: 4 }`.
   Inner content Box already uses `maxWidth: 480, width: '100%'` — keep it.

4. Audio player panel Box: uses `p: '22px'` — change to `p: { xs: 2, sm: '22px' }` so it doesn't overflow on 320px.

5. Progress header Box: uses `maxWidth: 480` — already correct; keep it.

6. Inline record buttons (idle/recording states): already 104×104px — meets D-04.

Decision refs: D-02, D-03, D-04, D-09, D-10
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make session/[id]/page.tsx responsive — permission screens, timer, playing/results layouts</name>
  <files>frontend/app/game/session/[id]/page.tsx, frontend/app/game/session/[id]/_components/RecordButton.tsx, frontend/app/game/session/[id]/_components/PhonemeChips.tsx</files>
  <read_first>
    frontend/app/game/session/[id]/page.tsx
    frontend/app/game/session/[id]/_components/RecordButton.tsx
    frontend/app/game/session/[id]/_components/PhonemeChips.tsx
  </read_first>
  <action>
    In `session/[id]/page.tsx`, apply the following responsive sx changes:

    CAM-DENIED SCREEN: On the outer `<Box sx={{ minHeight: '100vh', ... px: 4, ... }}>`, change `px: 4` to `px: { xs: 3, sm: 4 }`. On the inner text-content Box (`<Box sx={{ textAlign: 'center' }}`), add `maxWidth: 480, mx: 'auto'`.

    RECORD (SPEAKING) STATE: On the outer speaking Box (`px: 3, py: 5`), change to `px: { xs: 2, sm: 3 }, py: { xs: 4, sm: 5 }`. The inner content Box already has `maxWidth: 384` — change to `maxWidth: { xs: '100%', sm: 384 }`.

    CIRCLE TIMER: Change `CircleTimer` to accept `size?: number` prop defaulting to 140. Replace the hardcoded `<svg width="140" height="140" viewBox="0 0 120 120">` with a `<Box component="svg" viewBox="0 0 120 120" sx={{ width: { xs: 110, sm: 140 }, height: { xs: 110, sm: 140 }, transform: 'rotate(-90deg)' }}>`. Remove `style={{ transform: 'rotate(-90deg)' }}` from the svg element since it moves to sx. Keep `viewBox="0 0 120 120"` and all inner circle/text elements unchanged.

    PLAYING STATE HEADER: Change the progress header Box from `px: 4, py: 2` to `px: { xs: 2, sm: 4 }, py: 2`.

    PLAYING STATE CONTENT: Change the inner content area Box (`px: 4, pb: 4`) to `px: { xs: 2, sm: 4 }, pb: { xs: 3, sm: 4 }`. Add `maxWidth: { sm: 600, md: 640 }, mx: 'auto'` to the inner `<Box sx={{ flex: 1, display: 'flex', flexDirection: 'column', alignItems: 'center', ... }}>` to center content on desktop.

    RESULTS STATE: Change the results outer Box from `py: 6, px: 4` to `py: { xs: 4, sm: 6 }, px: { xs: 2, sm: 4 }`.

    RecordButton.tsx: Read the file. The circular button is already 104×104px across all states — this satisfies D-04 (104 > 44). No size change needed. Add a comment `{/* D-04: 104px meets 44px minimum touch target */}` near the size declaration only if it doesn't already exist.

    PhonemeChips.tsx: Read the file. The outer Box already has `display: 'flex', flexWrap: 'wrap'`. Verify this is present. If for any reason `flexWrap: 'wrap'` is missing, add it. Otherwise no change.

    Do NOT change any recording logic, audio handling, timer intervals, VAD logic, or data submission logic.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | grep -E "session|RecordButton|PhonemeChips" || echo "No TS errors in session files"</automated>
  </verify>
  <acceptance_criteria>
    - `session/[id]/page.tsx` contains `px: { xs: 3, sm: 4 }` on the cam-denied outer Box
    - `session/[id]/page.tsx` contains `maxWidth: 480, mx: 'auto'` on the cam-denied inner text Box
    - `session/[id]/page.tsx` CircleTimer no longer has hardcoded `width="140" height="140"` attributes
    - `session/[id]/page.tsx` CircleTimer svg Box has `sx={{ width: { xs: 110, sm: 140 }, height: { xs: 110, sm: 140 } }}`
    - `session/[id]/page.tsx` results Box contains `py: { xs: 4, sm: 6 }, px: { xs: 2, sm: 4 }`
    - `RecordButton.tsx` button circle is still 104px in width and height for all states
    - `PhonemeChips.tsx` outer Box has `flexWrap: 'wrap'`
    - TypeScript build passes with no errors
  </acceptance_criteria>
  <done>Session page is responsive: permission screen centered, timer scales on mobile, results page has responsive padding. RecordButton confirmed at 104px. PhonemeChips wraps on narrow screens.</done>
</task>

<task type="auto">
  <name>Task 2: Make vocab/[id]/page.tsx and listen/[id]/page.tsx responsive — image, permission screens, layouts</name>
  <files>frontend/app/game/vocab/[id]/page.tsx, frontend/app/game/listen/[id]/page.tsx</files>
  <read_first>
    frontend/app/game/vocab/[id]/page.tsx
    frontend/app/game/listen/[id]/page.tsx
  </read_first>
  <action>
    VOCAB PAGE (frontend/app/game/vocab/[id]/page.tsx):

    IMAGE AREA (per D-10): The image Box currently has `width: 240, height: 200`. Change to:
    `width: { xs: '90vw', sm: 240 }, maxWidth: { xs: '90vw', sm: 320 }, height: { xs: 'auto', sm: 200 }, maxHeight: { xs: '35vh', sm: 280 }`.
    The `overflow: 'hidden'` and `border: '4px solid ...'` are kept as-is. The `<img>` inside keeps `objectFit: 'cover'` but change `width: '100%', height: '100%'` to `width: '100%', height: '100%', maxHeight: 'inherit'` to let the height collapse correctly on mobile.

    MIC-DENIED SCREEN: Change outer Box `px: 4` to `px: { xs: 3, sm: 4 }`. On the inner Box that wraps the heading and body text (`<Box sx={{ textAlign: 'center' }}`), add `maxWidth: 480, mx: 'auto'`.

    RESULTS STATE: Change outer Box from `py: 6, px: 4` to `py: { xs: 4, sm: 6 }, px: { xs: 2, sm: 4 }`.

    PLAYING STATE outer Box: change `px: 3, py: 4` to `px: { xs: 2, sm: 3 }, py: { xs: 3, sm: 4 }`.

    LISTEN PAGE (frontend/app/game/listen/[id]/page.tsx):

    MIC-DENIED SCREEN: Same as vocab — change outer Box `px: 4` to `px: { xs: 3, sm: 4 }`. Add `maxWidth: 480, mx: 'auto'` to the inner content Box.

    RESULTS STATE: Change outer Box from `py: 6, px: 4` to `py: { xs: 4, sm: 6 }, px: { xs: 2, sm: 4 }`.

    PLAYING STATE outer Box: change `px: 3, py: 4` to `px: { xs: 2, sm: 3 }, py: { xs: 3, sm: 4 }`.

    AUDIO PLAYER PANEL (per D-03): The audio panel Box uses `p: '22px'` — change to `p: { xs: 2, sm: '22px' }` to prevent overflow on 320px screens.

    Inline record buttons in both pages are already 104×104px circular buttons — do not change their size.

    Do NOT change any audio recording, scoring, navigation, or state management logic in either file.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | grep -E "vocab|listen" || echo "No TS errors in vocab/listen files"</automated>
  </verify>
  <acceptance_criteria>
    - `vocab/[id]/page.tsx` image Box contains `width: { xs: '90vw', sm: 240 }` and `maxHeight: { xs: '35vh', sm: 280 }`
    - `vocab/[id]/page.tsx` mic-denied outer Box contains `px: { xs: 3, sm: 4 }`
    - `vocab/[id]/page.tsx` results Box contains `py: { xs: 4, sm: 6 }, px: { xs: 2, sm: 4 }`
    - `listen/[id]/page.tsx` audio player panel Box contains `p: { xs: 2, sm: '22px' }`
    - `listen/[id]/page.tsx` mic-denied outer Box contains `px: { xs: 3, sm: 4 }`
    - `listen/[id]/page.tsx` results Box contains `py: { xs: 4, sm: 6 }, px: { xs: 2, sm: 4 }`
    - Both inline record button circles remain at 104×104px
    - TypeScript build passes with no errors in vocab or listen files
  </acceptance_criteria>
  <done>Vocab and listen game pages are responsive: vocab image uses viewport-relative dimensions on mobile, permission screens are centered, audio panel doesn't overflow on 320px, results pages have responsive padding.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| microphone→MediaRecorder | Audio captured from user mic — unchanged in this plan |
| audio URL→HTMLAudioElement | Listen game plays audio from backend URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-02-01 | Information Disclosure | Audio prompt URLs visible in DOM | accept | URLs serve pre-uploaded teacher audio; no PII beyond lesson content |
| T-14-02-02 | Denial of Service | Autoplay block on mobile browsers | accept | listen/page.tsx already has catch fallback: `audio.play().catch(() => setAudioPlayState('idle'))` |
| T-14-02-SC | Tampering | npm installs (none in this plan) | accept | No new dependencies; pure sx responsive changes |
</threat_model>

<verification>
After both tasks complete:

1. Run `cd frontend && npx tsc --noEmit` — zero errors.
2. Run `cd frontend && npm run build` — build succeeds.
3. Visual: session page at 375px — progress dots visible, timer is 110px, no horizontal scroll.
4. Visual: vocab game at 375px — image fills ~90% width, no overflow.
5. Visual: listen game at 375px — audio player panel fits within screen, no overflow.
6. Visual: all three pages at 1440px — content centered.
7. Touch test: RecordButton tappable on mobile (104px circle).
</verification>

<success_criteria>
1. Session, vocab, and listen game pages render without horizontal scroll at 320px.
2. All three pages render centered on 1440px desktop.
3. CircleTimer scales from 110px (mobile) to 140px (tablet+).
4. Vocab image uses `90vw` max on mobile with `35vh` height constraint.
5. TypeScript and build pass with zero new errors.
</success_criteria>

<output>
Create `.planning/phases/14-game-responsive/14-02-SUMMARY.md` when done.
</output>
