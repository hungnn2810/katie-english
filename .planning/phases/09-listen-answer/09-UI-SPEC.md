---
phase: 9
slug: listen-answer
status: draft
shadcn_initialized: false
preset: none
created: 2026-06-03
---

# Phase 9 — UI Design Contract: Listen & Answer Exercise

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (MUI — no shadcn) |
| Preset | not applicable |
| Component library | Material UI (already installed, used in all phases) |
| Icon library | lucide-react (already installed — Mic, Play, RotateCcw, ChevronRight, PartyPopper, Headphones, CheckCircle2, GripVertical, Plus, X, Hash) |
| Font | var(--font-inter), system-ui, sans-serif (from baseTheme) |

Source: `frontend/lib/theme.ts` (baseTheme), `frontend/app/game/vocab/[id]/page.tsx` (existing game pattern)

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, chip spacing, inline padding |
| sm | 8px | Compact element spacing (button inner, chip row gap) |
| md | 16px | Default element spacing (card inner padding, field gap) |
| lg | 24px | Section padding (card vertical gap) |
| xl | 32px | Layout gaps (between major sections) |
| 2xl | 48px | Major section breaks (page top padding) |
| 3xl | 64px | Page-level spacing (results screen score display) |

Exceptions:
- Mic record button circle: 96×96px diameter (matches existing game pattern from VOCAB/PHONICS pages)
- Progress bar pill: height 8px, width 32px (matches existing session progress header)
- Audio player button: 56×56px (new — audio play/replay control, distinct from mic record)
- Touch target minimum for primary actions: 48px height (existing game button pattern)

---

## Typography

Source: `frontend/lib/theme.ts` (baseTheme), observed usage in `game/session/[id]/page.tsx` and `game/vocab/[id]/page.tsx`.

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 14px | 400 | 1.5 |
| Label | 16px | 600 | 1.4 |
| Heading | 24px | 700 | 1.2 |
| Display | 30px | 900 | 1.0 |

Named exception — Score Display (numeric score render only):
- Playing state composite score: 48px / weight 900 / line-height 1.0 / fontVariantNumeric: tabular-nums
- Results screen final score: 72px / weight 900 / line-height 1.0 / fontVariantNumeric: tabular-nums

These two sizes are sub-variants of the single "Score Display" exception slot. They do not add to the type scale. Total declared scale slots: 4 (Body, Label, Heading, Display) + 1 named exception = 5 entries, within the ≤ 4 scale + exception allowance.

Note: `0.75rem` (12px) is used inline for micro-labels (results card question label row, item number chips) as a one-off — it is NOT a declared scale slot.

---

## Color

Source: `frontend/lib/colors.ts`, `frontend/lib/theme.ts`, `frontend/components/TeacherShell.tsx`

### Student Game Screens (app.*.student subdomain)

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | #2D0B2E (gameBg) | Full-screen game background |
| Secondary (30%) | rgba(255,255,255,0.10) | Content cards, prompt containers, keyword chip rows |
| Accent (10%) | #A78BFA (purple) | Active progress pip, record button active ring |
| Destructive | #ef4444 | Active recording ring + stop square |

Accent reserved for: active progress bar pill only, record-state active ring indicator. Never used for static decorative elements.

Score color function (reuse existing `scoreHexColor`):
- score >= 80: #7BD88F (green)
- score >= 50: #FFD166 (amber)
- score < 50: #FF7B7B (red/coral)

Semantic threshold banner (CONTEXT.md D-09):
- semantic_score < 0.2: amber banner (#fbbf24) — "hãy thử lại, nghe kỹ câu hỏi nhé"
- BFA errors: amber banner (#fbbf24) — same pattern as VOCAB/PHONICS

### Teacher Creation Page (TeacherShell layout on app.* subdomain)

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | #F7F9FC | Page background (baseTheme background.default) |
| Secondary (30%) | #FFFFFF | Paper cards, upload drop zones |
| Accent (10%) | #F0623A (teacherAccent) | Primary save button, active sidebar nav indicator |
| Destructive | #FF7B7B (error.main) | Remove item icon button, delete confirmation |

Accent reserved for: "Save Homework" primary CTA button, active nav pip in TeacherShell sidebar. Never used as decoration or borders on non-interactive elements.

---

## Screen Inventory

### Screen 1: Teacher — LISTEN Homework Creation Page

**Route:** `/teacher/homework/create/listen`
**Shell:** TeacherShell (existing, minWidth 1280)
**Pattern reference:** `VocabCreationPage.tsx` — exact same structural pattern

**Layout:**
- Single-column scrollable content area, maxWidth 720px, mx auto, px 4, pb 6
- Page title via TeacherShell `title` prop: "New Listen & Answer Homework"

**Sections (top to bottom):**

1. **Homework Name field** — MUI TextField, fullWidth, label "Homework name", required
2. **Items list** — @dnd-kit SortableContext (verticalListSortingStrategy), same pattern as VocabCreationPage
3. **"Add Item" button** — outlined Button, startIcon=Plus (lucide), fullWidth at bottom of list
4. **Save button** — contained Button, fullWidth, label "Save Homework", bgcolor teacherAccent (#F0623A)

**ListenItemCard (SortableVocabItemCard equivalent):**

Each card is a MUI Paper (elevation=1, borderRadius 2, p 1.5, border 1px solid divider).

Card internal layout (flex row, gap 2, alignItems flex-start):
- **Drag handle** (GripVertical icon, 20px, color text.secondary, cursor grab — same as VocabCreationPage)
- **Item number chip** (Box, bgcolor primary.light, color primary.contrastText, fontSize `0.75rem` one-off, fontWeight 700, px 1, py 0.5, borderRadius 1, min-width 24px, text-align center)
- **Main content** (flex column, gap 2, flex 1):
  - **Audio upload zone** — Box, dashed border 2px (#E2E8F0), borderRadius 2, p 2, textAlign center, minHeight 80px. States:
    - Empty: "Upload audio prompt (mp3 / wav / webm)" in Typography color text.secondary, fontSize 14. Click triggers hidden file input.
    - Uploading: CircularProgress size 24 centered.
    - Uploaded: horizontal flex — headphones icon (Headphones, 20px, color primary.main) + filename Typography (fontSize 14, truncated) + X IconButton to remove.
  - **Expected answer field** — TextField, label "Expected answer", placeholder "e.g. The cat is red", fullWidth, size small
  - **Keywords field** — TextField, label "Keywords (comma-separated)", placeholder "e.g. red, cat", fullWidth, size small, helperText "Keywords student must say to score well"
- **Remove button** — X IconButton, color error, alignSelf flex-start (top-right of card)

**Validation (inline, no modal):**
- Missing homework name: MUI Alert severity="error" above save button
- Item with no audio: Alert severity="warning" inline in card, text "Upload an audio prompt"
- Item with empty keywords: Alert severity="warning", text "Add at least one keyword"
- Save disabled (Button disabled prop) when any required field is empty

**No destructive confirmations in this phase** — item removal is immediate (X button), consistent with VocabCreationPage pattern.

---

### Screen 2: Student — LISTEN Game Page

**Route:** `/game/listen/[id]`
**Shell:** none — full-screen game, same as VocabGamePage pattern
**Constraint:** minWidth 1024 (laptop/PC, ages 6–12)
**Background:** `gradients.gameBg` (#2D0B2E) — matches all other game types

**Page states (same state machine as VocabGamePage):**

| State | Visual |
|-------|--------|
| loading | CircularProgress 48px, rgba(255,255,255,0.7), centered on gameBg |
| mic-check | Same as loading with label "Requesting microphone access…" |
| mic-denied | Icon + "Microphone Required" heading + "Try Again" button (pinkHighlight gradient) + "← Back to Homework" link — same as VocabGamePage |
| error | "#FF7B7B Session not found. Please go back and try again." + "← Back to Homework" back link — same as all game pages |
| ready | "Ready?" heading + "Listen to each question, then record your answer" subtext + "Start" button (primaryPurple gradient) |
| playing | Audio player + recording controls (detailed below) |
| uploading | CircularProgress 48px color #FFD166 + "Scoring and saving…" label |
| results | Full results screen (detailed below) |

**Progress header (playing state):**
- Identical to VocabGamePage: progress pills (height 8, width 32, borderRadius 9999) + item counter "N / M"
- Active item: background #A78BFA; completed: #ffffff80; pending: #ffffff20

**Playing state — Per-Item Layout (flex column, alignItems center, gap 3, maxWidth 480, mx auto):**

1. **Audio prompt player block** — Box, bgcolor rgba(255,255,255,0.1), borderRadius 3, p 2, width 100%, display flex, alignItems center, gap 2:
   - Headphones icon (28px, white) on left
   - Prompt number label: "Question N" — Typography fontSize 14, color rgba(255,255,255,0.6), fontWeight 600
   - Play/Replay button — circular Box component="button", 56×56px, border 2px solid rgba(255,255,255,0.4), borderRadius 50%, display flex, alignItems center, justifyContent center:
     - Idle (not yet played): Play icon (lucide Play, 24px, white)
     - Playing: CircularProgress 24px, color rgba(255,255,255,0.7)
     - Played/replay: RotateCcw icon (lucide, 20px, white)
   - Auto-plays once on item load (HTMLAudioElement.play() in useEffect on currentIndex change)
   - Replay resets and plays again

2. **Recording controls** — identical 3-state mic button from existing session/vocab game:
   - idle: white transparent circle 96×96, Mic 40px white, label "Tap to record"
   - recording: red ping ring + stop square button 96×96, label "Recording… tap to stop"
   - scoring: spinner 40px, label "Scoring…"
   - recorded: green check circle 96×96 (border rgba(52,211,153,0.5), bg rgba(52,211,153,0.15), CheckCircle2 40px #34d399)

3. **Feedback zone** (visible after recordState === 'recorded'):
   - Semantic error banner (score < 0.2): Box, bgcolor rgba(251,191,36,0.15), border 1px solid #fbbf24, borderRadius 2, px 2, py 1 — "hãy thử lại, nghe kỹ câu hỏi nhé"
   - BFA error: same amber banner, text from BFA_ERROR_MESSAGES map (identical to existing games)
   - Transcript display: Typography, color rgba(255,255,255,0.8), fontSize 16, fontStyle italic — "Bạn nói: \"[transcript]\""
   - Matched keywords row: flex wrap, gap 0.5 — each matched keyword as MUI Chip, size small, bgcolor rgba(52,211,153,0.2), color #34d399, fontWeight 700
   - PhonemeChips component (reuse existing) — shown only when BFA feedback available
   - Composite score: Typography, fontSize 48px (Score Display exception — playing sub-variant), fontWeight 900, fontVariantNumeric tabular-nums, color from scoreHexColor(compositeScore * 100)

4. **Action button**:
   - After scored, no BFA error: "Next →" / "View Results" (last item) — greenSecondary gradient, same as VocabGamePage
   - After BFA error: "Try Again" — outlined button, border rgba(255,255,255,0.3), color white

**Playing state — item-level scoring flow:**
1. Item loads → audio auto-plays → play indicator shows
2. Student presses mic circle → recording starts
3. Student presses stop circle → scoring starts (CircularProgress shown)
4. Score returns → transcript + keywords + chips reveal with fadeIn animation
5. Student presses Next → moves to next item

---

**Results screen:**

Full-screen results identical to VocabGamePage results. maxWidth 560, mx auto, py 6, px 4.

- PartyPopper icon 64×64, rgba(255,255,255,0.1) bg, borderRadius 3
- "Homework Complete!" Typography 30px weight 900
- Final composite score: 72px (Score Display exception — results sub-variant), weight 900, fontVariantNumeric tabular-nums, color from scoreHexColor
- Per-item cards (flex column, gap 1.5):
  - Each card: Box bgcolor rgba(255,255,255,0.1), borderRadius 3, px 2.5, py 2
  - Card content (flex column, gap 1):
    - Row 1: "Question N" label (`0.75rem` one-off, uppercase, rgba(255,255,255,0.6)) + composite score % on right (24px, weight 900)
    - Row 2: transcript Typography (14px, italic, rgba(255,255,255,0.8))
    - Row 3: matched keywords chips (same as playing state feedback zone)
    - Row 4 (if BFA available): PhonemeChips
    - Row 5 (if BFA error): amber error message
    - Row 6: score breakdown micro-row — "Semantic: X% · Pronunciation: Y%" (Typography `0.75rem` one-off, rgba(255,255,255,0.5))
- "Finish" Button: fullWidth, py 2, borderRadius 3, primaryPurple gradient, fontSize 18, fontWeight 900

---

## Component Inventory

### Reused (zero changes needed)

| Component | Source | Used in |
|-----------|--------|---------|
| PhonemeChips | `app/game/session/[id]/_components/PhonemeChips.tsx` | Student result per-item cards |
| gradients, scoreHexColor | `lib/colors.ts` | All student game screen backgrounds + score colors |
| shake, fadeIn, slideUp keyframes | `lib/theme.ts` | fadeIn on score reveal (same as VocabGamePage) |
| BFA_ERROR_MESSAGES | Inline constant in game pages | Audio gate error display |
| pickAudioMimeType | Inline fn in game pages | MediaRecorder MIME selection |
| AuthGate (requiredRole="STUDENT") | `components/AuthGate.tsx` | All student page state wrappers |
| TeacherShell | `components/TeacherShell.tsx` | Teacher creation page layout |
| @dnd-kit (DndContext, SortableContext, useSortable) | Already installed | ListenItem sequence reorder |

### New components (Phase 9 only)

| Component | File | Description |
|-----------|------|-------------|
| ListenCreationPage | `app/teacher/homework/_components/ListenCreationPage.tsx` | Teacher creation page, mirrors VocabCreationPage structure |
| ListenGamePage | `app/game/listen/[id]/page.tsx` | Student game page, mirrors VocabGamePage |
| AudioPromptPlayer | Inline in ListenGamePage | Audio element + play/replay button (not extracted — same pattern as game inline components) |

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Teacher page title (TeacherShell) | New Listen & Answer Homework |
| Teacher save CTA | Save Homework |
| Teacher add item CTA | Add Question |
| Teacher audio upload placeholder | Upload audio prompt (mp3 / wav / webm) |
| Teacher keywords helper | Keywords student must say to score well |
| Student ready heading | Ready? |
| Student ready subtext | Listen to each question, then record your answer |
| Student start CTA | Start |
| Student audio play label (first play) | Play question |
| Student audio replay label | Replay |
| Student record label (idle) | Tap to record |
| Student recording label | Recording… tap to stop |
| Student scoring label | Scoring… |
| Student transcript prefix | Bạn nói: |
| Student next CTA | Next → |
| Student last item CTA | View Results |
| Student try again (BFA error) | Try Again |
| Student semantic error (score < 0.2) | hãy thử lại, nghe kỹ câu hỏi nhé |
| Student uploading label | Scoring and saving… |
| Student results heading | Homework Complete! |
| Student results finish CTA | Finish |
| Student results score breakdown | Semantic: X% · Pronunciation: Y% |
| Student save error | Recording could not be saved |
| Student save success | Your recording has been saved |
| Empty state (no items on teacher page) | No questions yet. Add your first question below. |
| Error state body | Session not found. Please go back and try again. |
| Error state CTA | ← Back to Homework |
| Mic denied heading | Microphone Required |
| Mic denied body | Microphone access is required. Please allow access and reload. |
| BFA: audio_too_short | Bấm lâu hơn nhé — ghi âm quá ngắn |
| BFA: audio_too_long | Ghi âm quá dài — nói dưới 15 giây |
| BFA: recording_too_noisy | Mic quá ồn — tìm chỗ yên tĩnh hơn |
| BFA: speech_not_detected | Không nghe rõ — nói to hơn nhé |
| BFA: wrong_language | Please speak in English |

**Destructive actions:** Item removal (X button on ListenItemCard) is immediate, no confirmation. Consistent with VocabCreationPage pattern. No delete-homework action in this phase.

---

## Interaction Contracts

### Audio Auto-Play
- `useEffect` fires on `currentIndex` change (playing state) and calls `audioRef.current.play()`.
- If browser blocks autoplay (policy), show play button in non-played state (Play icon) so student can tap manually.
- Audio element is hidden (`display: none`). Only the custom play/replay button is visible.

### Recording State Machine
```
idle → (tap mic) → recording → (tap stop) → scoring → recorded
recorded → (tap "Try Again") → idle          [only on BFA error]
recorded → (tap "Next →") → [advance item or results]
```
Identical to VocabGamePage. No timer needed — student controls when to stop.

### Audio Upload (Teacher)
- Hidden `<input type="file" accept="audio/*">` triggered by clicking upload zone.
- On file select: set uploading=true, call `uploadAudioPrompt(file)`, set audioUrl in draft, set uploading=false.
- File size limit communicated to user if upload fails: Alert severity="error" "File too large or unsupported format".
- Accepted: mp3, wav, webm (matches CONTEXT.md D-07).

### Keyword Chips Display (Student Result)
- Matched keywords displayed as MUI Chips, size small, bgcolor rgba(52,211,153,0.2), color #34d399.
- Non-matched keywords (if any in bfaFeedback) not shown separately — PhonemeChips covers pronunciation detail.
- Keywords appear after scoring completes, with `fadeIn` animation (0.3s ease).

### Sequence Reorder (Teacher)
- @dnd-kit PointerSensor + KeyboardSensor (sortableKeyboardCoordinates).
- Drag handle: GripVertical icon, cursor grab, touch action none.
- Active drag: Paper elevation 6, opacity 0.5 on source item.
- On drag end: arrayMove + update local draft order array.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none — MUI only | not applicable |
| third-party | none | not applicable |

No new registries introduced. All components from MUI (already installed) + lucide-react (already installed) + @dnd-kit (already installed since Phase 2 / 03-05-PLAN).

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
