---
phase: 18
plan: 08
type: execute
wave: 8
depends_on: ["18-07"]
files_modified:
  - frontend/app/teacher/homework/[id]/try/page.tsx
  - frontend/messages/en/teacher.json
  - frontend/messages/vi/teacher.json
autonomous: true
requirements: [i18n-04, i18n-05]
must_haves:
  truths:
    - "Try/Preview page renders fully in Vietnamese when VI is selected and fully in English when EN is selected across all 5 homework-type branches (PHONICS, SPEAKING, READING, VOCABULARY, LISTEN)"
  artifacts:
    - path: "frontend/messages/en/teacher.json"
      provides: "adds homeworkTry top-level namespace"
    - path: "frontend/messages/vi/teacher.json"
      provides: "adds homeworkTry top-level namespace (mirrored keys)"
  key_links:
    - from: "frontend/app/teacher/homework/[id]/try/page.tsx"
      to: "frontend/messages/en/teacher.json"
      via: "useTranslations('teacher.homeworkTry')"
      pattern: "useTranslations\\('teacher\\.homeworkTry'\\)"
---

<objective>
Extract all hardcoded UI strings from `frontend/app/teacher/homework/[id]/try/page.tsx` (1034 lines — the largest single file in the Teacher portal), the interactive teacher-facing preview of a homework assignment covering all 5 homework types.

Purpose: This is the heaviest single-file extraction in the phase (per `18-PATTERNS.md`, mirrors the READING interactive preview branch added in Phase 3 Plan 03-06). Split into two tasks by structural boundary (shared preview-renderer components vs. the main type-dispatch flow) to stay within a single plan's context budget.
Output: File fully translation-driven; `teacher.json` extended with a `homeworkTry` namespace in both locales.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-multi-language-support-across-all-pages/18-CONTEXT.md
@.planning/phases/18-multi-language-support-across-all-pages/18-PATTERNS.md

<interfaces>
<!-- next-intl usage pattern (established 18-01) -->
`const t = useTranslations('teacher.<namespace>');` then `t('key')` / `t('key', { var })`, single-brace ICU interpolation.

<!-- frontend/app/teacher/homework/[id]/try/page.tsx structural map (1034 lines) -->
`function shuffle<T>(arr)` (line 28) — pure logic, no strings, skip.
`function PreviewBanner()` (line 37-66) — likely a "You are previewing this homework as a teacher" banner, static text.
`function MatchingRenderer({...})` (line 67-198) — READING matching-activity interactive preview (image/word pairs, click-to-match feedback).
`function FillBlankRenderer({...})` (line 199-297) — READING fill-in-blank interactive preview.
`function PhonemeTag({ op })` (line 298-334) — renders a single phoneme's correctness state; likely short labels or just colored chips (verify text content during read).
`function pickAudioMimeType()` (line 335-349) — pure logic, no strings, skip.
`export default function TeacherTryHomeworkPage()` (line 350-1034) — the main flow: dispatches rendering per `HomeworkType` (PHONICS/SPEAKING/READING/VOCABULARY/LISTEN), record/play controls, submit-preview flow (no real DB writes — per Phase 3's `03-06` decision, this page previews client-side only), and any toasts for microphone/recording errors.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extract PreviewBanner + MatchingRenderer + FillBlankRenderer + PhonemeTag</name>
  <files>frontend/app/teacher/homework/[id]/try/page.tsx, frontend/messages/en/teacher.json, frontend/messages/vi/teacher.json</files>
  <read_first>frontend/app/teacher/homework/[id]/try/page.tsx, frontend/messages/en/teacher.json, frontend/messages/vi/teacher.json</read_first>
  <action>
Read lines ~1-350 of `frontend/app/teacher/homework/[id]/try/page.tsx` in full. Add a `homeworkTry` top-level key to both message files with sub-namespaces `previewBanner`, `matching`, `fillBlank`, `phonemeTag` covering every static string in these four function components: the preview-mode banner text, matching-activity instructions/feedback text (correct/incorrect states), fill-in-blank instructions/choice-selection feedback, and any phoneme-state labels (correct/similar/wrong/missing — cross-check naming consistency with the existing `PhonemeChips` component pattern from Phase 5 if referenced in this file).
In `try/page.tsx`: import `useTranslations` from `next-intl`. Add `const t = useTranslations('teacher.homeworkTry.previewBanner');` inside `PreviewBanner`, `const t = useTranslations('teacher.homeworkTry.matching');` inside `MatchingRenderer`, `const t = useTranslations('teacher.homeworkTry.fillBlank');` inside `FillBlankRenderer`, `const t = useTranslations('teacher.homeworkTry.phonemeTag');` inside `PhonemeTag` (4 separate hook calls, one per component). Replace every hardcoded string in these 4 components with `t('...')`.
  </action>
  <acceptance_criteria>
    - `frontend/messages/en/teacher.json` and `frontend/messages/vi/teacher.json` both have `homeworkTry.previewBanner`, `.matching`, `.fillBlank`, `.phonemeTag` with matching structure
    - `PreviewBanner`, `MatchingRenderer`, `FillBlankRenderer`, `PhonemeTag` each call `useTranslations` independently
    - No hardcoded English or Vietnamese JSX text node remains in lines 1-350 of the file (spot check via read-back of the modified range)
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>PreviewBanner, MatchingRenderer, FillBlankRenderer, PhonemeTag fully translation-driven.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Extract TeacherTryHomeworkPage main flow</name>
  <files>frontend/app/teacher/homework/[id]/try/page.tsx, frontend/messages/en/teacher.json, frontend/messages/vi/teacher.json</files>
  <read_first>frontend/app/teacher/homework/[id]/try/page.tsx, frontend/messages/en/teacher.json, frontend/messages/vi/teacher.json</read_first>
  <action>
Read lines ~350-1034 of `frontend/app/teacher/homework/[id]/try/page.tsx` in full. Add a `homeworkTry.page` sub-namespace to both message files covering: per-type section headers/instructions (PHONICS/SPEAKING/READING/VOCABULARY/LISTEN), record/play button labels and states (recording/idle/playing), submit-preview button, any microphone-permission or recording-error toast/alert text, and navigation ("back") text.
In `try/page.tsx`: add `const t = useTranslations('teacher.homeworkTry.page');` inside `TeacherTryHomeworkPage`. Replace every hardcoded string in this component with `t('...')`, including any `showToast(` calls found during the full read (translate the fallback literal, leave `err.message` pass-through unchanged per the established convention).
  </action>
  <acceptance_criteria>
    - `frontend/messages/en/teacher.json` and `frontend/messages/vi/teacher.json` both have `homeworkTry.page` with matching structure
    - `TeacherTryHomeworkPage` calls `useTranslations('teacher.homeworkTry.page')`
    - Any `showToast(` call site discovered in this component sources its fallback message from `t(...)`, not an inline literal
    - `grep -c "useTranslations" frontend/app/teacher/homework/[id]/try/page.tsx` returns `5` (4 from Task 1 + 1 from this task)
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>TeacherTryHomeworkPage main flow fully translation-driven across all 5 homework-type branches.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Teacher's own preview audio recording (client-side only, no DB write per Phase 3 decision) → local playback UI | No new server-side trust boundary; preview stays entirely client-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-16 | Injection (XSS) | `t()`-rendered preview labels/instructions | accept | Plain React text children, no `dangerouslySetInnerHTML`. |
| T-18-17 | Tampering | `messages/{en,vi}/teacher.json` `homeworkTry` keys | accept | Static, build-time JSON. |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit -p tsconfig.json` — no new type errors
- Manual: switch locale, open `/teacher/homework/[id]/try` for a PHONICS, a SPEAKING, and a READING homework (the three types most likely to exercise all four Task-1 renderer components), confirm all text switches language
</verification>

<success_criteria>
1. `frontend/app/teacher/homework/[id]/try/page.tsx` (1034 lines) contains zero hardcoded user-facing strings.
2. `teacher.json` (both locales) has a `homeworkTry` namespace covering `previewBanner`/`matching`/`fillBlank`/`phonemeTag`/`page`.
3. Any toasts discovered in the file are catalog-sourced.
</success_criteria>

<output>
Create `.planning/phases/18-multi-language-support-across-all-pages/18-08-SUMMARY.md` when done
</output>
