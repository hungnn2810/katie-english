---
phase: 18
plan: 04
type: execute
wave: 4
depends_on: ["18-03"]
files_modified:
  - frontend/app/teacher/sessions/page.tsx
  - frontend/app/teacher/import/page.tsx
  - frontend/messages/en/teacher.json
  - frontend/messages/vi/teacher.json
autonomous: true
requirements: [i18n-04, i18n-05]
must_haves:
  truths:
    - "Sessions and Import pages render fully in Vietnamese when VI is selected and fully in English when EN is selected"
    - "The 1 toast on sessions/page.tsx sources its message from the translation catalog"
  artifacts:
    - path: "frontend/messages/en/teacher.json"
      provides: "adds sessions, import top-level namespaces"
    - path: "frontend/messages/vi/teacher.json"
      provides: "adds sessions, import top-level namespaces (mirrored keys)"
  key_links:
    - from: "frontend/app/teacher/sessions/page.tsx"
      to: "frontend/messages/en/teacher.json"
      via: "useTranslations('teacher.sessions')"
      pattern: "useTranslations\\('teacher\\.sessions'\\)"
    - from: "frontend/app/teacher/import/page.tsx"
      to: "frontend/messages/en/teacher.json"
      via: "useTranslations('teacher.import')"
      pattern: "useTranslations\\('teacher\\.import'\\)"
---

<objective>
Extract all hardcoded UI strings from `frontend/app/teacher/sessions/page.tsx` (263 lines — session list/filter table with `formatDuration`/`ScoreBadge` helpers) and `frontend/app/teacher/import/page.tsx` (169 lines — bulk-import upload flow, currently English-only static copy).

Purpose: Continues the mid-size page batch; import page's result/error table rendering (rows sourced from `ImportResponse`) needs careful handling since it displays server-computed counts, not just static labels.
Output: Both pages fully translation-driven; `teacher.json` extended with `sessions`/`import` namespaces in both locales.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-multi-language-support-across-all-pages/18-CONTEXT.md

<interfaces>
<!-- next-intl usage pattern (established 18-01) -->
`const t = useTranslations('teacher.<namespace>');` then `t('key')` / `t('key', { var })`, single-brace ICU interpolation.

<!-- frontend/app/teacher/sessions/page.tsx key elements -->
`function formatDuration(startedAt, completedAt)` (line 25) returns `'—'` for zero/negative duration, or `\`${m}m ${s}s\`` / `\`${s}s\`` — these unit suffixes ("m", "s") are compact and language-neutral in this codebase's existing convention; leave as-is unless a Vietnamese-specific duration format is required (it is not — D-13 keeps date/time-adjacent formatting locale-independent, and duration follows the same spirit). `function ScoreBadge({ score })` (line 33) renders `'—'` for missing scores — also language-neutral, leave as-is. Page has filter controls (student/assignment/date-range selects — check for placeholder/label strings), a results table with column headers, and one `showToast(e instanceof Error ? e.message : 'Failed to load sessions', 'error')` (line 67).

<!-- frontend/app/teacher/import/page.tsx key elements (currently 100% English, no toasts — uses local error state instead) -->
Intro card: `"Bulk Import"` heading, `"Download the template, fill in your data, and upload."` subtext (lines 44-49). Rest of file (not yet read in full): file-picker button, upload button, download-template button, and a results/error table rendered from `ImportResponse` / `isImportError` (imported from `@/lib/import-api`) — read the full file before extracting since the result-table column headers and per-row error messages are not yet catalogued here.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extract sessions/page.tsx</name>
  <files>frontend/app/teacher/sessions/page.tsx, frontend/messages/en/teacher.json, frontend/messages/vi/teacher.json</files>
  <read_first>frontend/app/teacher/sessions/page.tsx, frontend/messages/en/teacher.json, frontend/messages/vi/teacher.json</read_first>
  <action>
Add a `sessions` top-level key to both message files covering: filter control labels/placeholders (student, assignment, date-from, date-to), the results table's column headers, empty-state text, and `toasts.load_error` for the `'Failed to load sessions'` fallback.
In `sessions/page.tsx`: import `useTranslations` from `next-intl`, `const t = useTranslations('teacher.sessions');` inside `SessionsPage`. Replace every hardcoded JSX text node (filter labels, table headers, empty state) with `t('...')`. Replace `showToast(e instanceof Error ? e.message : 'Failed to load sessions', 'error')` with `showToast(e instanceof Error ? e.message : t('toasts.load_error'), 'error')`. Leave `formatDuration`'s `m`/`s` unit suffixes and `ScoreBadge`'s `'—'` em-dash unchanged — these are compact, language-neutral notations already used consistently across the codebase (e.g. `formatDuration` in this file, `ScoreBadge` elsewhere), not prose requiring translation.
  </action>
  <acceptance_criteria>
    - `frontend/messages/en/teacher.json` and `frontend/messages/vi/teacher.json` both have a top-level `sessions` key with matching structure
    - `frontend/app/teacher/sessions/page.tsx` imports and calls `useTranslations('teacher.sessions')`
    - `grep -c "showToast(e instanceof Error ? e.message : 'Failed to load sessions'" frontend/app/teacher/sessions/page.tsx` returns `0`
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>sessions/page.tsx fully translation-driven; load-error toast normalized.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Extract import/page.tsx</name>
  <files>frontend/app/teacher/import/page.tsx, frontend/messages/en/teacher.json, frontend/messages/vi/teacher.json</files>
  <read_first>frontend/app/teacher/import/page.tsx, frontend/messages/en/teacher.json, frontend/messages/vi/teacher.json, frontend/lib/import-api.ts</read_first>
  <action>
Read the full `frontend/app/teacher/import/page.tsx` file (169 lines) and `frontend/lib/import-api.ts`'s `ImportResponse`/`isImportError` shapes to identify every static string, including the results table's column headers and any error-row rendering. Add an `import` top-level key to both message files covering: `"Bulk Import"` heading, `"Download the template, fill in your data, and upload."` subtext, file-picker/upload/download-template button labels, the results table structure (headers for imported counts — classes/students/homework — and any row-level validation error display), and the local `error` state's fallback string `'Upload failed'`.
In `import/page.tsx`: import `useTranslations` from `next-intl`, `const t = useTranslations('teacher.import');` inside `TeacherImportPage`. Replace every hardcoded string with `t('...')`, including `setError(e instanceof Error ? e.message : 'Upload failed')` → `setError(e instanceof Error ? e.message : t('toasts.upload_failed'))` (or an appropriately named non-toast key if this page uses local Alert state rather than `showToast` — confirm from the read and use a consistent key name, e.g. `errors.upload_failed`, if it's not actually a toast call).
  </action>
  <acceptance_criteria>
    - `frontend/messages/en/teacher.json` and `frontend/messages/vi/teacher.json` both have a top-level `import` key with matching structure
    - `frontend/app/teacher/import/page.tsx` imports and calls `useTranslations('teacher.import')`
    - `grep -c "Bulk Import\|'Upload failed'" frontend/app/teacher/import/page.tsx` returns `0`
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>import/page.tsx fully translation-driven, including results-table headers and upload-failed fallback.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `.xlsx` upload result (`ImportResponse`) → results table rendering | Server-computed counts/row errors render alongside translated column headers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-08 | Injection (XSS) | Import results table (row error messages from backend) | accept | Row error text originates from the existing `ImportResponse`/`isImportError` backend contract (unchanged by this phase) and is rendered as plain React text children, same as before this extraction — no new rendering path introduced. |
| T-18-09 | Tampering | `messages/{en,vi}/teacher.json` `sessions`/`import` keys | accept | Static, build-time JSON. |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit -p tsconfig.json` — no new type errors
- Manual: switch locale, load `/teacher/sessions` (with filters applied) and `/teacher/import` (upload a test file), confirm all text switches language
</verification>

<success_criteria>
1. `frontend/app/teacher/sessions/page.tsx` and `frontend/app/teacher/import/page.tsx` contain zero hardcoded user-facing strings.
2. `teacher.json` (both locales) has matching `sessions`/`import` namespaces.
3. Sessions' load-error toast is catalog-sourced.
</success_criteria>

<output>
Create `.planning/phases/18-multi-language-support-across-all-pages/18-04-SUMMARY.md` when done
</output>
