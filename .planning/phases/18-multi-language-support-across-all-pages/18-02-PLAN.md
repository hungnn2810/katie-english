---
phase: 18
plan: 02
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - frontend/app/teacher/page.tsx
  - frontend/app/teacher/login/page.tsx
  - frontend/app/teacher/schedule/page.tsx
  - frontend/messages/en/teacher.json
  - frontend/messages/vi/teacher.json
autonomous: true
requirements: [i18n-04, i18n-05]
must_haves:
  truths:
    - "Teacher dashboard, login, and schedule pages render fully in Vietnamese when VI is selected and fully in English when EN is selected — no leftover hardcoded strings in either direction"
    - "The one toast on each of these three pages (dashboard load error, login invalid-credentials, schedule has none) sources its message from the translation catalog, not an inline literal"
  artifacts:
    - path: "frontend/messages/en/teacher.json"
      provides: "adds dashboard, login, schedule top-level namespaces"
    - path: "frontend/messages/vi/teacher.json"
      provides: "adds dashboard, login, schedule top-level namespaces (mirrored keys)"
  key_links:
    - from: "frontend/app/teacher/page.tsx"
      to: "frontend/messages/en/teacher.json"
      via: "useTranslations('teacher.dashboard')"
      pattern: "useTranslations\\('teacher\\.dashboard'\\)"
    - from: "frontend/app/teacher/login/page.tsx"
      to: "frontend/messages/en/teacher.json"
      via: "useTranslations('teacher.login')"
      pattern: "useTranslations\\('teacher\\.login'\\)"
    - from: "frontend/app/teacher/schedule/page.tsx"
      to: "frontend/messages/en/teacher.json"
      via: "useTranslations('teacher.schedule')"
      pattern: "useTranslations\\('teacher\\.schedule'\\)"
---

<objective>
Extract all hardcoded UI strings from the three smallest, most self-contained Teacher-portal pages — dashboard (`/teacher`), login (`/teacher/login`), and schedule (`/teacher/schedule`) — into the `teacher.json` catalog started in 18-01, applying next-intl's `useTranslations` hook and normalizing the one toast on the dashboard/login pages per D-11.

Purpose: First real extraction plan; validates the manual-extraction pattern (per RESEARCH.md Open Question #1 — manual chosen over codemod) on small files before applying it to the larger pages in later waves. Also fixes the existing bilingual inconsistency where `schedule/page.tsx` is Vietnamese-only and `login/page.tsx`/`page.tsx` are English-only (CONTEXT.md finding).
Output: Three page files fully driven by `useTranslations`, `teacher.json` extended with `dashboard`/`login`/`schedule` namespaces in both locales.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-multi-language-support-across-all-pages/18-CONTEXT.md
@.planning/phases/18-multi-language-support-across-all-pages/18-01-SUMMARY.md

<interfaces>
<!-- next-intl usage pattern established in 18-01 -->
`import { useTranslations } from 'next-intl';` then `const t = useTranslations('teacher.<namespace>');` then `t('key')` or `t('key', { var: value })` using single-brace `{var}` ICU interpolation (NOT `{{var}}`).
Toast pattern established: `showToast(t('toasts.<key>'), 'success' | 'error')` — keep `err instanceof Error ? err.message : t('toasts.<fallback_key>')` for error branches; `err.message` itself (backend-originated text) stays untranslated, out of this phase's scope.

<!-- frontend/app/teacher/page.tsx key structural elements (dashboard) -->
Imports `getClasses, getStudents, getHomeworkList, getPendingStudents, getPasswordResetRequests` from `@/lib/admin-api`. Local `formatRelativeTime(date)` helper produces English strings ("in 5 min", "tomorrow", "in 3 days") — these ARE user-facing (shown in an "upcoming classes" widget) and must be extracted too. One `showToast` call at line 106: `showToast(err instanceof Error ? err.message : 'Failed to load dashboard.', 'error')`.

<!-- frontend/app/teacher/login/page.tsx (145 lines, full file) -->
Static strings: "Teacher Portal" + "the smart way" heading, subtitle "Create homework, manage classes, and track student progress with AI-powered scoring.", feature list `[{ text: 'Manage your classes and students' }, { text: 'AI-scored pronunciation homework' }]`, "Teacher Login" heading, "Sign in to your teacher account" subtitle, `TextField` labels "Email"/"Password", placeholders "teacher@katie.com"/"••••••••", submit button "Signing in..."/"Sign in", one `showToast('Invalid credentials', 'error')` at line 41.

<!-- frontend/app/teacher/schedule/page.tsx (85 lines, full file, currently Vietnamese-only) -->
`FILTERS` array: `{ key: 'INPROGRESS', label: 'Đang học' }, { key: 'PENDING', label: 'Chờ khai giảng' }, { key: 'ALL', label: 'Tất cả' }, { key: 'ENDED', label: 'Đã kết thúc' }`. Count text `{withSchedule.length} lớp có lịch học`. Empty state: "Không có lớp nào có lịch học" / "Yêu cầu admin thêm lịch học cho lớp của bạn". No toast on this page.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extract dashboard page.tsx</name>
  <files>frontend/app/teacher/page.tsx, frontend/messages/en/teacher.json, frontend/messages/vi/teacher.json</files>
  <read_first>frontend/app/teacher/page.tsx, frontend/messages/en/teacher.json, frontend/messages/vi/teacher.json</read_first>
  <action>
Add a `dashboard` top-level key to both `frontend/messages/en/teacher.json` and `frontend/messages/vi/teacher.json` covering every static string in `frontend/app/teacher/page.tsx`: page/section headings, stat card labels, the "upcoming classes" widget's relative-time phrasing (as ICU-interpolated templates, e.g. `"in_minutes": "in {count} min"` / `"trong {count} phút nữa"`, `"tomorrow"`, `"in_hours": "in {count}h"` / `"trong {count} giờ nữa"`, `"in_days": "in {count} days"` / `"trong {count} ngày nữa"`), empty-state copy, and a `dashboard.toasts.load_error` key for the existing `'Failed to load dashboard.'` fallback.
In `page.tsx`: import `useTranslations` from `next-intl`, add `const t = useTranslations('teacher.dashboard');` at the top of the component. Replace `formatRelativeTime`'s hardcoded return strings with `t('in_minutes', { count: Math.max(mins, 1) })` / `t('tomorrow')` / `t('in_hours', { count: hours })` / `t('in_days', { count: days })` (pass `t` into the helper or convert it to a closure that captures `t`). Replace the `showToast(err instanceof Error ? err.message : 'Failed to load dashboard.', 'error')` fallback string with `t('toasts.load_error')`. Replace every other hardcoded JSX text node in this file with the corresponding `t('...')` call, adding new keys to the JSON files as needed for anything not pre-listed above.
  </action>
  <acceptance_criteria>
    - `frontend/messages/en/teacher.json` and `frontend/messages/vi/teacher.json` both have a top-level `dashboard` key with identical key structure
    - `frontend/app/teacher/page.tsx` imports and calls `useTranslations('teacher.dashboard')`
    - `grep -c "showToast('Failed to load dashboard" frontend/app/teacher/page.tsx` returns `0`
    - No JSX text node in `frontend/app/teacher/page.tsx` is a bare English string literal (spot-check: `grep -n ">[A-Z][a-z].*<" frontend/app/teacher/page.tsx` returns no matches for former hardcoded headings)
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>Dashboard page fully translation-driven; relative-time widget uses ICU interpolation; load-error toast normalized.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Extract login page.tsx</name>
  <files>frontend/app/teacher/login/page.tsx, frontend/messages/en/teacher.json, frontend/messages/vi/teacher.json</files>
  <read_first>frontend/app/teacher/login/page.tsx, frontend/messages/en/teacher.json, frontend/messages/vi/teacher.json</read_first>
  <action>
Add a `login` top-level key to both message files covering: heading `"Teacher Portal"` + `"the smart way"`, subtitle, the two feature-list strings (`"Manage your classes and students"`, `"AI-scored pronunciation homework"`), form heading `"Teacher Login"`, form subtitle `"Sign in to your teacher account"`, field labels `"Email"`/`"Password"`, submit button states `"Signing in..."`/`"Sign in"`, and `login.toasts.invalid_credentials` for `'Invalid credentials'`. Note: this page renders standalone (before the authenticated `TeacherShell` tree, via `TeacherLayoutClient`'s `if (!user) return <>{children}</>` branch) but is still inside the `NextIntlClientProvider` established in 18-01's `layout.tsx`, so `useTranslations` works here too.
In `login/page.tsx`: import `useTranslations` from `next-intl`, `const t = useTranslations('teacher.login');`, replace every hardcoded string identified above with `t('...')` calls, and replace `showToast('Invalid credentials', 'error')` with `showToast(t('toasts.invalid_credentials'), 'error')`. Leave the `placeholder="teacher@katie.com"` and `placeholder="••••••••"` example-value placeholders as-is (they are illustrative example data, not translatable UI copy) unless they contain language-specific words — `teacher@katie.com` and the password dots do not, so leave them untouched.
  </action>
  <acceptance_criteria>
    - `frontend/messages/en/teacher.json` and `frontend/messages/vi/teacher.json` both have a top-level `login` key with identical key structure
    - `frontend/app/teacher/login/page.tsx` imports and calls `useTranslations('teacher.login')`
    - `grep -c "showToast('Invalid credentials'" frontend/app/teacher/login/page.tsx` returns `0`
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>Login page fully translation-driven, invalid-credentials toast sourced from teacher.json.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Extract schedule page.tsx (normalize VI-only inconsistency)</name>
  <files>frontend/app/teacher/schedule/page.tsx, frontend/messages/en/teacher.json, frontend/messages/vi/teacher.json</files>
  <read_first>frontend/app/teacher/schedule/page.tsx, frontend/messages/en/teacher.json, frontend/messages/vi/teacher.json</read_first>
  <action>
Add a `schedule` top-level key to both message files covering the `FILTERS` array labels (`INPROGRESS`/`PENDING`/`ALL`/`ENDED` — reuse the same status-label wording pattern that will later appear in `classes.classStatus` in Plan 18-03; if that key doesn't exist yet, use local keys `schedule.filters.inProgress`/`pending`/`all`/`ended` for now), the count text template `"{count} classes have a schedule"` / `"{count} lớp có lịch học"`, and the empty-state heading/subtext.
In `schedule/page.tsx`: import `useTranslations` from `next-intl`, `const t = useTranslations('teacher.schedule');`. Change `FILTERS` from a hardcoded array to build labels via `t(...)` at render/construction time (e.g. `const FILTERS = [{ key: 'INPROGRESS', label: t('filters.inProgress') }, ...]` computed inside the component body, not as a module-level const, since `t` is only available inside the component). Replace the count text `{withSchedule.length} lớp có lịch học` with `t('scheduledCount', { count: withSchedule.length })`. Replace the two empty-state strings with `t('emptyTitle')` / `t('emptySubtitle')`.
  </action>
  <acceptance_criteria>
    - `frontend/messages/en/teacher.json` and `frontend/messages/vi/teacher.json` both have a top-level `schedule` key with identical key structure
    - `frontend/app/teacher/schedule/page.tsx` imports and calls `useTranslations('teacher.schedule')`
    - `grep -c "Đang học\|Chờ khai giảng\|Tất cả\|Đã kết thúc\|lớp có lịch học" frontend/app/teacher/schedule/page.tsx` returns `0` (no remaining hardcoded Vietnamese)
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>Schedule page fully translation-driven in both EN and VI (previously VI-only hardcoded).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `t()` interpolated strings → MUI `Typography`/`Alert` children | Translation catalog content rendered as React children |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-04 | Tampering | `messages/{en,vi}/teacher.json` | accept | Static, build-time-bundled JSON; not user-writable at runtime; no attacker-reachable write path exists in this phase. |
| T-18-05 | Injection (XSS) | `showToast(t('toasts.invalid_credentials'))` / dashboard relative-time strings | accept | React renders all `t(...)` output as plain text children (no `dangerouslySetInnerHTML` used anywhere in these three files); standard JSX escaping applies. |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit -p tsconfig.json` — no new type errors
- Manual: switch locale via LanguageSwitcher, load `/teacher`, `/teacher/login` (log out first), `/teacher/schedule` — confirm all visible text switches language, including the schedule filter pills (previously VI-only) and the login page (previously EN-only)
</verification>

<success_criteria>
1. `frontend/app/teacher/page.tsx`, `frontend/app/teacher/login/page.tsx`, `frontend/app/teacher/schedule/page.tsx` contain zero hardcoded user-facing strings.
2. `teacher.json` (both locales) has matching `dashboard`/`login`/`schedule` namespaces.
3. The dashboard load-error toast and login invalid-credentials toast are both catalog-sourced (D-11 progress).
</success_criteria>

<output>
Create `.planning/phases/18-multi-language-support-across-all-pages/18-02-SUMMARY.md` when done
</output>
