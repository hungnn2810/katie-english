---
phase: 18
plan: 05
type: execute
wave: 5
depends_on: ["18-04"]
files_modified:
  - frontend/app/teacher/tuition/page.tsx
  - frontend/app/admin/tuition/_components/TuitionConfigForm.tsx
  - frontend/app/admin/tuition/_components/GenerateRecordsModal.tsx
  - frontend/app/admin/tuition/_components/TuitionReportTable.tsx
  - frontend/messages/en/teacher.json
  - frontend/messages/vi/teacher.json
autonomous: true
requirements: [i18n-04, i18n-05]
must_haves:
  truths:
    - "Teacher-portal tuition page (config tab, generate-records tab, report tab) renders fully in Vietnamese when VI is selected and fully in English when EN is selected — the pre-existing VI-only hardcoding (CONTEXT.md finding) is fully normalized"
    - "The 3 toasts across TuitionConfigForm/GenerateRecordsModal/TuitionReportTable source their messages from the translation catalog"
    - "Currency amounts keep their exact vi-VN toLocaleString formatting and VNĐ suffix regardless of UI language (D-12 — not translated)"
  artifacts:
    - path: "frontend/messages/en/teacher.json"
      provides: "adds tuition top-level namespace"
    - path: "frontend/messages/vi/teacher.json"
      provides: "adds tuition top-level namespace (mirrored keys)"
  key_links:
    - from: "frontend/app/teacher/tuition/page.tsx"
      to: "frontend/messages/en/teacher.json"
      via: "useTranslations('teacher.tuition')"
      pattern: "useTranslations\\('teacher\\.tuition'\\)"
    - from: "frontend/app/admin/tuition/_components/TuitionReportTable.tsx"
      to: "frontend/messages/en/teacher.json"
      via: "useTranslations('teacher.tuition.report')"
      pattern: "useTranslations\\('teacher\\.tuition"
---

<objective>
Extract all hardcoded UI strings — currently 100% Vietnamese — from `frontend/app/teacher/tuition/page.tsx` and the three shared tuition components it renders directly (`TuitionConfigForm.tsx`, `GenerateRecordsModal.tsx`, `TuitionReportTable.tsx`, all under `frontend/app/admin/tuition/_components/`), while explicitly leaving currency formatting (D-12) and date formatting (D-13) untouched.

Purpose: This is the CONTEXT.md-flagged case of the pre-existing EN/VI inconsistency (D-11) — tuition module is Vietnamese-only while classes/homework are English-only. This plan is the concrete fix for that specific example.
**Scope note:** `frontend/app/admin/tuition/_components/PaymentRecordDialog.tsx` and `ZaloSendModal.tsx` are verified (via `grep -rn "PaymentRecordDialog|ZaloSendModal" frontend/app`) to be imported ONLY by `frontend/app/admin/tuition/page.tsx` (admin portal, out of scope per D-10) — NOT by `frontend/app/teacher/tuition/page.tsx`. They are excluded from this plan's `files_modified` because they are unreachable from the Teacher portal; touching them would extend scope into the deferred Admin portal migration. `TuitionConfigForm.tsx`, `GenerateRecordsModal.tsx`, and `TuitionReportTable.tsx` ARE imported directly by `frontend/app/teacher/tuition/page.tsx` and are therefore in scope.
Output: All 4 in-scope files fully translation-driven; `teacher.json` extended with a `tuition` namespace in both locales.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-multi-language-support-across-all-pages/18-CONTEXT.md

<interfaces>
<!-- next-intl usage pattern (established 18-01) -->
`const t = useTranslations('teacher.<namespace>');` then `t('key')` / `t('key', { var })`, single-brace ICU interpolation.

<!-- frontend/app/teacher/tuition/page.tsx (189 lines, full file, 100% Vietnamese) -->
`export default function TeacherTuitionPage()`. One `showToast(err instanceof Error ? err.message : 'Tải danh sách lớp thất bại', 'error')` (line 48). Empty-classes state: `"Bạn chưa được phân công lớp nào."`. Page heading `"Quản lý học phí"`. Class-selector `FormLabel` `"Lớp học"`. No-class-selected text `"Chọn một lớp để quản lý học phí."`. Three `Tab` labels: `"Cấu hình"`, `"Tạo phiếu thu"`, `"Báo cáo"`. Tab 1 renders `<TuitionConfigForm>`. Tab 2: intro text `"Tạo phiếu thu học phí cho tất cả học sinh trong lớp theo tháng."`, button `"Tạo phiếu thu tháng này"`, renders `<GenerateRecordsModal>`. Tab 3: `FormLabel`s `"Tháng"`/`"Năm"`, renders `<TuitionReportTable>` or fallback text `"Chọn lớp để xem báo cáo."`.
CRITICAL — this file does NOT import `TuitionConfigForm`/`GenerateRecordsModal`/`TuitionReportTable` from a teacher-local path; it imports them from `'../../admin/tuition/_components/...'` (relative cross-app import). Do not move or rename these shared component files — only edit their string content in place.

<!-- frontend/app/admin/tuition/_components/TuitionConfigForm.tsx (155 lines, 100% Vietnamese) -->
`export default function TuitionConfigForm({...})`. Two `showToast` calls: `showToast('Cấu hình học phí đã lưu', 'success')` (line 70), `showToast(err instanceof Error ? err.message : 'Lưu thất bại', 'error')` (line 74). Form fields (per-buổi rate, book fee, due-day) — read the full file for exact label text before extracting. D-12 note: any currency-amount `TextField`/display in this file must keep its numeric formatting logic untouched — only the surrounding `label`/placeholder text translates.

<!-- frontend/app/admin/tuition/_components/GenerateRecordsModal.tsx (79 lines) -->
`export default function GenerateRecordsModal({...})`. `showToast('Tạo phiếu thu thành công', 'success')` (line 34), `showToast(err instanceof Error ? err.message : 'Tạo phiếu thu thất bại', 'error')` (line 38). Read full file for modal title/body/button text.

<!-- frontend/app/admin/tuition/_components/TuitionReportTable.tsx (227 lines) -->
`function StatusBadge({ status }: { status: 'PAID' | 'PENDING' | 'OVERDUE' })` (line 28) — status labels need translation. `export default function TuitionReportTable({...})` (line 42). `showToast(err instanceof Error ? err.message : 'Tải báo cáo thất bại', 'error')` (line 81). This component almost certainly renders currency amounts via `toLocaleString('vi-VN')` + a `VNĐ` suffix — per D-12, locate every such call site and confirm the extraction task does NOT alter the numeric formatting call itself, only adjacent labels/headers.

<!-- frontend/lib/teacher-tuition-api.ts -->
Imported by teacher/tuition/page.tsx for `getTuitionConfig, updateTuitionConfig, createTuitionRecords, getTuitionReport` — these are passed as `getConfigFn`/`updateConfigFn`/`createRecordsFn`/`getReportFn` props into the shared components (dependency-injection pattern so the same component works with both admin-JWT and teacher-JWT API variants). Do not alter this prop-injection pattern.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extract teacher/tuition/page.tsx</name>
  <files>frontend/app/teacher/tuition/page.tsx, frontend/messages/en/teacher.json, frontend/messages/vi/teacher.json</files>
  <read_first>frontend/app/teacher/tuition/page.tsx, frontend/messages/en/teacher.json, frontend/messages/vi/teacher.json</read_first>
  <action>
Add a `tuition` top-level key to both message files with a `page` sub-namespace covering: page heading, empty-classes state text, class-selector label, no-class-selected text, the 3 tab labels, tab-2 intro text + button, tab-3 `FormLabel`s ("Month"/"Year" and Vietnamese equivalents), fallback "select class to view report" text, and `page.toasts.load_classes_error` for the `'Tải danh sách lớp thất bại'` fallback.
In `teacher/tuition/page.tsx`: import `useTranslations` from `next-intl`, `const t = useTranslations('teacher.tuition.page');` inside `TeacherTuitionPage`. Replace every hardcoded Vietnamese string identified above with `t('...')`. Replace `showToast(err instanceof Error ? err.message : 'Tải danh sách lớp thất bại', 'error')` with `showToast(err instanceof Error ? err.message : t('toasts.load_classes_error'), 'error')`.
  </action>
  <acceptance_criteria>
    - `frontend/messages/en/teacher.json` and `frontend/messages/vi/teacher.json` both have `tuition.page` with matching structure
    - `frontend/app/teacher/tuition/page.tsx` imports and calls `useTranslations('teacher.tuition.page')`
    - `grep -c "Quản lý học phí\|Cấu hình\|Tạo phiếu thu\|Báo cáo" frontend/app/teacher/tuition/page.tsx` returns `0`
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>teacher/tuition/page.tsx fully translation-driven (previously 100% hardcoded Vietnamese).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Extract TuitionConfigForm.tsx + GenerateRecordsModal.tsx</name>
  <files>frontend/app/admin/tuition/_components/TuitionConfigForm.tsx, frontend/app/admin/tuition/_components/GenerateRecordsModal.tsx, frontend/messages/en/teacher.json, frontend/messages/vi/teacher.json</files>
  <read_first>frontend/app/admin/tuition/_components/TuitionConfigForm.tsx, frontend/app/admin/tuition/_components/GenerateRecordsModal.tsx, frontend/messages/en/teacher.json, frontend/messages/vi/teacher.json</read_first>
  <action>
Add `tuition.configForm` and `tuition.generateModal` sub-namespaces to both message files covering every form field label, helper text, button, and both files' 2 toasts each (`configForm.toasts.saved`/`save_error`, `generateModal.toasts.created`/`create_error`).
In `TuitionConfigForm.tsx`: import `useTranslations` from `next-intl`, `const t = useTranslations('teacher.tuition.configForm');`. Replace `showToast('Cấu hình học phí đã lưu', 'success')` with `showToast(t('toasts.saved'), 'success')`, `showToast(err instanceof Error ? err.message : 'Lưu thất bại', 'error')` with `showToast(err instanceof Error ? err.message : t('toasts.save_error'), 'error')`, and all form field labels. Do NOT modify any currency/number formatting logic in this file (D-12) — only label text.
In `GenerateRecordsModal.tsx`: import `useTranslations`, `const t = useTranslations('teacher.tuition.generateModal');`. Replace `showToast('Tạo phiếu thu thành công', 'success')` with `showToast(t('toasts.created'), 'success')`, `showToast(err instanceof Error ? err.message : 'Tạo phiếu thu thất bại', 'error')` with `showToast(err instanceof Error ? err.message : t('toasts.create_error'), 'error')`, and all modal title/body/button text.
  </action>
  <acceptance_criteria>
    - `frontend/messages/en/teacher.json` and `frontend/messages/vi/teacher.json` both have `tuition.configForm` and `tuition.generateModal` with matching structure
    - Both files import and call `useTranslations`
    - `grep -c "Cấu hình học phí đã lưu\|Lưu thất bại\|Tạo phiếu thu thành công\|Tạo phiếu thu thất bại" frontend/app/admin/tuition/_components/TuitionConfigForm.tsx frontend/app/admin/tuition/_components/GenerateRecordsModal.tsx` returns `0`
    - No `toLocaleString` or `VNĐ`-suffix line in `TuitionConfigForm.tsx` was modified (diff review: currency formatting logic byte-identical to before this task)
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>TuitionConfigForm.tsx and GenerateRecordsModal.tsx fully translation-driven; currency formatting untouched per D-12.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Extract TuitionReportTable.tsx</name>
  <files>frontend/app/admin/tuition/_components/TuitionReportTable.tsx, frontend/messages/en/teacher.json, frontend/messages/vi/teacher.json</files>
  <read_first>frontend/app/admin/tuition/_components/TuitionReportTable.tsx, frontend/messages/en/teacher.json, frontend/messages/vi/teacher.json</read_first>
  <action>
Add a `tuition.report` sub-namespace to both message files covering: `StatusBadge`'s three status labels (`PAID`/`PENDING`/`OVERDUE`), table column headers, totals/summary row labels, empty-state text, and `report.toasts.load_error` for `'Tải báo cáo thất bại'`.
In `TuitionReportTable.tsx`: import `useTranslations` from `next-intl`, `const t = useTranslations('teacher.tuition.report');` inside both `StatusBadge` and `TuitionReportTable` (2 separate calls, since `StatusBadge` is its own function component). Replace status labels via `t(\`status.${status}\`)`-style lookup (or explicit per-status `t()` calls), all column headers, and `showToast(err instanceof Error ? err.message : 'Tải báo cáo thất bại', 'error')` with `showToast(err instanceof Error ? err.message : t('toasts.load_error'), 'error')`. Per D-12: locate every `toLocaleString('vi-VN')` call and `VNĐ` suffix in this file and leave them completely unmodified — only translate the column header/label text around them, never the amount formatting itself.
  </action>
  <acceptance_criteria>
    - `frontend/messages/en/teacher.json` and `frontend/messages/vi/teacher.json` both have `tuition.report` with matching structure including 3 status labels
    - `frontend/app/admin/tuition/_components/TuitionReportTable.tsx` imports `useTranslations` and calls it in both `StatusBadge` and `TuitionReportTable`
    - `grep -c "toLocaleString('vi-VN')" frontend/app/admin/tuition/_components/TuitionReportTable.tsx` returns the SAME count as before this task (no formatting call removed or altered — confirm via read before/after)
    - `grep -c "Tải báo cáo thất bại" frontend/app/admin/tuition/_components/TuitionReportTable.tsx` returns `0`
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>TuitionReportTable.tsx fully translation-driven; StatusBadge labels and toast normalized; currency formatting preserved exactly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Teacher-portal tuition page → shared `admin/tuition/_components/*` | Cross-app-boundary component reuse; edits here also affect the (out-of-scope, unmigrated) Admin portal's rendering of the same components |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-10 | Tampering | Shared `TuitionConfigForm.tsx`/`GenerateRecordsModal.tsx`/`TuitionReportTable.tsx` also rendered by `admin/tuition/page.tsx` | mitigate | These components read translations via `useTranslations('teacher.tuition.*')` scoped to the `teacher` namespace; since `admin/tuition/page.tsx` is NOT wrapped in `NextIntlClientProvider` in this phase (admin layout untouched, D-10), rendering them there would throw a missing-context error at runtime for the admin portal until Admin's own i18n migration phase wires an equivalent provider — this is a known, accepted forward-compatibility gap explicitly flagged for the Admin follow-up phase, not a security issue, but MUST be called out in this plan's SUMMARY so it isn't silently discovered later. |
| T-18-11 | Information Disclosure | Currency amounts (`toLocaleString('vi-VN')`) | accept | Real monetary values already visible to authenticated teachers pre-phase; this plan changes zero currency-display logic (D-12). |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit -p tsconfig.json` — no new type errors
- Manual: switch locale, exercise all 3 tuition tabs (config, generate, report) on `/teacher/tuition`, confirm every label/toast switches language while currency amounts and `VNĐ` suffix stay identical in both locales
- Diff review: `git diff frontend/app/admin/tuition/_components/TuitionConfigForm.tsx frontend/app/admin/tuition/_components/TuitionReportTable.tsx` — confirm no `toLocaleString`/currency-formatting lines appear in the diff
</verification>

<success_criteria>
1. `frontend/app/teacher/tuition/page.tsx` and the 3 in-scope shared tuition components contain zero hardcoded Vietnamese strings.
2. `teacher.json` (both locales) has a `tuition` namespace with `page`/`configForm`/`generateModal`/`report` sub-keys.
3. All 4 toasts (1 page + 2 configForm/generateModal + 1 report) are catalog-sourced.
4. Currency formatting (`toLocaleString('vi-VN')`, `VNĐ` suffix) is byte-identical to pre-phase state.
5. `PaymentRecordDialog.tsx`/`ZaloSendModal.tsx` (admin-only, unreachable from Teacher portal) are untouched.
</success_criteria>

<output>
Create `.planning/phases/18-multi-language-support-across-all-pages/18-05-SUMMARY.md` when done
</output>
