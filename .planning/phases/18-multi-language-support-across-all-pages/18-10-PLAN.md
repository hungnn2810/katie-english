---
phase: 18
plan: 10
type: execute
wave: 10
depends_on: ["18-09"]
files_modified:
  - frontend/app/teacher/homework/_components/VocabCreationPage.tsx
  - frontend/app/teacher/homework/_components/ListenCreationPage.tsx
  - frontend/messages/en/teacher.json
  - frontend/messages/vi/teacher.json
autonomous: true
requirements: [i18n-04, i18n-05]
must_haves:
  truths:
    - "Vocabulary and Listen homework creation/edit pages render fully in Vietnamese when VI is selected and fully in English when EN is selected"
    - "The 2 showToast calls per file (4 total) source their message from the translation catalog"
  artifacts:
    - path: "frontend/messages/en/teacher.json"
      provides: "adds vocabCreate, listenCreate top-level namespaces"
    - path: "frontend/messages/vi/teacher.json"
      provides: "adds vocabCreate, listenCreate top-level namespaces (mirrored keys)"
  key_links:
    - from: "frontend/app/teacher/homework/_components/VocabCreationPage.tsx"
      to: "frontend/messages/en/teacher.json"
      via: "useTranslations('teacher.vocabCreate')"
      pattern: "useTranslations\\('teacher\\.vocabCreate'\\)"
    - from: "frontend/app/teacher/homework/_components/ListenCreationPage.tsx"
      to: "frontend/messages/en/teacher.json"
      via: "useTranslations('teacher.listenCreate')"
      pattern: "useTranslations\\('teacher\\.listenCreate'\\)"
---

<objective>
Extract all hardcoded UI strings from `frontend/app/teacher/homework/_components/VocabCreationPage.tsx` (505 lines — `SortableVocabItemCard`, `VocabCreationPage`) and `frontend/app/teacher/homework/_components/ListenCreationPage.tsx` (545 lines — `SortableListenItemCard`, `ListenCreationPage`), reached via the thin wrapper pages at `/teacher/homework/create/{vocabulary,listen}` and `/teacher/homework/[id]/edit/{vocabulary,listen}` confirmed to have no strings of their own (Plan 18-06).

Purpose: Final pair of the three creation-flow `_components` files (after ReadingCreationPage in 18-09), both structurally similar (sortable item-card editor + top-level save/load page).
Output: Both files fully translation-driven; `teacher.json` extended with `vocabCreate`/`listenCreate` namespaces in both locales.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-multi-language-support-across-all-pages/18-CONTEXT.md

<interfaces>
<!-- next-intl usage pattern (established 18-01) -->
`const t = useTranslations('teacher.<namespace>');` then `t('key')` / `t('key', { var })`, single-brace ICU interpolation.

<!-- frontend/app/teacher/homework/_components/VocabCreationPage.tsx structural map (505 lines) -->
`function SortableVocabItemCard({...})` (48-238) — per-item image-upload + word-label card with drag handle.
`export function VocabCreationPage({ editId }: { editId?: number })` (239-505) — `.catch(() => showToast('Failed to load homework.', 'error'))` (263), `showToast(err instanceof Error ? err.message : 'Failed to save.', 'error')` (356).

<!-- frontend/app/teacher/homework/_components/ListenCreationPage.tsx structural map (545 lines) -->
`function SortableListenItemCard({...})` (50-254) — per-item audio-upload/TTS + keyword field card with drag handle.
`export function ListenCreationPage({ editId }: { editId?: number })` (255-545) — `.catch(() => showToast('Failed to load homework.', 'error'))` (288), `showToast(err instanceof Error ? err.message : 'Failed to save.', 'error')` (401).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extract VocabCreationPage.tsx</name>
  <files>frontend/app/teacher/homework/_components/VocabCreationPage.tsx, frontend/messages/en/teacher.json, frontend/messages/vi/teacher.json</files>
  <read_first>frontend/app/teacher/homework/_components/VocabCreationPage.tsx, frontend/messages/en/teacher.json, frontend/messages/vi/teacher.json</read_first>
  <action>
Read the full file. Add `vocabCreate.itemCard` and `vocabCreate.page` sub-namespaces to both message files covering every field label/button/instruction in `SortableVocabItemCard` (image-upload prompt, word-label field, remove-item button, drag-handle `aria-label`) and `VocabCreationPage` (page heading, title/class-assignment fields, add-item button, save/cancel, and `toasts.load_error`/`toasts.save_error`).
In the file: import `useTranslations` from `next-intl`. Add `const t = useTranslations('teacher.vocabCreate.itemCard');` inside `SortableVocabItemCard` and `const t = useTranslations('teacher.vocabCreate.page');` inside `VocabCreationPage`. Replace `.catch(() => showToast('Failed to load homework.', 'error'))` (263) with `.catch(() => showToast(t('toasts.load_error'), 'error'))`, `showToast(err instanceof Error ? err.message : 'Failed to save.', 'error')` (356) with `showToast(err instanceof Error ? err.message : t('toasts.save_error'), 'error')`, and every remaining hardcoded string.
  </action>
  <acceptance_criteria>
    - `frontend/messages/en/teacher.json` and `frontend/messages/vi/teacher.json` both have `vocabCreate.itemCard` and `vocabCreate.page` with matching structure
    - `SortableVocabItemCard` and `VocabCreationPage` each call `useTranslations` independently
    - `grep -c "'Failed to load homework.'\|'Failed to save.'" frontend/app/teacher/homework/_components/VocabCreationPage.tsx` returns `0`
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>VocabCreationPage.tsx fully translation-driven; both toasts normalized.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Extract ListenCreationPage.tsx</name>
  <files>frontend/app/teacher/homework/_components/ListenCreationPage.tsx, frontend/messages/en/teacher.json, frontend/messages/vi/teacher.json</files>
  <read_first>frontend/app/teacher/homework/_components/ListenCreationPage.tsx, frontend/messages/en/teacher.json, frontend/messages/vi/teacher.json</read_first>
  <action>
Read the full file. Add `listenCreate.itemCard` and `listenCreate.page` sub-namespaces to both message files covering every field label/button/instruction in `SortableListenItemCard` (audio-upload/TTS prompt, keyword field, remove-item button, drag-handle `aria-label`) and `ListenCreationPage` (page heading, title/class-assignment fields, add-item button, save/cancel, and `toasts.load_error`/`toasts.save_error`).
In the file: import `useTranslations` from `next-intl`. Add `const t = useTranslations('teacher.listenCreate.itemCard');` inside `SortableListenItemCard` and `const t = useTranslations('teacher.listenCreate.page');` inside `ListenCreationPage`. Replace `.catch(() => showToast('Failed to load homework.', 'error'))` (288) with `.catch(() => showToast(t('toasts.load_error'), 'error'))`, `showToast(err instanceof Error ? err.message : 'Failed to save.', 'error')` (401) with `showToast(err instanceof Error ? err.message : t('toasts.save_error'), 'error')`, and every remaining hardcoded string.
  </action>
  <acceptance_criteria>
    - `frontend/messages/en/teacher.json` and `frontend/messages/vi/teacher.json` both have `listenCreate.itemCard` and `listenCreate.page` with matching structure
    - `SortableListenItemCard` and `ListenCreationPage` each call `useTranslations` independently
    - `grep -c "'Failed to load homework.'\|'Failed to save.'" frontend/app/teacher/homework/_components/ListenCreationPage.tsx` returns `0`
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>ListenCreationPage.tsx fully translation-driven; both toasts normalized.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Teacher-authored vocab word labels / listen keywords → item-card editor UI | Teacher-entered content, not student-entered; low-risk actor |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-20 | Injection (XSS) | `t()`-rendered editor labels alongside teacher-entered word/keyword content | accept | Rendered as separate React text children throughout (no `dangerouslySetInnerHTML`); unchanged from pre-existing behavior. |
| T-18-21 | Tampering | `messages/{en,vi}/teacher.json` `vocabCreate`/`listenCreate` keys | accept | Static, build-time JSON. |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit -p tsconfig.json` — no new type errors
- Manual: switch locale, create a Vocabulary homework and a Listen homework, confirm all UI text and all 4 toasts switch language
</verification>

<success_criteria>
1. `frontend/app/teacher/homework/_components/VocabCreationPage.tsx` and `ListenCreationPage.tsx` contain zero hardcoded user-facing strings.
2. `teacher.json` (both locales) has matching `vocabCreate`/`listenCreate` namespaces.
3. All 4 `showToast` call sites (2 per file) are catalog-sourced.
4. This plan completes string extraction for all 19 Teacher-portal pages plus all in-scope shared components (D-09 fully delivered).
</success_criteria>

<output>
Create `.planning/phases/18-multi-language-support-across-all-pages/18-10-SUMMARY.md` when done
</output>
