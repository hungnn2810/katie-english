---
phase: 18
plan: 07
type: execute
wave: 7
depends_on: ["18-06"]
files_modified:
  - frontend/app/teacher/homework/[id]/page.tsx
  - frontend/app/teacher/homework/[id]/session/[sessionId]/page.tsx
  - frontend/messages/en/teacher.json
  - frontend/messages/vi/teacher.json
autonomous: true
requirements: [i18n-04, i18n-05]
must_haves:
  truths:
    - "Homework detail page (type meta, assignment list, per-student progress) and session result detail page (per-activity/per-item result rows) render fully in Vietnamese when VI is selected and fully in English when EN is selected"
  artifacts:
    - path: "frontend/messages/en/teacher.json"
      provides: "adds homeworkDetail, sessionDetail top-level namespaces"
    - path: "frontend/messages/vi/teacher.json"
      provides: "adds homeworkDetail, sessionDetail top-level namespaces (mirrored keys)"
  key_links:
    - from: "frontend/app/teacher/homework/[id]/page.tsx"
      to: "frontend/messages/en/teacher.json"
      via: "useTranslations('teacher.homeworkDetail')"
      pattern: "useTranslations\\('teacher\\.homeworkDetail'\\)"
    - from: "frontend/app/teacher/homework/[id]/session/[sessionId]/page.tsx"
      to: "frontend/messages/en/teacher.json"
      via: "useTranslations('teacher.sessionDetail')"
      pattern: "useTranslations\\('teacher\\.sessionDetail'\\)"
---

<objective>
Extract all hardcoded UI strings from `frontend/app/teacher/homework/[id]/page.tsx` (277 lines — `TYPE_META`, `ProgressRing`, `TeacherHomeworkDetailPage`) and `frontend/app/teacher/homework/[id]/session/[sessionId]/page.tsx` (470 lines — `MatchingResultRow`, `FillInBlankResultRow`, `ActivityResultCard`, `VocabResultRow`, `TeacherSessionDetailPage`).

Purpose: These two read-heavy result/detail pages have no `showToast` calls (verified via grep — neither file appears in the 38-call-site inventory) but carry a large volume of static labels (homework type names, per-activity score breakdowns) that are exercised every time a teacher reviews results.
Output: Both files fully translation-driven; `teacher.json` extended with `homeworkDetail`/`sessionDetail` namespaces in both locales.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-multi-language-support-across-all-pages/18-CONTEXT.md

<interfaces>
<!-- next-intl usage pattern (established 18-01) -->
`const t = useTranslations('teacher.<namespace>');` then `t('key')` / `t('key', { var })`, single-brace ICU interpolation.

<!-- frontend/app/teacher/homework/[id]/page.tsx structural map (277 lines) -->
`const TYPE_META: Record<HomeworkType, {label, icon, color, bg}>` (line 14): `PHONICS: 'Phonics'`, `SPEAKING: 'Speaking'`, `READING: 'Reading'`, `VOCABULARY: 'Vocabulary'`, `LISTEN: 'Listen'` — `HomeworkType` imported from `@/lib/admin-api`. `function ProgressRing({...})` (line 28) — pure SVG, no text, skip. `export default function TeacherHomeworkDetailPage()` (line 40) — assignment list, per-student progress display, delete-assignment confirm flow (uses `deleteAssignment` from `@/lib/admin-api`, no toast visible in the excerpt read so far — verify while reading the full file whether a toast exists here before assuming none).

<!-- frontend/app/teacher/homework/[id]/session/[sessionId]/page.tsx structural map (470 lines) -->
`function scoreHex/scoreBg/scoreLabel(score)` (lines 15-33) — likely produce color/label like "Excellent"/"Good"/"Needs work" bucketed by score; if `scoreLabel` returns user-facing text, it needs translation (verify while reading). `function MatchingResultRow({ r })` (35), `function FillInBlankResultRow({ r })` (61), `function ActivityResultCard({ activityResult })` (89), `function VocabResultRow({ r })` (173) — each renders per-item score/correctness labels for READING and PHONICS/VOCABULARY activity types respectively. `export default function TeacherSessionDetailPage()` (220) — page chrome, student/session header info, activity-type section headers.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extract homework/[id]/page.tsx</name>
  <files>frontend/app/teacher/homework/[id]/page.tsx, frontend/messages/en/teacher.json, frontend/messages/vi/teacher.json</files>
  <read_first>frontend/app/teacher/homework/[id]/page.tsx, frontend/messages/en/teacher.json, frontend/messages/vi/teacher.json</read_first>
  <action>
Read the full file (it was only partially read during planning — verify whether any `showToast` calls exist beyond what was sampled, and extract them too if found). Add a `homeworkDetail` top-level key to both message files covering: `typeMeta` labels (PHONICS/SPEAKING/READING/VOCABULARY/LISTEN — keyed by type, same pattern as `homeworkCreate.hwTypes` from Plan 18-06), page heading/breadcrumb, assignment-list column headers or card labels, per-student progress text, delete-assignment confirmation copy, and a `toasts` sub-namespace for any toast calls found during the full read.
In `homework/[id]/page.tsx`: import `useTranslations` from `next-intl`, `const t = useTranslations('teacher.homeworkDetail');` inside `TeacherHomeworkDetailPage`. Resolve `TYPE_META`'s `label` field at render time via `t(\`typeMeta.${hw.type}\`)` (module-level const can't call hooks — keep `TYPE_META` for icon/color/bg, look up label text separately). Replace all other hardcoded strings, and any toast calls discovered on the full read, with `t('...')`.
  </action>
  <acceptance_criteria>
    - `frontend/messages/en/teacher.json` and `frontend/messages/vi/teacher.json` both have a top-level `homeworkDetail` key with matching structure, including 5 `typeMeta` entries
    - `frontend/app/teacher/homework/[id]/page.tsx` imports and calls `useTranslations('teacher.homeworkDetail')`
    - Any `showToast(` call site discovered in this file during the full read sources its message from `t(...)`, not an inline literal
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>homework/[id]/page.tsx fully translation-driven, TYPE_META labels resolved at render time.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Extract homework/[id]/session/[sessionId]/page.tsx</name>
  <files>frontend/app/teacher/homework/[id]/session/[sessionId]/page.tsx, frontend/messages/en/teacher.json, frontend/messages/vi/teacher.json</files>
  <read_first>frontend/app/teacher/homework/[id]/session/[sessionId]/page.tsx, frontend/messages/en/teacher.json, frontend/messages/vi/teacher.json</read_first>
  <action>
Read the full file. Add a `sessionDetail` top-level key to both message files covering: `scoreLabel(score)`'s bucketed labels IF it returns user-facing text (verify during read — if it returns only a color/CSS value, leave the function unchanged and note this in the SUMMARY), `MatchingResultRow`/`FillInBlankResultRow`/`ActivityResultCard`/`VocabResultRow`'s column headers and correctness labels (e.g. "Correct"/"Incorrect", "Expected"/"Your answer"), and `TeacherSessionDetailPage`'s page chrome (student name header, session metadata labels, activity-type section headers), plus a `toasts` sub-namespace for any toast calls discovered.
In the file: import `useTranslations` from `next-intl`, add `const t = useTranslations('teacher.sessionDetail');` inside each of the 5 function components that render user-facing text (`MatchingResultRow`, `FillInBlankResultRow`, `ActivityResultCard`, `VocabResultRow`, `TeacherSessionDetailPage` — 5 separate hook calls, following the same per-component pattern used in Plans 18-03/18-06 since these are independent function components in one file). If `scoreLabel` returns translatable text, either convert it to accept `t` as a parameter or inline its logic where it's called (since it's a plain function, not a component, it cannot call `useTranslations` itself). Replace every hardcoded string and any toast calls found with `t('...')`.
  </action>
  <acceptance_criteria>
    - `frontend/messages/en/teacher.json` and `frontend/messages/vi/teacher.json` both have a top-level `sessionDetail` key with matching structure
    - `frontend/app/teacher/homework/[id]/session/[sessionId]/page.tsx` imports `useTranslations` and calls it in at least the 5 identified components
    - If `scoreLabel` returns user-facing text, it no longer contains hardcoded English/Vietnamese literals (either parameterized with `t` or inlined at call sites)
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>session/[sessionId]/page.tsx fully translation-driven across all 5 result-rendering components.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Student-submitted answer text (READING fill-in-blank, matching pairs) → `MatchingResultRow`/`FillInBlankResultRow` rendering | Student-controlled input flows into the teacher-facing results view alongside translated column headers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-14 | Injection (XSS) | Student answer text rendered next to `t()`-translated labels in result rows | accept | This is pre-existing behavior unchanged by this phase — student answer text was already rendered as plain React text children before this extraction; this plan only translates the surrounding static labels, introducing no new rendering path for student-controlled data. |
| T-18-15 | Tampering | `messages/{en,vi}/teacher.json` `homeworkDetail`/`sessionDetail` keys | accept | Static, build-time JSON. |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit -p tsconfig.json` — no new type errors
- Manual: switch locale, open a homework detail page and drill into a session result for a READING and a PHONICS/VOCABULARY submission, confirm all labels switch language
</verification>

<success_criteria>
1. `frontend/app/teacher/homework/[id]/page.tsx` and `frontend/app/teacher/homework/[id]/session/[sessionId]/page.tsx` contain zero hardcoded user-facing strings.
2. `teacher.json` (both locales) has matching `homeworkDetail`/`sessionDetail` namespaces.
3. Any toast call sites discovered during the full-file reads are catalog-sourced.
</success_criteria>

<output>
Create `.planning/phases/18-multi-language-support-across-all-pages/18-07-SUMMARY.md` when done
</output>
