---
phase: 18
plan: 09
type: execute
wave: 9
depends_on: ["18-08"]
files_modified:
  - frontend/app/teacher/homework/_components/ReadingCreationPage.tsx
  - frontend/messages/en/teacher.json
  - frontend/messages/vi/teacher.json
autonomous: true
requirements: [i18n-04, i18n-05]
must_haves:
  truths:
    - "Reading homework creation/edit page (matching editor, fill-in-blank editor, activity reorder cards) renders fully in Vietnamese when VI is selected and fully in English when EN is selected"
    - "The 2 showToast calls source their message from the translation catalog"
  artifacts:
    - path: "frontend/messages/en/teacher.json"
      provides: "adds readingCreate top-level namespace"
    - path: "frontend/messages/vi/teacher.json"
      provides: "adds readingCreate top-level namespace (mirrored keys)"
  key_links:
    - from: "frontend/app/teacher/homework/_components/ReadingCreationPage.tsx"
      to: "frontend/messages/en/teacher.json"
      via: "useTranslations('teacher.readingCreate')"
      pattern: "useTranslations\\('teacher\\.readingCreate'\\)"
---

<objective>
Extract all hardcoded UI strings from `frontend/app/teacher/homework/_components/ReadingCreationPage.tsx` (758 lines — `MatchingActivityEditor`, `FillInBlankActivityEditor`, `SortableActivityCard`, `ReadingCreationPage`), reached from `/teacher/homework/create/reading` and `/teacher/homework/[id]/edit/[reading route]` via the thin wrapper pages confirmed to have no strings of their own (Plan 18-06).

Purpose: This is the most structurally complex creation UI in the phase (drag-and-drop reorder via `@dnd-kit`, sentence tokenization for fill-in-blank, per-Phase-3 decisions). Split by structural boundary: the two activity-type editors + sortable card (Task 1) vs. the top-level page orchestrating save/load (Task 2).
Output: File fully translation-driven; `teacher.json` extended with a `readingCreate` namespace in both locales.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-multi-language-support-across-all-pages/18-CONTEXT.md

<interfaces>
<!-- next-intl usage pattern (established 18-01) -->
`const t = useTranslations('teacher.<namespace>');` then `t('key')` / `t('key', { var })`, single-brace ICU interpolation.

<!-- frontend/app/teacher/homework/_components/ReadingCreationPage.tsx structural map (758 lines) -->
`function tokenizeSentence(sentence)` (52), `function reindexBlanks(segments)` (57), `function toggleBlankAt(segments, i)` (62), `function reconstructSegments(fillBlanks)` (82) — pure data-transform helpers, no UI strings, skip.
`function MatchingActivityEditor({...})` (132-257) — image-upload + word-label pair editor.
`function FillInBlankActivityEditor({...})` (258-397) — sentence tokenizer UI, chip-toggle-to-blank interaction, distractor-choice inputs.
`function SortableActivityCard({...})` (398-481) — `@dnd-kit`-based drag handle + reorder card, likely has a drag-handle `aria-label` and per-activity-type badge text.
`export function ReadingCreationPage({ editId }: { editId?: number })` (482-758) — top-level page: load-for-edit flow (`showToast(err instanceof Error ? err.message : 'Failed to load homework.', 'error')` at line 527), save flow (`showToast(err instanceof Error ? err.message : 'Failed to save.', 'error')` at line 630), sequence controls, title/class-assignment fields.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extract MatchingActivityEditor + FillInBlankActivityEditor + SortableActivityCard</name>
  <files>frontend/app/teacher/homework/_components/ReadingCreationPage.tsx, frontend/messages/en/teacher.json, frontend/messages/vi/teacher.json</files>
  <read_first>frontend/app/teacher/homework/_components/ReadingCreationPage.tsx, frontend/messages/en/teacher.json, frontend/messages/vi/teacher.json</read_first>
  <action>
Read lines ~132-481 of the file in full. Add `readingCreate.matchingEditor`, `readingCreate.fillBlankEditor`, `readingCreate.sortableCard` sub-namespaces to both message files covering every field label, button, helper/instruction text, and any `aria-label` strings (drag handles, remove-item buttons) in these three components.
In the file: import `useTranslations` from `next-intl`. Add `const t = useTranslations('teacher.readingCreate.matchingEditor');` inside `MatchingActivityEditor`, `const t = useTranslations('teacher.readingCreate.fillBlankEditor');` inside `FillInBlankActivityEditor`, `const t = useTranslations('teacher.readingCreate.sortableCard');` inside `SortableActivityCard` (3 separate hook calls). Replace every hardcoded string, including `aria-label` attribute values, with `t('...')`.
  </action>
  <acceptance_criteria>
    - `frontend/messages/en/teacher.json` and `frontend/messages/vi/teacher.json` both have `readingCreate.matchingEditor`, `.fillBlankEditor`, `.sortableCard` with matching structure
    - `MatchingActivityEditor`, `FillInBlankActivityEditor`, `SortableActivityCard` each call `useTranslations` independently
    - No hardcoded English text node remains in lines 132-481 of the file (spot check via read-back)
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>MatchingActivityEditor, FillInBlankActivityEditor, SortableActivityCard fully translation-driven.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Extract ReadingCreationPage main component</name>
  <files>frontend/app/teacher/homework/_components/ReadingCreationPage.tsx, frontend/messages/en/teacher.json, frontend/messages/vi/teacher.json</files>
  <read_first>frontend/app/teacher/homework/_components/ReadingCreationPage.tsx, frontend/messages/en/teacher.json, frontend/messages/vi/teacher.json</read_first>
  <action>
Read lines ~482-758 of the file in full. Add a `readingCreate.page` sub-namespace to both message files covering: page heading (create vs. edit mode), title/class-assignment field labels, sequence-control instructions, save/cancel buttons, and `toasts.load_error`/`toasts.save_error` for the two existing `showToast` fallback strings.
In the file: add `const t = useTranslations('teacher.readingCreate.page');` inside `ReadingCreationPage`. Replace `showToast(err instanceof Error ? err.message : 'Failed to load homework.', 'error')` (line 527) with `showToast(err instanceof Error ? err.message : t('toasts.load_error'), 'error')`, `showToast(err instanceof Error ? err.message : 'Failed to save.', 'error')` (line 630) with `showToast(err instanceof Error ? err.message : t('toasts.save_error'), 'error')`, and every remaining hardcoded string in this component.
  </action>
  <acceptance_criteria>
    - `frontend/messages/en/teacher.json` and `frontend/messages/vi/teacher.json` both have `readingCreate.page` with matching structure, including `toasts.load_error`/`toasts.save_error`
    - `ReadingCreationPage` calls `useTranslations('teacher.readingCreate.page')`
    - `grep -c "'Failed to load homework.'\|'Failed to save.'" frontend/app/teacher/homework/_components/ReadingCreationPage.tsx` returns `0`
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>ReadingCreationPage main component fully translation-driven; both toasts normalized.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Teacher-authored reading-activity content (sentences, distractor words, image labels) → matching/fill-blank editor UI | Teacher-entered content, not student-entered; low-risk actor |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-18 | Injection (XSS) | `t()`-rendered editor labels alongside teacher-entered sentence/word content | accept | Editor labels and teacher content are rendered as separate React text children throughout (no `dangerouslySetInnerHTML`); unchanged from pre-existing rendering behavior. |
| T-18-19 | Tampering | `messages/{en,vi}/teacher.json` `readingCreate` keys | accept | Static, build-time JSON. |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit -p tsconfig.json` — no new type errors
- Manual: switch locale, create a new reading homework with one matching activity and one fill-in-blank activity, reorder them via drag-and-drop, save — confirm all UI text and both toasts switch language
</verification>

<success_criteria>
1. `frontend/app/teacher/homework/_components/ReadingCreationPage.tsx` contains zero hardcoded user-facing strings.
2. `teacher.json` (both locales) has a `readingCreate` namespace covering `matchingEditor`/`fillBlankEditor`/`sortableCard`/`page`.
3. Both `showToast` call sites are catalog-sourced.
</success_criteria>

<output>
Create `.planning/phases/18-multi-language-support-across-all-pages/18-09-SUMMARY.md` when done
</output>
