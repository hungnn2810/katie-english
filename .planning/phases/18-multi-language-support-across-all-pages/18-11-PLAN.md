---
phase: 18
plan: 11
type: execute
wave: 11
depends_on: ["18-10"]
files_modified:
  - frontend/app/teacher/page.tsx
  - frontend/app/teacher/login/page.tsx
  - frontend/app/teacher/schedule/page.tsx
  - frontend/app/teacher/classes/page.tsx
  - frontend/app/teacher/students/page.tsx
  - frontend/app/teacher/sessions/page.tsx
  - frontend/app/teacher/import/page.tsx
  - frontend/app/teacher/tuition/page.tsx
  - frontend/app/teacher/homework/page.tsx
  - frontend/app/teacher/homework/create/page.tsx
  - frontend/app/teacher/homework/[id]/page.tsx
  - frontend/app/teacher/homework/[id]/try/page.tsx
  - frontend/app/teacher/homework/[id]/session/[sessionId]/page.tsx
  - frontend/app/teacher/homework/_components/ReadingCreationPage.tsx
  - frontend/app/teacher/homework/_components/VocabCreationPage.tsx
  - frontend/app/teacher/homework/_components/ListenCreationPage.tsx
  - frontend/app/admin/tuition/_components/TuitionConfigForm.tsx
  - frontend/app/admin/tuition/_components/GenerateRecordsModal.tsx
  - frontend/app/admin/tuition/_components/TuitionReportTable.tsx
autonomous: true
requirements: [i18n-04]
must_haves:
  truths:
    - "Every showToast( call site in frontend/app/teacher/** sources its message from the teacher.json translation catalog (via t(...)), with zero remaining inline string-literal messages — D-11 fully closed, not just 'mostly closed'"
    - "The full Jest suite (all frontend tests, not just this phase's) and a production build both pass after 10 waves of sequential edits to the same messages/{en,vi}/teacher.json files"
  artifacts:
    - path: "frontend/messages/en/teacher.json"
      provides: "final, complete Teacher-portal translation catalog (no further additions expected after this plan)"
    - path: "frontend/messages/vi/teacher.json"
      provides: "final, complete Teacher-portal translation catalog (mirrored keys, no further additions expected after this plan)"
  key_links:
    - from: "frontend/app/teacher/**/*.tsx"
      to: "frontend/messages/en/teacher.json"
      via: "every showToast( argument is either t('...') or `err instanceof Error ? err.message : t('...')`, never a bare string literal"
      pattern: "showToast\\('"
---

<objective>
Close out D-11 with a repo-wide grep audit confirming zero remaining hardcoded `showToast(` string literals anywhere under `frontend/app/teacher/**`, fix any stragglers found, and re-run the full test suite + production build after 10 waves of sequential edits to the shared `messages/{en,vi}/teacher.json` files.

Purpose: Plans 18-02 through 18-10 each targeted a specific known toast inventory (38 sites originally counted via `grep -c "showToast(" frontend/app/teacher`), but since these plans ran sequentially against the same shared JSON catalog across 10 waves, this closing plan is the single point where the full picture is verified rather than assumed. Per VALIDATION.md's "Manual-Only Verifications" table, the toast catalog completeness check is explicitly called out as needing cross-referencing all call sites — this plan automates that cross-reference.
Output: Confirmed-zero-straggler grep result, green full Jest suite, green production build.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-multi-language-support-across-all-pages/18-CONTEXT.md
@.planning/phases/18-multi-language-support-across-all-pages/18-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Grep audit for stray showToast( literals + remediate</name>
  <files>frontend/app/teacher/**/*.tsx, frontend/app/admin/tuition/_components/*.tsx, frontend/messages/en/teacher.json, frontend/messages/vi/teacher.json</files>
  <read_first>frontend/messages/en/teacher.json, frontend/messages/vi/teacher.json</read_first>
  <action>
Run `grep -rn "showToast(" frontend/app/teacher --include="*.tsx"` and `grep -rn "showToast(" frontend/app/admin/tuition/_components --include="*.tsx"` to enumerate every current `showToast(` call site (should total 38 across the 10 originally-inventoried Teacher-portal files, plus 4 in the 3 in-scope shared tuition components from Plan 18-05, per the CONTEXT.md/RESEARCH.md counts). For each match, inspect the call: if the message argument is a bare single-quoted or double-quoted string literal (not `t(...)` and not exclusively `err.message`/`err instanceof Error ? err.message : t(...)`), it is a straggler that a prior wave missed. For any straggler found: add the missing key to both `frontend/messages/en/teacher.json` and `frontend/messages/vi/teacher.json` under that file's existing namespace (established in Plans 18-02 through 18-10 — do not invent a new top-level namespace; match the file's already-established pattern), import `useTranslations` if not already imported in that file, and replace the literal with `t('toasts.<key>')`.
Also grep for any remaining bare Vietnamese-looking or English-looking JSX text nodes that Plans 18-02–18-10 may have missed outside of toast calls, specifically re-checking `frontend/app/teacher/tuition/page.tsx` and the 3 shared tuition components (highest-risk files given they started 100% Vietnamese) — `grep -n "Chào\|lớp\|học sinh\|Xóa\|Lưu" frontend/app/teacher/tuition/page.tsx frontend/app/admin/tuition/_components/TuitionConfigForm.tsx frontend/app/admin/tuition/_components/GenerateRecordsModal.tsx frontend/app/admin/tuition/_components/TuitionReportTable.tsx` as a targeted straggler check (these Vietnamese words are unlikely to appear in code/comments, only in leftover hardcoded UI text).
  </action>
  <acceptance_criteria>
    - `grep -rn "showToast('" frontend/app/teacher --include="*.tsx"` (single-quote-literal form) returns zero matches
    - `grep -rn "showToast(\"" frontend/app/teacher --include="*.tsx"` (double-quote-literal form) returns zero matches
    - `grep -rn "showToast('" frontend/app/admin/tuition/_components --include="*.tsx"` returns zero matches
    - `grep -c "showToast(" frontend/app/teacher --include="*.tsx" -r` still totals 38 (call sites weren't deleted, only their argument form changed)
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && grep -rn "showToast('" app/teacher --include="*.tsx"; grep -rn 'showToast("' app/teacher --include="*.tsx"; echo "exit:$?"</automated>
  </verify>
  <done>Zero remaining showToast( calls with inline string-literal messages anywhere under frontend/app/teacher or the in-scope tuition shared components.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Full suite + production build verification</name>
  <files>frontend/messages/en/teacher.json, frontend/messages/vi/teacher.json</files>
  <read_first>frontend/jest.config.ts</read_first>
  <action>
Run the full Jest suite (`cd frontend && npm test`) and the production build (`cd frontend && npm run build`) after all 10 extraction waves. If the build fails due to a missing translation key (next-intl throws at runtime for `t('some.key')` calls where `some.key` doesn't exist in the loaded locale's JSON — this manifests as a runtime error, not necessarily a `tsc` type error, since `messages` are typed loosely unless a stricter typed-messages setup was added), locate the offending key by cross-referencing the error against `frontend/messages/en/teacher.json` and `frontend/messages/vi/teacher.json`, and add the missing key to whichever locale file is missing it (copy the sibling locale's key structure, translate the value). If both `en` and `vi` JSON files have diverged in key structure (e.g. one has a key the other doesn't) from 10 waves of sequential edits, reconcile them so both files have byte-identical key structure (values differ, keys match).
  </action>
  <acceptance_criteria>
    - `cd frontend && npm test` exits 0
    - `cd frontend && npm run build` exits 0
    - A JSON structural diff of `frontend/messages/en/teacher.json` vs `frontend/messages/vi/teacher.json` (comparing only key paths, not values) shows zero differences
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm test && npm run build</automated>
  </verify>
  <done>Full Jest suite green, production build green, en/vi teacher.json key structures are identical.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new — this plan is verification-only, touching only strings inside already-in-scope files | |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-22 | Tampering | Straggler remediation edits (Task 1) | accept | Same low-risk profile as every prior extraction plan in this phase — static string replacement, no new logic or attack surface. |
</threat_model>

<verification>
- `cd frontend && npm test` — full suite green
- `cd frontend && npm run build` — production build succeeds
- `grep -rn "showToast('" frontend/app/teacher --include="*.tsx"` and the double-quote variant both return empty
</verification>

<success_criteria>
1. Zero `showToast(` call sites under `frontend/app/teacher/**` or the in-scope tuition shared components use an inline string-literal message.
2. `messages/en/teacher.json` and `messages/vi/teacher.json` have identical key structures.
3. Full Jest suite and production build both pass.
</success_criteria>

<output>
Create `.planning/phases/18-multi-language-support-across-all-pages/18-11-SUMMARY.md` when done
</output>
