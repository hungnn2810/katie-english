---
phase: 18
plan: 06
type: execute
wave: 6
depends_on: ["18-05"]
files_modified:
  - frontend/app/teacher/homework/page.tsx
  - frontend/app/teacher/homework/create/page.tsx
  - frontend/messages/en/teacher.json
  - frontend/messages/vi/teacher.json
autonomous: true
requirements: [i18n-04, i18n-05]
must_haves:
  truths:
    - "Homework list page (filter tabs, cards, HomeworkModal creation form, AssignModal) and the type-picker create entry page render fully in Vietnamese when VI is selected and fully in English when EN is selected"
    - "All 12 showToast calls in homework/page.tsx source their message from the translation catalog"
  artifacts:
    - path: "frontend/messages/en/teacher.json"
      provides: "adds homework, homeworkCreate top-level namespaces"
    - path: "frontend/messages/vi/teacher.json"
      provides: "adds homework, homeworkCreate top-level namespaces (mirrored keys)"
  key_links:
    - from: "frontend/app/teacher/homework/page.tsx"
      to: "frontend/messages/en/teacher.json"
      via: "useTranslations('teacher.homework')"
      pattern: "useTranslations\\('teacher\\.homework'\\)"
    - from: "frontend/app/teacher/homework/create/page.tsx"
      to: "frontend/messages/en/teacher.json"
      via: "useTranslations('teacher.homeworkCreate')"
      pattern: "useTranslations\\('teacher\\.homeworkCreate'\\)"
---

<objective>
Extract all hardcoded UI strings from the two largest homework-list-adjacent files: `frontend/app/teacher/homework/page.tsx` (985 lines — `HomeworkModal`, `AssignModal`, `HwCard`, `HomeworkPage`) and `frontend/app/teacher/homework/create/page.tsx` (188 lines — the `CreateHomeworkPage` type picker).

Purpose: `homework/page.tsx` is the single largest file in the phase and carries 12 of the 38 total toast call sites — highest-value single extraction target for D-11.
Output: Both files fully translation-driven; `teacher.json` extended with `homework`/`homeworkCreate` namespaces in both locales.

**Note on the 6 tiny wrapper pages** (`homework/create/{listen,reading,vocabulary}/page.tsx`, `homework/[id]/edit/{,listen/,vocabulary/}page.tsx`, each 6-10 lines): confirmed via read that they contain zero JSX text — each is a thin `'use client'` wrapper that imports and renders `<ReadingCreationPage />`/`<VocabCreationPage />`/`<ListenCreationPage />` with no strings of its own. They require no changes in this phase; all their rendered text lives in the `_components/*CreationPage.tsx` files handled in Plans 18-09/18-10.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-multi-language-support-across-all-pages/18-CONTEXT.md

<interfaces>
<!-- next-intl usage pattern (established 18-01) -->
`const t = useTranslations('teacher.<namespace>');` then `t('key')` / `t('key', { var })`, single-brace ICU interpolation.

<!-- frontend/app/teacher/homework/page.tsx structural map (985 lines) -->
`const ACCENT = colors.teacherAccent;` (line 34).
`function HomeworkModal({...})` (line 46-~492) — PHONICS/SPEAKING creation form, "parts" editor. Toasts: `showToast('Add at least one part.', 'error')` (139), `showToast('Each part needs at least one word.', 'error')` (140), `showToast(form.speakingMode === 'FREE_SPEAK' ? 'Enter keywords (comma-separated).' : 'Enter the text to speak.', 'error')` (143), `showToast(err instanceof Error ? err.message : 'Failed to save.', 'error')` (163).
`function AssignModal({...})` (line 493-602) — class-assignment form. Toasts: `showToast('Select at least one class.', 'error')` (512), `showToast('Set an end date.', 'error')` (513), `showToast(err instanceof Error ? err.message : 'Failed to assign.', 'error')` (521).
`function HwCard({...})` (line 603-700) — card rendering for the list grid.
`export default function HomeworkPage()` (line 701-985) — page chrome, filter tabs, list. Toasts: `showToast(editingId !== null ? 'Homework updated!' : 'Homework created!', 'success')` (765), `showToast('Homework assigned!', 'success')` (771), `showToast('Deleted.', 'success')` (871, 946 — two call sites with identical text, both from delete-confirm flows for different card states), `showToast(err instanceof Error ? err.message : 'Delete failed.', 'error')` (872, 946).
That is 12 total `showToast` call sites in this file.

<!-- frontend/app/teacher/homework/create/page.tsx (188 lines, full file) -->
`const HW_TYPES` array (line 12): `[{ type: 'PHONICS', label: 'Phonics', ... }, { type: 'SPEAKING', label: 'Speaking', ... }, { type: 'VOCABULARY', label: 'Vocabulary', ... }, { type: 'LISTEN', label: 'Listen', ... }]` — `label` values need translation (the `type`/`route`/`color`/`icon` fields do not). `export default function CreateHomeworkPage()`: "Back to Homework" button, "1 · Choose type" / "2 · Build word list" step labels, `TextField` labels "Title"/"Assign to", word-chip add form `TextField` label "Add a word" + placeholder "type a word and press Add" + "Add" button, footer "Cancel"/"Publish homework" buttons. No toasts in this file.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extract homework/create/page.tsx</name>
  <files>frontend/app/teacher/homework/create/page.tsx, frontend/messages/en/teacher.json, frontend/messages/vi/teacher.json</files>
  <read_first>frontend/app/teacher/homework/create/page.tsx, frontend/messages/en/teacher.json, frontend/messages/vi/teacher.json</read_first>
  <action>
Add a `homeworkCreate` top-level key to both message files covering: `hwTypes` labels (Phonics/Speaking/Vocabulary/Listen — keyed by type so `HW_TYPES`' `label` field can look up `t(\`hwTypes.${type}\`)`), "Back to Homework", the two step labels, `TextField` labels ("Title"/"Assign to"/"Add a word"), the word-chip add placeholder, and footer button labels ("Cancel"/"Publish homework"/"Add").
In `create/page.tsx`: import `useTranslations` from `next-intl`, `const t = useTranslations('teacher.homeworkCreate');` inside `CreateHomeworkPage`. Since `HW_TYPES` is a module-level `const` array (needed for its `type`/`route`/`color`/`icon` fields to build the type-selector grid), keep the array structure but resolve `t.label` at render time inside the `.map()` callback via `t(\`hwTypes.${t_item.type}\`)` rather than storing translated text in the module-level array (module consts can't call hooks). Replace all other hardcoded strings with `t('...')`.
  </action>
  <acceptance_criteria>
    - `frontend/messages/en/teacher.json` and `frontend/messages/vi/teacher.json` both have a top-level `homeworkCreate` key with matching structure, including 4 `hwTypes` entries
    - `frontend/app/teacher/homework/create/page.tsx` imports and calls `useTranslations('teacher.homeworkCreate')`
    - `grep -c "Back to Homework\|Choose type\|Build word list\|Publish homework" frontend/app/teacher/homework/create/page.tsx` returns `0`
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>homework/create/page.tsx fully translation-driven, HW_TYPES labels resolved at render time.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Extract HomeworkModal + AssignModal in homework/page.tsx</name>
  <files>frontend/app/teacher/homework/page.tsx, frontend/messages/en/teacher.json, frontend/messages/vi/teacher.json</files>
  <read_first>frontend/app/teacher/homework/page.tsx, frontend/messages/en/teacher.json, frontend/messages/vi/teacher.json</read_first>
  <action>
Add `homework.homeworkModal` and `homework.assignModal` sub-namespaces to both message files covering every form field/label/button in `HomeworkModal` (lines ~46-492: PHONICS parts editor, SPEAKING mode fields) and `AssignModal` (lines ~493-602: class checkboxes, due-date picker), plus their 7 combined toasts: `homeworkModal.toasts.part_required`, `word_required`, `keywords_required`, `script_required`, `save_error`; `assignModal.toasts.class_required`, `end_date_required`, `assign_error`.
In `homework/page.tsx`: import `useTranslations` from `next-intl`, add `const t = useTranslations('teacher.homework.homeworkModal');` inside `HomeworkModal` and a separate `const t = useTranslations('teacher.homework.assignModal');` inside `AssignModal` (these are two distinct function components in the same file — each needs its own hook call). Replace the 4 `showToast` calls inside `HomeworkModal` (lines 139, 140, 143, 163) and the 3 inside `AssignModal` (lines 512, 513, 521) with their `t('toasts.<key>')` equivalents, and every hardcoded form field label/placeholder/button text in both components.
  </action>
  <acceptance_criteria>
    - `frontend/messages/en/teacher.json` and `frontend/messages/vi/teacher.json` both have `homework.homeworkModal` and `homework.assignModal` with matching structure, 5 + 3 toast keys respectively
    - `HomeworkModal` and `AssignModal` each call `useTranslations` independently
    - `grep -c "showToast('Add at least one part\|showToast('Each part needs\|showToast('Select at least one class\|showToast('Set an end date" frontend/app/teacher/homework/page.tsx` returns `0`
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>HomeworkModal and AssignModal fully translation-driven; 7 of the file's 12 toasts normalized.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Extract HwCard + HomeworkPage in homework/page.tsx</name>
  <files>frontend/app/teacher/homework/page.tsx, frontend/messages/en/teacher.json, frontend/messages/vi/teacher.json</files>
  <read_first>frontend/app/teacher/homework/page.tsx, frontend/messages/en/teacher.json, frontend/messages/vi/teacher.json</read_first>
  <action>
Add `homework.card` and `homework.page` sub-namespaces to both message files covering `HwCard`'s rendered labels (type badge text, submission-count phrasing, due-date badge, assign/edit/delete action button labels) and `HomeworkPage`'s filter tabs, page heading, list/empty states, and 5 toast keys: `page.toasts.homework_updated`, `homework_created`, `homework_assigned`, `deleted`, `delete_failed`.
In `homework/page.tsx`: add `const t = useTranslations('teacher.homework.card');` inside `HwCard` and `const t = useTranslations('teacher.homework.page');` inside `HomeworkPage` (same pattern as Task 2 — one hook call per function component). Replace the 5 remaining `showToast` calls (lines 765, 771, 871/946 `'Deleted.'`, 872/946 `'Delete failed.'`) with their `t('toasts.<key>')` equivalents. Replace all remaining hardcoded strings in `HwCard` and `HomeworkPage` (filter tab labels, headings, empty state, card action buttons).
  </action>
  <acceptance_criteria>
    - `frontend/messages/en/teacher.json` and `frontend/messages/vi/teacher.json` both have `homework.card` and `homework.page` with matching structure, including 5 toast keys under `page.toasts`
    - `HwCard` and `HomeworkPage` each call `useTranslations` independently
    - `grep -c "showToast(" frontend/app/teacher/homework/page.tsx | xargs -I{} test {} -eq 12` succeeds implicitly — i.e. `grep -c "showToast(t(\|showToast(editingId\|showToast(err instanceof Error ? err.message : t(" frontend/app/teacher/homework/page.tsx` accounts for all 12 original call sites now sourcing from `t(...)`
    - `grep -c "showToast('Homework updated\|showToast('Homework created\|showToast('Homework assigned\|showToast('Deleted.'\|showToast('Delete failed" frontend/app/teacher/homework/page.tsx` returns `0`
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>HwCard and HomeworkPage fully translation-driven; all 12 toasts in homework/page.tsx now catalog-sourced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `HW_TYPES` type-selector grid → `t(\`hwTypes.${type}\`)` dynamic key lookup | Key segment is `type`, a fixed enum from a module-level const array, never user input — no injection surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-12 | Injection (XSS) | `t()`-rendered homework titles/toasts | accept | All rendered as plain React text children; no `dangerouslySetInnerHTML` in this file. |
| T-18-13 | Tampering | `messages/{en,vi}/teacher.json` `homework`/`homeworkCreate` keys | accept | Static, build-time JSON. |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit -p tsconfig.json` — no new type errors
- Manual: switch locale, exercise homework create (PHONICS + SPEAKING), assign, delete flows on `/teacher/homework`, and the type picker on `/teacher/homework/create` — confirm every toast/label switches language
</verification>

<success_criteria>
1. `frontend/app/teacher/homework/page.tsx` and `frontend/app/teacher/homework/create/page.tsx` contain zero hardcoded user-facing strings.
2. `teacher.json` (both locales) has matching `homework`/`homeworkCreate` namespaces.
3. All 12 `showToast` call sites in `homework/page.tsx` are catalog-sourced (D-11 progress).
</success_criteria>

<output>
Create `.planning/phases/18-multi-language-support-across-all-pages/18-06-SUMMARY.md` when done
</output>
