---
phase: 04-student-ux-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/app/game/homework/page.tsx
autonomous: false
requirements:
  - STUDENT-01
  - STUDENT-02
tags:
  - frontend
  - nextjs
  - tailwind
  - ux

must_haves:
  truths:
    - "Student homework list renders cards sorted by endDate ascending (earliest due date first)."
    - "An assignment whose endDate is in the past displays an 'Overdue' badge in the highlight (red #FF7B7B) color, replacing the days-left badge."
    - "An assignment with daysLeft === 0 still displays 'Due today' in the highlight badge (existing behavior preserved)."
    - "An assignment with daysLeft === 1 still displays '1 day left' in the highlight badge (existing behavior preserved)."
    - "An assignment with daysLeft > 1 still displays '{N} days left' in the neutral white/20 badge (existing behavior preserved)."
    - "A completed assignment (bestScore !== null) still displays the 'Best: {score}%' green badge regardless of due date (existing behavior preserved)."
    - "Completed assignments are mixed into the sorted list by endDate — no separate completed section (D-05)."
    - "The page still constrains itself to minWidth: 1024 (laptop/PC target per D-01); no responsive or touch-target work introduced."
  artifacts:
    - path: "frontend/app/game/homework/page.tsx"
      provides: "Student homework list page with endDate-ascending sort and Overdue badge variant"
      contains:
        - "[...data].sort((a, b) => new Date(a.endDate).getTime() - new Date(b.endDate).getTime())"
        - "daysLeft < 0 ? 'Overdue'"
  key_links:
    - from: "frontend/app/game/homework/page.tsx (useEffect at lines 40-46)"
      to: "setAssignments state"
      via: "inline .sort() inside .then() callback"
      pattern: "setAssignments\\(\\[\\.\\.\\.data\\]\\.sort"
    - from: "frontend/app/game/homework/page.tsx (badge JSX inside cards .map at lines 179-187)"
      to: "daysLeft urgency state machine"
      via: "daysLeft < 0 branch renders 'Overdue' with bg-highlight (via existing daysLeft <= 1 className condition)"
      pattern: "daysLeft < 0 \\? 'Overdue'"
---

<objective>
Apply two surgical edits to the student homework list page so assignments render in due-date order with overdue items visually flagged.

Purpose: Closes the only two remaining v1 requirements (STUDENT-01 ordered list; STUDENT-02 — revised by D-01 to laptop/PC only, satisfied by retaining minWidth: 1024 and adding the Overdue badge as the user-visible cue). After this plan, all 21 v1 requirements are implemented.

Output: Updated `frontend/app/game/homework/page.tsx` with (1) endDate-ascending sort inside the setAssignments callback and (2) an "Overdue" badge variant for daysLeft < 0. No other files change. No backend changes. No new dependencies.

Scope is narrow by design — both changes touch the same file and are reviewed together at one checkpoint at the end.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-student-ux-polish/04-CONTEXT.md
@.planning/phases/04-student-ux-polish/04-PATTERNS.md
@.planning/phases/04-student-ux-polish/04-UI-SPEC.md
@frontend/app/game/homework/page.tsx
@frontend/lib/admin-api.ts
@frontend/lib/colors.ts

<interfaces>
<!-- Key types and contracts the executor needs. No codebase exploration required. -->

From `frontend/lib/admin-api.ts` (lines 288-297):
```
export interface AssignmentItem {
  id: number;
  homeworkId: number;
  endDate: string;          // ISO date string; the field this plan sorts on
  homework: HomeworkItem;
  classes: AssignmentClass[];
  sessions?: GameSession[];
  _count?: { sessions: number };
  createdAt: string;
}

export const getAvailableHomework = (studentId: number) =>
  req<AssignmentItem[]>(`/game/homework/${studentId}`);
```

From `frontend/lib/colors.ts` (line 5):
```
highlight: '#FF7B7B'   // coral red — Tailwind utility: bg-highlight, text-highlight
```

Current callback in `frontend/app/game/homework/page.tsx` (lines 40-46) — modify the `.then(setAssignments)` line:
```
useEffect(() => {
  if (!user.studentId) { setLoading(false); return; }
  getAvailableHomework(user.studentId)
    .then(setAssignments)
    .catch(() => setError('Failed to load homework'))
    .finally(() => setLoading(false));
}, [user.studentId]);
```

Current badge JSX in `frontend/app/game/homework/page.tsx` (lines 179-187) — modify ONLY the text expression on line 185:
```
{bestScore !== null ? (
  <span className="text-xs font-bold px-3 py-1 rounded-full bg-brand-green text-white">
    Best: {bestScore}%
  </span>
) : (
  <span className={`text-xs font-bold px-3 py-1 rounded-full ${daysLeft <= 1 ? 'bg-highlight text-white' : 'bg-white bg-opacity-20 text-white'}`}>
    {daysLeft <= 0 ? 'Due today' : daysLeft === 1 ? '1 day left' : `${daysLeft} days left`}
  </span>
)}
```

`daysLeft` is computed at line 165 as `Math.ceil((dueDate.getTime() - Date.now()) / 86400000)`.

Canonical badge state machine (from 04-UI-SPEC.md):

| Condition | Badge Text | className |
|---|---|---|
| bestScore !== null | `Best: {score}%` | `bg-brand-green text-white` |
| daysLeft < 0 | `Overdue` | `bg-highlight text-white` |
| daysLeft === 0 | `Due today` | `bg-highlight text-white` |
| daysLeft === 1 | `1 day left` | `bg-highlight text-white` |
| daysLeft > 1 | `{N} days left` | `bg-white bg-opacity-20 text-white` |

Important: the existing className conditional `daysLeft <= 1 ? 'bg-highlight text-white' : 'bg-white bg-opacity-20 text-white'` already covers all three highlight cases (daysLeft < 0, daysLeft === 0, daysLeft === 1) because all are `<= 1`. The className therefore needs NO change. Only the text expression changes.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sort assignments by endDate ascending and add Overdue badge text branch</name>
  <files>frontend/app/game/homework/page.tsx</files>
  <read_first>
    - frontend/app/game/homework/page.tsx (read in full — file is 230 lines; you must see current sort callback at lines 40-46 and current badge JSX at lines 179-187)
    - .planning/phases/04-student-ux-polish/04-CONTEXT.md (decisions D-01 through D-05)
    - .planning/phases/04-student-ux-polish/04-PATTERNS.md (exact target snippets for both edits)
    - .planning/phases/04-student-ux-polish/04-UI-SPEC.md (Badge Variants table — canonical state machine)
    - frontend/lib/admin-api.ts (AssignmentItem.endDate definition — confirm field name and string type)
    - frontend/lib/colors.ts (confirm `highlight` token exists at #FF7B7B and `bg-highlight` Tailwind utility is available)
  </read_first>
  <action>
Make exactly two surgical edits to `frontend/app/game/homework/page.tsx`. Do NOT touch any other file. Do NOT introduce new components, hooks, imports, color tokens, spacing values, or animation.

EDIT 1 — Sort by endDate (implements D-02, D-03, D-05; STUDENT-01):
- Location: the `useEffect` block at lines 40-46 that calls `getAvailableHomework(user.studentId)`.
- Change: replace the single line `.then(setAssignments)` with `.then((data) => setAssignments([...data].sort((a, b) => new Date(a.endDate).getTime() - new Date(b.endDate).getTime())))`.
- Comparator must be exactly `(a, b) => new Date(a.endDate).getTime() - new Date(b.endDate).getTime()` (ascending by endDate timestamp).
- Use `[...data]` spread to avoid mutating the response array in place.
- Do not introduce a separate sort helper, useMemo, or sorting on render — sort once at fetch time per D-03.
- Do NOT add a separate "completed" section — completed assignments stay mixed by date per D-05.

EDIT 2 — Overdue badge text branch (implements D-04; STUDENT-01):
- Location: the non-completed `<span>` badge inside the cards `.map()` at lines 184-186 (the else branch of `bestScore !== null ? ... : ...`).
- Change ONLY the JSX text expression on line 185. Replace `{daysLeft <= 0 ? 'Due today' : daysLeft === 1 ? '1 day left' : ` followed by a template literal `${daysLeft} days left` followed by `}` with the four-branch expression: `{daysLeft < 0 ? 'Overdue' : daysLeft === 0 ? 'Due today' : daysLeft === 1 ? '1 day left' : ` followed by the same template literal `${daysLeft} days left` followed by `}`.
- The "Overdue" string must be literal `Overdue` — capital O, no period, no emoji, no surrounding whitespace.
- Do NOT change the className expression on line 184. The existing `daysLeft <= 1 ? 'bg-highlight text-white' : 'bg-white bg-opacity-20 text-white'` already styles daysLeft < 0 as red because negative numbers satisfy `<= 1`. Verifier confirms via the className grep assertion.
- Do NOT change the completed-branch badge (the `Best: {bestScore}%` span using `bg-brand-green`). It stays exactly as written.
- Do NOT add animation, pulse, blink, or any motion to any badge (per 04-UI-SPEC.md animation decision).
- Do NOT change any other JSX, imports, layout, styling, or the `minWidth: 1024` style on line 61 (D-01 requires it to stay).

After both edits, the only diffs vs HEAD must be: (a) one line in the `.then` callback replaced with the sort callback, and (b) the badge text expression on line 185 gaining the `daysLeft < 0 ? 'Overdue' :` prefix branch. Total expected diff: 2 lines changed (one in each location), no lines added or removed.
  </action>
  <verify>
    <automated>cd /Users/hung/Documents/katie-english/frontend && npx tsc --noEmit 2>&1 | tail -20 && grep -c "setAssignments(\[\.\.\.data\]\.sort" app/game/homework/page.tsx && grep -c "new Date(a\.endDate)\.getTime() - new Date(b\.endDate)\.getTime()" app/game/homework/page.tsx && grep -c "daysLeft < 0 ? 'Overdue'" app/game/homework/page.tsx && grep -c "daysLeft === 0 ? 'Due today'" app/game/homework/page.tsx && grep -c "daysLeft <= 1 ? 'bg-highlight text-white' : 'bg-white bg-opacity-20 text-white'" app/game/homework/page.tsx && grep -c "minWidth: 1024" app/game/homework/page.tsx && grep -c "daysLeft <= 0 ? 'Due today'" app/game/homework/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "setAssignments(\[\.\.\.data\]\.sort" frontend/app/game/homework/page.tsx` returns `1` (sort present exactly once).
    - Source assertion: `grep -c "new Date(a\.endDate)\.getTime() - new Date(b\.endDate)\.getTime()" frontend/app/game/homework/page.tsx` returns `1` (comparator present exactly once).
    - Source assertion: `grep -c "daysLeft < 0 ? 'Overdue'" frontend/app/game/homework/page.tsx` returns `1` (new branch present).
    - Source assertion: `grep -c "daysLeft === 0 ? 'Due today'" frontend/app/game/homework/page.tsx` returns `1` (Due-today now triggered by equality, not `<= 0`).
    - Source assertion: `grep -c "daysLeft === 1 ? '1 day left'" frontend/app/game/homework/page.tsx` returns `1` (one-day branch preserved exactly).
    - Source assertion: `grep -c "daysLeft <= 0 ? 'Due today'" frontend/app/game/homework/page.tsx` returns `0` (old text branch fully replaced — this is the regression guard).
    - Source assertion: `grep -c "daysLeft <= 1 ? 'bg-highlight text-white' : 'bg-white bg-opacity-20 text-white'" frontend/app/game/homework/page.tsx` returns `1` (className unchanged).
    - Source assertion: `grep -c "Best: {bestScore}%" frontend/app/game/homework/page.tsx` returns `1` (completed-badge branch unchanged).
    - Source assertion: `grep -c "minWidth: 1024" frontend/app/game/homework/page.tsx` returns `1` (D-01 — laptop/PC constraint preserved).
    - CLI output: `cd frontend && npx tsc --noEmit` exits 0 with no errors referencing `app/game/homework/page.tsx`.
    - CLI output: `git diff --numstat frontend/app/game/homework/page.tsx` shows `2 2` (2 lines added, 2 lines removed) — both edits are single-line replacements.
  </acceptance_criteria>
  <done>
    Both edits applied to `frontend/app/game/homework/page.tsx`. TypeScript compiles. No other files modified. Diff is limited to the two surgical sites identified in 04-PATTERNS.md and totals 2 lines changed.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — sort order and Overdue badge render correctly</name>
  <files>frontend/app/game/homework/page.tsx</files>
  <read_first>
    - frontend/app/game/homework/page.tsx (after Task 1 edits)
    - .planning/phases/04-student-ux-polish/04-UI-SPEC.md (Badge Variants table — to confirm visual contract)
  </read_first>
  <what-built>
    Task 1 modified `frontend/app/game/homework/page.tsx` so that (a) the student homework list is sorted by `endDate` ascending before render, and (b) assignments whose due date has passed (`daysLeft < 0`) show a red "Overdue" badge instead of the previous behavior (which incorrectly labeled past-due items as "Due today" with a negative implicit count).
  </what-built>
  <action>
Pause execution and request the user perform the visual verification steps in <how-to-verify>. Do NOT modify any file. Do NOT proceed past this task without an explicit resume signal from the user. If the user reports a defect, do not fix it inside this task — return control so the orchestrator can spawn a follow-up plan or in-place revision.
  </action>
  <how-to-verify>
    Prerequisites: backend running at `http://localhost:3001`, frontend at `http://localhost:3000`, at least one student account with at least 3 assignments in different due-date states (one overdue, one due today, one due in the future). If test data does not exist, the verifier should briefly note that and the user can decide to seed data first or accept on code review only.

    1. Start the frontend dev server: `cd frontend && npm run dev`. Wait for "Ready" message.
    2. Open `http://localhost:3000/login` in a browser on a laptop/PC viewport (≥ 1024px wide — do NOT test on tablet/phone; D-01 explicitly removes tablet support).
    3. Log in as a student account that has assignments with mixed due dates including at least one in the past.
    4. Navigate to `/game/homework`.
    5. Confirm the assignment cards render in ascending due-date order (earliest endDate first, latest endDate last). Card 1's badge should reference an earlier or equal date than card 2's badge, etc.
    6. Confirm that any assignment whose due date is in the past displays the red badge with text exactly "Overdue" (capital O, no other text).
    7. Confirm that an assignment whose due date is today displays the red badge with text "Due today".
    8. Confirm that an assignment whose due date is tomorrow displays the red badge with text "1 day left".
    9. Confirm that an assignment whose due date is 2+ days away displays the neutral white/20 badge with text "{N} days left".
    10. Confirm that a completed assignment (one with at least one finished session) displays the green "Best: {N}%" badge and that this card is positioned in the list by its endDate — NOT pinned to top or bottom.
    11. Confirm no animation/pulse/blink on the Overdue badge (it is static, like Due today).
    12. Confirm the page still has the dark game-bg gradient and the existing `minWidth: 1024` (resize the window narrower than 1024px; horizontal scroll should appear, NOT a responsive collapse).

    If no test data exists for "overdue", you may seed one manually: as a teacher account, create an assignment with `endDate` set to yesterday and assign it to the student's class, then reload the student page.
  </how-to-verify>
  <verify>
    <automated>echo "Manual checkpoint — verification performed by human via browser. No automated command. Resume on explicit 'approved' signal."</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: Cards render in ascending endDate order on every reload.
    - Behavior: An overdue assignment shows a red badge with literal text "Overdue".
    - Behavior: A due-today assignment shows a red badge with literal text "Due today".
    - Behavior: A tomorrow assignment shows a red badge with literal text "1 day left".
    - Behavior: A future-date (>1 day) assignment shows a neutral white-tint badge with "{N} days left".
    - Behavior: A completed assignment shows the green "Best: {N}%" badge in its date-sorted position (not pinned).
    - Behavior: No animation on the Overdue badge.
    - Behavior: Page does not respond to viewport widths < 1024 (horizontal scroll appears) — D-01 preserved.
    - Resume signal: User types one of `approved`, `approved with notes`, or describes a specific defect to fix.
  </acceptance_criteria>
  <resume-signal>Type "approved" if all checks pass. Otherwise describe the defect (which check failed, what was observed).</resume-signal>
  <done>
    User has typed "approved" (or "approved with notes"). All eight behavior assertions above held during their browser session. Any noted-but-non-blocking observations are recorded in the SUMMARY for follow-up.
  </done>
</task>

</tasks>

<verification>
- TypeScript: `cd frontend && npx tsc --noEmit` exits 0.
- All eight source assertions from Task 1 acceptance_criteria pass.
- `git diff --numstat frontend/app/game/homework/page.tsx` reports `2 2`.
- No file outside `frontend/app/game/homework/page.tsx` appears in `git status` after Task 1.
- Human verification (Task 2) confirms render order and badge variants behave per the UI Design Contract.
</verification>

<success_criteria>
- STUDENT-01 satisfied: Homework list renders ascending by endDate; overdue items visually distinct via red "Overdue" badge.
- STUDENT-02 satisfied: With device target revised to laptop/PC only (D-01), the existing `minWidth: 1024` constraint is preserved and no responsive work is introduced — requirement closed by virtue of the device-scope change locked in CONTEXT.md.
- All 21 v1 requirements are now implemented across phases 1–4.
- Only one file changed in this phase; diff is small (≤ 2 lines changed) and reviewable in under a minute.
</success_criteria>

<output>
After both tasks complete and human verification returns "approved", create `.planning/phases/04-student-ux-polish/04-01-SUMMARY.md` capturing:
- Final diff scope (one file, line counts via `git diff --numstat`)
- D-01 → D-05 mapping to actual code locations (line numbers after edits)
- Confirmation that STUDENT-01 and STUDENT-02 are both closed
- Any user notes from the checkpoint
</output>
