---
plan_id: 01-03
phase: 01
title: "Wave 1 housekeeping: D-22 MIME mapping, D-24 stale migration cleanup, D-12/D-18 commit untracked files"
wave: 1
depends_on: []
files_modified:
  - backend/src/bfa/bfa.service.ts
  - backend/src/homework/image.controller.ts
  - backend/src/game/game.controller.ts
  - backend/src/game/game.dto.ts
  - backend/src/game/game.repository.ts
  - backend/src/game/game.service.spec.ts
  - backend/src/class/class.repository.ts
  - frontend/app/teacher/homework/[id]/page.tsx
  - frontend/app/teacher/page.tsx
  - backend/prisma/migrations/20260507000003_add_speaking_part/
  - backend/prisma/migrations/20260507000004_add_image_part/
  - backend/prisma/migrations/20260507000005_add_phonics_results/
  - backend/prisma/migrations/20260508000001_redesign_homework/
  - backend/prisma/migrations/20260509000001_homework_parts_words/
autonomous: true
requirements_addressed:
  - SPEAK-01
  - SPEAK-02
  - SPEAK-05
  - SPEAK-07
tags:
  - housekeeping
  - cleanup
  - migrations
  - bfa
  - frontend
  - backend

must_haves:
  truths:
    - "bfa.service.ts maps audio/m4a, video/quicktime, audio/ogg, and audio/aac to their proper extensions (m4a, mov, ogg, aac) via a single mimeToExt helper used by both align() and transcribe()."
    - "The 5 stale exploratory migration folders (20260507000003 through 20260509000001) are removed from the working tree."
    - "backend/src/homework/image.controller.ts is committed to main (was untracked)."
    - "frontend/app/teacher/homework/[id]/page.tsx (teacher detail redesign — Open/Closed badges, completion counts, assignment grouping, SPEAKING image+text) is committed to main."
    - "All other currently-modified-but-untracked Phase 1 files (game.controller.ts/dto/repository/spec, class.repository.ts, frontend/app/teacher/page.tsx) are committed to main as-is."
    - "frontend/app/teacher/homework/[id]/try/page.tsx is NOT committed in this plan — it is the subject of the 01-05 try-mode rewrite and must stay modified."
    - "backend/src/bfa/bfa.dto.ts, bfa-service/main.py, bfa-service/Dockerfile are NOT touched in this plan — they belong to the already-created 01-02 BFA plan."
  verification:
    - "grep -c 'function mimeToExt' backend/src/bfa/bfa.service.ts returns 1"
    - "grep -c \"mimeType.includes('m4a')\" backend/src/bfa/bfa.service.ts returns 1"
    - "grep -c \"mimeType.includes('quicktime')\" backend/src/bfa/bfa.service.ts returns 1"
    - "grep -c \"mimeType.includes('ogg')\" backend/src/bfa/bfa.service.ts returns 1"
    - "grep -c \"mimeType.includes('aac')\" backend/src/bfa/bfa.service.ts returns 1"
    - "grep -v '^#' backend/src/bfa/bfa.service.ts | grep -cE \"const ext = mimeType.includes\\('webm'\\) \\? 'webm' : mimeType.includes\\('mp4'\\) \\? 'mp4' : 'wav'\" returns 0 (old ternary fully replaced)"
    - "test ! -d backend/prisma/migrations/20260507000003_add_speaking_part && echo absent (exits 0)"
    - "test ! -d backend/prisma/migrations/20260507000004_add_image_part && echo absent (exits 0)"
    - "test ! -d backend/prisma/migrations/20260507000005_add_phonics_results && echo absent (exits 0)"
    - "test ! -d backend/prisma/migrations/20260508000001_redesign_homework && echo absent (exits 0)"
    - "test ! -d backend/prisma/migrations/20260509000001_homework_parts_words && echo absent (exits 0)"
    - "git ls-files backend/src/homework/image.controller.ts | wc -l returns 1"
    - "git status --porcelain backend/src/homework/image.controller.ts returns empty"
    - "git status --porcelain frontend/app/teacher/homework/[id]/page.tsx returns empty"
    - "git status --porcelain backend/src/bfa/bfa.service.ts returns empty"
    - "git status --porcelain backend/src/game/game.controller.ts returns empty"
    - "git status --porcelain backend/src/game/game.dto.ts returns empty"
    - "git status --porcelain backend/src/game/game.repository.ts returns empty"
    - "git status --porcelain backend/src/game/game.service.spec.ts returns empty"
    - "git status --porcelain backend/src/class/class.repository.ts returns empty"
    - "git status --porcelain frontend/app/teacher/page.tsx returns empty"
    - "git status --porcelain frontend/app/teacher/homework/[id]/try/page.tsx returns 'M  frontend/app/teacher/homework/[id]/try/page.tsx' or similar M marker (must remain modified — handled by 01-05)"
    - "cd backend && npx tsc --noEmit exits 0"
    - "cd backend && npx jest game.service.spec.ts --no-coverage exits 0"
---

<objective>
Close out Wave 1 housekeeping for Phase 1: fix the MIME-type extension mapping in `bfa.service.ts` so non-webm/mp4 uploads (m4a, mov, ogg, aac) are filed with the correct extension before BFA processing (D-22); delete five stale exploratory migration folders that were never committed (D-24); and commit the already-written Phase 1 untracked files (image.controller.ts D-18, teacher homework detail redesign D-12, plus the other Phase 1 untracked backend/frontend files that have correct code but no commit).

Purpose:
- Unblocks 01-05 which adds a new try-speak endpoint to `game.controller.ts` — that file must be committed before 01-05's edits land cleanly.
- Unblocks 01-04 which adds calcFreeSpeak test cases to `game.service.spec.ts` — Wave 1 commits the current spec file as-is, then 01-04 appends D-05 unit tests on top.
- Cleans up Prisma migration drift so `npx prisma migrate status` reports only the migrations that are actually in source control.
- Fixes the MIME extension bug now (in the same commit as the other bfa-related untracked file, `bfa.service.ts`) so transcription of mobile-recorded files (Quicktime / m4a) works.

Output:
- `bfa.service.ts` with a single `mimeToExt(mimeType)` helper covering 7 cases (webm, mp4, m4a, mov, ogg, aac, wav).
- 5 stale migration directories removed from `backend/prisma/migrations/`.
- 9 untracked/modified Phase 1 files committed to `main` (image.controller.ts is new; the other 8 are modified).
- `frontend/app/teacher/homework/[id]/try/page.tsx` deliberately left untouched and uncommitted for 01-05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-speaking-homework/01-CONTEXT.md
@.planning/phases/01-speaking-homework/01-RESEARCH.md
@.planning/phases/01-speaking-homework/01-PATTERNS.md
@backend/src/bfa/bfa.service.ts
@backend/src/homework/image.controller.ts
@backend/src/game/game.controller.ts
@frontend/app/teacher/homework/[id]/page.tsx
@backend/prisma/migrations/migration_lock.toml
@backend/prisma/schema.prisma

<interfaces>
Current `bfa.service.ts` MIME ternary (duplicated at lines 18 and 33 inside `align()` and `transcribe()`):
  const ext = mimeType.includes('webm') ? 'webm' : mimeType.includes('mp4') ? 'mp4' : 'wav';

Required after D-22: single helper function `mimeToExt(mimeType: string): string` at module scope (top of file, after imports, before `@Injectable()`):
  - webm → webm
  - mp4 → mp4
  - m4a → m4a
  - quicktime → mov
  - ogg → ogg
  - aac → aac
  - default → wav

Both call sites become `const ext = mimeToExt(mimeType);` — no other behavior changes.

`backend/src/homework/image.controller.ts` (new, untracked, 22 lines): registers `GET /homework/image/:key(*)` with `StorageService` for serving speaking prompt images. Already correct — needs git add only. Already registered in `HomeworkModule.controllers` per 01-PATTERNS.md.

Stale Prisma migration folders to delete (D-24, all 5 confirmed untracked via `git status` — none committed):
  backend/prisma/migrations/20260507000003_add_speaking_part/
  backend/prisma/migrations/20260507000004_add_image_part/
  backend/prisma/migrations/20260507000005_add_phonics_results/
  backend/prisma/migrations/20260508000001_redesign_homework/
  backend/prisma/migrations/20260509000001_homework_parts_words/
The current committed Prisma history ends at `20260510000001_add_speaking_mode/` which reflects actual DB state. `migration_lock.toml` references the provider only; no per-migration references to remove.

Files modified in working tree that this plan commits as-is (no code changes — pure git staging) — per 01-RESEARCH.md "Untracked Files to Commit":
  backend/src/game/game.controller.ts        — already-written speakingMode handling
  backend/src/game/game.dto.ts                — already-written speakingMode handling
  backend/src/game/game.repository.ts         — already-written homework lookup
  backend/src/game/game.service.spec.ts       — speakingMode mock + existing tests
  backend/src/class/class.repository.ts       — preceding work, unrelated to Phase 1 logic
  frontend/app/teacher/homework/[id]/page.tsx — D-12 teacher detail redesign (assignment grouping, Open/Closed badges, completion count, delete button, SPEAKING image+text)
  frontend/app/teacher/page.tsx               — preceding teacher portal work

Files deliberately NOT committed in this plan (handled by later plans):
  frontend/app/teacher/homework/[id]/try/page.tsx  → 01-05 (full rewrite to file-upload flow)

Files NOT in this plan (handled by 01-02 already created):
  bfa-service/main.py, bfa-service/Dockerfile, backend/src/bfa/bfa.dto.ts
</interfaces>
</context>

<tasks>

<task id="T-01" title="D-22: Refactor bfa.service.ts MIME extension mapping into mimeToExt helper">
  <objective>
Replace the duplicated minimal MIME ternary in `bfa.service.ts` (currently `webm | mp4 | wav` only) with a single `mimeToExt` helper covering all mobile-recording MIME types (m4a, mov, ogg, aac) so the BFA service receives files with the correct extension hint — required for ffmpeg auto-detection of iOS Quicktime and Android m4a uploads.
  </objective>
  <read_first>
    - backend/src/bfa/bfa.service.ts (full file — 44 lines; lines 11-29 is `align()`, lines 31-42 is `transcribe()`; the broken ternary is at lines 18 and 33)
    - .planning/phases/01-speaking-homework/01-CONTEXT.md (decision D-22)
    - .planning/phases/01-speaking-homework/01-RESEARCH.md (section "D-22: MIME Extension Mapping Fix", "Don't Hand-Roll" table row)
    - .planning/phases/01-speaking-homework/01-PATTERNS.md (section "backend/src/bfa/bfa.service.ts — D-22: fix MIME extension mapping")
  </read_first>
  <action>
Modify `backend/src/bfa/bfa.service.ts` with two precise edits — do NOT touch any other file in this task:

1. **Add a module-scope helper function** immediately after the `import { BfaAlignResult, WhisperXResult } from './bfa.dto';` line (currently line 4), before `@Injectable()` (currently line 6). The function:
   - Signature: `function mimeToExt(mimeType: string): string`
   - Body: ordered if-chain returning the matching extension, default `'wav'`. Order matters because `audio/mp4` and `audio/m4a` both contain neither's full string but `m4a` should match before any future loose `mp4` check. Use the exact ordering below:
     ```
     if (mimeType.includes('webm')) return 'webm';
     if (mimeType.includes('m4a')) return 'm4a';
     if (mimeType.includes('mp4')) return 'mp4';
     if (mimeType.includes('quicktime')) return 'mov';
     if (mimeType.includes('ogg')) return 'ogg';
     if (mimeType.includes('aac')) return 'aac';
     return 'wav';
     ```
   - Reasoning for `m4a` before `mp4`: `audio/m4a` MIME contains the substring `m4a` but does NOT contain `mp4`; however a future audit-style MIME like `audio/x-mp4a-latm` could trip the wrong branch if the order is reversed. Putting `m4a` first is defensive and matches 01-PATTERNS.md.

2. **Replace both call sites** (currently identical ternary on lines 18 and 33):
   - In `align()` (line 18): replace `const ext = mimeType.includes('webm') ? 'webm' : mimeType.includes('mp4') ? 'mp4' : 'wav';` with `const ext = mimeToExt(mimeType);`
   - In `transcribe()` (line 33): replace the identical ternary with `const ext = mimeToExt(mimeType);`

Do NOT change the `form.append('audio', ...)` calls. Do NOT alter the `axios.post` URLs, headers, or timeouts. Do NOT add new imports.
  </action>
  <acceptance_criteria>
    - Source: `grep -c 'function mimeToExt' backend/src/bfa/bfa.service.ts` returns `1`.
    - Source: `grep -c "mimeType.includes('webm')" backend/src/bfa/bfa.service.ts` returns `1` (now only inside `mimeToExt`).
    - Source: `grep -c "mimeType.includes('m4a')" backend/src/bfa/bfa.service.ts` returns `1`.
    - Source: `grep -c "mimeType.includes('mp4')" backend/src/bfa/bfa.service.ts` returns `1`.
    - Source: `grep -c "mimeType.includes('quicktime')" backend/src/bfa/bfa.service.ts` returns `1`.
    - Source: `grep -c "mimeType.includes('ogg')" backend/src/bfa/bfa.service.ts` returns `1`.
    - Source: `grep -c "mimeType.includes('aac')" backend/src/bfa/bfa.service.ts` returns `1`.
    - Source: `grep -c 'const ext = mimeToExt(mimeType)' backend/src/bfa/bfa.service.ts` returns `2` (one per method).
    - Source (regression guard): `grep -cE "mimeType.includes\\('webm'\\) \\? 'webm' : mimeType.includes\\('mp4'\\) \\? 'mp4'" backend/src/bfa/bfa.service.ts` returns `0` (old ternary fully removed).
    - CLI: `cd backend && npx tsc --noEmit` exits `0` (no new TS errors).
    - Behavior: `mimeToExt('audio/m4a')` returns `'m4a'`; `mimeToExt('video/quicktime')` returns `'mov'`; `mimeToExt('audio/ogg')` returns `'ogg'`; `mimeToExt('audio/aac')` returns `'aac'`; `mimeToExt('audio/wav')` returns `'wav'`; `mimeToExt('audio/webm')` returns `'webm'`; `mimeToExt('video/mp4')` returns `'mp4'`. These are testable via a one-liner: `cd backend && node -e "require('ts-node/register'); const {default: x} = require('./src/bfa/bfa.service'); console.log('OK')"` — pure structural test, since `mimeToExt` is not exported. The grep assertions above are the primary gate.
  </acceptance_criteria>
</task>

<task id="T-02" title="D-24: Delete 5 stale Prisma migration folders">
  <objective>
Remove the 5 untracked stale exploratory migration folders so `backend/prisma/migrations/` only contains migrations that match the committed Prisma history. The committed migration `20260510000001_add_speaking_mode` is the source of truth.
  </objective>
  <read_first>
    - backend/prisma/migrations/migration_lock.toml (confirm provider is postgresql and there are no per-migration references)
    - backend/prisma/schema.prisma (confirm `SpeakingMode` enum is at line 21 and `speakingMode` field is at line 114 — current schema matches `20260510000001_add_speaking_mode`)
    - .planning/phases/01-speaking-homework/01-CONTEXT.md (decision D-24)
    - .planning/phases/01-speaking-homework/01-RESEARCH.md (section "Pitfall 5: Stale Migration Folders Conflict with Prisma", "Open Question 2")
  </read_first>
  <action>
Run two sanity checks then execute the deletions. Do all work from the repo root.

1. **Verify migration status (informational, do not block):** Run `cd backend && npx prisma migrate status` and visually confirm that NONE of the 5 stale folder names (20260507000003_add_speaking_part, 20260507000004_add_image_part, 20260507000005_add_phonics_results, 20260508000001_redesign_homework, 20260509000001_homework_parts_words) appear in the "Applied" or "Pending" lists with timestamps before `20260510000001`. If any are reported as applied to the dev DB, STOP and surface a checkpoint — do NOT delete. If they are all absent from the status output (or reported as drift-only items), proceed.

2. **Verify untracked status:** Run `git status --porcelain backend/prisma/migrations/ | grep '^??'`. Expected output: exactly the 5 stale folders listed in `<files_modified>`. If git reports any of them as tracked (i.e. `M` or empty prefix), STOP — they were committed somewhere and this plan is the wrong scope.

3. **Delete the 5 folders** using `rm -rf` (they are untracked, so `git rm` is not needed):
   - `rm -rf backend/prisma/migrations/20260507000003_add_speaking_part`
   - `rm -rf backend/prisma/migrations/20260507000004_add_image_part`
   - `rm -rf backend/prisma/migrations/20260507000005_add_phonics_results`
   - `rm -rf backend/prisma/migrations/20260508000001_redesign_homework`
   - `rm -rf backend/prisma/migrations/20260509000001_homework_parts_words`

4. **Confirm cleanup:** Run `ls backend/prisma/migrations/` and verify the output lists only the committed migrations from `20240101000000_init` through `20260510000001_add_speaking_mode` plus `migration_lock.toml` — no stale entries.

Do NOT modify `migration_lock.toml`. Do NOT touch any migration that is currently committed. Do NOT run `npx prisma migrate dev` or `migrate reset`.
  </action>
  <acceptance_criteria>
    - CLI: `test ! -d backend/prisma/migrations/20260507000003_add_speaking_part` exits `0`.
    - CLI: `test ! -d backend/prisma/migrations/20260507000004_add_image_part` exits `0`.
    - CLI: `test ! -d backend/prisma/migrations/20260507000005_add_phonics_results` exits `0`.
    - CLI: `test ! -d backend/prisma/migrations/20260508000001_redesign_homework` exits `0`.
    - CLI: `test ! -d backend/prisma/migrations/20260509000001_homework_parts_words` exits `0`.
    - CLI: `ls backend/prisma/migrations/ | grep -E '^20260507000003|^20260507000004|^20260507000005|^20260508000001|^20260509000001' | wc -l` returns `0`.
    - CLI: `git status --porcelain | grep -cE 'backend/prisma/migrations/20260507000003|backend/prisma/migrations/20260507000004|backend/prisma/migrations/20260507000005|backend/prisma/migrations/20260508000001|backend/prisma/migrations/20260509000001'` returns `0`.
    - Behavior: `ls backend/prisma/migrations/20260510000001_add_speaking_mode/` still lists `migration.sql` (the source-of-truth migration is preserved).
    - Behavior: `cat backend/prisma/migrations/migration_lock.toml` is unchanged.
  </acceptance_criteria>
</task>

<task id="T-03" title="Commit untracked Phase 1 files (D-12 teacher detail, D-18 image controller, D-22 bfa.service.ts, plus 6 sibling untracked files)">
  <objective>
Stage and commit the 9 Phase 1 files that are currently untracked or modified-uncommitted in the working tree:
- 1 NEW file (D-18): `backend/src/homework/image.controller.ts`
- 1 MODIFIED file from T-01 (D-22): `backend/src/bfa/bfa.service.ts`
- 1 MODIFIED file (D-12): `frontend/app/teacher/homework/[id]/page.tsx` (teacher homework detail redesign)
- 6 MODIFIED sibling Phase 1 files that already contain correct code per RESEARCH.md gap analysis: `backend/src/game/game.controller.ts`, `backend/src/game/game.dto.ts`, `backend/src/game/game.repository.ts`, `backend/src/game/game.service.spec.ts`, `backend/src/class/class.repository.ts`, `frontend/app/teacher/page.tsx`.

DO NOT commit `frontend/app/teacher/homework/[id]/try/page.tsx` — that file remains modified for 01-05 to rewrite.
DO NOT commit BFA-side files (`bfa-service/main.py`, `bfa-service/Dockerfile`, `backend/src/bfa/bfa.dto.ts`) — those belong to 01-02.
  </objective>
  <read_first>
    - backend/src/bfa/bfa.service.ts (after T-01 edits — confirm mimeToExt helper present)
    - backend/src/homework/image.controller.ts (full file, 22 lines — confirm logic correct)
    - frontend/app/teacher/homework/[id]/page.tsx (skim to confirm Open/Closed badges + completion count + assignment grouping + delete button + SPEAKING image+text are present per 01-UI-SPEC.md Screen 6)
    - backend/src/game/game.controller.ts (confirm speakingMode-aware code per RESEARCH.md "Already Done" table)
    - backend/src/game/game.service.spec.ts (confirm existing tests reference speakingMode mocks; 01-04 will add calcFreeSpeak tests on top of whatever is committed here)
    - .planning/phases/01-speaking-homework/01-CONTEXT.md (decisions D-12, D-18, D-22)
    - .planning/phases/01-speaking-homework/01-RESEARCH.md (section "Untracked Files to Commit")
  </read_first>
  <action>
Run a sequence of git commands from the repo root. Do NOT use `git add -A` or `git add .`.

1. **Stage exactly these 9 files by explicit path** (image.controller.ts is new; the rest are modified):
   ```
   git add backend/src/homework/image.controller.ts \
           backend/src/bfa/bfa.service.ts \
           frontend/app/teacher/homework/[id]/page.tsx \
           backend/src/game/game.controller.ts \
           backend/src/game/game.dto.ts \
           backend/src/game/game.repository.ts \
           backend/src/game/game.service.spec.ts \
           backend/src/class/class.repository.ts \
           frontend/app/teacher/page.tsx
   ```

2. **Verify the staged diff includes ONLY those 9 paths**:
   ```
   git diff --cached --name-only | sort
   ```
   Expected output is exactly those 9 paths (alphabetical). If `frontend/app/teacher/homework/[id]/try/page.tsx`, `bfa-service/main.py`, `bfa-service/Dockerfile`, `backend/src/bfa/bfa.dto.ts`, `.planning/`, `.claude/`, `.vscode/`, or any prisma migration folder appears in the cached diff, unstage with `git restore --staged <path>` and do not commit it.

3. **Verify the working tree still shows try/page.tsx as modified**:
   ```
   git status --porcelain frontend/app/teacher/homework/[id]/try/page.tsx
   ```
   Expected: a line starting with `M ` (modified, unstaged) — this is the file 01-05 will rewrite.

4. **Run typecheck before committing**:
   ```
   cd backend && npx tsc --noEmit
   ```
   Must exit `0`. If errors appear, fix them in-scope (most likely in bfa.service.ts from T-01 or in a sibling game file). Do NOT skip this step.

5. **Run unit tests**:
   ```
   cd backend && npx jest game.service.spec.ts --no-coverage
   ```
   Must exit `0`. If a test fails, fix the test (NOT the production code) so the existing pre-Phase-1-continuation state is captured cleanly. 01-04 adds the calcFreeSpeak tests separately.

6. **Create a single commit** using a HEREDOC:
   ```
   git commit -m "$(cat <<'EOF'
   feat(01): Phase 1 housekeeping — D-22 MIME map, D-18 image controller, D-12 teacher detail

   - bfa.service.ts: introduce mimeToExt() covering webm/mp4/m4a/mov/ogg/aac/wav, applied in both align() and transcribe() (D-22).
   - homework/image.controller.ts: commit GET /homework/image/:key for serving speaking prompt images (D-18).
   - frontend teacher homework detail page: redesigned with assignment grouping, Open/Closed badges, completion counts, due dates, delete-assignment button, SPEAKING picture+text display (D-12).
   - Commit sibling Phase 1 backend/frontend changes (game controller/dto/repository/spec, class repository, teacher page) that were uncommitted from prior work.

   Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
   EOF
   )"
   ```

7. **Verify commit success** with `git log -1 --pretty=%s` — must match the regex `^feat\(01\): Phase 1 housekeeping`.

Do NOT push to remote. Do NOT amend any earlier commit. If the pre-commit hook fails, fix the underlying issue (do not retry with `--no-verify`), then re-stage and create a NEW commit (not `--amend`).
  </action>
  <acceptance_criteria>
    - CLI: `git log -1 --pretty=%s` output matches regex `^feat\(01\): Phase 1 housekeeping`.
    - CLI: `git show --stat HEAD --format= | tail -n +2 | awk '{print $1}' | sort` lists exactly these 9 paths (and no others): `backend/src/bfa/bfa.service.ts`, `backend/src/class/class.repository.ts`, `backend/src/game/game.controller.ts`, `backend/src/game/game.dto.ts`, `backend/src/game/game.repository.ts`, `backend/src/game/game.service.spec.ts`, `backend/src/homework/image.controller.ts`, `frontend/app/teacher/homework/[id]/page.tsx`, `frontend/app/teacher/page.tsx`.
    - CLI: `git status --porcelain backend/src/homework/image.controller.ts` returns empty (file now tracked + clean).
    - CLI: `git ls-files backend/src/homework/image.controller.ts | wc -l` returns `1`.
    - CLI: `git status --porcelain backend/src/bfa/bfa.service.ts` returns empty.
    - CLI: `git status --porcelain frontend/app/teacher/homework/[id]/page.tsx` returns empty.
    - CLI: `git status --porcelain backend/src/game/game.controller.ts backend/src/game/game.dto.ts backend/src/game/game.repository.ts backend/src/game/game.service.spec.ts backend/src/class/class.repository.ts frontend/app/teacher/page.tsx` returns empty (all 6 paths clean).
    - CLI: `git status --porcelain frontend/app/teacher/homework/[id]/try/page.tsx | cut -c1-2` outputs ` M` (modified, unstaged — intentionally preserved for 01-05).
    - Behavior: `git log --oneline | head -2` shows this new commit followed by the previous HEAD commit; no prior commit was amended.
    - Behavior: After T-02 deletions, the only Prisma migration folders present are the originally-committed ones plus `20260510000001_add_speaking_mode`; none of the 5 stale folders appear in `ls backend/prisma/migrations/`.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- All source assertions in T-01, T-02, T-03 pass.
- `cd backend && npx tsc --noEmit` exits 0.
- `cd backend && npx jest game.service.spec.ts --no-coverage` exits 0.
- 5 stale migration folders are gone; `20260510000001_add_speaking_mode` is preserved.
- 9 Phase 1 files committed; `try/page.tsx` deliberately uncommitted; 01-02-owned files (main.py, Dockerfile, bfa.dto.ts) not touched.
- No file in this plan overlaps `<files_modified>` of 01-02 — both can run as Wave 1 in parallel.
</verification>

<success_criteria>
- D-12 closed: teacher homework detail redesign is committed to main.
- D-18 closed: image.controller.ts is tracked in git.
- D-22 closed: bfa.service.ts contains mimeToExt() handling 7 MIME branches; both align() and transcribe() use it.
- D-24 closed: 5 stale migration folders deleted; Prisma history matches committed source.
- Working tree shows only `frontend/app/teacher/homework/[id]/try/page.tsx` and non-Phase-1 files (`.claude/settings.local.json`, `.planning/`, `.vscode/`, etc.) as modified/untracked.
</success_criteria>

<output>
After T-01, T-02, T-03 complete, create `.planning/phases/01-speaking-homework/01-03-SUMMARY.md` capturing:
- The exact HEAD commit hash and the 9 files committed (`git show --stat HEAD --format=%h`).
- Confirmation of which Prisma migration folders remain (run `ls backend/prisma/migrations/`).
- D-12, D-18, D-22, D-24 mapping to actual file states.
- Note that `frontend/app/teacher/homework/[id]/try/page.tsx` is preserved as modified for 01-05.
- Confirmation that `npx tsc --noEmit` and `npx jest game.service.spec.ts` both exit 0.
</output>

<must_haves>
  <truths>
    - "bfa.service.ts has a mimeToExt helper covering webm/mp4/m4a/quicktime/ogg/aac/wav."
    - "Both bfa.service.ts methods (align, transcribe) use mimeToExt(mimeType) — the old inline ternary is gone."
    - "5 stale Prisma migration folders (20260507000003 through 20260509000001) are deleted from the working tree."
    - "backend/src/homework/image.controller.ts is now tracked in git."
    - "frontend/app/teacher/homework/[id]/page.tsx (D-12 redesign) is committed to main."
    - "8 other Phase 1 untracked files are committed in the same commit."
    - "frontend/app/teacher/homework/[id]/try/page.tsx remains modified (not committed) so 01-05 can rewrite it."
    - "bfa-service/main.py, bfa-service/Dockerfile, backend/src/bfa/bfa.dto.ts are not modified by this plan (they belong to 01-02)."
  </truths>
  <verification>
    - "grep -c 'function mimeToExt' backend/src/bfa/bfa.service.ts returns 1."
    - "grep -c 'const ext = mimeToExt(mimeType)' backend/src/bfa/bfa.service.ts returns 2."
    - "test ! -d backend/prisma/migrations/20260507000003_add_speaking_part exits 0 (and the same for the other 4 stale folders)."
    - "git ls-files backend/src/homework/image.controller.ts | wc -l returns 1."
    - "git status --porcelain backend/src/homework/image.controller.ts backend/src/bfa/bfa.service.ts frontend/app/teacher/homework/[id]/page.tsx returns empty."
    - "git status --porcelain frontend/app/teacher/homework/[id]/try/page.tsx | cut -c1-2 returns ' M'."
    - "cd backend && npx tsc --noEmit exits 0."
    - "cd backend && npx jest game.service.spec.ts --no-coverage exits 0."
    - "git log -1 --pretty=%s matches '^feat\\(01\\): Phase 1 housekeeping'."
  </verification>
</must_haves>

<threat_model>
  <threats>
    <threat severity="medium">ASVS V14 (configuration / data integrity at boundary): Deleting 5 untracked Prisma migration folders could cause Prisma drift if the dev database has any of them applied. Mitigation: T-02 step 1 runs `npx prisma migrate status` as an informational check and instructs the executor to STOP if any of the stale names appear as "Applied" — preventing accidental schema drift. CONTEXT.md D-24 explicitly classifies these as "stale exploratory history" never committed; assumption A4 in RESEARCH.md flags the residual risk to dev DB state.</threat>
    <threat severity="low">ASVS V5 (input validation, MIME boundary): The MIME-to-extension mapping is a hint passed to ffmpeg in the BFA service. An attacker-controlled MIME string (e.g. `audio/aac; whatever`) could currently coerce branch selection but cannot cause command injection because the value is appended to FormData and used only as a filename suffix — ffmpeg auto-detects actual container format regardless of extension. Mitigation: mimeToExt uses `String.includes` (no regex), returns one of 7 hard-coded literals, so the output is always one of `webm|mp4|m4a|mov|ogg|aac|wav`. No path traversal or shell metacharacters can leak through.</threat>
    <threat severity="low">ASVS V12 (file & resource integrity): A commit that accidentally includes try/page.tsx would break 01-05's rewrite assumption. Mitigation: T-03 step 2 enforces `git diff --cached --name-only | sort` review against an exact 9-path allowlist; T-03 step 3 verifies try/page.tsx remains in the modified-but-unstaged state. The acceptance criteria include a positive `' M'` check on try/page.tsx after the commit.</threat>
    <threat severity="low">Information disclosure via committed code: `backend/src/homework/image.controller.ts` exposes a public-style GET endpoint. Mitigation: 01-PATTERNS.md confirms the controller is mounted inside `HomeworkModule` which already enforces auth via `@UseGuards(AuthGuard)` at the controller class level (per 01-RESEARCH.md "Security Domain" — `StorageService.getObject` is bucket-scoped to MinIO objects already managed by authenticated uploads). No new public surface is introduced; access controls inherit from the existing module configuration. Defer hardening (e.g. presigned URLs) to a later phase if required.</threat>
    <threat severity="low">Concurrent-merge risk during git staging: If another working-tree change appears between `git add` and `git commit`, the commit could pick up unintended files. Mitigation: T-03 step 2 mandates `git diff --cached --name-only | sort` validation immediately before commit; the acceptance criteria assert the exact 9-path output via `git show --stat HEAD`. If a checker spots drift, the commit can be reset and re-created (no `--amend` permitted by CLAUDE.md / get-shit-done conventions).</threat>
  </threats>
</threat_model>