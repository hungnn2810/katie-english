---
plan_id: 01-04
phase: 01
title: "Wave 2 scoring + result: D-05 calcFreeSpeak word-boundary+fuzzy, D-16 FREE_SPEAK student result screen"
wave: 2
depends_on: [01-03]
files_modified:
  - backend/src/game/game.scoring.ts
  - backend/src/game/game.service.spec.ts
  - frontend/app/game/session/[id]/page.tsx
  - frontend/lib/admin-api.ts
autonomous: true
requirements_addressed:
  - SPEAK-06
  - SPEAK-03
  - SPEAK-04
tags:
  - scoring
  - frontend
  - tests
  - levenshtein

must_haves:
  truths:
    - "calcFreeSpeak in game.scoring.ts uses a word-boundary regex (\\bkeyword\\b) as the first match attempt; bare substring includes() is gone."
    - "If the word-boundary check fails, calcFreeSpeak falls back to a per-token Levenshtein similarity check at >= 0.75 threshold using the existing exported levenshtein() function in the same file."
    - "Keyword strings with regex special characters (e.g. \"cat.\", \"isn't\") do not throw — they are escaped before being wrapped in \\b...\\b."
    - "The {score, matchedWords, totalWords} contract returned by calcFreeSpeak is unchanged — only the internal matching logic changes."
    - "game.service.spec.ts contains a `describe('calcFreeSpeak'...)` block with at least 5 test cases covering: exact word-boundary match, no false-positive substring match (catapult vs cat), fuzzy fallback hit, fuzzy fallback miss, regex-special-char keyword."
    - "Student session results screen (pageState='results', speaking branch) shows the speaking picture prominently for FREE_SPEAK when item.pictureUrl is set, and shows 'Keywords matched: N/N' below the score using matchedWords/totalWords stored on the item."
    - "Student session results screen for FREE_SPEAK does NOT show the raw comma-separated keyword list as the visible 'text' value (per D-16)."
    - "SCRIPT_MATCH result rendering is unchanged from current behavior (D-17)."
    - "frontend/lib/admin-api.ts SpeakingResult interface includes matchedWords and totalWords (already present at lines 337-338) — handleSpeakingUpload in session/[id]/page.tsx propagates r.matchedWords and r.totalWords onto the items state so the results card can read them."
  verification:
    - "grep -c 'function matchesKeyword' backend/src/game/game.scoring.ts returns 1"
    - "grep -c 'new RegExp' backend/src/game/game.scoring.ts returns 1"
    - "grep -c 'text.includes(kw)' backend/src/game/game.scoring.ts returns 0"
    - "grep -c 'matchesKeyword(text, kw)' backend/src/game/game.scoring.ts returns 1"
    - "grep -c 'levenshtein(w, kw)' backend/src/game/game.scoring.ts returns 1"
    - "grep -cE 'describe\\(.calcFreeSpeak' backend/src/game/game.service.spec.ts returns 1"
    - "grep -cE \"calcFreeSpeak\\(\" backend/src/game/game.service.spec.ts returns ≥ 5"
    - "cd backend && npx jest game.service.spec.ts -t calcFreeSpeak --no-coverage exits 0"
    - "cd backend && npx tsc --noEmit exits 0"
    - "grep -c 'matchedWords' frontend/app/game/session/\\[id\\]/page.tsx returns ≥ 2 (passed into items state + read in results card)"
    - "grep -c 'totalWords' frontend/app/game/session/\\[id\\]/page.tsx returns ≥ 2"
    - "grep -c \"speakHw\\?.speakingMode === 'FREE_SPEAK'\" frontend/app/game/session/\\[id\\]/page.tsx returns ≥ 1"
    - "grep -c 'Keywords matched:' frontend/app/game/session/\\[id\\]/page.tsx returns 1"
    - "cd frontend && npx tsc --noEmit exits 0"
---

<objective>
Close the two remaining same-wave decisions for Phase 1 speaking accuracy + UX:

1. **D-05 (backend scoring):** Replace the substring-based keyword match in `calcFreeSpeak` with a two-stage matcher — first a word-boundary regex (so "catapult" no longer counts as keyword "cat"), then a Levenshtein-similarity fallback at >= 0.75 (so transcription errors like "set" → "sit" still count). The existing exported `levenshtein()` is reused; no new dependency. Add unit tests covering both stages plus regex-special-character safety.

2. **D-16 (student result screen):** In `frontend/app/game/session/[id]/page.tsx`, change the speaking-branch result card so FREE_SPEAK shows the image prompt prominently and a "Keywords matched: N/N" line under the score (using values returned by `saveSpeakingResult`). SCRIPT_MATCH rendering is unchanged. The raw comma-separated keyword list is NOT shown to students (per CONTEXT.md D-16).

Purpose:
- D-05: Stops false positives like "catapult" matching "cat", which is the largest accuracy gap in the current FREE_SPEAK pipeline. Levenshtein fallback maintains tolerance for WhisperX mishearings without sacrificing precision.
- D-16: Closes the visual gap between teacher intent (image-prompted free speech) and student feedback (currently a generic transcript card with no image, exposing the keyword list which leaks the answer).

Output:
- `backend/src/game/game.scoring.ts` with a private `matchesKeyword(transcript, kw)` helper and a 1-line edit to the `kws.filter(...)` call in `calcFreeSpeak`.
- `backend/src/game/game.service.spec.ts` extended with a `describe('calcFreeSpeak', ...)` block — 5+ test cases.
- `frontend/app/game/session/[id]/page.tsx` results card updated for FREE_SPEAK: image rendered, keyword count rendered, raw `text` hidden.
- `frontend/app/game/session/[id]/page.tsx` `handleSpeakingUpload` updated to store `matchedWords` and `totalWords` on the items state.
- `frontend/lib/admin-api.ts` no interface changes required (already has matchedWords/totalWords on SpeakingResult), but the SessionItem interface in `session/[id]/page.tsx` gains optional `matchedWords?: number` and `totalWords?: number` fields.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-speaking-homework/01-CONTEXT.md
@.planning/phases/01-speaking-homework/01-RESEARCH.md
@.planning/phases/01-speaking-homework/01-PATTERNS.md
@.planning/phases/01-speaking-homework/01-UI-SPEC.md
@backend/src/game/game.scoring.ts
@backend/src/game/game.service.spec.ts
@frontend/app/game/session/[id]/page.tsx
@frontend/lib/admin-api.ts

<interfaces>
**Current `calcFreeSpeak`** (backend/src/game/game.scoring.ts lines 65-81). The broken line is 75:
  const matched = kws.filter((kw) => text.includes(kw)).length;

**Required pattern** (per 01-PATTERNS.md "backend/src/game/game.scoring.ts — D-05" section): insert a private helper above `calcFreeSpeak` and change line 75 to `kws.filter((kw) => matchesKeyword(text, kw))`.

**Re-used dependency:** `levenshtein(a: string, b: string): number` is already exported at line 1 of game.scoring.ts — reuse directly. Do NOT define a second levenshtein. The existing tokenize() helper at lines 14-19 is also available if needed for word splitting; D-05 uses `.split(/\s+/)` (matches the algorithm specified in 01-PATTERNS.md).

**Test framework:** Jest (NestJS backend). The spec at `backend/src/game/game.service.spec.ts` is committed by 01-03 in its current state with `calcSpeakingScore` and `calcScore` describe blocks but no `calcFreeSpeak` block. This plan ADDS a `describe('calcFreeSpeak', ...)` block — append it after the existing `describe('calcSpeakingScore', ...)` (currently ends at line 112).

**Import already needed in the spec:** `calcFreeSpeak` is not currently imported at line 7. Add it to the existing import list: `import { calcScore, levenshtein, calcSpeakingScore, calcFreeSpeak } from './game.scoring';`

**Frontend SpeakingResult interface** (frontend/lib/admin-api.ts lines 332-339) — ALREADY has matchedWords and totalWords. No interface change needed:
  interface SpeakingResult {
    id, sessionId, transcribedText?, score, matchedWords, totalWords
  }

**Current SessionItem interface** (session/[id]/page.tsx lines 13-24) — has `text, pictureUrl?, transcribed, score`. ADD optional `matchedWords?: number` and `totalWords?: number` fields.

**Current handleSpeakingUpload** (session/[id]/page.tsx lines 342-361) sets items state at line 348-355 with the speakingText as `text:`. Per D-16, when speakingMode === 'FREE_SPEAK' the raw `speakingText` (comma-separated keywords) MUST NOT be shown as the visible label — the card pulls the image from `pictureUrl` and renders "Keywords matched: N/N" instead.

**Current speaking branch in results card** (session/[id]/page.tsx lines 553-568):
  <div className="text-white/60 text-xs font-semibold uppercase mb-2">🎤 Speaking</div>
  <div className="flex items-start justify-between gap-4">
    <div className="flex-1">
      <div className="text-white font-medium text-sm mb-1">{item.text}</div>
      <div className="text-white/70 text-sm">You said: "{item.transcribed || '—'}"</div>
    </div>
    <div className="text-2xl font-black tabular-nums shrink-0" style={{ color: scoreHexColor(item.score) }}>
      {item.score}%
    </div>
  </div>

**Required structure** (per 01-UI-SPEC.md Screen 3 "Student Session: Results Screen (pageState === 'results', FREE_SPEAK branch)" and 01-PATTERNS.md "D-16"):
  - For FREE_SPEAK + item.pictureUrl: render <img> at top with `rounded-xl overflow-hidden mb-3 max-h-40 w-full object-contain` and `alt="Speaking prompt"`.
  - Mode label: "🎤 Speaking · Free Speak" or "🎤 Speaking · Script Match" — text-white/60 text-xs font-bold uppercase mb-2 (note: 01-UI-SPEC.md sets font-bold not font-semibold per the typography contract).
  - For FREE_SPEAK: do NOT render `{item.text}` (it is the keyword list — secret from the student).
  - For SCRIPT_MATCH: keep the existing `{item.text}` line (target text the student was asked to say).
  - Below score row, add "Keywords matched: {matchedWords}/{totalWords}" — text-white/70 text-sm — ONLY when FREE_SPEAK and matchedWords/totalWords are defined.

**Important detection:** Per RESEARCH.md "Pitfall 4", `speakHw` state is initialized in `useEffect` (lines 108-120) and is NOT cleared at results time. `results?.speakingResults?.[0]` is also available because `handleSpeakingUpload` stores the `completeSession` response into `results`. Either source works; using the items state (with matchedWords/totalWords stored per-item) is simpler and matches the SessionItem pattern.

**Mode label copy contract** (01-UI-SPEC.md "Copywriting Contract" + Result section): "🎤 Speaking · Free Speak" / "🎤 Speaking · Script Match" — NOT plain "🎤 Speaking".
</interfaces>
</context>

<tasks>

<task id="T-01" title="D-05: Implement matchesKeyword helper and update calcFreeSpeak in game.scoring.ts">
  <objective>
Replace the substring-based keyword match with a two-stage matcher (word-boundary regex first, Levenshtein fuzzy fallback at >= 0.75 similarity). Escape regex special characters so user-supplied keywords cannot trip `new RegExp(...)`. Reuse the existing exported `levenshtein()` in the same file.
  </objective>
  <read_first>
    - backend/src/game/game.scoring.ts (full file — 82 lines; levenshtein at lines 1-12, tokenize at lines 14-19, calcFreeSpeak at lines 65-81)
    - .planning/phases/01-speaking-homework/01-CONTEXT.md (decision D-05, also note D-06 keeping calcSpeakingScore unchanged)
    - .planning/phases/01-speaking-homework/01-RESEARCH.md (section "D-05: Keyword Matching Algorithm", "Pitfall 1: Regex Escaping in Word-Boundary Match", "Code Examples — Current calcFreeSpeak / Required calcFreeSpeak")
    - .planning/phases/01-speaking-homework/01-PATTERNS.md (section "backend/src/game/game.scoring.ts — D-05: replace bare includes() in calcFreeSpeak" for the exact code pattern)
  </read_first>
  <action>
Modify `backend/src/game/game.scoring.ts` with two edits:

1. **Insert a new private helper function `matchesKeyword` immediately above the existing `calcFreeSpeak` declaration** (currently line 65). The helper body — exactly as specified in 01-PATTERNS.md and 01-RESEARCH.md "Required calcFreeSpeak":
   - Signature: `function matchesKeyword(transcript: string, kw: string): boolean`
   - Step 1: Escape regex specials in `kw` via `const escaped = kw.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');`. The escape set must include exactly these 13 metacharacters: `. * + ? ^ $ { } ( ) | [ ] \`.
   - Step 2: Build the pattern as `new RegExp(\`\\b${escaped}\\b\`, 'i')` and `.test(transcript)` — if true, return true.
   - Step 3: Tokenize `transcript` into words via `const words = transcript.toLowerCase().split(/\s+/).filter(Boolean);`. (Do NOT call the existing `tokenize()` helper because it strips non-letter characters; the task specifies whitespace splitting.)
   - Step 4: For each token `w`, compute `maxLen = Math.max(w.length, kw.length)`. If `maxLen === 0`, return false (defensive guard). Otherwise compute `sim = 1 - levenshtein(w, kw) / maxLen`. If any `sim >= 0.75`, return true.
   - Otherwise return false.
   - Make the helper non-exported (file-private). `levenshtein` is already exported at line 1 — reuse it directly via the same-module reference (no import needed).

2. **Update `calcFreeSpeak`** — change ONLY the inner `.filter(...)` argument:
   - Line 75 currently reads: `const matched = kws.filter((kw) => text.includes(kw)).length;`
   - Replace with: `const matched = kws.filter((kw) => matchesKeyword(text, kw)).length;`
   - Do NOT modify any other line. The `text = transcript.toLowerCase()` at line 74 stays (the regex is case-insensitive via the `i` flag, so this is harmless; the fuzzy fallback uses `transcript.toLowerCase().split(...)` inside the helper).
   - Do NOT change the return shape `{ score, matchedWords, totalWords }`. Do NOT change the formula `Math.round((matched / kws.length) * 100)`.

Do NOT modify `levenshtein`, `tokenize`, `calcScore`, or `calcSpeakingScore`. Do NOT add new imports. Do NOT export `matchesKeyword`.

Reasoning for keeping `text = transcript.toLowerCase()` at the call boundary: the regex stage uses the original transcript (case-insensitive via the `i` flag); the fuzzy stage does its own `.toLowerCase()` on tokens. Passing `text` (already lowercased) into the helper is harmless — `\b` boundaries are unaffected by case folding and `transcript.toLowerCase().split(/\s+/)` is idempotent.
  </action>
  <acceptance_criteria>
    - Source: `grep -c 'function matchesKeyword' backend/src/game/game.scoring.ts` returns `1`.
    - Source: `grep -c 'export function matchesKeyword' backend/src/game/game.scoring.ts` returns `0` (helper is file-private).
    - Source: `grep -c 'new RegExp(' backend/src/game/game.scoring.ts` returns `1` (only inside matchesKeyword).
    - Source: `grep -c 'text.includes(kw)' backend/src/game/game.scoring.ts` returns `0` (old substring match fully replaced).
    - Source: `grep -c 'matchesKeyword(text, kw)' backend/src/game/game.scoring.ts` returns `1` (new call site in calcFreeSpeak).
    - Source: `grep -c 'levenshtein(w, kw)' backend/src/game/game.scoring.ts` returns `1`.
    - Source: `grep -cE '\\\\b\\$\\{escaped\\}\\\\b' backend/src/game/game.scoring.ts` returns `1` (word-boundary anchors around the escaped keyword).
    - Source: `grep -cE 'replace\\(/\\[' backend/src/game/game.scoring.ts` returns `1` (regex-escape replace call).
    - Source: `grep -c '>= 0.75' backend/src/game/game.scoring.ts` returns `1`.
    - CLI: `cd backend && npx tsc --noEmit` exits `0`.
    - Behavior (verified in T-02): "catapult" vs keyword "cat" → no match; "i love cats" vs keyword "cat" → match (word-boundary, "cats" plural is fuzzy match at >= 0.75); "set" vs keyword "sit" → match (fuzzy, sim=0.666... no wait, levenshtein=1, maxLen=3, sim=1-1/3=0.666 — does NOT pass 0.75 threshold; this is correct — only single-char substitutions on words ≥4 chars hit the threshold).
  </acceptance_criteria>
</task>

<task id="T-02" title="D-05 tests: Add calcFreeSpeak describe block to game.service.spec.ts">
  <objective>
Validate the new matchesKeyword behavior end-to-end via calcFreeSpeak: word-boundary exact match counts, substring false-positive does NOT count, fuzzy near-match counts at the threshold, fuzzy below-threshold does NOT count, regex-special-character keywords do not throw.
  </objective>
  <read_first>
    - backend/src/game/game.service.spec.ts (full file — confirm existing import at line 7, existing describe blocks at lines 59 / 70 / 88 — append the new block after line 112)
    - backend/src/game/game.scoring.ts (after T-01 edits — sanity check matchesKeyword and calcFreeSpeak are present)
    - .planning/phases/01-speaking-homework/01-CONTEXT.md (decision D-05)
    - .planning/phases/01-speaking-homework/01-RESEARCH.md (sections "Validation Architecture", "Phase Requirements -> Test Map", "Pitfall 1")
    - .planning/phases/01-speaking-homework/01-PATTERNS.md (matchesKeyword reference implementation)
  </read_first>
  <action>
Modify `backend/src/game/game.service.spec.ts` with two edits — do NOT touch any other test file:

1. **Update the import on line 7** from:
   ```
   import { calcScore, levenshtein, calcSpeakingScore } from './game.scoring';
   ```
   to:
   ```
   import { calcScore, levenshtein, calcSpeakingScore, calcFreeSpeak } from './game.scoring';
   ```

2. **Append a new `describe('calcFreeSpeak', ...)` block** immediately after the existing `describe('calcSpeakingScore', ...)` block (which currently ends at line 112, just before the `// ── GameService.savePhonicsResult ───` separator comment at line 114). Insert these test cases inside the new describe block — exact assertions and exact `it` titles:

   ```
   // ── calcFreeSpeak ─────────────────────────────────────────────────────────────

   describe('calcFreeSpeak', () => {
     it('returns 0 score and 0 matchedWords for empty keyword list', () => {
       const r = calcFreeSpeak('hello world', '');
       expect(r.score).toBe(0);
       expect(r.matchedWords).toBe(0);
       expect(r.totalWords).toBe(0);
     });

     it('counts word-boundary exact match', () => {
       const r = calcFreeSpeak('I have a cat at home', 'cat');
       expect(r.matchedWords).toBe(1);
       expect(r.totalWords).toBe(1);
       expect(r.score).toBe(100);
     });

     it('does NOT count substring inside a larger word (catapult does not match cat)', () => {
       const r = calcFreeSpeak('the catapult fires rocks', 'cat');
       expect(r.matchedWords).toBe(0);
       expect(r.score).toBe(0);
     });

     it('counts fuzzy match at >= 0.75 Levenshtein similarity (single-char substitution on long word)', () => {
       // "elephnt" vs "elephant": levenshtein=1, maxLen=8, sim=0.875 >= 0.75 → match
       const r = calcFreeSpeak('I saw an elephnt today', 'elephant');
       expect(r.matchedWords).toBe(1);
     });

     it('does NOT count fuzzy match below 0.75 similarity (short words)', () => {
       // "set" vs "sit": levenshtein=1, maxLen=3, sim=0.666 < 0.75 → NO match
       const r = calcFreeSpeak('please set down', 'sit');
       expect(r.matchedWords).toBe(0);
     });

     it('counts case-insensitive word-boundary match', () => {
       const r = calcFreeSpeak('The Cat sat down', 'cat');
       expect(r.matchedWords).toBe(1);
     });

     it('handles regex special characters in keyword without throwing', () => {
       // ensure `cat.` keyword does not throw, and does not falsely match unrelated text
       expect(() => calcFreeSpeak('a regular cat is here', 'cat.')).not.toThrow();
       // "cat." regex with \b boundaries — \b is between word and non-word, so "cat." in transcript "(cat.)" would match but plain "cat" alone in transcript would NOT match exact "cat." keyword via word-boundary stage; fuzzy stage: levenshtein("cat","cat.")=1, maxLen=4, sim=0.75 >= 0.75 → match
       const r = calcFreeSpeak('a regular cat is here', 'cat.');
       // either word-boundary "cat." does not match in transcript "cat" (no trailing dot), but fuzzy fallback hits exactly at 0.75 → match
       expect(r.matchedWords).toBe(1);
     });

     it('scores partial match proportionally with multiple keywords', () => {
       // keywords: "cat", "sits", "mat"; transcript has only "cat" and "mat" → 2/3
       const r = calcFreeSpeak('the cat is on the mat', 'cat, sits, mat');
       expect(r.matchedWords).toBe(2);
       expect(r.totalWords).toBe(3);
       expect(r.score).toBe(67); // round(2/3 * 100) = 67
     });

     it('handles empty transcript with non-empty keywords', () => {
       const r = calcFreeSpeak('', 'cat, dog');
       expect(r.matchedWords).toBe(0);
       expect(r.totalWords).toBe(2);
       expect(r.score).toBe(0);
     });
   });
   ```

Do NOT remove or modify any existing test. Do NOT change `mockPhonicsSession` / `mockSpeakingSession` helpers. The new describe block must remain self-contained — it tests `calcFreeSpeak` as a pure function and does not require any module mocks.

If a test fails because the matchesKeyword fuzzy boundary case (e.g. `"cat.")` produces a different outcome than the expected `matchedWords === 1`, leave the test assertion as written and fix the implementation in T-01 to match. The contract is: 0.75 is INCLUSIVE (≥), per CONTEXT.md D-05.
  </action>
  <acceptance_criteria>
    - Source: `grep -cE \"^import \\{ calcScore, levenshtein, calcSpeakingScore, calcFreeSpeak \\} from\" backend/src/game/game.service.spec.ts` returns `1`.
    - Source: `grep -cE \"describe\\(.calcFreeSpeak\" backend/src/game/game.service.spec.ts` returns `1`.
    - Source: `grep -cE \"calcFreeSpeak\\(\" backend/src/game/game.service.spec.ts` returns `≥ 9` (≥9 test invocations across the new block plus the import line).
    - Source: `grep -c \"does NOT count substring inside a larger word\" backend/src/game/game.service.spec.ts` returns `1` (the false-positive guard test is present).
    - Source: `grep -c \"counts fuzzy match at >= 0.75\" backend/src/game/game.service.spec.ts` returns `1`.
    - Source: `grep -c \"handles regex special characters in keyword without throwing\" backend/src/game/game.service.spec.ts` returns `1`.
    - CLI: `cd backend && npx jest game.service.spec.ts -t calcFreeSpeak --no-coverage` exits `0` (all new test cases pass).
    - CLI: `cd backend && npx jest game.service.spec.ts --no-coverage` exits `0` (no regression in existing levenshtein / calcScore / calcSpeakingScore / GameService describe blocks).
    - CLI: `cd backend && npx tsc --noEmit` exits `0`.
  </acceptance_criteria>
</task>

<task id="T-03" title="D-16: Update student session results screen for FREE_SPEAK (image + Keywords matched: N/N)">
  <objective>
Render the FREE_SPEAK speaking branch of the results card with the speaking picture prominently at the top and a "Keywords matched: N/N" line under the score; suppress the raw comma-separated keyword list (item.text). SCRIPT_MATCH branch keeps its current "Said: {text}" layout (D-17 unchanged). Update handleSpeakingUpload to thread `matchedWords` and `totalWords` from the SpeakingResult response onto the items state.
  </objective>
  <read_first>
    - frontend/app/game/session/[id]/page.tsx (lines 13-24 for SessionItem; lines 84-90 for speakHw state; lines 108-135 for the useEffect that initializes speakHw; lines 342-361 for handleSpeakingUpload; lines 553-568 for the speaking results branch to be replaced)
    - frontend/lib/admin-api.ts (lines 332-339 — SpeakingResult interface already has matchedWords + totalWords; lines 240, 319 for SpeakingMode export)
    - .planning/phases/01-speaking-homework/01-CONTEXT.md (decisions D-16, D-17)
    - .planning/phases/01-speaking-homework/01-RESEARCH.md (section "Pitfall 4: D-16 Result Screen — speakHw State Not Available at Results Time" — important; section "Gap Analysis" row for D-16)
    - .planning/phases/01-speaking-homework/01-PATTERNS.md (section "frontend/app/game/session/[id]/page.tsx — D-16: FREE_SPEAK result screen update" for full JSX pattern)
    - .planning/phases/01-speaking-homework/01-UI-SPEC.md (section "Screen 3 — Student Session: Results Screen (pageState === 'results', FREE_SPEAK branch)" for exact layout + copy + class names)
  </read_first>
  <action>
Modify `frontend/app/game/session/[id]/page.tsx` with three coordinated edits:

1. **Extend the `SessionItem` interface** (lines 13-24) to carry matchedWords and totalWords:
   - Add `matchedWords?: number;` and `totalWords?: number;` as optional fields. Place them after `transcribed: string;` and before `score: number;` for readability. Existing fields stay.

2. **Update `handleSpeakingUpload` items-setter** (lines 348-355). The current block reads:
   ```
   setItems([{
     kind: 'speaking',
     text: speakHw?.speakingText ?? '',
     pictureUrl: speakHw?.speakingPictureUrl ?? undefined,
     transcribed: r.transcribedText ?? '',
     score: r.score,
     state: 'done',
   }]);
   ```
   Replace with:
   ```
   setItems([{
     kind: 'speaking',
     text: speakHw?.speakingText ?? '',
     pictureUrl: speakHw?.speakingPictureUrl ?? undefined,
     transcribed: r.transcribedText ?? '',
     matchedWords: r.matchedWords,
     totalWords: r.totalWords,
     score: r.score,
     state: 'done',
   }]);
   ```
   Do NOT change anything else in `handleSpeakingUpload` (the setPageState calls, the try/catch, the completeSession await, the results setter). `r` is the `SpeakingResult` returned from `saveSpeakingResult` — both `r.matchedWords` and `r.totalWords` are guaranteed non-undefined per the admin-api interface.

3. **Replace the speaking branch in the results card** (currently lines 553-568, inside the `pageState === 'results'` block's `items.map(...)`). The new structure must conditionally render based on `speakHw?.speakingMode`:
   - Mode label: `"🎤 Speaking · Free Speak"` when FREE_SPEAK, `"🎤 Speaking · Script Match"` when SCRIPT_MATCH, falling back to `"🎤 Speaking"` if `speakHw` is somehow null. Class: `text-white/60 text-xs font-bold uppercase mb-2`. (Note: the existing code uses `font-semibold` for this label; the UI-SPEC contract says `font-bold` — apply font-bold here per 01-UI-SPEC.md Typography section.)
   - For FREE_SPEAK + `item.pictureUrl`: render an `<img>` block above the score row:
     ```
     <div className="rounded-xl overflow-hidden mb-3 max-h-40">
       {/* eslint-disable-next-line @next/next/no-img-element */}
       <img src={item.pictureUrl} alt="Speaking prompt" className="w-full object-contain" />
     </div>
     ```
   - Score row: keep the existing `flex items-start justify-between gap-4` row, but conditionally render the `{item.text}` line ONLY when `speakHw?.speakingMode !== 'FREE_SPEAK'`. This keeps SCRIPT_MATCH showing the target text (D-17 unchanged) and hides the keyword list for FREE_SPEAK (D-16).
   - Transcript line (`You said: "..."`) stays for both modes.
   - For FREE_SPEAK + `item.matchedWords !== undefined && item.totalWords !== undefined`: render below the transcript:
     ```
     <div className="text-white/70 text-sm mt-1">
       Keywords matched: {item.matchedWords}/{item.totalWords}
     </div>
     ```
   - Keep the score display (`{item.score}%` with `scoreHexColor`) on the right side. Class: `text-2xl font-black tabular-nums shrink-0`.

   Concretely, the new JSX for the speaking branch (inside `} : (` on line 553) is:
   ```
   <div>
     <div className="text-white/60 text-xs font-bold uppercase mb-2">
       🎤 Speaking{speakHw?.speakingMode === 'FREE_SPEAK' ? ' · Free Speak' : speakHw?.speakingMode === 'SCRIPT_MATCH' ? ' · Script Match' : ''}
     </div>
     {speakHw?.speakingMode === 'FREE_SPEAK' && item.pictureUrl && (
       <div className="rounded-xl overflow-hidden mb-3 max-h-40">
         {/* eslint-disable-next-line @next/next/no-img-element */}
         <img src={item.pictureUrl} alt="Speaking prompt" className="w-full object-contain" />
       </div>
     )}
     <div className="flex items-start justify-between gap-4">
       <div className="flex-1">
         {speakHw?.speakingMode !== 'FREE_SPEAK' && (
           <div className="text-white font-medium text-sm mb-1">{item.text}</div>
         )}
         <div className="text-white/70 text-sm">
           You said: <span className="text-white italic">"{item.transcribed || '—'}"</span>
         </div>
         {speakHw?.speakingMode === 'FREE_SPEAK' && item.matchedWords !== undefined && item.totalWords !== undefined && (
           <div className="text-white/70 text-sm mt-1">
             Keywords matched: {item.matchedWords}/{item.totalWords}
           </div>
         )}
       </div>
       <div className="text-2xl font-black tabular-nums shrink-0" style={{ color: scoreHexColor(item.score) }}>
         {item.score}%
       </div>
     </div>
   </div>
   ```

Do NOT modify the `item.kind === 'phonics'` branch (lines 524-552). Do NOT change the outer `<div className="bg-white bg-opacity-10 rounded-2xl px-5 py-4">` wrapper (line 523). Do NOT modify any other pageState branch (upload, uploading, loading, error). Do NOT touch the camera/MediaRecorder code paths. Do NOT modify `frontend/lib/admin-api.ts` — the SpeakingResult interface already includes matchedWords/totalWords (verified at admin-api.ts lines 337-338).
  </action>
  <acceptance_criteria>
    - Source: `grep -c 'matchedWords?: number' frontend/app/game/session/\\[id\\]/page.tsx` returns `1` (SessionItem field added).
    - Source: `grep -c 'totalWords?: number' frontend/app/game/session/\\[id\\]/page.tsx` returns `1`.
    - Source: `grep -c 'matchedWords: r.matchedWords' frontend/app/game/session/\\[id\\]/page.tsx` returns `1` (handleSpeakingUpload sets it).
    - Source: `grep -c 'totalWords: r.totalWords' frontend/app/game/session/\\[id\\]/page.tsx` returns `1`.
    - Source: `grep -c \"Keywords matched: \" frontend/app/game/session/\\[id\\]/page.tsx` returns `1`.
    - Source: `grep -c \"speakHw?.speakingMode === 'FREE_SPEAK'\" frontend/app/game/session/\\[id\\]/page.tsx` returns `≥ 2` (image conditional + suppress-keyword-list conditional + Keywords-matched conditional = 3 occurrences; minimum 2).
    - Source: `grep -c \"speakHw?.speakingMode !== 'FREE_SPEAK'\" frontend/app/game/session/\\[id\\]/page.tsx` returns `1` (the suppression of item.text for FREE_SPEAK).
    - Source: `grep -c 'alt=\"Speaking prompt\"' frontend/app/game/session/\\[id\\]/page.tsx` returns `≥ 2` (one in upload state at line 392, one new in results state).
    - Source: `grep -c \"' · Free Speak'\" frontend/app/game/session/\\[id\\]/page.tsx` returns `1`.
    - Source: `grep -c \"' · Script Match'\" frontend/app/game/session/\\[id\\]/page.tsx` returns `1`.
    - Source (regression guard for D-16): the old generic line `<div className=\"text-white font-medium text-sm mb-1\">{item.text}</div>` is now wrapped in a conditional — `grep -c \"speakHw?.speakingMode !== 'FREE_SPEAK' && (\" frontend/app/game/session/\\[id\\]/page.tsx` returns `1`.
    - CLI: `cd frontend && npx tsc --noEmit` exits `0`.
    - CLI (smoke): `cd frontend && npx next build 2>&1 | grep -E '(error|failed)' | head -10` returns empty (no build error). If `next build` is too heavy for verification, fall back to `cd frontend && npx tsc --noEmit` alone.
    - Behavior (manual sanity): with a SPEAKING + FREE_SPEAK homework, after upload the results card shows: mode label "🎤 Speaking · Free Speak", the speaking image at top, the transcript, and "Keywords matched: N/N" under the score — and does NOT show the raw `cat, sits, mat` keyword list.
    - Behavior (manual sanity): with a SPEAKING + SCRIPT_MATCH homework, after upload the results card shows: mode label "🎤 Speaking · Script Match", NO image, the target text, the transcript, and the score — no "Keywords matched" line.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- All source assertions in T-01, T-02, T-03 pass.
- `cd backend && npx tsc --noEmit` exits 0.
- `cd backend && npx jest game.service.spec.ts --no-coverage` exits 0.
- `cd frontend && npx tsc --noEmit` exits 0.
- No file outside `backend/src/game/game.scoring.ts`, `backend/src/game/game.service.spec.ts`, `frontend/app/game/session/[id]/page.tsx` is modified by this plan.
- `frontend/lib/admin-api.ts` is read for interface validation but NOT modified (SpeakingResult.matchedWords/totalWords already exist).
- Wave 2 dependency on 01-03 is satisfied because game.service.spec.ts must be committed by 01-03 in its pre-D-05 state before this plan appends the new describe block.
</verification>

<success_criteria>
- D-05 closed: calcFreeSpeak no longer false-positives on substring matches; word-boundary regex catches the majority of intended matches; fuzzy fallback at ≥ 0.75 catches transcription noise; regex-special-character keywords do not throw.
- D-05 tests: ≥ 5 unit test cases (false-positive guard + fuzzy hit + fuzzy miss + regex-escape + partial multi-keyword scoring) all pass; existing tests do not regress.
- D-16 closed: FREE_SPEAK student results screen shows image prompt at top, mode label "🎤 Speaking · Free Speak", suppresses the raw keyword list, and renders "Keywords matched: N/N" under the score.
- D-17 honored: SCRIPT_MATCH student results screen is functionally unchanged (still shows target text, transcript, score — no image, no "Keywords matched" line).
</success_criteria>

<output>
After T-01, T-02, T-03 complete, create `.planning/phases/01-speaking-homework/01-04-SUMMARY.md` capturing:
- Final diff stat for the 3 files modified.
- D-05, D-16 mapping to actual code locations (line numbers after edits in game.scoring.ts and session/[id]/page.tsx).
- Confirmation `cd backend && npx jest game.service.spec.ts -t calcFreeSpeak --no-coverage` exits 0 with all new test cases passing.
- Confirmation `cd frontend && npx tsc --noEmit` exits 0.
- Note: SpeakingResult.matchedWords/totalWords were already present in admin-api.ts — no admin-api change needed; the SessionItem interface was extended instead.
</output>

<must_haves>
  <truths>
    - "calcFreeSpeak uses word-boundary regex + Levenshtein >= 0.75 fallback; bare includes() is gone."
    - "matchesKeyword escapes regex special characters before building the pattern."
    - "calcFreeSpeak has at least 5 unit test cases including a false-positive guard (catapult vs cat) and a fuzzy fallback hit."
    - "FREE_SPEAK student results render the speaking image and 'Keywords matched: N/N' instead of the raw keyword list."
    - "SCRIPT_MATCH student results are unchanged (target text + transcript + score)."
    - "SessionItem interface gains optional matchedWords / totalWords fields; handleSpeakingUpload stores them from the SpeakingResult response."
    - "frontend/lib/admin-api.ts is not modified (SpeakingResult.matchedWords/totalWords already present)."
  </truths>
  <verification>
    - "grep -c 'function matchesKeyword' backend/src/game/game.scoring.ts returns 1."
    - "grep -c 'text.includes(kw)' backend/src/game/game.scoring.ts returns 0."
    - "grep -cE \"describe\\(.calcFreeSpeak\" backend/src/game/game.service.spec.ts returns 1."
    - "cd backend && npx jest game.service.spec.ts -t calcFreeSpeak --no-coverage exits 0."
    - "grep -c 'Keywords matched:' frontend/app/game/session/[id]/page.tsx returns 1."
    - "grep -c \"speakHw?.speakingMode !== 'FREE_SPEAK'\" frontend/app/game/session/[id]/page.tsx returns 1."
    - "cd frontend && npx tsc --noEmit exits 0."
    - "cd backend && npx tsc --noEmit exits 0."
  </verification>
</must_haves>

<threat_model>
  <threats>
    <threat severity="medium">ASVS V5 input validation (regex injection via user-supplied keywords): Teacher-supplied keyword strings flow directly into `new RegExp(`\b${kw}\b`)`. Unescaped, a keyword like `(.*)` or `[abc]` would either throw or match unintended ranges, distorting scores. Mitigation: T-01 escapes 13 regex metacharacters (`. * + ? ^ $ { } ( ) | [ ] \`) via `kw.replace(/[.*+?^${}()|[\]\\]/g, '\\$&')` before pattern construction. T-02 includes a dedicated `handles regex special characters in keyword without throwing` test case verifying no exception is raised and the fuzzy fallback still computes correctly.</threat>
    <threat severity="medium">Scoring correctness / data integrity (ASVS V14 — boundary integrity): A regression in calcFreeSpeak silently overscores or underscores all FREE_SPEAK submissions. Mitigation: T-02 adds 9 unit test cases pinning the contract: empty keywords (score=0), word-boundary exact match (score=100), substring false-positive (score=0), fuzzy hit/miss at the 0.75 threshold, case-insensitive match, regex-escape, partial multi-keyword (2/3), empty transcript. These run on every CI invocation (`jest game.service.spec.ts`) and on every executor verification pass.</threat>
    <threat severity="low">Information disclosure to student (ASVS V14 — output encoding / minimum exposure): The previous result card exposed the comma-separated keyword list as `{item.text}`, leaking the teacher's intended answer to the student post-submission. Mitigation: T-03 conditionally suppresses `{item.text}` for FREE_SPEAK mode, replacing it with image + score + keywords-matched count — student sees the prompt and feedback but never the answer key.</threat>
    <threat severity="low">Stored XSS via image URL or filename in alt text (ASVS V5 input validation, V14 output encoding): `item.pictureUrl` is rendered into `<img src={...} />` and the alt text is a hard-coded constant. The `pictureUrl` value originates from `speakHw.speakingPictureUrl` which is teacher-supplied via the homework creation form and stored in Postgres via Prisma. Mitigation: React JSX auto-escapes string attributes; the URL is passed unmodified to the browser, which will not execute it. The only risk path is a maliciously-crafted URL coercing the browser into navigation, but `<img>` does not navigate. Accepted: existing pattern is consistent with the upload state (session/[id]/page.tsx line 392 already renders the same URL the same way), so no incremental risk introduced by this plan.</threat>
    <threat severity="low">Test-suite drift across plans (process integrity): If 01-03 commits game.service.spec.ts without the calcFreeSpeak block and 01-04 later appends one, an executor running tests between the two plans will not validate D-05 behavior. Mitigation: Wave order is explicit — `depends_on: [01-03]`. The verification gate `cd backend && npx jest game.service.spec.ts -t calcFreeSpeak --no-coverage exits 0` will not pass until both plans are complete; an executor running 01-04 against an unfinished 01-03 will hit a test-file-not-found or import error and stop.</threat>
  </threats>
</threat_model>