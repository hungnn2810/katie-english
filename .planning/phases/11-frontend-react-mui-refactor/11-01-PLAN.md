---
phase: 11-frontend-react-mui-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/lib/theme.ts
  - frontend/lib/student-theme.ts
autonomous: true
requirements: [DS-01]
must_haves:
  truths:
    - "baseTheme shape.borderRadius = 4 (MUI base unit)"
    - "baseTheme palette matches design system: primary #4F9DFF, secondary #6ED6C1, error #FF7B7B, warning #FFD166, background.default #F7F9FC"
    - "baseTheme typography fontFamily is Inter"
    - "baseTheme MuiButton textTransform none, fontWeight 600"
    - "baseTheme MuiCard has borderRadius 16, border 1px solid #E2E8F0, boxShadow shadow-1"
    - "baseTheme MuiChip has borderRadius 999, fontWeight 700, fontSize 12px"
    - "baseTheme MuiTableCell header has fontSize 11px, fontWeight 700, textTransform uppercase, letterSpacing 0.06em, color #94A3B8"
    - "studentTheme palette.primary.main = #A78BFA"
    - "studentTheme shape.borderRadius = 6"
    - "teacherTheme exported from theme.ts with palette.primary.main #F0623A"
    - "adminTheme exported from theme.ts with palette.primary.main #4F9DFF"
  artifacts:
    - path: "frontend/lib/theme.ts"
      provides: "baseTheme, teacherTheme, adminTheme exports"
      contains: "F0623A"
    - path: "frontend/lib/student-theme.ts"
      provides: "studentTheme export"
      contains: "A78BFA"
---

<objective>
Tighten the MUI theme tokens to exactly match the Katie English Design System.

The theme already has the right palette base. This plan adds:
1. MuiCard component override (radius 16px, border, shadow-1, hover shadow-4)
2. MuiChip pill shape (999px, 700 weight, 12px)
3. MuiTableCell header styling (11px, 700, uppercase, 0.06em tracking, #94A3B8)
4. teacherTheme and adminTheme exported variants (same base, different primary accent)
5. Confirm studentTheme has borderRadius 6 and purple primary

All downstream plans (11-02 through 11-05) consume these theme tokens via MUI sx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-frontend-react-mui-refactor/11-CONTEXT.md
@.planning/phases/11-frontend-react-mui-refactor/11-UI-SPEC.md

<design_tokens>
From `Katie English Design System/colors_and_type.css`:

Shadow scale:
  shadow-1: 0 1px 3px rgba(15,23,42,0.10), 0 1px 2px rgba(15,23,42,0.06)
  shadow-3: 0 4px 12px rgba(15,23,42,0.10)
  shadow-4: 0 6px 16px rgba(15,23,42,0.12)
  shadow-8: 0 12px 28px rgba(15,23,42,0.18)

Card spec (from ui_kits/teacher/ui.jsx Card component):
  background: #fff
  border: 1px solid #E2E8F0
  borderRadius: 16px
  hover: translateY(-2px), boxShadow shadow-3

Typography button (from ui_kits/teacher/ui.jsx Btn):
  fontWeight: 600, fontSize: 14px (15px in design CSS .btn-label), textTransform: none

MuiTableCell header (from ui_kits/teacher/screens.jsx TableShell):
  fontSize: 11px, fontWeight: 700, textTransform: uppercase, letterSpacing: 0.06em, color: #94A3B8
  padding: 12px 22px (override via sx on individual tables, not theme)

Portal accents:
  Teacher: #F0623A
  Admin:   #4F9DFF
  Student: #A78BFA
</design_tokens>

<current_theme_state>
frontend/lib/theme.ts already has:
  palette.primary.main = '#4F9DFF'
  palette.background.default = '#F7F9FC'
  MuiButton textTransform: none, fontWeight: 600
  shape.borderRadius: 4

Missing: MuiCard override, MuiChip pill, teacherTheme, adminTheme exports.

frontend/lib/student-theme.ts already has:
  palette.primary.main = '#A78BFA'
  shape.borderRadius: 6
This file is correct — verify only.
</current_theme_state>
</context>

<tasks>
## Task 1 — Update frontend/lib/theme.ts

Add to baseTheme.components:

```typescript
MuiCard: {
  styleOverrides: {
    root: {
      borderRadius: 16,
      border: '1px solid #E2E8F0',
      boxShadow: '0 1px 3px rgba(15,23,42,0.10), 0 1px 2px rgba(15,23,42,0.06)',
    },
  },
},
MuiChip: {
  styleOverrides: {
    root: {
      borderRadius: 999,
      fontWeight: 700,
      fontSize: 12,
    },
  },
},
```

Export two portal-specific themes at bottom of file:

```typescript
export const teacherTheme = createTheme(baseTheme, {
  palette: {
    primary: { main: '#F0623A', contrastText: '#ffffff' },
  },
});

export const adminTheme = createTheme(baseTheme, {
  palette: {
    primary: { main: '#4F9DFF', contrastText: '#ffffff' },
  },
});
```

## Task 2 — Verify frontend/lib/student-theme.ts

Confirm file already exports `studentTheme` with:
- `palette.primary.main = '#A78BFA'`
- `shape.borderRadius = 6`

No changes needed unless these are wrong.

## Task 3 — Commit

Commit message: `feat(11): update MUI theme tokens to match design system`
</tasks>
